export const meta = {
  name: 'plan-a-fixers',
  description: 'Fold the verifier issues and spec coverage gaps into the four Plan A parts, then re-verify consistency',
  phases: [
    { title: 'Fix', detail: 'three parallel fixers (S, M+D, F) amend their parts' },
    { title: 'Reverify', detail: 'one verifier re-checks names/signatures/ordering across amended parts' },
  ],
}
const PLAN = { type: 'object', required: ['markdown', 'changes'], properties: { markdown: { type: 'string' }, changes: { type: 'array', items: { type: 'string' } } } }
const VERDICT = { type: 'object', required: ['ok', 'issues'], properties: { ok: { type: 'boolean' }, issues: { type: 'array', items: { type: 'object', required: ['task', 'problem', 'fix'], properties: { task: { type: 'string' }, problem: { type: 'string' }, fix: { type: 'string' } } } } } }

const T = '/private/tmp/claude-501/-Volumes-4TB-BAD-Halbert/dd1ab561-6856-4d58-ac80-5e3c97b11259/tasks'
const WT = '/Users/ericbintner/.config/superpowers/worktrees/Halbert/continuous-conversation'
const CONTRACTS = '/private/tmp/claude-501/-Volumes-4TB-BAD-Halbert/dd1ab561-6856-4d58-ac80-5e3c97b11259/scratchpad/plan-a-contracts.md'
const SPEC = WT + '/documentation/design/continuous-conversation-and-watched-terminals-2026-08-26.md'

const RULES = [
  'You are amending ONE PART of an implementation plan (writing-plans format: ### Task An, Files, checkbox steps with FULL code, exact commands with expected output, commit step; no placeholders; no "similar to").',
  'Read: your part file, the verifier verdict at ' + T + '/plan_verdict.md, the contracts at ' + CONTRACTS + ', the spec at ' + SPEC + ' (sections 5-8, 11-12), and the worktree code at ' + WT + ' for any file you add edits to. Do NOT call mcp__prep__* tools. Do NOT edit repository files. You may read the other parts (' + T + '/plan_S.md, plan_M.md, plan_D.md, plan_F.md) to keep names consistent.',
  'Return the COMPLETE amended part as markdown (every original task, with your fixes applied in place and new tasks inserted at the right position using suffix letters, e.g. A6c, A9d, A11b, A17b) plus a list of the changes you made. Preserve everything you did not need to change verbatim.',
  'New tests must be real code; new implementation must be full code with exact text anchors into the current worktree files (line numbers optional).',
].join('\n')

phase('Fix')
const fixed = await parallel([
  () => agent(RULES + `
YOUR PART: plan_S.md (tasks A1..A6b). Apply: (1) A1b red-step expectation -> "7 failed, 13 passed" (see verdict). (2) A6b green-step expectation -> "154 passed". (3) A1: add the compact_boundaries table to the schema (spec section 8: thread_id TEXT, trigger TEXT, pre_tokens INTEGER, post_tokens INTEGER, preserved_message_ids TEXT json, summary_message_id INTEGER, created_at REAL) with a test asserting it exists; no writers yet (Plan A default-off). (4) A4: add 'scanner' to the network domain keywords with a test. (5) New task A6c — ThreadManager.merge_back(new_thread_id: str) -> str | None: if the new thread's paused predecessor (parent_thread_id or the thread whose paused_at is most recent and status=paused) is within the grace window, UPDATE messages SET conversation_id=<prev> for the new thread's rows, set new thread status='merged', merged_into=<prev>, reopen prev (status='open', paused_at=NULL), refresh prev receipt, delete the merged thread's receipts_fts row; return prev id. Make resume_thread(thread_id, from_thread_id) call merge_back when from_thread_id was opened from thread_id within the grace window (turns_since_pause < GRACE_TURNS and paused_at within GRACE_MINUTES) instead of a plain reopen. Tests: merge moves rows, marks merged, search_receipts excludes merged, resume within grace merges, resume after grace reopens without merging. (6) New task A6d — retract_recall appends a system-origin row (origin='system', visible_in_timeline=0, content "admin retracted recall of '<title>'") to the open thread, and begin_turn collects such system rows newer than the last human row into TurnContext.notes: list[str]; build_hint renders them as a line "Note: <text>" inside <continuity>. Tests for both. (7) A5: live-terminal guard is Plan B — add a one-line note in tick()'s docstring that Plan B adds "never close while a terminal session of this thread is open". Keep all other content verbatim.
`, { label: 'fix:S', phase: 'Fix', schema: PLAN }),

  () => agent(RULES + `
YOUR PARTS: plan_M.md (A7..A11) AND plan_D.md (A12..A13) — return them concatenated in order (M then D) in the markdown field. Apply: (1) A10 BLOCKING fixture fix exactly as the verdict says (_FakeResp content on two lines: done:false with "hi", then done:true with ""). (2) A7 executor.py line references: drop the numbers, keep text anchors. (3) A12b step 3: insert ONLY run_conversation_boot_hooks after the existing _find_config_registry; do not re-list _find_config_registry. (4) A9a: when _supersede_paused_turn evicts an AWAITING_CONFIRMATION session that has turn_context, call thread_manager.end_turn(turn, assistant_text="", blocks=[{"tool": pending["tool"], "args": pending.get("args", {}), "result": "not run — superseded", "exit": None, "status": "superseded"}], terminal_session_ids=[], diff_proposals=[], status="cancelled") so the receipt records it; add a test. (5) A9a: emit StreamEvent.conversation_status(session_id, "waiting", ...) (use the existing conversation_status factory signature — read events.py) BEFORE awaiting the turn_lock when the lock is already held (self.turn_lock.locked()); add a test with the slow fake LLM asserting the second caller's first event is that status. (6) A9: somatic blocks use ctx.thread_id when set — in _emit_somatic_block (and the SqliteConversationStore.add_somatic_block call site if any) pass ctx.thread_id or ctx.session_id; add/adjust a test. (7) A9/A8: tools_supported — in agents/llm_client.py (LLMClientAdapter) and model/client.py, when the client falls back to a no-tools retry (the 400/404/422 branch the verdict/audit describe at client.py ~394-409), set an attribute tools_supported=False on the client/adapter (default None); in _handle_planning pass tools_supported=getattr(self.llm, "tools_supported", None) into the hint builder via a new keyword build_hint(..., tools_supported=None) — the S part owns build_hint, so instead implement it in the prompt layer: CONTINUITY_PREAMBLE gets a variant without the "call recall_thread / new_thread" sentence, selected when tools_supported is False; test it. (8) New task A8b: intake/budget.py raise the conversation bucket for MEDIUM to 1600 and LARGE to 2400 (keep totals consistent — read the file and adjust other buckets so the tier total is unchanged); context/assembler.py _format_conversation: if the first history row is role=system and starts with "[Earlier in this subject:" render it as its own "## Earlier in this subject" block placed before the recent turns and outside the newest-first walk, and skip should_summarize/compress_conversation_history for the remaining rows when that receipt row is present; tests for both. (9) New task A11b: POST /api/agent/message/{message_id}/redact -> store.redact_message(message_id) (add the store method in this task: content and blocks_json replaced with "[redacted by admin]", messages_fts row rewritten, thread receipt refreshed via ThreadManager.refresh_receipt(thread_id) if it exists or build_receipt+upsert_receipt) returning {ok, thread_id}; test with TestClient. (10) A11: thread_recalled event data gains last_turn_id (the recalled thread's newest turn_id) so the UI can scroll to it — set it wherever thread_recalled is emitted (auto path in process() and the meta-tool path); adjust event factory signature thread_recalled(session_id, thread_id, title, date, match_terms, mode, last_turn_id=None) in A7 and its test. (11) Note in A9c that self.cancelled is legacy and conversation_status is the live path. Keep everything else verbatim.
`, { label: 'fix:M+D', phase: 'Fix', schema: PLAN }),

  () => agent(RULES + `
YOUR PART: plan_F.md (A14..A20). Apply: (1) A18 step 9 counts -> "Test Files 15 passed (15)", "Tests 82 passed (82)" and remove the arithmetic note; state A19 brings it to 17 / 88. (2) A15 test header line 2 -> "// Copyright (C) 2024-2026 Eric Bintner and Halbert Contributors". (3) A14 step 4: drop line numbers, keep the text anchor. (4) tsc ordering: move the deletion of listAgentConversations/getAgentConversation/deleteAgentConversation (and its "removed wrappers" test block) from A14 into A18 so every commit typechecks; adjust A14's note accordingly and A18's steps/tests. (5) A16/A18 chip: the thread chip button's onClick loads the timeline around the recalled thread's last turn (useTimeline gains loadAround(turnId) that fetches ?around= and replaces the page, then scrolls to [data-turn-id=turnId] via scrollIntoView) — the thread_recalled event now carries last_turn_id (backend adds it): extend the useAgentStream reducer (A15) to store recalled.lastTurnId; the chip's title attribute shows "matched: <terms>"; the chip is cleared when a thread_started event arrives (expiry on pause) — add tests for each. (6) A17: add a second visually-hidden region <div role="alert" aria-live="assertive"> in HostShell fed by announce(text, {assertive: true}); wire it from the tool_confirmation_required event path in useAgentStream (call announce with "<ai_name> is waiting for your approval" — read how the confirmation event is handled today and where ai_name/display name is available; if not available in the hook, announce "Waiting for your approval"); test the two regions. (7) New task A17b: per-turn "Forget this" action in Timeline.tsx — a small button on each stored turn (aria-label "Forget this turn") that calls api.redactMessage(messageId) for the user and assistant rows (add api.redactMessage(messageId: number) -> POST /api/agent/message/{id}/redact in api.ts) and replaces the turn's contents locally with "[redacted by admin]"; test with a mocked fetch. (8) Note under A17 that CurrentTopicLabel needs no voice rendering (a title has no pronouns) — spec section 11's "follows the voice setting" applies to the continuity text only. Keep everything else verbatim; make sure the total test counts you state at the end of A19 are recomputed after your additions (state them as "at least" if uncertain).
`, { label: 'fix:F', phase: 'Fix', schema: PLAN }),
])
const [S2, MD2, F2] = fixed
log('fixers done: ' + fixed.filter(Boolean).length + '/3')

phase('Reverify')
const verdict = await agent('You are re-verifying an amended four-part implementation plan for name/signature/ordering consistency. Do NOT call mcp__prep__* tools. Do NOT edit files. Read the contracts at ' + CONTRACTS + '. Check across the parts below: every function/method/field/event/tool/endpoint used in one part is defined (with the same signature) in another part or the contracts; new tasks (A6c, A6d, A8b, A11b, A17b and any others) are referenced consistently (e.g. thread_recalled last_turn_id in A7/A11/A15/A16; redact endpoint shape in A11b vs api.redactMessage; TurnContext.notes and build_hint notes; tools_supported attribute name); no placeholders; test-count expectations are self-consistent with the tasks that precede them; no task tests import something created later. Report only real problems with exact fixes. ok=true only if nothing blocking remains.\n\n=== PART S ===\n' + (S2 ? S2.markdown : '(missing)') + '\n=== PART M+D ===\n' + (MD2 ? MD2.markdown : '(missing)') + '\n=== PART F ===\n' + (F2 ? F2.markdown : '(missing)') + '\n', { label: 'reverify', phase: 'Reverify', schema: VERDICT })

return { S: S2 ? S2.markdown : null, MD: MD2 ? MD2.markdown : null, F: F2 ? F2.markdown : null, changes: { S: S2 ? S2.changes : [], MD: MD2 ? MD2.changes : [], F: F2 ? F2.changes : [] }, verdict: verdict }
