export const meta = {
  name: 'plan-a-writers',
  description: 'Write Plan A (conversation floor + hidden threads) as bite-sized TDD tasks across four areas against the worktree code, then verify cross-area consistency',
  phases: [
    { title: 'Write', detail: 'four parallel planners: store/threads, state-machine/prompts/route, migrations+e2e, frontend' },
    { title: 'Verify', detail: 'one verifier checks type/signature consistency and spec coverage across the four parts' },
  ],
}

const PLAN = { type: 'object', required: ['markdown'], properties: { markdown: { type: 'string', description: 'The plan section in the writing-plans format: ### Task An: ... with Files, checkbox steps, complete code, exact commands, expected output, commit step' } } }
const VERDICT = { type: 'object', required: ['issues', 'coverage_gaps', 'ok'], properties: { ok: { type: 'boolean' }, issues: { type: 'array', items: { type: 'object', required: ['task', 'problem', 'fix'], properties: { task: { type: 'string' }, problem: { type: 'string' }, fix: { type: 'string' } } } }, coverage_gaps: { type: 'array', items: { type: 'string' } } } }

const WT = '/Users/ericbintner/.config/superpowers/worktrees/Halbert/continuous-conversation'
const CONTRACTS = '/private/tmp/claude-501/-Volumes-4TB-BAD-Halbert/dd1ab561-6856-4d58-ac80-5e3c97b11259/scratchpad/plan-a-contracts.md'
const PYTEST = 'cd ' + WT + '/halbert_core && /Volumes/4TB-BAD/Halbert/.venv/bin/python -m pytest tests/<file> -q -p no:cacheprovider'
const VITEST = 'cd ' + WT + '/halbert_core/halbert_core/dashboard/frontend && npx vitest run <path>'

const RULES = [
  'You are writing ONE PART of an implementation plan in the "writing-plans" format for an engineer with zero context. HARD RULES:',
  '- Read FIRST: ' + CONTRACTS + ' (binding contracts: names, signatures, schema, event names, ordering). Then read the spec sections it points to, then the actual code files you will change IN THE WORKTREE ' + WT + ' (never the /Volumes/4TB-BAD/Halbert checkout). Do NOT call any mcp__prep__* tool. Do NOT edit files. Do NOT run scripts/. You may run existing tests read-only to see current output.',
  '- Every task: "### Task A<n>: <name>" with **Files:** (Create/Modify with exact paths and line ranges for Modify; Test: path), then checkbox steps: write the failing test (full code), run it (exact command, expected failure text), write the implementation (FULL code, not a description - show the actual functions/edits with enough surrounding context to apply them), run tests (exact command, expected PASS), commit (git add <pathspec files> && git commit -m "..." - subject + optional body, NO trailers).',
  '- No placeholders: no TBD/TODO/"handle edge cases"/"similar to task N"; repeat code where needed. Use the exact names from the contracts; if you must add a helper not in the contracts, define it fully and note it under a "Contract additions" line at the end of your part so the verifier can propagate it.',
  '- Tests: backend with: ' + PYTEST + ' ; frontend with: ' + VITEST + ' and "npx tsc --noEmit -p ." from the frontend dir. Follow the existing test style in the repo (look at halbert_core/tests/test_conversation_sqlite.py and frontend src/hooks/*.test.ts).',
  '- Keep tasks 2-5 minutes each; split large ones. Your output is ONLY the markdown for your tasks (the schema field), starting directly with the first "### Task".',
].join('\n')

phase('Write')
const parts = await parallel([
  () => agent(RULES + `
YOUR PART: planner S - tasks A1..A6 (storage schema/WAL/append/update, receipt module, search_receipts + FTS rebuild, intake signals aliases/regex/canonical_entities, thread_signals decide/build_hint, ThreadManager). Read: halbert_core/halbert_core/agents/conversation_sqlite.py (all), agents/conversation.py (Conversation/Message, from_dict, content helpers), agents/blocks.py (content_to_text), intake/signals.py (all), intake/pipeline.py (MessageIntake), context/watermark.py (temporal gate constant), tests/test_conversation_sqlite.py, tests/test_session_affinity.py, tests/test_intake_signals* if present. Also read agents/session_affinity.py to decide whether decide() reuses its keyword scoring (prefer reusing its tokenisation helper if clean; otherwise leave it untouched and note it as dead code for Plan C cleanup).
Deliver tasks A1-A6 exactly per the contracts sections 1-4, with full code for: schema migration (PRAGMA table_info checks, schema_version table, FTS rebuild when version < 2), append_message/update_message/update_thread/get_thread/list_threads/current_open_thread/list_turns/recent_messages/search_receipts/upsert_receipt, save() no longer touching messages, build_receipt/provisional_title/refined_title, ENTITY_ALIASES/canonical_entities/MessageSignals fields, decide()/build_hint() (with the exact hint text shape), and ThreadManager (begin_turn/new_thread/resume_thread/recall/end_turn/mark_interrupted/tick/retract_recall/current + get_thread_manager). Tests must include: WAL pragma set; append+FTS in one transaction and a failing append returning None without corrupting; punctuation queries 'smb.conf' and "what's" do not abort search; alias 'cifs' -> 'samba'; decide(): detour stays, gap-only -> stale not new, gap+shift -> open_new, anaphora with no signals -> stay + strong candidate from most recent closed; strong match reopens paused; ThreadManager end-to-end: begin_turn twice in a row yields history containing the first exchange; new_thread pauses and tick() closes after GRACE; receipt contains a Commands line from blocks_json.
`, { label: 'plan:store-threads', phase: 'Write', schema: PLAN }),

  () => agent(RULES + `
YOUR PART: planner M - tasks A7..A11 (events + StateContext + tool schemas + safety; prompts continuity/history; state machine lock + inline meta-tools + begin/end turn wiring + ctx.terminal_session_ids + store_interaction removal; num_ctx; route /message wiring + timeline endpoints + diff persistence + removals). Read: agents/events.py (all), agents/states.py (StateContext + AgentState transitions table ~line 60-115), agents/state_machine.py (process 190-270, _drive, _transition, _handle_planning 620-760, _handle_executing and _run_tool_streaming 928-1060, _handle_responding 1236-1380, confirm_action ~350-440, cancel ~438), tools/executor.py (get_schemas 78-200, execute dispatch ~204-260), tools/safety.py (classify 288-330), prompts/agent_prompts.py (build_planning_prompt ~190-250, build_response_prompt 253-330, voice helpers 48-69), model/client.py (Ollama payload ~460-520), agents/llm_client.py (LLMClientAdapter chat/stream), dashboard/routes/agent.py (get_agent 70-200, /message 640-760, confirm 725-760, cancel ~785, diff apply/reject ~930-990, conversations 880-930), intake/budget.py (ContextBudget), tests/test_state_machine.py, tests/test_tool_calling_bridge.py (note 4 tests there fail at baseline - do not change those expectations), tests/test_agent_routes* if present, and the contracts sections 5-6 (plus 4 for the ThreadManager API you call - treat it as given).
Deliver A7-A11 with full code: the four event factories; StateContext fields; the three tool schemas + execute stubs + SAFE classification; build_planning_prompt/build_response_prompt signatures with continuity/history rendered immediately before the query section (show the exact insertion against the current file text); turn_lock in __init__, 'async with self.turn_lock' around process()/confirm_action() bodies (keep generator semantics correct: acquire at start, release in finally), _transition(PLANNING) inside try, _handle_meta_tool with thread_started/thread_recalled emission and PLANNING re-entry without loop_count increment, begin_turn/end_turn calls (end_turn in the finally with status derived from cancelled/error), ctx.terminal_session_ids collection in _run_tool_streaming, removal of memory.store_interaction; compute_num_ctx + per-model cache + options.num_ctx in both chat and stream payloads + PLANNING num_predict 1024 (make sure test_tool_calling_bridge expectations for payload shape still hold - read those tests); /message rewiring (remove force-reset; pass thread_manager), GET /api/agent/timeline, GET /api/agent/thread/current, DELETE recall, diff apply/reject fallback to the store, deletion of /agent/conversations endpoints. Tests: events to_dict shapes; safety classify SAFE for the three names; prompt contains continuity before the task heading and history before the query; state machine test with a fake LLM that returns a new_thread tool call -> thread_started emitted, loop_count unchanged, PLANNING re-entered once; lock test: two concurrent process() calls serialize using a slow fake LLM; num_ctx test on payload; route test with FastAPI TestClient for /timeline and /thread/current using a temp DB (look at how existing route tests build the app or the router).
`, { label: 'plan:state-machine-route', phase: 'Write', schema: PLAN }),

  () => agent(RULES + `
YOUR PART: planner D - tasks A12..A13 (migrations + startup hooks + deletions; backend e2e). Read: agents/conversation.py (ConversationStore, SessionStore, get_conversation_store, from_dict), dashboard/routes/conversations.py (its JSON shape and CONVERSATIONS_DIR), dashboard/app.py (router includes ~200-260, startup/shutdown ~420-500), agents/handlers/*.py and agents/__init__.py (re-exports), dashboard/routes/agent.py (conversations endpoints 880-930 and get_agent), every importer of ConversationStore/get_conversation_store/SessionStore/handlers (grep -rn across halbert_core/halbert_core and halbert_core/tests), tests/test_conversation_sqlite.py (migration tests ~174-197), and contracts sections 8, 4 (ThreadManager API), 1 (store API). Deliver: A12 - agents/migrations.py with migrate_legacy_conversations handling BOTH JSON shapes via append_message + build_receipt + threads status=closed, idempotent via a migrations_done table; startup call in dashboard/app.py (after the store singleton exists) plus ThreadManager.mark_interrupted(); deletions (routes/conversations.py + include, JSON ConversationStore/SessionStore/get_* and their importers repointed, handlers package + re-export, /agent/conversations endpoints if planner M's task did not already - make your deletion step idempotent: "if still present"), and test updates for every test that imported deleted symbols (list each file and the exact change). A13 - tests/test_thread_e2e.py: build the real AgentStateMachine with a fake LLM (mirror tests/test_state_machine.py fixtures) and a ThreadManager over a temp SQLite DB; assert (1) two process() calls: the second call's PLANNING prompt (capture via the fake LLM) contains the first exchange's text; (2) a fake LLM that returns a new_thread tool call on the third call produces thread_started, and the store shows the old thread paused and the new one open; (3) after tick() with a monkeypatched clock past GRACE the old thread is closed with a receipt indexed and recall('samba') returns it; (4) a fourth call whose message contains 'as we did' + a receipt term yields a continuity hint containing 'Pulled in:' in the PLANNING prompt without any tool call. Use the FastAPI route only if a TestClient fixture already exists; otherwise drive the state machine directly.
`, { label: 'plan:migrations-e2e', phase: 'Write', schema: PLAN }),

  () => agent(RULES + `
YOUR PART: planner F - tasks A14..A20 (frontend). Read: src/types/ (existing types dir, e.g. llm.ts for style), src/lib/api.ts (request helper + agent conversations wrappers ~358-370), src/hooks/useAgentStream.ts (all), src/hooks/useAgentStream.terminal.test.ts and src/hooks/useTerminalSessions.test.ts (test style, how fetch/SSE is mocked), src/components/agent/AgentChat.tsx (all 917 lines - conversation state 200-340, header/dropdown 570-645, message list 650-720, composer 520-560, footer ~890), src/components/agent/ContextBar.tsx, src/components/agent/ToolExecutionCard.tsx, src/components/agent/DiffBlock.tsx, src/components/agent/TetherChip.tsx, src/components/agent/InlineTerminals.tsx, src/components/agent/TerminalTile.tsx (60-160), src/components/agent/HostGreeting.tsx, src/components/shell/HostShell.tsx, src/components/agent/index.ts and src/hooks/index.ts (barrels), tailwind.config.js (colors.status ~75-84), src/test/ setup (vitest setup file, jsdom), package.json scripts, and the contracts section 7. Also skim the spec section 11 for the day-divider markup and live-region rules.
Deliver A14-A20 with full TS/TSX code: types/timeline.ts; api.ts additions + removals; hooks/useTimeline.ts (fetch, paging with before, appendLive, byDay grouping with the exact label rules, currentThread state) + test; useAgentStream reducers for thread_started/thread_recalled/turn_persisted/thread_store_error, AgentSession fields, reset() no longer touching the terminal store + test; ContextBar 'thread' type + button semantics + tailwind status.*-line aliases (read tokens.css names first: find /shared-tokens/tokens.css via the frontend's alias or public path and cite the exact --color-status-*-line variables); components/agent/Timeline.tsx + CurrentTopicLabel.tsx + lib/announce.ts + the live region in HostShell + tests; AgentChat rewire (show the exact blocks to delete by line range and the new JSX; the live in-flight assistant block stays as-is and is appended to the timeline on response_complete via useTimeline.appendLive built from session state - define the mapping function turnFromSession(session, userMessage, response): TimelineTurn); TerminalTile replay-on-mount + InlineTerminals static chip + tests (mock '@xterm/xterm' with a class exposing open/write/reset/dispose/onData/loadAddon and assert write() received the pre-existing output). A20: a Playwright smoke under frontend/e2e/continuity.smoke.mjs using the playwright package if present in node_modules (check), otherwise document the manual check list; it must not be part of npm test. Keep every new component on canonical token classes (no hex, no tailwind palette colours like blue-500); cite the token names you use.
`, { label: 'plan:frontend', phase: 'Write', schema: PLAN }),
])

const [S, M, D, F] = parts
log('planners done: ' + parts.filter(Boolean).length + '/4')

phase('Verify')
const verdict = await agent('You are the consistency verifier for a four-part implementation plan. Do NOT call mcp__prep__* tools. Do NOT edit files. Read the contracts at ' + CONTRACTS + ' and the spec at ' + WT + '/documentation/design/continuous-conversation-and-watched-terminals-2026-08-26.md sections 3-8 and 11-14. Then check the four plan parts below against each other and the contracts: (1) every function/method/field/event/tool/endpoint name and signature used in one part matches its definition in another part or the contracts (list every mismatch with task ids and the exact fix); (2) any "Contract additions" declared by a part that other parts must know about; (3) placeholders (TBD/TODO/similar to/handle edge cases/steps without code); (4) spec coverage for Plan A scope (section 14 Plan A bullet): list requirements with no task; (5) ordering hazards (a task test importing something created in a later task). Be exhaustive and concrete. Return ok=true only if there are zero blocking mismatches.\n\n=== PART S ===\n' + (S ? S.markdown : '(missing)') + '\n=== PART M ===\n' + (M ? M.markdown : '(missing)') + '\n=== PART D ===\n' + (D ? D.markdown : '(missing)') + '\n=== PART F ===\n' + (F ? F.markdown : '(missing)') + '\n', { label: 'verify:consistency', phase: 'Verify', schema: VERDICT })

return { S: S ? S.markdown : null, M: M ? M.markdown : null, D: D ? D.markdown : null, F: F ? F.markdown : null, verdict: verdict }
