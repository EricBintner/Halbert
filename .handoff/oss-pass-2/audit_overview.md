# Audit overview — 17 units, confirmed = survived the combined origin+halbert refuter; (lost) = full audit text lost, titles from the orchestrator digest

## A01-promotion
SUMMARY: Halbert's port (continuity/promotion.py, wired at threads.py:355-360 and recall_memory.py:239-243) faithfully lifts the origin's *shape* — usage-fact signals, hashed queries, distinct-day recurrence, count/diversity gates, fail-soft persistence, signals only for surfaced evidence — and is stronger in one respect (it stores no content at all). It is materially weaker on the mechanics that make the origin's ranking trustworthy, and it has no consumer. Verified against OpenClaw code (short-term-promotion-{record,utils,store,apply}.ts, short-term-promotion.ts, memory-workspace-lock.ts, dreaming.ts, tools.ts, memory-forget.ts) and by a scratchpad reproduction: (1) rank_candidates is called by nothing and the Consolidator never reads the store, so 'memory graduates because it was useful' is not yet true (G1, high); (2) recency is a record-time snapshot that never ages, so decay is a no-op on p
- A01-promotion-G1 [confirmed/medium]: The ranker has no consumer: promotion signals are write-only and the Consolidator never reads them | fix: Phase A follow-through that does not touch the R9 fence: give Consolidator an optional promotion_store; in consolidate() call rank_candidates(list of PromotionC
- A01-promotion-G2 [confirmed/high]: Recency never decays: last_recalled_days_ago is a record-time snapshot, so 'decay multiplies ranking' is a no-op in production | fix: Persist last_recalled_at as an epoch (already available as updated_at) and derive last_recalled_days_ago at read time: signals(key, now=None) computes (now - la
- A01-promotion-G3 [confirmed/medium]: Persisted recall_count is discarded on reload and replaced by the distinct-query count | fix: In _load select recall_count (and query_diversity, recall_days) from promotion_signals and trust them; recompute only the derived set sizes as a consistency che
- A01-promotion-G4 [confirmed/medium]: No origin class on signals: guest/private-mode turns and system-written claims earn promotion evidence | fix: Add an actor/origin column (ACTOR_USER/ACTOR_AGENT/ACTOR_SYSTEM/guest) to promotion_signals, merged least-trusted-wins as the origin does; skip recording when _
- A01-promotion-G5 [confirmed/medium]: forget_request does not reach the promotion tables, and ERASURE_LIMITS does not say so | fix: Minimum: add the promotion tables to ERASURE_LIMITS. Better: store request_id (provenance.current_turn is already a ContextVar) on promotion_queries rows and ha
- A01-promotion-G6 [confirmed/medium]: No bounds anywhere: unbounded hash/day sets and tables, and unclamped score components let diversity swamp the ranking | fix: Clamp each component to [0,1] (utility = log1p(n)/log1p(10); diversity = min(1, d/5); spread = min(1, days/5); recency = clamp(exp(...))), cap the per-key hash 
- A01-promotion-G7 [confirmed/medium]: Weaker hard gates: no minimum score, no maximum age, diversity gate is 2 on queries only instead of 3 on max(queries, days) | fix: Add MIN_SCORE (calibrate after G2/G6 make the score bounded), MAX_AGE_DAYS (30) applied to the derived age, and raise the diversity gate to max(query_diversity,
- A01-promotion-G8 [confirmed/medium]: Recall-loop hygiene is a dead parameter: no caller ever marks recalled-derived content | fix: At the recall_memory site, consult the current turn's recalled_json / thread_receipt_block (available via the ThreadManager) and pass provenance='recalled_conte
- A01-promotion-G9 [confirmed/low]: No append-only memory event journal (recall.recorded / recall.skipped / promotion.applied) | fix: Append a recall.recorded row to the existing event ledger (continuity/timeline.py, the ratified event store per CD-5) from record_recall, and a promotion.ranked
- A01-promotion-G10 [refuted/low]: Cross-process lost updates: aggregates are computed in memory and upserted whole, so two processes overwrite each other's counts | fix: Make the SQL the source of truth for counters: UPDATE promotion_signals SET recall_count = recall_count + 1 ... inside BEGIN IMMEDIATE (the _write_txn pattern f
- A01-promotion-G11 [confirmed/low]: Query hash is not case-folded or whitespace-normalized, so trivial variants inflate diversity | fix: Hash ' '.join(query.split()).casefold() instead of the raw text.
- A01-promotion-G12 [confirmed/low]: Load path does not validate rows: one malformed day string silently degrades the whole store to in-memory | fix: Wrap the per-row parse in try/except, skip the bad row with a warning, and keep _conn attached; consider a one-time repair that deletes unparseable day rows.
- A01-promotion-G13 [confirmed/low]: No dangling-signal audit: signals outlive the thread or ledger key they describe | fix: Give rank_candidates (or the G1 consumer) a liveness predicate: thread:<id> must exist in the conversation store; (subject, predicate) must have an open row in 
- A01-promotion-G14 [confirmed/low]: Relevance (avg gate score) is accumulated but has zero weight in the ranking | fix: Add a clamped relevance term with a modest weight (origin: 0.30; Halbert could use 0.2 and rebalance) once G6 bounds the other components.
- A01-promotion-G15 [refuted/low]: Phase-signal (light/REM) boost and the dreaming sweep's repair-then-rank-then-apply cadence | fix: None; revisit only if the R5 harness un-gates an LLM consolidation pass, at which point a 'consolidator considered this key' counter is the analogue.
BUGS:
- [confirmed] Reload collapses recall_count to the number of distinct query hashes and corrupts the running average
- [confirmed] Recency snapshot never ages; updated_at is written and never read
- [confirmed] Score components are unclamped and negative days_ago boosts recency above 1
- [confirmed] avg_score is maintained but ignored by the ranker; the 'utility not confidence' test passes vacuously
- [confirmed] provenance='recalled_content' has no caller
- [confirmed] Private-mode / guest turns record promotion signals
- [confirmed] Singleton pins the data dir at first use, unlike the sibling recall path
- [confirmed] rank_candidates does not clamp limit; a negative limit silently drops the tail
- [confirmed] One malformed day row disables persistence for the whole process
- [confirmed] Second connection to state_ledger.db without the StateStore durability/secure-delete PRAGMAs

## A02-eval-harness
SUMMARY: The R5 harness ports the Hermes compaction-eval shape faithfully in three places (content-keyed question-bank invariance — stronger than Hermes's path-keyed cache; an always-present ceiling arm printed first; finish_reason=length as a never-stored failure) and the verdict contract ports gates-before-judge, the closed vocabulary, the evidence-demanding prompt and the parse/transport breaker split. The port is NOT as solid as the origin on the instrument's integrity: run_exam hands the answerer the gold answer (G1, high — the closed-book claim is only true for the built-in regex answerer), judge_score has no hedging check so any longer/negated/punctuated correct answer scores partial and the packet's own hedging-marker spec was dropped (G2), and there is no recovery arm although retrieval over receipts is Halbert's real production answer to a destroyed region (G3). The verdict module is a 
- A02-eval-harness-G1 [confirmed/high]: run_exam hands the answerer the gold answer — the closed-book constraint is only true for the built-in regex answerer | fix: Change run_exam to call answer_fn(question: str) (or a Question view without gold) and keep gold in the ExamVerdict only; update context_answerer to take the qu
- A02-eval-harness-G2 [confirmed/medium]: judge_score treats any longer answer containing gold as 'hedged partial' — no hedging check, so confident-correct, negated and punctuated answers all score 1 | fix: Normalise (strip punctuation/case), score 2 when gold is present and no hedge marker from _HEDGES precedes it and no negation ('not', 'never', 'isn't') is withi
- A02-eval-harness-G3 [confirmed/medium]: No RECOVERY arm: the harness never measures retrieval over the destroyed region, which is Halbert's actual production answer to consolidation loss | fix: Add recovery_policy(base_policy, index_over_region, top_k) that concatenates the base arm's retained text with a deterministic keyword query per question (token
- A02-eval-harness-G4 [confirmed/medium]: Gate continuation prompt keeps the HEAD of the gate output; the origin keeps the TAIL where the diagnostic is | fix: In _bound keep the last MAX_GATE_OUTPUT_CHARS characters (prefix with '[gate output truncated]\n'), and apply gate_output_limit before adding the marker so the 
- A02-eval-harness-G5 [confirmed/medium]: A gate that raises crashes verdict(); the origin converts it into a failed gate with a diagnostic | fix: Wrap gate.check in try/except Exception; treat an exception as a failed gate whose continuation prompt is the gate's output plus '[gate could not run: <type>: <
- A02-eval-harness-G6 [confirmed/low]: No gate retry exhaustion and no turn budget — a permanently failing gate (or an ok/garbage-alternating judge) continues forever | fix: Give JudgeCircuit (the one mutable object) per-gate attempt counters and a turn counter; after max_retries consecutive failures of the same gate return BLOCKED 
- A02-eval-harness-G7 [confirmed/low]: No timeout around the judge call — a hung local model stalls the turn instead of counting as a transport failure | fix: Add judge_timeout to verdict()/JudgeCircuit; run the judge under a deadline (thread + join(timeout), or require the wired judge to accept a timeout kwarg and ra
- A02-eval-harness-G8 [confirmed/low]: WAIT directive accepts pid <= 0, seconds == 0 and a bare WAIT with no target; the origin requires positive targets and downgrades a targetless WAIT to continue | fix: Require pid >= 1 and seconds > 0; treat a directive-less WAIT as CONTINUE (Hermes) or as a parse failure (consistent with Halbert's closed vocabulary) — either 
- A02-eval-harness-G9 [confirmed/low]: Breakers reset only on a clean verdict; a flaky transport interleaved with parse failures trips the parse breaker on non-consecutive failures | fix: In verdict(): on a transport failure set circuit.parse_failures = 0; on a parse failure set circuit.transport_failures = 0 (mirror goals.py:1281-1282). Keep the
- A02-eval-harness-G10 [confirmed/low]: Breakers are silently inert unless the caller keeps the JudgeCircuit; the origin persists the counters in session state | fix: Make circuit required whenever judge is not None (fail loudly), or keep a module-level default circuit keyed by id(judge). Persisting across process restarts ca
- A02-eval-harness-G11 [confirmed/low]: Per-question verdicts are computed then discarded; the committed rows cannot show which planted fact an arm lost | fix: Attach exam.verdicts to ArmRow (or a second verdicts-<date>.jsonl written by main()) and include source_turn so losses map back to region position.
- A02-eval-harness-G12 [confirmed/low]: Region messages are shared mutable objects across arms; a policy that edits content corrupts later arms' retained context | fix: Pass copy.deepcopy(region_messages) (or freeze EvalMessage with frozen=True) per arm invocation.
- A02-eval-harness-G13 [confirmed/low]: Scorecard rows lack the region's token count, so retained-fraction (kept %) is not derivable from the committed evidence | fix: Add region_tokens to ArmRow and a 'kept' column (retained/region) to the arms table.
- A02-eval-harness-G14 [confirmed/low]: 'FAILED-RETRYABLE' carries no retry — the origin retries a truncated summary (backoff or a one-shot larger-budget fallback) before giving up | fix: Give Arm an attempts budget (default 2) honoured by _run_arm for PolicyFailed(retryable=True); record attempts on the row.
- A02-eval-harness-G15 [confirmed/low]: No unchanged-workspace fingerprint: an identical failing gate is re-run every turn | fix: Optional fingerprint callable on JudgeCircuit/GateState; skip re-running a gate whose last failure fingerprint matches, replay its output, advance attempts (tie
- A02-eval-harness-G16 [confirmed/low]: Judge input is unbounded; the origin caps goal/response/contract text before the judge sees it | fix: Bound the rendered claims (tail-biased, like gate output) before calling the judge; expose the constant next to MAX_GATE_OUTPUT_CHARS.
- A02-eval-harness-G17 [confirmed/low]: Region-scoping tripwire only proves the arm receives the region; the origin also proves head and tail survive verbatim | fix: Assert immutability of thread.messages outside the region after every arm (cheap: compare content_digest of the outside slices before/after).
- A02-eval-harness-G18 [confirmed/low]: No age bound on a pid wait — the origin expires a pid/session barrier after 30 minutes | fix: Add a deadline field to WaitDirective populated by verdict() (now + min(requested, MAX_BARRIER_WAIT_S)) so callers park against a bounded deadline.
BUGS:
- [confirmed] MatrixReport.render() and main() crash with TypeError when an OK arm scored zero questions (recall is None)
- [confirmed] The '[gate output truncated]' marker never survives: _bound appends it after slicing, then verdict() re-slices to gate_output_limit
- [confirmed] judge_score exact-match is punctuation- and whitespace-fragile; a correct '47-29.' scores partial
- [confirmed] _run_arm catches only PolicyFailed; any other exception in a policy aborts the whole matrix with no rows written
- [confirmed] run_matrix derives the default region and the reported turns from threads[0] only
- [confirmed] ParseCircuitOpen and TransportCircuitOpen are exported and imported by the tests but never raised anywhere
- [confirmed] _HEDGES is dead code and the docstring above it claims the LLM and programmatic judges 'agree on what hedged means'

## A03-redaction
SUMMARY: The lift is architecturally sound where Halbert diverged on purpose (dispatch-level choke point, full placeholder instead of hinted masks, no off-switch, parity pin between the two surfaces) but the registry port is less solid than the origin on five mechanical invariants: (1) pattern-then-registry ordering leaks fragments of acked values that contain an IP/JWT/email shape (origin is exact-first everywhere); (2) sorted() insertion lets FIFO eviction drop the raw value and keep only encoded forms; (3) MIN length 4 vs 6 makes short acked values blanket-redact prose; (4) urllib.quote does not match the encoded forms real HTTP egress produces; (5) sequential replace can re-match its own placeholder. Two gaps are about coverage rather than the registry itself: json.dumps(default=str) stringifies bytes/objects AFTER mcp_response, so a non-dict handler result bypasses the Tier 2 boundary entire
- A03-redaction-G1 [confirmed/high]: Non-JSON-native values skip the choke point and are stringified after it (json.dumps default=str) | fix: Two layers: (1) in result_redaction._redact_value, coerce before redacting — bytes -> decode('utf-8','replace') then the string branch; any other non-native sca
- A03-redaction-G2 [confirmed/medium]: Registry pass runs after the pattern pass, so a pattern rewrite inside a registered value defeats exact matching and leaks the remainder | fix: Swap the order in _redact_value: registry first (exact forms are unaffected by later pattern passes, the reverse is not true), then redact_text. Also apply the 
- A03-redaction-G3 [confirmed/low]: Raw secret form is not registered newest; FIFO eviction can drop the live value while keeping only encoded variants | fix: In register(): iterate the encoded variants (excluding the raw) first, then insert/move the raw value last; do not sort. Optionally evict per value rather than 
- A03-redaction-G4 [confirmed/low]: MCP client credentials (bearer tokens, server env values, URL-borne secrets) are never registered in the variant registry | fix: At MCPAuthConfig.resolve_token (mcp/config.py:183) and at server-config load for env values, call get_global_registry().register(value) (plus the bare token if 
- A03-redaction-G5 [confirmed/medium]: MCP dispatcher catch-all redacts the error text with patterns only; the registry pass is skipped | fix: Introduce security/result_redaction.redact_error_text(msg) = get_global_registry().redact_text(redact_text(msg, prose=True)) (registry first per G2) and use it 
- A03-redaction-G6 [confirmed/medium]: Minimum registrable length is 4, not 6; four- and five-character acked values blanket-redact ordinary prose | fix: Raise MIN_SECRET_VARIANT_LEN to 6 to match the origin; if short acked values must still be protected, register them only with word-boundary matching (a compiled
- A03-redaction-G7 [confirmed/low]: Only urllib.parse.quote's encoding is registered; encodeURIComponent-style ('%2F') and quote_plus ('+' for space) surface forms evade the registry | fix: In _variants_of, register urllib.parse.quote(value, safe="-_.!~*'()") (encodeURIComponent-equivalent) and urllib.parse.quote_plus(value) in addition to quote(va
- A03-redaction-G8 [confirmed/low]: No circular-reference guard in the structural walker | fix: Thread an id()-keyed seen-set through _redact_value/_redact_dict (add on entry, remove on exit, as the origin does so shared non-cyclic references still redact)
- A03-redaction-G9 [confirmed/low]: Log lines are not redacted; the dispatcher logs the raw exception before returning the redacted error | fix: Give JsonFormatter a format() that applies redact_error_text (G5's helper) to the rendered message and to string fields; Hermes's RedactingFormatter (agent/reda
- A03-redaction-G10 [confirmed/low]: Redaction seams fail open on exception, and the unsynchronized global registry makes that path reachable | fix: Add a threading.Lock to SecretVariantRegistry guarding register() and the cache rebuild (snapshot the tuple under the lock, scan outside it). Make verbose_text 
- A03-redaction-G11 [confirmed/low]: Sequential per-form replacement lets a later (shorter) form match inside an earlier replacement | fix: Build one compiled alternation of re.escape(form) sorted longest-first (rebuilt with the cache) and do a single re.sub with the placeholder; a single pass canno
- A03-redaction-G12 [refuted/low]: Display seam applies only the exact-value registry, not the pattern set | fix: Apply get_global_registry().redact_text then redact_text(prose=True) in verbose_text (registry first per G2), or record explicitly in DECISIONS.md that the loca
BUGS:
- [confirmed] Serializer coercion happens after the choke point (default=str): bytes, Path, Enum, dataclass and any object with __str__ bypass redaction
- [confirmed] Raw form can be evicted before its encoded variants because variants are inserted in sorted() order
- [confirmed] Placeholder self-collision: a registered value that is a substring of '<secret>' corrupts every marker
- [REJECTED] Process-global registry has no lock and the reply/tee seams fail open on exception
- [confirmed] mcp_args_preview and _absent_reason bypass the registry
- [REJECTED] Reply-seam registry pass is gated on an 80-character echo-guard match, so short acked values are never registry-redacted in assistant replies

## A04-execute-code
SUMMARY: Halbert's execute_code faithfully ports the Hermes enforcement core (generated read-only stub module from an intersection, per-run secret token, fixed token→allow-list→budget→dispatch→log order with free refusals, capped head/tail stdout with a write-through spill, tmpdir/sys.path hygiene) and is stronger on policy: every stub call traverses ToolExecutor.execute with the guest floor and RoleGate cap, refusals carry the interactive audit shape, an empty intersection fails closed, and the stub module cannot be shadowed. The port is weaker exactly where Hermes relies on owning a child process. Three high gaps follow from the in-process thread model without compensating logic: (G1) the monitor loop has no deadline, so any script that swallows or never reaches the injected KeyboardInterrupt (an `except BaseException` retry wrapper, input(), Event().wait()) makes run_script never return and we
- A04-execute-code-G1 [confirmed/high]: The run never returns when the worker thread survives the KeyboardInterrupt injection — the monitor loop has no deadline | fix: Make the first loop break as soon as `timed_out` is set, keep the 10 s grace loop, then abandon the daemon thread: return the timeout error, retire the host (G2
- A04-execute-code-G2 [confirmed/high]: No post-settle disarm of the dispatch hook: a thread the script left behind can dispatch tools under the old session/role after the run ended | fix: Add `ScriptRpcHost.retire()` (sets `self._retired = True`); make `handle()` check it first and return `{'error': 'the script run that created this tool handle h
- A04-execute-code-G3 [confirmed/high]: Unbounded spill file and output-as-activity: an infinite print loop never times out and writes to disk until the volume is full | fix: Add MAX_SPILL_BYTES (5 MiB like the origin): once exceeded stop writing to the spill (append one '[spill capped]' marker), stop counting output as activity, and
- A04-execute-code-G4 [confirmed/medium]: Script stdout and traceback are not redacted before entering the model context and conversation store | fix: In run_script, pass the result dict through `security.result_redaction.redact_result()` (structural + text passes) before returning, and strip ANSI. Leave the 0
- A04-execute-code-G5 [confirmed/medium]: __name__ is '__halbert_script__', so the `if __name__ == "__main__":` idiom runs nothing and the run silently succeeds with empty stdout | fix: Use `__main__` (and add `__builtins__` explicitly as the origin does); update the pinned test. If a distinct name is wanted for introspection, expose it as a se
- A04-execute-code-G6 [confirmed/low]: Partial stdout is discarded (and the spill deleted) when the script raises — the model must rerun to recover work already done | fix: Return the capped `stdout`, `truncated` and `spill_path` alongside `error`/`traceback_tail` (the error stays structured; only the field set widens); keep the sp
- A04-execute-code-G7 [confirmed/low]: The schema never tells the model which tools exist in halbert_tools, and a missing-stub failure carries no hint | fix: Derive the execute_code schema description per turn from `derive_script_tool_sets(executor)` (the executor already rebuilds schemas per guest state) listing the
- A04-execute-code-G8 [confirmed/medium]: Script stderr is not captured: warnings and library errors go to the server log and never reach the model | fix: Swap sys.stderr for a second capture (10 KiB head/tail, in-memory only, no spill) for the run's duration; return it as `stderr` when non-empty; restore idempote
- A04-execute-code-G9 [confirmed/medium]: A user stop or turn cancellation does not interrupt a running script | fix: Give run_script a stop predicate (a ContextVar-bound callable the state machine sets to `lambda: self.cancelled.get(session_id)`) polled in the monitor loop, an
- A04-execute-code-G10 [confirmed/low]: SystemExit is reported as a script error with a traceback | fix: Catch SystemExit separately: code None/0 → success; non-zero → structured error `exit_status` without a traceback tail.
- A04-execute-code-G11 [confirmed/low]: Budget check-then-decrement is not atomic across script threads | fix: A `threading.Lock` in ScriptRpcHost around the budget check + decrement + counter increment (dispatch itself stays outside the lock).
BUGS:
- [confirmed] The deterministic AST gate misses obvious spellings of the very affordances it names
- [confirmed] The monitor keeps injecting KeyboardInterrupt while the worker is unwinding, and can abort the worker's own finally before `done.set()`
- [confirmed] An in-flight tool call counts as inactivity, so one slow tool call (> 30 s) kills a productive run
- [confirmed] Process-wide sys.stdout / sys.modules / sys.path mutation with no serialization — two overlapping runs corrupt each other and every other thread's pri
- [confirmed] ScriptRpcHost.handle() can raise into the script on a non-ASCII token, contradicting its own contract
- [confirmed] max_tool_calls=0 is treated as 'unset' and becomes the default 25

## A05-echo-display-digest
SUMMARY: The echo guard's detection core (80-char window, whitespace normalization, every-offset scan, bounded chunk store, hash-only warning) is a faithful port of boot-echo-guard.ts, and the Hermes display seam (1000/16 tail-kept cap, one factory seam, no raw copy shipped) is ported and in one respect stronger. The port is not as solid as the origins in three places that matter in Halbert's own single-user setting: (1) the redact step behind the guard only replaces whole registered forms, so a partial or re-wrapped echo of an acked secret is flagged, logged as redacted, and delivered anyway -- OpenClaw suppresses the whole text; (2) the display seam exists only on live SSE events -- a timeline reload serves persisted tool blocks with raw args/results, which OpenClaw's history projection never does; (3) the seam runs the exact-value registry but not the forced pattern pass Hermes applies. The tu
- A05-echo-display-digest-G1 [confirmed/high]: A partial or re-wrapped echo is flagged but not redacted; the warning claims redacted:true | fix: Give EchoGuard a deterministic redact(text) that replaces every maximal run of matched windows in the RAW text (build a regex from the matched window with \s+ b
- A05-echo-display-digest-G2 [confirmed/medium]: Timeline reload serves persisted tool blocks with raw args and results (no display projection on history reads) | fix: In get_timeline (the route choke point, keeping the store at full fidelity per display_transport.py:8-11) map each block through verbose_text for args/result an
- A05-echo-display-digest-G3 [confirmed/low]: Display seam runs the exact-value registry only; no forced pattern redaction | fix: Compose ingestion.redaction.redact_text (deterministic pattern pass) before the registry pass inside verbose_text, mirroring Hermes' force=True; keep the cap af
- A05-echo-display-digest-G4 [confirmed/medium]: Digest records only handler successes; no started/failed/cancelled status, and a non-zero-exit command counts as a success | fix: In ToolExecutor.execute record a started effect for WRITE_PLANE_TOOLS before invoking the handler and update its status in the success/timeout/exception branche
- A05-echo-display-digest-G5 [confirmed/medium]: Echo guard covers only the final-reply commit and top-level tee strings; tool display copies, the spoken digest tail, audit summary, and error/confirmation text bypass it | fix: Move _scrub_text (guard + registry, and G1's redact) into security/ and have verbose_text, spoken_tail's caller, StreamEvent.error and tool_confirmation_require
- A05-echo-display-digest-G6 [confirmed/low]: Spoken digest is unbounded (origin caps at 12 effects) | fix: Cap per-effect lines at 12 (counts line stays exact), append 'and N more'.
- A05-echo-display-digest-G7 [confirmed/low]: No structured truncated fact on capped tool events; nothing lets the UI fetch the full row | fix: Have _cap_text return (text, omitted_lines, omitted_chars); factories add result_truncated/args_truncated; leave fetch-full for the day the store keeps full res
- A05-echo-display-digest-G8 [confirmed/low]: Cap cuts mid-line and its header hides the char cut when both caps fire | fix: Mirror Hermes: compute start = max(line_start, len-max_chars); if start > line_start advance to the next newline; header f'[showing tail; omitted {lines} lines 
- A05-echo-display-digest-G9 [confirmed/low]: A redaction exception at the display seam propagates into the turn instead of failing closed | fix: Wrap the redact+cap in try/except returning '' (never the raw value) and log at debug.
- A05-echo-display-digest-G10 [confirmed/low]: No chat-input sanitizer (NFC, null-byte rejection, C0/DEL stripping) | fix: A 15-line sanitizer in the message route before process(); deterministic.
- A05-echo-display-digest-G11 [confirmed/low]: Window set lives for the process and is bounded per window, not per value: one large note can evict every earlier secret | fix: Store windows per value with a per-value cap (e.g. stride-sampled windows beyond 512) and a value-level FIFO; optionally forget a value when its egress ack TTL 
- A05-echo-display-digest-G12 [refuted/low]: Tee scrubs only top-level string values | fix: Recurse dict/list values the way verbose_text does.
- A05-echo-display-digest-G13 [refuted/low]: Bounded, generation-guarded retry owner for digest delivery | fix: n/a
- A05-echo-display-digest-G14 [refuted/low]: Effects that land after the tail is spoken (background tasks in the same turn) are dropped | fix: At turn end, if the digest grew after the tail was emitted, write a second audit rollup row ('late effects') -- speech is over, the chain is not.
- A05-echo-display-digest-G15 [confirmed/low]: Binary/media payloads are capped as text rather than replaced by a size fact | fix: In verbose_text, replace known binary keys (image, data, blob) with a byte-count string before capping.
- A05-echo-display-digest-G16 [refuted/low]: Display projection of provider-internal fields, usage/cost, and channel envelopes | fix: n/a
BUGS:
- [confirmed] echo_guard_flagged / tee_echo_guard_flagged log 'redacted': true unconditionally
- [confirmed] Streaming draft crosses the wire and renders before the guard runs
- [REJECTED] Short-value whole-text match contradicts the documented 80-char floor
- [confirmed] One oversized note evicts every earlier value's windows
- [confirmed] Both caps firing produce a header that names only the omitted lines
- [confirmed] record_effect treats a non-zero-exit run_command as a successful effect
- [confirmed] Spoken digest tail and its audit rollup bypass the registry and the guard

## A06-scheduler (lost)
REFUTER SUMMARY: 14 gaps checked against the origin code and the whole Halbert core/tests/.handoff; 10 stand, 4 refuted (G6, G12, G13, G14). All 7 halbert_own_bugs confirmed. Every origin file:line in the audit that I opened was accurate — Hermes cron/occurrences.py, jobs.py (claim_job_for_fire, _evaluate_due_job, _fast_forward_missed_recurring, _classify_dispatch_lateness, _write_marker, _claim_is_live), executions.py (MAX_TERMINAL_EXECUTIONS, _prune_unlocked, _owner_is_live), monitor.py errors='replace', scheduler.py (_apply_monitor_gate, tick flock, empty-response soft failure); OpenClaw timer-catchup.ts, timer-runnable.ts (#91944), startup-run-repair.ts, run-recovery.ts, run-receipt-store.ts, channel-health-monitor.ts and its clock-rollback test. Two audit failure scenarios contain a mechanism error that does not kill the gap but MUST be corrected before dispatch: in G2 the base-job 'recovery AND cat
- A06-scheduler-G1 [confirmed/high]: Occurrence-level idempotency is never wired: no dispatch path records or checks a scheduled instant — verdict: ORIGIN VERIFIED. Hermes cron/occurrences.py:1-36 is exactly as cited: scheduled_instant() canonicalises an aware ISO instant and completed_occurrence() queries executions for job_id+scheduled_instant+
- A06-scheduler-G2 [confirmed/medium]: Boot recovery re-run and boot catch-up are uncoordinated: an interrupted recurring job can be fired twice at one boot, concurrently, and neither run excludes the cron's own fire — verdict: ORIGIN VERIFIED. OpenClaw src/cron/service/timer-catchup.ts:43-72 (collectStartupCatchupJobs) skips any job where hasActiveCronRun(job) or findActiveCronRunReceiptInDatabase(...) is truthy. run-recove
- A06-scheduler-G3 [confirmed/high]: Slots missed while the process is alive but the machine is asleep are dropped by APScheduler's 60s misfire grace; catch-up exists only at process boot — verdict: ORIGIN VERIFIED. OpenClaw timer-runnable.ts:110-131: once hasScheduledNextRunAtMs(next) && nowMs >= next the job is runnable with no lateness bound (the only suppressor is an already-completed slot). 
- A06-scheduler-G4 [confirmed/medium]: Clock rollback is mis-classified as an exhausted restart budget and escalates to persistent safe-mode, disabling every scheduled job — verdict: ORIGIN VERIFIED. OpenClaw channel-health-monitor.ts:95-99 pruneOldRestarts filters out isFutureDateTimestampMs stamps; :104-109 a future startedAt does not trap startup grace; :199-205 a future lastRe
- A06-scheduler-G5 [confirmed/medium]: receipts.json has no retention: every run appends a receipt (and an occurrence) and every write rewrites and fsyncs the whole file — verdict: ORIGIN VERIFIED. Hermes cron/executions.py:27 MAX_TERMINAL_EXECUTIONS = 1000 and :128-136 _prune_unlocked deletes only rows in ('completed','failed','unknown'), newest-first with LIMIT -1 OFFSET N — i
- A06-scheduler-G6 [refuted/low]: Owner liveness is pid-only: a reused pid keeps a dead run 'running' forever and boot recovery never fires for it — verdict: ORIGIN VERIFIED (Hermes cron/executions.py:47 process_started_at column, :107-118 _owner_is_live compares _process_start_time(pid) to the stored value and fails safe when it cannot prove death; OpenCl
- A06-scheduler-G7 [confirmed/low]: Monitor-hash gate is applied only to boot catch-up, never to the regular cron fire, so the baseline goes stale and the idle-cost benefit is unrealised — verdict: ORIGIN VERIFIED. Hermes cron/scheduler.py:1307-1344 _apply_monitor_gate, docstring 'Must run BEFORE any agent machinery so an unchanged tick costs no LLM/delivery', called on every fire; cron/monitor.
- A06-scheduler-G8 [confirmed/low]: The closed status set has no producers beyond ok/error and no consumer: delivered_to_user, delivery_failed and blocked_config are dead vocabulary — verdict: ORIGIN VERIFIED in spirit and in one exact line: Hermes cron/scheduler.py:2992-2995 downgrades d.success when the final response is empty ('Empty final_response is a soft failure so last_status is not
- A06-scheduler-G9 [confirmed/medium]: A corrupt receipts.json takes the whole scheduler down at construction (no containment), while a corrupt monitor store is contained — verdict: ORIGIN VERIFIED. Hermes cron/occurrences.py:28-36 catches every ledger exception, logs a warning and returns False (keep the occurrence eligible) rather than propagating; cron/scheduler.py:3563-3575 w
- A06-scheduler-G10 [confirmed/low]: No schedule-activation guard: a slot computed from an edited morning_report time can be served as 'missed' although it never existed under the old schedule — verdict: ORIGIN VERIFIED verbatim: OpenClaw timer-runnable.ts:47-54 — 'Slots computed from freshly edited scheduling inputs never existed before those inputs took effect, so they are not missed runs. lastRunAt
- A06-scheduler-G11 [confirmed/low]: No on-disk scheduler liveness or lateness markers: 'scheduler thread dead' is indistinguishable from 'nothing due', and a late catch-up leaves no operator-visible trace — verdict: ORIGIN VERIFIED. Hermes cron/jobs.py:1128-1147 _write_marker + record_ticker_heartbeat, whose docstring names the exact need ('so `cron status` can tell "alive but failing" from "firing"', #32612/#328
- A06-scheduler-G12 [refuted/low]: One-shot retirement diagnostic and skip-mode are policy-only: no production caller can reach RETIRE or ADVANCE_ONLY — verdict: ORIGIN VERIFIED (Hermes cron/jobs.py:2845-2857 _retire_expired_oneshot in the live due scan; OpenClaw timer-catchup.ts:98-117 honours cronConfig.skipMissedJobs, recomputes next_run and logs 'cron: ski
- A06-scheduler-G13 [refuted/low]: Multi-instance receipt adjudication, foreign-receipt monitors, external-worker handoff fences and cross-process tick locks are not ported — verdict: ORIGIN VERIFIED (OpenClaw timer-scheduler.ts:190-197 recoverNonTerminalCronRunReceipts + enrollForeignReceipt; run-recovery.ts:75-92 receipt identity as the recovery CAS with 'superseded'/'live' verdi
- A06-scheduler-G14 [refuted/low]: Startup catch-up horizon is 7 days: a slot missed by more than a week is not served and not reported — verdict: ORIGIN VERIFIED (Hermes cron/jobs.py:2824-2842 _fast_forward_missed_recurring has no horizon at all — it fires once however stale and calls record_catch_up_occurrence(); jobs.py:1195-1197 bumps the ca
BUGS:
- [confirmed] Registration overwrites the per-job record, erasing last-run facts, and the catch-up run's completion lands on a different record — so every boot afte
- [confirmed] Task-level failures are recorded as 'ok' receipts and 'completed' state because the proactive tasks swallow exceptions and return {'status':'error'}
- [confirmed] After a wall-clock timeout the task thread keeps running with live side effects while the receipt says 'error' and the retry decorator starts a second
- [confirmed] Restart-budget hold on clock rollback escalates to persistent safe-mode with a contradictory 'restart_budget_exhausted' line
- [confirmed] Guardrail rejection leaves no receipt at all, so a 'rejected' run is invisible to the receipt-based liveness story
- [confirmed] _persist_restart_ledger keeps future-dated stamps and fsyncs the file but not its directory; _load_restart_ledger silently drops a malformed ledger
- [confirmed] Monitor probe output with undecodable filenames raises inside evaluate(), so the gate never persists a baseline and the catch-up runs ungated on every

## A07-interrupt-algebra
SUMMARY: The pure core is faithfully ported (generation claim under one lock, single-shot, decline-not-double-fire; steer into the last tool result; observable verdict for every arrival; stale-flag hygiene; a stronger no-tool-result fallback and a more truthful stop outcome). Around it, three things weaken the port materially in Halbert's own setting. (1) The steer path runs before every admission gate the voice/claims work added: an unidentified voice speaker or a raw POST can inject text into the owner's running admin turn with no receipt, no role, no origin tag, and the injected marker is bare, so the model is told nothing about its authority — G1/G4 (high/medium). (2) The stop verb is weaker than Hermes's in three ways the packet did not intend: '/stop' during a tool is demoted to a steer (Hermes reserves 'yield, never kill' for guidance and hard-kills on /stop), a stop cannot abort an in-fli
- A07-interrupt-algebra-G1 [confirmed/medium]: Mid-turn steer bypasses the voice relay receipt and every admission/claim gate: any unidentified voice (or raw POST) arrival can inject text into the owner's running admin turn | fix: Move the midturn call below the C1/C2 stamping block in routes/agent.py so the arrival's stamped modality/claim/role are known, and pass them to handle_midturn_
- A07-interrupt-algebra-G2 [confirmed/high]: An explicit /stop while a tool runs is demoted to a steer: the user cannot stop a running command with the verb the algebra names 'stop' | fix: In decide_midturn, restrict the tool-batch demotion to non-command text (Rule 3) and keep Rule 1 STOP unconditional; in _run_tool_streaming, poll self.cancelled
- A07-interrupt-algebra-G3 [confirmed/medium]: A steer accepted after a stop, or after the answer is committed, is confirmed to the user (steer_accepted) and then discarded; the origin refuses it so the surface queues it as the next turn | fix: request_steer refuses (accepted=False, reason) when self.cancelled.get(session_id) is set or when the turn's generation has moved past the start generation (sel
- A07-interrupt-algebra-G4 [confirmed/medium]: The steer marker is bare ('[steered] ') with no prompt-side contract: the model has no instruction that it is a user message with full authority, nor an anti-lookalike rule | fix: Give the marker an open/close delimiter pair generated per turn or reserved (as Hermes does) and add a one-paragraph note to the prompt assembly (Tier 0/1 confi
- A07-interrupt-algebra-G5 [confirmed/medium]: A stop cannot abort an in-flight model request; it lands only at the next yielded event | fix: Wrap each model call in a task the machine owns (asyncio.ensure_future) and make request_stop's _abort also cancel the current model task (store it on ctx as ct
- A07-interrupt-algebra-G6 [confirmed/low]: Two quick follow-ups: the second steer erases the first's words (replace-not-grow), where the origin concatenates and pins that no accepted steer is ever lost | fix: Make the slot concatenate (existing + '\n' + text) — apply_steer_to_results already supports it — and report merged=True / pending_count in steer_accepted. The 
- A07-interrupt-algebra-G7 [refuted/low]: A stop while the turn waits on a subagent neither reaches the wait loop nor cancels the subagent; the origin demotes interrupts around subagents and propagates hard stops to children | fix: In await_subagent_completion, check self.cancelled.get(session_id) each poll and, when set, call self.subagents.cancel(handle_id) and return; in request_stop's 
- A07-interrupt-algebra-G8 [confirmed/low]: No yield primitive: a steer typed during a long foreground command is parked behind it until the command exits | fix: Only after G5/redirect: add a per-execution yield flag on the tool executor (asyncio.Event on the ExecutionContext); request_steer sets it for the running execu
- A07-interrupt-algebra-G9 [confirmed/low]: A turn queued behind the lock cannot be stopped, and a stop of the running turn does not cancel queued turns | fix: Register queued sessions in a self._queued: Dict[session_id, asyncio.Event/flag] before awaiting the lock; _acquire_turn_lock re-checks the flag after acquiring
- A07-interrupt-algebra-G10 [confirmed/medium]: No turn-liveness watchdog: a wedged turn holds the one conversation's lock forever; only later waiters time out | fix: Add a monotonic activity stamp (ctx.last_activity, description) touched at the same seams Hermes touches (handler entry, each tool start/complete, each stream c
- A07-interrupt-algebra-G11 [confirmed/low]: A turn interrupted by a crash is healed at boot but never resumed; the origin auto-continues a fresh one with a bounded attempt count | fix: Founder decision first (a host steward re-running a turn that ran commands is a policy question). If accepted: on boot, for rows mark_interrupted() heals whose 
- A07-interrupt-algebra-G12 [refuted/low]: No global emergency stop: nothing pauses new autonomous work (scheduler, heartbeat, new turns) without killing in-flight work | fix: A sentinel file under Halbert's data dir (deterministic check, two os.stat calls) consulted by the scheduler's dispatch, the heartbeat tick, and the /message ro
- A07-interrupt-algebra-G13 [confirmed/medium]: The dashboard's stop button and unmount cleanup POST /api/agent/cancel (unconditional flag), bypassing the generation claim the packet lifted; the claim protects only a typed '/stop' | fix: Point the frontend's cancel() and unmount cleanup at /api/agent/stop, and make cancel_session delegate to request_stop for an in-flight turn (keeping its teardo
BUGS:
- [confirmed] An empty or whitespace-only arrival while busy is reported as steer_accepted and parks an empty string in the slot
- [confirmed] request_stop mirrors only half of cancel_session's paused rule: a turn paused on a confirmation while the lock is still held gets a flag nobody polls,
- [confirmed] A voice arrival that is steered leaves its single-use relay receipt unspent
- [confirmed] test_stop_and_redirect_share_safety pins nothing

## A08-store-hardening (lost)
REFUTER SUMMARY: 13 gaps checked against both sides; 10 stand, 3 refuted (G6 and G15 for architecture, G13 on the facts). Every origin claim held when read at the real lines, but the audit's line numbers are unreliable in both repos and must not be pasted into a packet unchecked: hermes_state_schema.py citations are shifted ~+150 (the FTS_STALE read is 836-843 not 1013-1022; _init_fts is 1020 not 1170; the file is only 1119 lines), hermes_state_fts.py's predicate is 301-308 not 312-321, preflight_db_writability is 532 not 428-470, _WRITE_PATIENCE_S is 349 not 23, and on the Halbert side upsert_receipt is 2339-2367 (not 2650), search_snippets 2570-2600 (not 2530), forget_request 2073-2103 (not 2129), append_message's user row is threads.py:407-415 (not 352-357). Substance re-verified at the true lines in every case. Four findings were confirmed by running code rather than reading it: a corrupt messages_ft
- A08-store-hardening-G1 [confirmed/medium]: Bare SQLITE_CORRUPT is classified as FTS-scoped; the store fails open and keeps committing into a structurally damaged file instead of quarantining — verdict: ORIGIN (line numbers shifted, substance exact): hermes_state_fts.py:301-308 `_is_fts_write_corruption_error` returns SQLITE_CORRUPT_VTAB when errorcode is present and otherwise requires `msg.startswit
- A08-store-hardening-G2 [confirmed/high]: Reopening over a really corrupt index bricks the store: the open path touches messages_fts before it reads the breadcrumb and only catches OperationalError — verdict: ORIGIN (cited lines shifted ~+150; behaviour verified at the real lines): hermes_state_schema.py:836-843 reads FTS_STALE_KEY from state_meta BEFORE any FTS DDL and calls `_drop_all_fts_triggers(cursor
- A08-store-hardening-G3 [confirmed/high]: There is no exit from fts_degraded: rebuild_fts has no caller, so search stays LIKE-only and redact_message fails with 500 forever — verdict: ORIGIN: hermes_state_schema.py:474-488 `_recover_stale_fts` rebuilds the stale index at open under admission; :490-537 `retry_deferred_fts_recovery` is an explicit in-process housekeeping-tick retry w
- A08-store-hardening-G4 [confirmed/medium]: Read-path corruption and receipts_fts corruption never enter fail-open; a corrupt receipts_fts rolls back the canonical receipt write on every turn — verdict: ORIGIN: hermes_state_search.py:871-891 `_match_rows` catches the corruption-parent DatabaseError on a MATCH READ, calls `_enter_fts_fail_open` and answers from canonical rows ('a live search never run
- A08-store-hardening-G5 [confirmed/medium]: WAL is enabled unconditionally on a SQLite the origin classifies as WAL-reset-vulnerable (measured 3.39.4 in the Halbert venv) — verdict: ORIGIN: hermes_state_wal.py:121-124 `is_sqlite_wal_reset_vulnerable` citing https://sqlite.org/wal.html#walresetbug; :178-181 'WAL-reset-bug builds never enable WAL on non-WAL files ... Gate deliberat
- A08-store-hardening-G6 [refuted/low]: Silent WAL refusal is not detected (the pragma's returned row is discarded) — verdict: ORIGIN holds: hermes_state_wal.py:232-240 'PRAGMA journal_mode=WAL RETURNS the resulting mode: macOS NFS, SMB/CIFS and the AgentFS overlay refuse WITHOUT raising. Trust the row, not the absence of an 
- A08-store-hardening-G7 [confirmed/medium]: A store that fails to open degrades silently and permanently: one WARNING, then every write returns None with no user-visible cause, no writability preflight, no recorded init error — verdict: ORIGIN (lines shifted; all four mechanisms found): hermes_state_guard.py:128-145 module-level `_last_init_error` / `_set_last_init_error` / `get_last_init_error` ('surfaced by /resume-style slash comm
- A08-store-hardening-G8 [confirmed/medium]: A zeroed or non-database file at the store path bricks the store on every boot instead of being quarantined aside so a fresh store can open — verdict: ORIGIN (lines shifted ~+15/+25): hermes_state_dbfile.py:168-194 `is_zeroed_state_db` -- 0-byte or NUL header, refuses to probe non-regular files ('probing a FIFO would block until a writer appears') a
- A08-store-hardening-G11 [confirmed/low]: No journal_size_limit: after rebuild_fts or a large merge the -wal keeps the high-water mark of the largest transaction forever — verdict: ORIGIN: hermes_state_wal.py:29 `_WAL_SIZE_LIMIT_BYTES = 64 * 1024 * 1024` with the comment 'SQLite's default journal_size_limit is -1 (unlimited)'; :74-77 `_apply_wal_size_limit` -- 'With the default 
- A08-store-hardening-G12 [confirmed/medium]: No live-DB test-isolation guard: the conversation store's default path ignores HALBERT_DATA_DIR and pytest can open the production conversations.db — verdict: ORIGIN cited exactly right: hermes_state.py:184-215 `_ensure_test_isolation` raises before any connection/mkdir/pragma when a pytest-context process resolves a production DB; hermes_state_guard.py:1-4
- A08-store-hardening-G13 [refuted/low]: Writes have 5 s of SQLite busy_timeout and no application-level patience; a turn whose append misses the lock is silently dropped — verdict: ORIGIN holds (line shifted): hermes_state.py:349 `_WRITE_PATIENCE_S, _TRANSCRIPT_WRITE_PATIENCE_S, _ACTIVITY_WRITE_PATIENCE_S = 20.0, 60.0, 0.5`; :615-640 the open path waits out a sibling's lock with
- A08-store-hardening-G14 [confirmed/low]: CREATE UNIQUE INDEX idx_one_open_leaf is unguarded on every open; the demotion heal that makes it safe is version-gated, so any later duplicate-open bricks the store — verdict: ORIGIN (line shifted): hermes_state_schema.py:996-1019 `_ensure_unique_title_index` -- on IntegrityError it clears duplicate titles and retries the CREATE, and catches sqlite3.Error around the repair 
- A08-store-hardening-G15 [refuted/low]: No file-replacement / WAL-generation guard on writes — verdict: ORIGIN holds: hermes_state.py:906-935 `_ensure_db_file_generation` mints a once-per-file state_meta token plus a PRAGMA application_id stamp; hermes_state_dbfile.py:123-149 `iter_deleted_sqlite_sideca
BUGS:
- [confirmed] _is_fts_write_corruption_error contradicts its own docstring: a bare 'malformed' message is classified as FTS-scoped even when sqlite_errorcode says S
- [confirmed] add_open_loop and close_open_loop commit OUTSIDE the store lock on the shared connection, so they can commit another thread's half-built transaction
- [confirmed] The open-path FTS block and _fts_recover catch only sqlite3.OperationalError, so a real SQLITE_CORRUPT (sqlite3.DatabaseError) from the index escapes 
- [confirmed] The conversation store's default path ignores HALBERT_DATA_DIR while the state ledger honours it (CFG-1 applied to one store only)
- [confirmed] forget_request has no error handling: any non-corruption SQLite error escapes as a raw exception, unlike every other store method, and on a runtime wi
- [confirmed] StateStore.__init__ runs journal_mode=WAL / secure_delete / darwin pragmas unguarded, the exact failure shape conversation_sqlite documents as 'used t
- [confirmed] search_snippets returns [] while degraded instead of a LIKE fallback, so recall silently loses matching_messages exactly when the index is untrusted

## A09-channels-voice-ingress
SUMMARY: The channel registry, server-stamped claim clamp, relay receipts (single-use, TTL, transcript-bound), the D-6 role cap and the observe-only tee are solid and in several places stronger than either origin (fail-closed UNVERIFIED default, receipt-bound identity, echo-guard seam on the tee). The port is weaker than the origins in what happens to a voice transcript BEFORE and BESIDE the stamp: (1) high — the busy-verb path is decided from channel.busy_verbs alone before the receipt is consumed, so an unidentified speaker in the room can steer the owner's running admin turn, defeating the unknown-speaker cap; (2) medium — no duplicate-transcript suppression although the pipeline re-reads the same 10s ring window on every VAD onset and every uplink socket auto-submits; (3) medium — no silence-hallucination filter, so 'Thank you.' from a TV becomes a persisted turn and a spoken reply. Lower: te
- A09-channels-voice-ingress-G1 [confirmed/high]: An unidentified voice speaker can steer the owner's running admin turn: busy verbs bypass the speaker-claim clamp and the relay receipt is never consulted on the mid-turn path | fix: In routes/agent.py move the receipt consumption + stamped_voice_fields block above the handle_midturn_arrival call and pass the stamped role/claim strength into
- A09-channels-voice-ingress-G2 [confirmed/medium]: No duplicate-transcript suppression: one utterance can become two or more turns | fix: Add a deterministic suppressor to dashboard/voice_relay.py (normalize: whitespace collapse, punctuation strip, lower; window 12s; exact or difflib.SequenceMatch
- A09-channels-voice-ingress-G3 [refuted/low]: No STT hallucination filter: silence/TV can produce 'Thank you.' turns | fix: Add halbert_core/audio/speech/transcript_filter.py with a deterministic is_silence_hallucination(text) (Hermes's phrase set + repeat regex, whole-utterance matc
- A09-channels-voice-ingress-G4 [confirmed/low]: Tee payloads carry no monotonic sequence, no timestamp and no turn-id assertion, so a second screen cannot detect gaps or order after a reconnect | fix: In TurnEventTee.publish stamp seq (per process, monotonic int) and ts (time.time()) onto the cleaned payload before fan-out; in _tee_publish skip (debug-log) wh
- A09-channels-voice-ingress-G5 [refuted/low]: The voice relay is bound after the speech loop is already live, so an utterance completing in the boot window is silently dropped | fix: Define _relay_voice_turn (it only needs a coordinator reference for get_ingress) before coordinator.start() and assign coordinator.on_voice_turn = _relay_voice_
- A09-channels-voice-ingress-G6 [confirmed/low]: No wake-name/wake-word requirement when more than one person is present; VAD alone opens a turn | fix: Add a deterministic 'shared room' rule to the speech track: when the last N observations contained more than one distinct speaker embedding (or any unidentified
BUGS:
- [confirmed] process() silently treats an unknown modality as a typed admin turn, contradicting the route's fail-closed registry
- [confirmed] Relay receipt is orphaned on the mid-turn path and the steer carries no speaker provenance
- [confirmed] Tee echo-guard seam scrubs only top-level string values; nested containers bypass it
- [confirmed] weakest_claim([]) raises ValueError where the origin function is total
- [confirmed] A matcher hit whose profile lookup fails is recorded as a verified claim with no subject
- [confirmed] The speech track reads the 10s window at speech ONSET, not after end-of-speech

## A10-tts (lost)
REFUTER SUMMARY: 14 gaps checked against the origin code and the whole Halbert core/tests/.handoff; 7 refuted, 7 stand. Every origin claim I checked held (all 14 origin_claim_holds=true) — the audit read the origin honestly. What it missed is one structural fact that reshapes half the unit: HALBERT ALREADY HAS A HARD, DETERMINISTIC SPOKEN-LENGTH CEILING, and it sits upstream of everything the audit examined. The engine's VoiceRiskPolicy word budget (haloysius modality/resolver.py:73 DEFAULT=35, :81 UNVERIFIED=12, :89 MULTI_OCCUPANT=25; effective_risk_policy at :244-292) is enforced cumulatively across the spoken segments by demuxer._apply_risk_policy (demuxer.py:832-882, called from assemble_payload at :958-960) via enforce_word_cap, and Halbert wires it unconditionally (modality_wiring.py:349 AreaContext(multi_occupant=True); :418,428 passes ctx.voice_risk_policy into assemble_payload). I ran the real p
- A10-tts-G1 [confirmed/low]: Spoken summary (and spoken reply) are not sanitized for reasoning tags, tool-call scaffolding or control tokens — verdict: ORIGIN CONFIRMED. openclaw src/tts/tts-core.ts:139 (strictReasoningTags: true), :143 (sanitizeAssistantVisibleText over the joined text blocks), :152 (throw 'No summary returned' when it sanitizes to 
- A10-tts-G2 [refuted/low]: Spoken summarization blocks the event loop: a synchronous requests call plus up to 30 s of advisory-lock wait runs inside the async turn generator — verdict: ORIGIN CONFIRMED. openclaw src/tts/tts-payload.ts:207-213 awaits summarizeText, and tts-core.ts:107-113 bounds it with an AbortController; Hermes gateway/streaming_tts_consumer.py:189-190 carries the 
- A10-tts-G3 [confirmed/medium]: Fence scanner misclassifies code-heaviness: unclosed, indented/list-nested, blockquoted fences are not counted, and any marker anywhere closes a fence — verdict: ORIGIN CONFIRMED line by line: openclaw src/tts/speech-text.ts:18-36 (unwrapFenceContainer: blockquote depth loop, list indent 4-8), :38-51 (parseFenceOpener allows <=3 spaces after the container unwr
- A10-tts-G4 [refuted/low]: No deterministic spoken-length ceiling when the summarizer is unavailable or fails: the full reply is read aloud — verdict: ORIGIN CONFIRMED: openclaw src/tts/tts-payload.ts:206-237 (summarization disabled -> truncateUtf16Safe(text, maxLength-3)+'...'; summary threw -> same; summary over config.maxTextLength -> truncated),
- A10-tts-G5 [confirmed/high]: The TTS quality rules run on only one of Halbert's two speech egress paths; the Wyoming/Home Assistant path has its own stripper and no code-heavy fallback, no summarization, no unclosed-fence handling — verdict: ORIGIN CONFIRMED: Hermes tools/tts_text_normalize.py:1-5 ('Shared by explicit TTS calls, gateway auto-TTS, voice-mode streaming and the web dashboard'), :208-219 prepare_spoken_text as the one pipelin
- A10-tts-G6 [confirmed/low]: A barge-in on spoken playback is not fed back to the model: the next turn assumes the user heard the whole reply — verdict: ORIGIN CONFIRMED: Hermes tools/tts_streaming.py:41-59 — SPEECH_INTERRUPTED_NOTE, _INTERRUPT_TTL_S=120.0, module-level _interrupted_at, mark_speech_interrupted/take_speech_interrupted with the comment 
- A10-tts-G7 [confirmed/high]: The model is never told the spoken budget, so every long voice reply needs a second model call instead of being written short in the first place — verdict: ORIGIN CONFIRMED: openclaw src/tts/tts-settings.ts:350-383 buildTtsSystemPromptHint, line 374 exactly: `Keep spoken text <=${settings.maxLength} chars to avoid auto-summary (summary ${settings.summari
- A10-tts-G8 [refuted/low]: Time-to-first-audio: each segment is synthesized as one batch clip; no sentence-level chunking before Piper — verdict: ORIGIN CONFIRMED: Hermes tools/tts_streaming.py:1-8 ('this module owns the *provider* half so playback starts on sentence one') and :61-95 (SENTENCE_BOUNDARY_RE after .!? or a blank line; SentenceChun
- A10-tts-G9 [refuted/low]: Prosody rate is passed to Piper speed without a clamp — verdict: ORIGIN CONFIRMED but for a reason Halbert does not have: openclaw gateway/server-methods/talk.ts:504-519 validates rateWpm/175 strictly inside (0.5,2.0) and Hermes tools/tts_tool.py:267-272 clamps a p
- A10-tts-G10 [confirmed/low]: Speech-mode cleanup lacks punctuation-run collapse, symbol-run removal, table projection and bullet-glyph rules — verdict: ORIGIN CONFIRMED: openclaw src/shared/text/strip-markdown.ts:89-107 cleanSpeechText (drop lines that are entirely punctuation/symbols, strip leading bullet glyphs, collapse 2+ symbol runs, '....'->'..
- A10-tts-G11 [refuted/low]: Summary prompt does not delimit the untrusted reply text — verdict: ORIGIN CONFIRMED: openclaw src/tts/tts-core.ts:122-131 puts the instruction and the reply in ONE user message with the reply wrapped as `<text_to_summarize>\n${text}\n</text_to_summarize>` (line 129).
- A10-tts-G12 [confirmed/low]: No record of whether a spoken reply was summarized, truncated, or fell back, nor its latency — verdict: ORIGIN CONFIRMED: openclaw src/tts/tts-payload.ts:35-43 (module-level lastTtsAttempt + get/set) and :261-296 (both the success and failure records carry timestamp, success, textLength, summarized, pro
- A10-tts-G13 [refuted/low]: Auto-TTS skipped for spoken text under 10 characters — verdict: ORIGIN CONFIRMED: openclaw src/tts/tts-payload.ts:194-196 (`if (!explicitTtsText && ttsText.trim().length < 10) return nextPayload;`) and :243-245 (the same check on the normalized text). The audit it
- A10-tts-G14 [refuted/low]: Channel path skips audio entirely for code-heavy replies instead of speaking the fallback line — verdict: ORIGIN CONFIRMED: openclaw src/tts/tts-payload.ts:202-206 returns without audio for a code-heavy reply, with the comment 'Skip noisy voice-note audio instead of telling channel users to look at a scre
BUGS:
- [confirmed] modality_resolved.speech_text and the spoken segments disagree for a code-heavy reply: the record never shows the fallback line the voice actually spe
- [confirmed] tests/test_speech_summarizer.py::test_short_reply_never_invokes_the_summarizer is vacuous — the summarizer IS invoked and the AssertionError it raises
- [confirmed] Docstring misattributes spoken summarization to Hermes; Hermes never summarizes, it chunks
- [confirmed] The 12 s summary timeout is not the real bound: the GPU advisory lock can add 30 s before the request starts, on the event loop

## A11-permission-lattice (lost)
REFUTER SUMMARY: All 12 gaps survive verification: every origin claim was confirmed by reading the cited TypeScript/Python myself (only two line refs were off — exec-approvals-core.ts:297 not :296, and role_gate.py's "recorded, never enforced" comment sits at 248-251 not 242-246), and Halbert genuinely lacks each behaviour after grepping the whole core, the test tree and the .handoff packet/design docs. Nothing refuted; four severities move. Kept high: G1 (ask_every_use is dropped at accept_profile's one writer and AskPolicy is enforced nowhere in the core — grep finds only a log line and axis_floor's constant OFF on both branches) and G2 (reproduced: 21 GRANTED profile rows make require() raise a bare ValueError instead of Denied — the audit's "24" counts 3 DENIED rows that never reach Scope.from_mapping). Lowered: G3 high→medium (D3-P5 task 2 already schedules "revoke stops" per subsystem and require()
- A11-permission-lattice-G1 [confirmed/high]: The ask axis is recorded, never enforced, and profile ask rows degrade to unconditional grants in the ledger — verdict: ORIGIN VERIFIED. openclaw src/infra/exec-approvals-policy.ts:11-29 is verbatim `if (params.ask === "always") return true;` then `if (params.durableApprovalSatisfied === true) return false;` then the o
- A11-permission-lattice-G2 [confirmed/high]: An unreadable persisted grant scope escapes require() as an untyped ValueError instead of a typed Denied — verdict: ORIGIN VERIFIED. agent-tools.before-tool-call.policy.ts:439-450 is a catch returning {blocked:true, kind:"failure", deniedReason:"plugin-before-tool-call", disposition: resolveToolErrorDiagnostic(...)
- A11-permission-lattice-G3 [confirmed/medium]: Consent narrowing after mint never reaches an open lease; nothing re-checks policy between evaluation and use — verdict: ORIGIN VERIFIED. exec-approvals-authorization.ts:34-140 assertCurrentUsageAuthorization re-resolves policy from file at commit; `if (security === "deny") throw new Error("Exec approval changed before 
- A11-permission-lattice-G4 [confirmed/medium]: text_shown_sha256 is accepted unverified — Gate 4's evidence field can hold a digest that resolves to no shipped wording — verdict: ORIGIN VERIFIED. capability-consent.ts:216-219 `if (acknowledgment.reviewToken !== currentReview.reviewToken) throwManagedPluginCapabilityConsentRequired(currentReview)` after re-reading the declared 
- A11-permission-lattice-G5 [confirmed/low]: A grant hashed to superseded wording stays live after the copy is widened; no re-consent rule exists — verdict: ORIGIN VERIFIED. capability-summary.ts:124-135 resolveAcceptedSurfaceCurrent makes an acceptance current only while acceptedSurfaceHash still equals computeDeclaredSurfaceHash(declared) (called at cap
- A11-permission-lattice-G6 [confirmed/low]: A Stop is not persisted and resume() is callable by any in-process code with no authority — verdict: ORIGIN VERIFIED. hermes tools/approval.py:43-45 verbatim '# Frozen at import: reading os.environ per call would let any skill running in the process set this and bypass every approval check (prompt-in
- A11-permission-lattice-G7 [confirmed/medium]: The Attentive/Present line ('it is whether anyone asked') is data only: require() mints turn-scoped grants with no turn, and QUIET is never computed — verdict: ORIGIN VERIFIED. hermes tools/approval.py:434-486 defines _Unattended + _SINGLE_QUERY_CTX/_CRON_CTX; 487-521 _unattended_deny blocks when ctx.mode()=='deny'; 808-831 the gate resolves every unattended
- A11-permission-lattice-G8 [confirmed/medium]: Leases have no expiry; max_session_lease is a datum the lease cannot even parse — verdict: ORIGIN VERIFIED with a framing note. exec-approvals-core.ts:297 `export const DEFAULT_EXEC_APPROVAL_TIMEOUT_MS = 1_800_000;` (the audit's :296 is off by one). exec-approval-lifecycle.ts:337-354 schedu
- A11-permission-lattice-G9 [confirmed/medium]: No primitive binds an approval to the digest of the artefact shown and re-validates it at apply time — verdict: ORIGIN VERIFIED verbatim. system-run-approval-binding.ts:283-315 snapshotFileOperandAtPath does realpathSync + statSync + sha256, and an unreadable file returns 'SYSTEM_RUN_DENIED: approval requires a
- A11-permission-lattice-G10 [confirmed/low]: The biometric 400-day TTL is a constant with no enforcement point in the writer — verdict: ORIGIN VERIFIED. hermes tools/approval.py:312-323 _persist_choice, docstring verbatim 'Tirith findings are session-max by design (no broad permanent allowlisting of content-level findings), so `always
- A11-permission-lattice-G11 [confirmed/low]: Lease revocation trips an event but runs no cleanups; a thread blocked in a capture call never exits — verdict: ORIGIN VERIFIED verbatim, whole 38-line file read. capability-lease.ts:9-21 retain(cleanup) runs the cleanup immediately then throws via assertActive when the lease is already inactive, otherwise regi
- A11-permission-lattice-G12 [confirmed/medium]: The widening bar is a string claim: surface, principal kind and authn are caller-supplied and never bound to the transport — verdict: ORIGIN VERIFIED. exec-approval-manager.types.ts:23-36 carries requestedByConnId/DeviceId/ClientId/deviceTokenAuth under the verbatim '// Requester bindings prevent another client from replaying an app
BUGS:
- [confirmed] require() raises a bare ValueError for 24 of the 47 shipped profile rows because Scope.from_mapping rejects every profile scope key
- [confirmed] ProfileProposal.ask_every_use is dropped at acceptance, so an ask row is written as an unconditional GRANTED
- [confirmed] Split clock in require(): the granted-scope fold honours the injected `now` but effective_capability folds consent with the wall clock
- [confirmed] Lease.__enter__ raising leaves the lease registered forever — the 'structurally honest' indicator can lie
- [confirmed] Projection content is derived outside its flock and `prior` outside the append lock — a benign two-writer race persists a stale projection and the nex
- [confirmed] halt=None is the one fail-open default in an otherwise deny-by-default evaluator
- [confirmed] record_from_payload catches KeyError/ValueError only; a malformed principal or count raises TypeError and escapes ConsentUnavailable
- [confirmed] Reach-root prefix check never matches children of a root that is '/' (root + os.sep == '//')

## A12-claims-admission
SUMMARY: The port is faithful where it was scoped: the four-rank ladder, the >= floor, fail-closed defaults (stronger than the origin's 'asserted' default), raw-id hashing, the decision record shape, first-block-wins, and fail-closed-on-unwritten-policy (with a registry-completeness pin the origin lacks) are all in persona/claims.py, persona/admission.py and routes/guest.py, and the channel-ceiling clamp is a genuine strengthening. What is not as solid as the origin: (1) decisions are discarded after the HTTP response - there is no once-consumed evidence binding or audit receipt (G1); (2) the ladder has exactly one consumer with one hard-coded threshold (voice_role_ceiling) and meets_floor is dead code - no capability can declare a claim floor (G2); (3) _admit evaluates every gate after a block, so later gates run on denied facts and can 500 (G3); (4) SKIP/OBSERVE effects are advertised but dispa
- A12-claims-admission-G1 [confirmed/medium]: Admission decisions are discarded after the response - no once-consumed evidence binding or audit receipt | fix: Add an AdmissionReceipt (dataclass: receipt_id, route_id, occurred_at, admission, decisive_gate, reason_code, gate ids, identity_coverage enforced|attribution-o
- A12-claims-admission-G2 [confirmed/medium]: No per-capability minimum claim floor - the ladder has one hard-coded threshold and meets_floor is dead code | fix: Add a claim axis to the five-axis evaluator: a deterministic CAPABILITY_CLAIM_FLOORS table (capability -> ClaimStrength, default none) consulted by effective_ca
- A12-claims-admission-G3 [confirmed/medium]: _admit evaluates every gate after the first block; later gates run on facts the earlier gate denied and can raise | fix: In _admit, stop the walk at the first gate whose effect is BLOCK (append it, then break) so decide_ingress sees a prefix graph; document that gate builders may 
- A12-claims-admission-G4 [confirmed/low]: SKIP and OBSERVE gate effects are defined but decide_ingress dispatches them | fix: In decide_ingress, after the BLOCK scan, scan for the first SKIP/OBSERVE gate and return that admission with it as decisive; or remove the unused constants so t
- A12-claims-admission-G5 [confirmed/low]: Reason codes are an open string set; nothing pins that every emitted code has a human message | fix: Make reason codes a frozenset/Enum in persona/admission.py (REASON_CODES), have _block validate membership, and key _REASON_TEXT off the same enum so completene
- A12-claims-admission-G6 [confirmed/medium]: The talk door (POST agent turn) produces no IngressDecision; the relay-receipt failure path is a silent downgrade with no decision record | fix: Wrap the talk door's three guards as gate builders (channel_registry, voice_receipt, claim_stamp) and run them through the same _admit/decide_ingress used by th
- A12-claims-admission-G7 [confirmed/low]: No record of whether the claim ladder was evaluated or changed the outcome, beyond a WARNING log line | fix: Return (effective_role, ClaimObservation) from effective_voice_role and thread the observation into the executor's _audit entry and the denial reason text; feed
- A12-claims-admission-G8 [confirmed/low]: guest_homes.yml is renamed into place without fsync | fix: In _save: after yaml.dump, f.flush(); os.fsync(f.fileno()) inside the with-block, then chmod and os.replace (optionally fsync the directory fd on darwin).
- A12-claims-admission-G9 [refuted/low]: No 'pairing-required' admission state or challenge-code enrollment (TTL, max pending, lockout, salted-hash codes) | fix: None now; the packet's C3 deferral stands. If built, lift Hermes's PairingStore shape (salted hash, constant-time compare, lockout, approve-by-request-id) and a
- A12-claims-admission-G11 [refuted/low]: Gate.allowed and Gate.effect can disagree; decide_ingress consults only effect | fix: Add __post_init__ validation on Gate (allowed must equal effect != BLOCK) or make allowed a derived property; keep _allow/_block as the only constructors.
BUGS:
- [confirmed] Claim derivation failure on a voice turn binds no claim, and 'no claim bound' means 'no cap' - the ladder fails open on its own exception path
- [confirmed] The keepalive network round trip runs while offer()/heartbeat()/withdraw() hold the module RLock
- [confirmed] decide_ingress dispatches SKIP/OBSERVE gates; weakest_claim raises on an empty iterable
- [confirmed] The local-admin predicate accepts the string hosts 'localhost' and 'testclient'
- [confirmed] persona_id is interpolated into the sibling URL path without escaping
- [confirmed] The 'home:<netloc>' owner namespace for pulled sessions is not fenced from operator-chosen peer node ids

## A13-skills (lost)
REFUTER SUMMARY: All 13 gaps survive adversarial verification: every cited origin file:line was opened and implements the claimed behaviour, and every Halbert claim was re-checked against the whole core, the test tree and .handoff/DESIGN-SKILLS-SYSTEM-2026-09-07.md. Nothing is refuted. Four severities were lowered on verified grounds. G1 high->medium: the base classifier already covers the audit's own examples (safety.py:326-338 has '/boot/', :119/:134 block mkfs., injection_check blocks zpool destroy), so a malformed user file costs the entire skills plane silently but not the safety net -- the ValueError itself was reproduced live. G6 and G8 medium->low: both latent, since no bundled SKILL.md declares `requires` or `platform` and the skill_events consumers are SK-6/SK-7, which do not exist. G12 low with medium confidence: prompts/agent_prompts.py:45-67 already exposes a construction-time stable_prefix_
- A13-skills-G1 [confirmed/medium]: One malformed user skill file aborts the entire load — builtins and their safety bindings vanish — verdict: ORIGIN VERIFIED: openclaw src/skills/loading/local-loader.ts:97-104 catches an untyped `catch (error)` around parseSkillFrontmatter -> per-file 'invalid' diagnostic + return null; :61-72 same for read
- A13-skills-G2 [confirmed/medium]: No reload path: skill edits never reach a running daemon, while the model's <location> read sees the new file (catalog/Track-A split-brain) — verdict: ORIGIN VERIFIED: openclaw src/skills/runtime/refresh.ts:68 SKILLS_WATCH_DEBOUNCE_MS=250; :494-520 createSkillsPathWatcher builds a chokidar watcher per watched dir with awaitWriteFinish stabilityThres
- A13-skills-G3 [confirmed/medium]: The catalog block carries no consultation instruction; the model is never told to read a skill's <location> — verdict: ORIGIN VERIFIED: openclaw src/agents/system-prompt.ts:255-275 buildSkillsSection emits '## Skills' + 'Scan <available_skills>. Clear match: read exact <location> with `${readToolName}`; obey.' + 'Seve
- A13-skills-G4 [confirmed/medium]: Description is unbounded at parse and render; the 60-char routing discipline is neither enforced on the bundled set nor applied to ingested packs — verdict: ORIGIN VERIFIED: hermes agent/skill_utils.py:716-733 SKILL_PROMPT_DESC_LIMIT=60 and extract_skill_description always truncates to limit-3 + '...'; tools/skill_manager_tool.py:130-165 rejects at create
- A13-skills-G5 [confirmed/medium]: No load-time content scan: user-root and ecosystem skill bodies reach messages[0] and the read tool unexamined — verdict: ORIGIN VERIFIED: openclaw src/skills/security/scanner.ts:220-266 SKILL_CONTENT_RULES -- literal-secret rule plus prompt-injection-ignore-instructions, prompt-injection-system, prompt-injection-tool (r
- A13-skills-G6 [confirmed/low]: `requires` is parsed but never evaluated; the catalog is not the honest set of runnable skills — verdict: ORIGIN VERIFIED: openclaw src/shared/config-eval.ts:79-124 evaluateRuntimeRequires (bins all-of, anyBins any-of, env presence, config-path truthy) and :126-149 evaluateRuntimeEligibility (os gate appl
- A13-skills-G7 [confirmed/medium]: skill_events rows are durable, session-linked, and outside every erasure path; both origins keep no durable per-session skill receipt — verdict: ORIGIN VERIFIED: openclaw src/skills/runtime/run-usage.ts:6-31 keeps a per-run in-memory Map (MAX_TRACKED_SKILL_USAGE_RUNS=1024, pruned) and :52-61 consumeRunSkillUsage deletes the entry when consumed
- A13-skills-G8 [confirmed/low]: Stamped skill ids are process-local; every id-less skill (all of them today) gets a new identity each daemon start, so telemetry history cannot be joined across restarts — verdict: ORIGIN VERIFIED: openclaw src/skills/types.ts:55-62 documents skillFile as 'Canonical source SKILL.md path used as the lifecycle identity' and src/skills/discovery/command-specs.ts:178 sets `skillFile
- A13-skills-G9 [confirmed/low]: CC invocation flags `disable-model-invocation` and `user-invocable` are ignored, breaking the ecosystem semantics the format claims — verdict: ORIGIN VERIFIED: openclaw src/skills/loading/frontmatter.ts:227-237 resolveSkillInvocationPolicy reads `user-invocable` (default true) and `disable-model-invocation` (default false); src/skills/discov
- A13-skills-G10 [confirmed/low]: Explicit `/name` invocation bypasses the platform filter, and there is no operator disable list — verdict: ORIGIN VERIFIED: hermes tools/skills_tool.py:549-554 -- skill_view refuses 'not supported on this platform' (readiness UNSUPPORTED) before serving even an explicit by-name load, then refuses a disable
- A13-skills-G11 [confirmed/low]: Colon-rich or punctuation-leading descriptions (common in CC packs) make the whole skill unparseable instead of being recovered — verdict: ORIGIN VERIFIED: openclaw packages/markdown-core/src/frontmatter.ts:83-126 normalizeFreeformFieldAtError -- FREEFORM_TEXT_FIELDS = {description, read_when, summary}; it locates the parser error positi
- A13-skills-G12 [confirmed/low]: The cache boundary is a literal marker line found by text; the head length is never recorded, so volatile content containing the marker can forge a second boundary — verdict: ORIGIN VERIFIED: hermes agent/skill_commands.py:60-73 append_user_instruction returns the exact prefix string built at construction time ('Single construction site guarantees the prefix is a byte-pref
- A13-skills-G13 [confirmed/low]: Reserved-name set is a static list; tools registered at runtime (HA tools, MCP bridge) are not reserved — verdict: ORIGIN VERIFIED: hermes agent/skill_commands.py:346-350 calls the live `resolve_command(cmd_name)` against the core command table at scan time and skips auto-registration on a hit; openclaw src/skills
BUGS:
- [confirmed] Aliased YAML nodes in frontmatter are expanded by str()/_as_tuple — a 347-byte frontmatter yields a 52-million-character description (verified), a 447
- [confirmed] `budget_multiplier: .nan` (a legal YAML float) survives parse, poisons compose, and raises out of ContextAssembler.assemble, failing every turn that m
- [confirmed] skill_for_path misattributes reads for bare `<name>.md` skills and its tie-break is insertion order, not name
- [confirmed] Read receipts are written before the existence/size checks, so a failed read still records a consultation
- [confirmed] Skill protected_paths/blocked patterns compare unnormalized strings: `/./boot/x`, `//boot/x` and symlink aliases bypass a protected path; `/boot` also
- [confirmed] ComposedSkills.allowed_tools is merged, warns 'all tools denied', and enforces nothing — the design says it compiles onto the turn
- [confirmed] The truncation notice tells the operator to run `halbert skills list`, a command that does not exist
- [confirmed] Six of nine bundled SKILL.md descriptions exceed the 60-character discipline the design binds Halbert-authored skills to, and no test pins it
- [confirmed] extends: a child cannot set a scalar back to the dataclass default, and `requires` is replaced rather than unioned
- [confirmed] Tests read the developer's real ~/.config/halbert/skills through daemon_skill_dirs(), so their outcome depends on the machine's home directory

## A14-utility-slot-model
SUMMARY: Halbert's utility slot (model/utility_slot.py, D-7) faithfully ports the shape of Hermes's aux ladder — explicit declaration first, opt-in live-catalog rung, legacy dict, main-model floor, every rung fail-soft — and is stronger on two founder axes (no model names anywhere; the pick must be strictly cheaper than the anchor). The port is weaker than the origin on the operational invariants around the probe and the pick: the catalog fetch is uncached with no negative cache (Hermes memoises and negatively caches for 120 s), the opt-in is a code kwarg rather than an operator config knob (Hermes: auxiliary.<task>.prefer_fast_model default False; OpenClaw: explicit-empty disables), only 'embed' is disqualified (Hermes excludes thinking/vision/tts/batch siblings), and a failing utility model is never rescued by the next rung. Two findings are Halbert-specific and matter most: (G4, high) the ladd
- A14-utility-slot-model-G1 [confirmed/low]: No operator control over the catalog rung: the live probe cannot be disabled and there is no 'utility = my chat model, no heuristics' state | fix: Make the probe an operator opt-in gated in config as Hermes does: `prefer_fast` becomes 'caller asks AND config allows' (`utility_model.catalog_pick: bool`, def
- A14-utility-slot-model-G2 [confirmed/low]: Catalog probe is uncached and has no negative cache: a dead or slow endpoint is re-dialed on every resolution | fix: Add a per-(url, provider) memo in utility_slot: (entries, monotonic) with a success TTL (e.g. 300 s, the Hermes _ENDPOINT_MODEL_CACHE_TTL) and a failure TTL (e.
- A14-utility-slot-model-G3 [confirmed/medium]: Only 'embed' is disqualified: reasoning/thinking, vision (-vl), and other non-chat siblings of the anchor's family can be picked as the utility model | fix: Reuse the generic token detectors Halbert already owns: skip an entry when utils.reasoning.is_reasoning_model(name) or capabilities._VISION_TOKEN_RE / 'embed' /
- A14-utility-slot-model-G4 [confirmed/high]: The utility pick bypasses the locality/secure policy: a ':cloud' catalog sibling or a cloud-provider utility slot can receive the spoken copy of a secure turn | fix: Add `require_local: bool = False` to resolve_aux_model/_resolve_aux; when set, every rung's candidate must pass llm_config.is_local_model(model, url, provider) 
- A14-utility-slot-model-G5 [confirmed/low]: No size floor, no context screening, and tie order unpinned in the 'smallest sibling' rule | fix: Add a deterministic tie-break (name) and an optional `min_params_b` (default e.g. 1.0) and `min_context` kwargs to _catalog_pick; when Ollama /api/show data is 
- A14-utility-slot-model-G6 [refuted/low]: Ollama catalog probe is unauthenticated even when the saved endpoint carries an api_key | fix: Send the same Bearer header on the Ollama path when api_key is non-empty (mirror client.py:527-528).
- A14-utility-slot-model-G7 [confirmed/low]: A failing utility model is never rescued by the next rung / the chat floor at request time | fix: Let _resolve_aux accept an `exclude` set of (model, url) and have the summarizer retry once with the ladder re-run excluding the failed pick (bounded by the sam
- A14-utility-slot-model-G8 [refuted/low]: Side-task usage is not recorded anywhere (no per-task accounting chokepoint) | fix: Record each utility call through the existing OutcomeStore (model/outcome_store.py) with a `task` column, or emit a turn-tee event ('aux_call', task, rung, mode
- A14-utility-slot-model-G9 [confirmed/medium]: The catalog rung cannot rank OpenAI-wire catalogs: family split on ':' and a size regex that requires ':' make it Ollama-only despite the provider allow-list | fix: Derive the family token by stripping a generic size/variant tail (reuse capabilities._SIZE_TAG_RE-style [:\-_]\d+(\.\d+)?b and the org prefix before '/') and pa
BUGS:
- [confirmed] Legacy rung is dead after the first store read in the process
- [confirmed] task provenance is documented but not implemented
- [confirmed] Synchronous probe + summary run inside the async responding handler and stall the event loop
- [confirmed] Raw file read + YAML parse on every resolution even when the declared slot answers
- [confirmed] loader.py references yaml without importing it
- [confirmed] _same_family prefix rule fuses distinct families that share a >=3-char prefix

## A15-heartbeat-catchup-wiring (lost)
REFUTER SUMMARY: 12 gaps checked against origin code and the whole Halbert core/tests/.handoff set; 9 stand, 3 refuted. Standing, in dispatch order: G1 (a served catch-up writes only to the `<id>:catchup` record while registration blanks the parent via engine.add_job's overwrite, so the same slot is re-served on every restart — verified end to end; lowered high->medium because the monitor-hash gate absorbs the detector_sweep repeats and the visible residue is a duplicate morning report per restart inside 12 h); G2 (occurrence idempotency has zero production callers and no UTC canonicalisation — the mechanism G1/G3 need, and the module docstring claims a guarantee it does not deliver); G5 (ticker liveness markers — origin-verified and explicitly ruled in by F-4 Option A, still unbuilt); G4 (no schedule-change guard although the prior record already carries str(cron_expr)); G3 (recovery and catch-up can ar
- A15-heartbeat-catchup-wiring-G1 [confirmed/medium]: A served catch-up run is invisible to the next boot, so the same missed slot is re-served on every restart (duplicate morning reports) — verdict: ORIGIN verified: openclaw service.restart-catchup-schedule-change.test.ts:141-195 starts two successive services and asserts runCommandJob toHaveBeenCalledTimes(1) both times, with lastRunAtMs landing
- A15-heartbeat-catchup-wiring-G2 [confirmed/medium]: Occurrence-level idempotency was built but never wired: no receipt carries a scheduled_instant and nothing calls occurrence_completed — verdict: ORIGIN verified: hermes cron/jobs.py:2506-2516 (claim path checks completed_occurrence and advances next_run instead of firing), 2928-2934 (due scan does the same), 2943 (job['_scheduled_instant'] car
- A15-heartbeat-catchup-wiring-G3 [confirmed/low]: No active-run / pending-recovery / imminent-slot guard: the same task can run two or three times concurrently at boot — verdict: ORIGIN verified: openclaw timer-catchup.ts:61-70 skips a job when hasActiveCronRun(job) OR findActiveCronRunReceiptInDatabase(...); timer-scheduler.ts:59-60 and 171-173 both bail while state.startupCa
- A15-heartbeat-catchup-wiring-G4 [confirmed/medium]: A slot that predates a schedule edit is treated as missed (OpenClaw #91944) — verdict: ORIGIN verified: openclaw timer-runnable.ts:47-54 — 'Slots computed from freshly edited scheduling inputs never existed before those inputs took effect ... (#91944)' guarded by job.state.scheduleActiv
- A15-heartbeat-catchup-wiring-G5 [confirmed/medium]: No persisted liveness for the thread-tick heartbeat or catch-up counter: 'ticker dead' is indistinguishable from 'nothing due' (the Option A follow-up) — verdict: ORIGIN verified: hermes cron/jobs.py:1128-1152 (liveness every iteration, success marker only on a clean tick — 'a ticker stuck failing every tick would otherwise keep the plain heartbeat fresh and fa
- A15-heartbeat-catchup-wiring-G6 [confirmed/low]: A BaseException inside a tick silently ends the heartbeat task — verdict: ORIGIN verified: hermes cron/scheduler_provider.py:412-421 catches BaseException with an explicit incident comment — 'Catch BaseException (not just Exception) so a SystemExit from a misbehaving provid
- A15-heartbeat-catchup-wiring-G7 [confirmed/low]: Stopping the heartbeat does not join an in-flight sweep, so shutdown can close stores under a running consolidation — verdict: ORIGIN verified: hermes agent/periodic_scheduler.py:45-50 (cancel(wait) 'additionally blocks until an in-flight run of this callback finishes — the analogue of thread.join(timeout=wait)') and 71-76 (_
- A15-heartbeat-catchup-wiring-G8 [confirmed/low]: A slot whose run failed is treated as served forever; no replay after a backoff window — verdict: ORIGIN verified: openclaw timer-runnable.ts:121-127 counts a slot as served only when lastRunStatus is 'ok' or 'skipped'; 114-118 isErrorBackoffPending with 'Error retry windows are anchored at run en
- A15-heartbeat-catchup-wiring-G9 [confirmed/low]: RETIRE decisions never write the retirement diagnostic (dormant path) — verdict: ORIGIN verified: hermes cron/jobs.py:2845-2856 _retire_expired_oneshot — 'With no claim stamped, retire it with a diagnostic (never silently delete)' — calls _write_missed_oneshot_diagnostic before sc
- A15-heartbeat-catchup-wiring-G10 [refuted/low]: No user-facing switch for skip-missed mode — verdict: ORIGIN verified: openclaw timer-catchup.ts:98-117 — under cronConfig.skipMissedJobs the job's nextRunAtMs is recomputed and committed before returning ('Run history must not readmit the skipped occurr
- A15-heartbeat-catchup-wiring-G11 [refuted/low]: Immediate catch-up runs are timestamped with the pre-probe clock; a slow probe can push them past APScheduler's 60 s misfire grace — verdict: ORIGIN: I read timer-catchup.ts:145 — const baseNow = params.state.deps.nowMs(); inside commitStartupCatchupRows. It is a fresh clock read used to stamp DEFERRED nextRunAtMs with a stagger offset (145
- A15-heartbeat-catchup-wiring-G12 [refuted/low]: Startup catch-up runs are not sequential and not fenced from the live schedule (OpenClaw runs them one at a time under a timer fence) — verdict: ORIGIN verified: openclaw timer-catchup.ts:414-441 executes candidates in a sequential for/await loop with the comment 'Catch-up execution stays sequential'; timer-scheduler.ts:59-60 and 171-173 both 
BUGS:
- [confirmed] Fixed 1-second race between the delayed scheduler start and the proactive-job registration thread
- [confirmed] Satellite guard for catch-up and for the idle tick read different sources of truth
- [confirmed] The `<id>:catchup` and `<id>:recovery` job records accumulate in the SchedulerEngine directory and inflate get_status counts
- [confirmed] Retirement diagnostic writer and occurrence idempotency are shipped as dead code
- [confirmed] The monitor-hash probe persists a listing of ~/.ssh (names, sizes, modes, mtimes) into the data directory

## A16-session-tree-compaction
SUMMARY: What shipped is T1 (additive tree/compaction columns, an unwired move_leaf, the title CAS ladder) and P3c (atomic get-or-open, v5 one-leaf heal, idx_one_open_leaf). Those pieces are as solid as the origin where they overlap and stronger in two places: the leaf invariant is a DB constraint rather than a cursor, and summary text is defanged at the prompt choke point (origins do neither). The mechanism the unit is named for — compaction/rotation — is not ported: compact_boundaries has no writer, context is a fixed 12-row window plus a ≤1500-char deterministic receipt, and the T2/T3 projection, branch-summary entries, unresolved-request carry and anti-thrash logic exist only as columns and design text. That deferral is founder-gated (deterministic v0 vs LLM), so the high-value near-term items are the deterministic ones that need no model: the unresolved-request extractor (G2), a persisted in
- A16-session-tree-compaction-G1 [confirmed/medium]: No rotation writer: a long thread's older turns are hard-trimmed to a fixed 12-row window plus a 1500-char deterministic receipt, never summarized iteratively | fix: Ship T3 v0 deterministically (founder Q2): at end_turn when the thread's row count exceeds a threshold, write the design §3.1 TXN with a deterministic generatio
- A16-session-tree-compaction-G2 [confirmed/medium]: Unresolved user request is not carried: the 'open loop' comes from the assistant's last message, and an interrupted question is not preserved as an ask | fix: Implement design §4.3's deterministic extractor in the store (one SQL over messages by turn_id/status), write it to compact_boundaries.unresolved_request at rot
- A16-session-tree-compaction-G3 [confirmed/low]: context_included is dead on both ends: no writer can set it and no reader honours it | fix: Add `context_included: bool = True` to append_message and `AND context_included = 1` to recent_messages/pending_notes; leave list_messages (timeline) unfiltered
- A16-session-tree-compaction-G4 [confirmed/low]: A newer-than-supported conversations.db opens silently instead of refusing | fix: In _ensure_schema after reading `version`: if version > SCHEMA_VERSION, rollback, log 'store written by a newer build (v%d > v%d); refusing to open', set self._
- A16-session-tree-compaction-G5 [confirmed/medium]: An interrupted turn leaves a dangling user question in the replayed history with no persisted fact that it was cut short | fix: In end_turn, when status != 'complete' and no assistant row is written, append an origin='assistant' status='interrupted' row with a fixed marker content (fence
- A16-session-tree-compaction-G6 [confirmed/low]: Anti-thrash machinery absent while its columns exist: no cooldown, no merge-max, no recovery probe, no clock-jump guard | fix: When T3 is written: gate on the row columns, write cooldown with SQL `MAX(existing, new)`, clamp any stored deadline to now + cap on read, count a rotation whos
- A16-session-tree-compaction-G7 [confirmed/low]: Branch summaries are not persisted: returning to a subject replays only an ephemeral soft landing and a hint line | fix: T2 per design §2.3: inside move_leaf (or the manager's _reopen_thread until move_leaf is wired) insert a template-built branch-summary system row into the retur
- A16-session-tree-compaction-G8 [refuted/low]: No user-turn idempotency: a retried POST appends the same user row twice | fix: Accept an optional client idempotency key on begin_turn (metadata['idempotency_key']); if the newest human row of the open thread carries the same key and no as
- A16-session-tree-compaction-G9 [refuted/low]: Cross-process turn lease not built; correctness under a second store instance rests on the one-leaf index turning races into refusals | fix: Not a candidate to build now (design §3.3). Record: when a second process attaches, add a `leases(conversation_id PRIMARY KEY, holder, expires_at)` table keyed 
- A16-session-tree-compaction-G10 [confirmed/low]: Compaction ledger atomicity, empty-summary guard and 'nothing fits' typed error have no Halbert counterpart | fix: Add to the design's rotate TXN: refuse empty/whitespace summary, refuse when coverage_end_id <= previous boundary's coverage_end_id (no progress), and run the r
BUGS:
- [confirmed] The typed tree columns are never populated by the real writer: _open_new_thread records provenance only in metadata, so move_leaf's 'stamp only when p
- [REJECTED] move_leaf uses a deferred transaction where its sibling get_or_open_thread deliberately uses BEGIN IMMEDIATE
- [confirmed] resume_thread / _reopen_thread trust the caller's from_thread_id; with the one-leaf index a stale belief now fails closed as 'Could not resume' instea
- [confirmed] get_or_open_thread and create_thread report a one-leaf index violation as 'thread already exists'

## A17-mcp-boundary
SUMMARY: Halbert's MCP boundary is genuinely stronger than either origin on the parts it chose to own — the egress redaction choke point with the universal camera gate, protocol-version validation, serialized session-expiry recovery, per-call fail-closed classification when a server vanishes from config, diff-based refresh that keeps a down server's tools, the hardened HTTP server, and stdlib-only stdio/HTTP transports. The port is markedly weaker on the CLIENT-side trust boundary, i.e. what a remote server can do to the host: (1) stdio servers inherit Halbert's whole environment, including HALBERT_MCP_TOKEN and every other server's token, so a child can call back into Halbert's own authenticated MCP server (G1, high); (2) the stdio reader dies on any line over 64 KiB — verified empirically — so large results and large tool lists disconnect the server rather than being bounded (G2, high, own bug)
- A17-mcp-boundary-G1 [confirmed/high]: Stdio MCP servers inherit Halbert's entire process environment (tokens, API keys, the MCP bearer token) | fix: In StdioTransport.__init__ build env from a whitelist (PATH, HOME, USER, LOGNAME, LANG, LC_ALL, TERM, SHELL, TMPDIR, XDG_*) merged with the server's config env;
- A17-mcp-boundary-G2 [confirmed/high]: Any stdio JSON-RPC line over 64 KiB kills the transport (asyncio StreamReader default limit); a large tool result or a big tools/list disconnects the server | fix: Pass limit=MAX_STDIO_FRAME_BYTES (e.g. 16 MiB) to asyncio.create_subprocess_exec; wrap readline() in try/except ValueError: drain the rest of the oversized line
- A17-mcp-boundary-G3 [confirmed/medium]: mcp_config.yml is an unconfirmed arbitrary-command-execution path: write_file to it classifies MEDIUM on macOS and the health monitor launches the new command within one tick; no spawn-time suspicious-shape check | fix: (a) Add get_config_dir() itself (not just ~/.config) to SENSITIVE_PATHS and classify a write to mcp_config.yml / being.yml as CRITICAL for the agent (the dashbo
- A17-mcp-boundary-G4 [refuted/low]: No size cap on an MCP tool result (stdio after G2, and HTTP bodies) before it reaches the local model | fix: Add MAX_RESULT_CHARS (e.g. 400,000 — sized to local num_ctx, not Hermes's 2 M) in bridge.format_tool_result with a 40/60 head-tail split and notice; in HTTPTran
- A17-mcp-boundary-G5 [confirmed/medium]: Remote tool descriptions enter the system prompt unsanitized: no injection-phrase rewrite, no length cap, no Unicode TAG stripping (on descriptions or result text) | fix: Add mcp/metadata.py with sanitize_metadata_text (regex rewrite of the two override phrases, strip U+E0000–E007F except valid emoji tag sequences, cap at 1,200 c
- A17-mcp-boundary-G6 [confirmed/low]: Non-text content blocks (image/audio base64, resources, _meta) are JSON-dumped raw into the model-facing result | fix: Extend format_tool_result to project block types like OpenClaw mcp-content.ts:61-102: text passes; image -> a notice with mime and decoded size (and, when a vis
- A17-mcp-boundary-G7 [confirmed/medium]: MCP tool results carry no untrusted/network provenance into the agent, so nothing downstream can treat them differently from the host's own tool output | fix: Add a provenance field (source='mcp', server) to ExecutionResult set by the bridge handler path (executor knows the name starts with MCP_TOOL_PREFIX); in the st
- A17-mcp-boundary-G8 [confirmed/medium]: Tool annotations (readOnlyHint / destructiveHint) are ignored; write-capable remote tools auto-execute at the MEDIUM default unless the operator enumerates them | fix: Keep the MEDIUM default (plan decision) but add one deterministic rule between per-tool and per-server overrides: annotations.destructiveHint == true -> HIGH; o
- A17-mcp-boundary-G9 [confirmed/medium]: Halbert-as-MCP-server tools bypass the agent's policy pipeline (ToolSafetyFramework / RoleGate / permission lattice / consent ledger) with bespoke per-handler gates | fix: This is the recorded B6 audit (memory: app-ui-access-build-state) — do not rebuild. Strengthening within B6: wrap TOOL_HANDLERS dispatch in server.py:1256-1281 
- A17-mcp-boundary-G10 [confirmed/medium]: Closing or killing a stdio server only signals the direct child; npx/uvx grandchildren survive a config-change reconnect or a forced kill | fix: Spawn with start_new_session=True, record os.getpgid(proc.pid), and in close() escalate os.killpg(pgid, SIGTERM) -> wait -> SIGKILL instead of proc.kill(); keep
- A17-mcp-boundary-G11 [confirmed/low]: notifications/tools/list_changed is dropped; a server that adds or removes tools at runtime is not re-registered until a config edit or reconnect | fix: In StdioTransport._read_loop, on method == 'notifications/tools/list_changed' set a per-transport flag (tools_changed = True) via an on_notification callback; t
- A17-mcp-boundary-G12 [confirmed/low]: Two tools from the same server with the same (sanitized) name are both registered with a suffix, so a duplicate/alias cannot be fenced per tool | fix: In MCPToolRegistry.register, pre-count sanitized components per server and exclude (skip + warning) any component with count > 1; keep suffixing for cross-serve
- A17-mcp-boundary-G13 [confirmed/low]: No circuit breaker: every call to a hung or dead server pays the full per-request timeout (30 s) although the health monitor already knows the state | fix: In make_tool_handler, consult health.get_active_monitor().health_record(server) — if health in (DOWN, UNRESPONSIVE) and now < next_retry_at, raise MCPDisconnect
- A17-mcp-boundary-G14 [confirmed/low]: Backoff resets on an answered handshake alone; a flapping server (handshakes, then dies) is relaunched every tick forever | fix: Keep the handshake as 'connected' but do not zero the counters in _reconnect; reset them in _probe_succeeded (health.py:395-407) only — i.e. after the server su
- A17-mcp-boundary-G15 [confirmed/low]: HTTP hygiene: no DELETE session termination on close, URL scheme not validated at load, Mcp-Session-Id follows a cross-origin redirect, and the library-provided Authorization strip is untested | fix: Add a best-effort DELETE in HTTPTransport.close() when _session_id is set (timeout 5 s, errors logged); validate scheme in _parse_server; use a requests.Session
- A17-mcp-boundary-G16 [confirmed/low]: Pagination lacks repeated-cursor detection and a whole-list deadline/byte budget | fix: In _list_paginated keep a seen-cursor set and raise on repeat; track json-encoded bytes per page and raise past MAX_LIST_BYTES (e.g. 4 MiB); wrap the loop in on
- A17-mcp-boundary-G17 [refuted/low]: Stdio stderr is DEVNULL'd, so a failing server leaves no diagnostic anywhere | fix: Pipe stderr and drain it in a second reader task into a bounded ring buffer (e.g. last 4 KiB, 8 KiB line cap) — the drain removes the undrained-pipe hazard the 
- A17-mcp-boundary-G18 [confirmed/low]: No per-server include/exclude tool filter and no input-schema normalization; every advertised tool (first 64) rides in every turn unchanged | fix: Add tools.include / tools.exclude to MCPServerConfig (validated like tool_risk, matched with the existing components_match rule plus fnmatchcase for globs) and 
- A17-mcp-boundary-G19 [confirmed/low]: No malware preflight for package-runner servers (npx/uvx) before first spawn | fix: Optional, opt-in (it is a network egress of package names, so surface it as a Settings switch like web search): before the first launch of a command whose basen
- A17-mcp-boundary-G20 [refuted/low]: Bare provider-prefixed credentials (ghp_..., sk-..., 'Bearer x') in a peer's error text pass Halbert's keyword-driven redactor | fix: Add a small deterministic prefix-pattern pass (ghp_/gho_/github_pat_/sk-/xox[abp]-/AKIA/Bearer <token>) to client._redact (and reuse it in security/result_redac
- A17-mcp-boundary-G21 [refuted/low]: SSRF / private-network redirect guard and env-proxy routing on the MCP HTTP fetch path | fix: None. Record as deliberately not lifted.
- A17-mcp-boundary-G22 [refuted/low]: OAuth 2.1 / per-requester identity / auth profiles for HTTP MCP servers | fix: None now; if lifted later, the single-user shape is 'operator identity only' with a token file under get_config_dir(), never per-requester.
- A17-mcp-boundary-G23 [refuted/low]: Cross-process discovery lock, lifecycle lease, lazy start from a schema cache | fix: None; revisit if a second process (halbertd / CLI) ever registers MCP tools.
BUGS:
- [confirmed] StdioTransport dies on any JSON-RPC line over 64 KiB (asyncio StreamReader default limit), turning large results and large tools/list answers into 'st
- [confirmed] On macOS the agent's write_file can rewrite mcp_config.yml at MEDIUM (no confirmation) and the health monitor launches the new command within one tick
- [confirmed] Server-initiated 'ping' requests are answered with JSON-RPC -32601 instead of an empty result
- [confirmed] initialize advertises non-existent client capabilities
- [confirmed] Server-side HA handlers call asyncio.run() inside the tool handler, which raises if the MCP server is ever hosted on the dashboard's running loop (the
- [confirmed] MCPToolError text (server-controlled) is interpolated into the failed ExecutionResult without any of the result-side hygiene (no cap, no tag stripping
