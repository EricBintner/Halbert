## terminal-tools-subagents — 96 verified mechanisms from HM01, HM02, HM03, HM05, HM06, HM07, HM08, HM11, HM12, HM14, HM15, HM17, HM18, HM19, OC01, OC03, OC07, OC08, OC09, OC11, OC12, OC13, OC14, OC15, OC18, OC19, OC24

Path convention: Hermes origin paths are relative to `/Volumes/Thunderbolt/AI/OSS/hermes-agent/`, OpenClaw paths to `/Volumes/Thunderbolt/AI/openclaw/`, Halbert paths to `/Volumes/4TB-BAD/Halbert/halbert_core/halbert_core/` unless written absolute. Line numbers are the verifier's corrected ones wherever the verifier corrected the reader. Effort marks on verifier-added items (ids ending `-M<n>`) are my estimates. Spot-checked in the repo on 2026-09-09 before writing: `tools/executor.py:785` (`# background is accepted but ignored (Plan C).`), `:832` (`create_subprocess_shell`, no `start_new_session`) and `:908` (the only kill, `proc.kill()`); `agents/state_machine.py:2022-2023` (`await_subagent_completion(self, timeout: float = 300.0)`); `streaming/agent_pool.py:312-331` (head/tail split at 312-313, `redact(head)`/`redact(tail)` at 330-331); `dashboard/frontend/src/hooks/useTerminalSessions.ts:310-321` (`onclose` marks `status = 'done'`, `onerror` is empty); `tools/safety.py` has zero occurrences of `git`; `mcp/client.py:239` and `streaming/pty.py:289-293` both spawn with a copy of `os.environ`; `model/client.py:457-465` coerces unparseable tool arguments to `{}`; `SubagentManager(` is constructed only under `halbert_core/tests/`. All eleven claims held.

### What Halbert has here today

The agent's shell surface is `tools/executor.py` `_run_command` (`create_subprocess_shell` at `:832`, an unbounded `out_buf.append` pump at `:854-866`, one fixed `asyncio.wait_for` at `:886`, `proc.kill()` at `:908`) plus the agent PTY pool (`streaming/agent_pool.py`, `streaming/session_manager.py`, `streaming/pty.py` with `os.setsid()` at `:269` and a SIGTERM-grace-SIGKILL ladder at `:360-436` that signals the pid rather than the group) whose block output is split head/tail and then redacted (`agent_pool.py:312-331`) before it reaches the UI (`publish_terminal_event` `:341-352`) and the durable row (`executor.py:806-822`). `background=true` is accepted and ignored (`executor.py:785`); the dashboard PTY tiles are a separate user-facing surface with no agent poll/wait/kill API, and the client never reattaches a dropped socket even though the server keeps the session alive for it (`dashboard/routes/websocket.py:150-155` vs `useTerminalSessions.ts:310-318`). Command classification is `tools/safety.py:642-720` `_classify_command` — raw-string regex, first match wins (`:678-700`), `_shell_segments` (`:567-619`) used only to suppress the SAFE tier — layered with `dashboard/routes/terminal.py:182-207`'s own early-return chain and `streaming/injection_check.py:85-95`; skills can only tighten via fnmatch (`safety.py:377-390`). Confirmation is unconditional (`executor.py:604-611`), staged at `state_machine.py:3468-3480`, and `_handle_awaiting_confirmation` (`:4464-4473`) is a no-op with no timeout and no origin check. The only agent-facing file-write tool is `tools/write_config.py` (yaml/json/ini/.service/.timer, whole-content replace, plain `open(path, "w")` at `:93/:205/:229/:265` behind a `shutil.copy2` `.bak`), guarded by `continuity/write_guard.py` read-before-write and the write half of the provenance ledger (`continuity/provenance.py:131`); `read_file` (`executor.py:971-1004`) opens everything as utf-8 text; `somatic/checkpoints.py` is a 50-deep in-memory byte stack. Tool-loop protection is `max_loops` (default 5, `state_machine.py:232/:1361-1368`) and `_already_called` (`:2764-2776`, applied on the general dispatch path at `:2718-2729`), with `model/client.py:457-465` silently turning malformed arguments into `{}`. `agents/subagent.py` gives a FIFO queue, `max_concurrent=2`, lifecycle events on the tee/SSE/proactive bus, and a handle with `parent_task_id`/`children` but no progress cursor, no cancellation token, no depth, no lease/ack delivery; `SubagentManager` is constructed nowhere in production and `spawn_subagent` is a no-op when `self.subagents is None` (`state_machine.py:2007`). Already lifted on these seams (waves 1-6): `execute_code` with host-side RPC (generated stub module, per-run token, allow-list, budget, write-through spill with head/tail cap — `tools/execute_code.py:252-330`, `tools/script_rpc_host.py`); the interrupt algebra (stop/steer/redirect, generation claims, `apply_steer_to_results` never-drop contract — `agents/steering.py`, `agents/turn_activity.py`); the turn-event tee with echo guard and display redact+cap seam; the terminal channel and channel registry with server-stamped claims; the five-axis permission modules, consent ledger and profiles; the redaction variant registry at the response choke point.

### Lift themes, ranked

#### Theme A — Shell executor: process ownership, output bounds, output hygiene (the `executor.py` / `agent_pool.py` seam)

**Process-group ownership plus a graded kill ladder on the shell-exec path** (OC13-packages-C5, OC08-daemon-process-worker-hooks-audit-C1; confirmed twice — Hermes `tools/process_registry.py:674-737` does the same tree-kill) — origin `packages/agent-core/src/harness/env/kill-tree.ts:16-63` (SIGTERM to the verified group leader, grace, SIGKILL; group kill only when leadership is proven), `src/process/exec-termination.ts:33-36, 100-121` (start-time captured at spawn and re-read before force-kill; `cleanup='uncertain'` on mismatch; "Signal delivery is not exit"), `src/process/owned-stdio.ts:50-84` (stdin EOF -> grace -> SIGTERM -> grace -> SIGKILL joined against `waitForExtinction`). Mechanism: a timeout or cancellation signals the whole process group, waits a bounded grace, escalates only for what is still alive, and never signals a pid whose identity it cannot prove. Halbert state (partial, verifier-sharpened): `tools/executor.py:832` spawns with no `start_new_session`/`preexec_fn` and `:908` is an immediate SIGKILL to `/bin/sh` only, so every shell timeout orphans grandchildren; `streaming/pty.py:269` is a session leader but `:362`/`:432` `os.kill(self._pid, ...)` — the pid, not the group. Task: pass `start_new_session=True` at `executor.py:832`, record `os.getpgid`, on timeout/cancel `os.killpg(pgid, SIGTERM)` -> grace -> `SIGKILL` reusing pty.py's grace/escalate loop factored into one helper; change pty.py's two kills to `killpg`. Test: spawn `sh -c 'sleep 60 & sleep 60'`, time out at 1 s, assert no `sleep` survives. Effort S. Prerequisites: none. Verifier corrected the reader's risk note: psutil is a hard dependency (`halbert_core/pyproject.toml:60`), so `psutil.Process(pid).create_time()` is available. Same shape closes audit gap A17-mcp-boundary-G10 for stdio MCP servers.

**PID identity fingerprint (pid + start_time) before trusting or killing any recorded pid** (HM11-gateway-core-C18, HM07-tools-terminal-files-checkpoints-C5; confirmed twice — OpenClaw `exec-termination.ts` above) — origin `gateway/status.py:213-257` (`terminate_pid` refuses the kill when `expected_start_time` disagrees within 1 ms; a documented svchost.exe kill motivates it), `gateway/lifecycle_ledger.py:107-122`, `gateway/delivery_ledger.py:215-235`, `tools/process_registry.py:596-638, 674-737, 1926-1979` (`_host_pid_is_ours`, a recycled pid counts as "our process exited", tree-kill children-before-parent, recovery adopts only on a match). Mechanism: a bare pid is not an identity; every act on a stored pid first compares the kernel start time captured when it was recorded. Halbert state (absent): `dashboard/parent_watchdog.py:30-37` and `scheduler/run_receipts.py:54-66` probe with bare `os.kill(pid, 0)`; pty.py's direct fork child is reaped by its own code and is correctly excluded. Task: one helper `utils/process_identity.py` with `get_process_start_time(pid)` and `pid_matches(pid, expected)`; use it in `parent_watchdog.parent_alive()` (the one hole that defeats the watchdog's purpose — a reused pid keeps an orphaned backend alive), in run_receipts liveness, and as the day-one identity field of Theme C's registry. Test: monkeypatch start time and assert refusal. Effort S. Also answers the refuted-but-origin-verified audit note A06-scheduler-G6.

**Cap-plus-spill for every tool result, redacted before the write, cut on a line boundary** (HM07-tools-terminal-files-checkpoints-C15 extends-lifted, HM08-tools-browser-web-media-voice-C5, OC08-daemon-process-worker-hooks-audit-C18; confirmed twice) — origin `tools/tool_result_storage.py:1-6, 38-53, 71-119` (three layers: per-tool caps, `maybe_persist_tool_result`, `enforce_turn_budget`; canonical host-side spill dir; 24 h prune), `tools/web_tools_truncate.py:59-121` (hash-named file, symlink-refusing exclusive create, 75/25 head/tail snapped to line boundaries, `read_file offset/limit` footer), `tools/browser_tool_snapshot.py:33-66` (`redact_sensitive_text(force=True)` BEFORE the disk write), `src/process/bounded-line-framer.ts:2-26` (throws the instant a pending line exceeds `maxBytes`; never truncates silently). Mechanism: any over-cap output is spilled whole (post-redaction) to a bounded, pruned spill dir and replaced in the model-visible result by a window plus an exact pointer, and the in-memory buffer is bounded so a runaway process cannot exhaust memory before its timeout. Halbert state (partial): `tools/execute_code.py:252-330` already does write-through spill + head/tail + elided marker — for its own stdout only, spilled RAW to `/tmp` with no redaction and cut mid-line; `tools/executor.py:854-866` `pump()` appends unbounded; `streaming/bounded_output.py` (63 lines) only marks truncation. Task: extract `_OutputCapture`'s contract into `tools/spill.py` (redact -> exclusive-create under the Halbert data dir -> line-boundary window -> pointer; prune-once-per-process), apply it to `executor.py`'s pump and to `bounded_output.py`, and fix execute_code's raw spill to route through the redaction choke point. Test: plant a secret across the head/tail seam and assert the spill file never contains it; assert a 50 MB stdout terminates the pump with a structured "output exceeded limit" and a spill pointer. Effort M. Constraint: "redaction at the response choke point" — the spill file is a response surface. Pairs with audit A04-execute-code-G3 (unbounded spill, output-as-activity) and G4/G8 (stdout/stderr not redacted, stderr not captured).

**Redact-before-cap ordering defect in the agent pool** (HM18-tui-gateway-ui-desktop-docs-C9 extends-lifted; verifier raised medium -> high) — origin `tui_gateway/tool_progress.py:8-45` (`_redact_tui_verbose_text` redacts FIRST with `force=True`, caps SECOND, returns `''` if the redactor is unavailable; the #34095 OOM provenance; "full output stays in the SQLite session untouched"). Mechanism: the UI-bound copy is redacted on the whole string before any severing, so a secret cannot straddle a truncation boundary and leak in halves. Halbert state (partial, live defect): `streaming/agent_pool.py:312-324` severs head (first 20 lines) and tail (last 4096 chars) and only then `:330-331` redacts each fragment independently; the comment at `:196` records this as intended; both halves ship to the UI (`:341-352`) and the durable row (`executor.py:806-822`). Task: redact `output_text` once before the split (or redact then split then re-check), keep the fragment passes as belt-and-braces; test with a token planted so that line 20 ends inside it. Effort S. Dispatch as a defect fix, not a feature. Related audit: A05-echo-display-digest-G2 (timeline reload serves raw persisted blocks) and G8 (cap header hides the char cut).

**ANSI-escape and Unicode-TAG stripper for subprocess output entering the model** (HM06-tools-approval-guards-C5) — origin `tools/ansi_strip.py:7-51` (full ECMA-48 coverage: CSI/OSC/DCS/nF/Fp/Fe/Fs/8-bit C1, fast-path pre-check), `:39-43` (`_UNICODE_TAG_SUB_RE` strips plane-14 tag characters — the invisible-injection channel — while preserving the emoji-flag tag sequence), `:54-77` (`sanitize_display_text` also folds `\r` so carriage-return overwrite cannot hide content in a replayed transcript). Mechanism: the model never sees raw control sequences it could copy into a file write, and never sees tokenizable-but-invisible instructions. Halbert state (partial): `rag/scrapers/linux_man.py:153` strips SGR only; `streaming/shell_integration.py:142,177` parses OSC 133 but strips nothing; `integrations/observation_text.py:60-102` drops Cc/Cf on the observation surface only. Task: one `security/text_hygiene.py` (`strip_ansi`, `strip_unicode_tags`, `sanitize_display_text` with the CR fold) applied at three seams: `executor.py` command results, `agent_pool.py` block output, and MCP tool results/descriptions (audit A17-G5 asks for exactly the tag strip). Test: an OSC-8 hyperlink and a U+E0041 sequence in stdout are absent from the observation. Effort S. Share the module with Theme I's display-boundary item (OC07-M1) so there is one sanitizer, not two.

**Deterministic exit-code/signal interpretation and output-pattern failure hints** (HM07-tools-terminal-files-checkpoints-C7) — origin `tools/terminal_tool_result.py:28-101` (`_SIGNAL_EXIT_NOTES`, `_interpret_signal_exit` hedging 128+n with "usually", `_EXIT_CODE_SEMANTICS` per base command: grep/rg 1 is "no matches, not an error", curl 6/7/22/28, git 1; pipeline base-command extraction skipping `VAR=val`), `tools/terminal_hints.py:1-43` (non-zero exit only, first match wins, `_SCAN_CHARS=4000`, pure function). Mechanism: two config-free tables append one short next-step line to a non-zero-exit result so the model does not spend a turn misdiagnosing "ModuleNotFoundError" as a missing package instead of a wrong venv. Halbert state (absent): no interpretation layer under `tools/` or `streaming/` (only `applescript_tools.py:221` special-cases -9). Task: `tools/terminal_hints.py` with the signal table and an ordered hint list (command-not-found, ModuleNotFoundError/venv, permission denied, merge conflict, 137 OOM, 124 timeout -> "use background=true" once Theme C exists) appended by `executor.py` `_run_command` and the pool block path; test each hint on a canned stderr. Effort S. Constraint: this is the founder's "never a model where a template suffices" rule applied to failure triage. Related audit: A05-G4 — `record_effect` counts a non-zero-exit `run_command` as a success; fix in the same change.

#### Theme B — Watched-terminal surface: reconnect, status escalation, labels, restart survival (`useTerminalSessions.ts`, `routes/websocket.py`, `session_manager.py`)

**Reattach a dropped PTY socket, then the three reconnect budgets** (HM18-tui-gateway-ui-desktop-docs-C18; verifier raised medium -> high) — origin `web/src/lib/pty-reconnect.ts:11-36` (1 s resume-event-burst throttle; 8 s wedged-CONNECTING force-close for the half-open mobile socket; 8 s pre-socket ticket timeout because a rejected/hung ticket request is invisible to both `onclose` and the CONNECTING timer; a fourth budget the reader missed, `PTY_RESUME_SANITIZE_WINDOW_MS = 30000`). Mechanism: three stuck-connection states get three independent budgets, each of which converges on `onclose -> scheduleReconnect`. Halbert state (absent, and the client contradicts the server): `dashboard/routes/websocket.py:150-155` deliberately keeps the PTY alive for reattach; `useTerminalSessions.ts:310-318` marks a live shell `status = 'done', exitCode = -1` on any close and `:321-323` ignores errors, so laptop sleep or a wifi flap shows a running shell as exited while the process keeps running server-side until the idle reaper kills it. Task: implement reattach first (`streaming/pty.py:130-152` already supports it), then the three budgets; test with a forced socket close asserting the session returns to `running` and scrollback is re-served. Effort S-M. Constraint: this is the founder-named surface ("user shells stay but are watched by the AI"). ROADMAP row TERM-1 / open call `R04-POOL`/`R04-F2`.

**Bounded wait on an already-running watched session, resolving on at-prompt or a pattern, returning the tail as evidence on timeout** (OC24-docs-qa-security-skills-C10) — origin `skills/tmux/scripts/wait-for-text.sh:1-83` (integer-validated timeout/interval, `capture-pane` polled against grep, timeout prints the captured tail before exit 1). Mechanism: a deterministic "is the command finished / is it waiting at a prompt" detector that fails with evidence rather than blind. Halbert state (partial, verifier-corrected): `streaming/session_manager.py:163-187` `is_at_prompt()`/`is_interactive()` on the OSC-133 parser are strictly better than capture-pane+grep and must not be replaced; `dashboard/routes/terminal.py:286-296` bounds one-shot commands only. Task: `session_manager.wait_for(session_id, *, pattern=None, timeout)` resolving on `at_prompt` or the pattern and returning `(matched, tail)`; expose it to the agent as the watched-terminal read primitive. Test: a session that never reaches the prompt returns `matched=False` with the last N lines. Effort S.

**Status controller: debounce/immediate split, soft+hard stall timers, serialized transitions, terminal lock** (OC18-channels-cron-second-pass-C6) — origin `src/channels/status-reactions.ts:73-77` (debounce 700 ms, stall soft 10 s, stall hard 30 s), `:219-224` (promise-chain serialization that chains on rejection too), `:270-288` (stall escalation fires `immediate:true, skipStallReset:true` so it cannot re-arm itself), `:334` (terminal lock), `:350-362`. Mechanism: "what state is this" is separated from "when do I push it"; a task that goes quiet is escalated to needs-attention by a timer, not by a human noticing. Halbert state (partial): `StatusLight.tsx`/`AggregateStatusLight.tsx` are render-only; `needs_attention` is produced ONLY by an explicit `terminal_needs_input` event (`hooks/useTerminalSessions.ts:234-242`, `hooks/useTasks.ts:53`); zero Python producers exist. Task: a server-side controller (Python, driving `TaskCardData.state`) with the three properties, fed by block/terminal events, emitting `needs_attention` on the hard stall; test that a task with no events for 30 s escalates once and does not flap. Effort M. Constraint: do NOT copy `DEFAULT_EMOJIS` (`:57-72`) — no emoji in UI; drive StatusLight's existing shape/glyph/text vocabulary from shared tokens.

**Progress labels: privilege marker first, home path shortened** (OC03-auto-reply-C12; verifier kept only half) — origin `src/auto-reply/tool-meta.ts:23` (`shortenHomeInString` on every entry), `:99-120` (`splitExecFlags` hoists `elevated`/`pty` ahead of the command body). Mechanism: on a host steward, "elevated" at the front of the label is a safety affordance and home-path shortening is a leak reduction. Halbert state (absent): `dashboard/frontend/src/components/agent/StatusStrip.tsx:25-50` `describeExecution` returns the raw command and raw absolute paths. Task: prefix `sudo`/elevated commands with a marker token and shorten `$HOME` in `describeExecution`; drop the brace-collapse grouping (cosmetic). Effort S. Constraint: no emoji; skip `display.emoji`.

**Restart-safe PTY lease and an allowlist-verified reaper** (OC19-extensions-identity-security-ops-C13; low) — origin `extensions/acpx/src/process-lease.ts:20-47`, `process-reaper.ts:21-51, 170-175` (a candidate is reaped only when lease id AND gateway instance id match AND the command line matches a fixed owned-package marker). Halbert state (absent): `streaming/session_manager.py:212-272` reaps only in-memory sessions, so a crashed dashboard leaves its PTY children unowned forever. Task: tag each spawned PTY with an instance id in its env, persist a lease file, and on boot reap only previous-instance `/bin/sh -c` children of Halbert's own pool whose lease matches. Effort M. Low — nuisance-grade on one host; pair with Theme C's checkpoint (HM07-C6) since they persist the same kind of record.

**Parent-death grace before self-termination** (HM18-tui-gateway-ui-desktop-docs-C13; low, rider) — origin `tui_gateway/slash_worker.py:34-35, 56-64, 106-108` (poll ppid, wait up to 5 s for in-flight work, watchdog armed before expensive setup). Halbert state (partial, verifier-corrected target): `dashboard/parent_watchdog.py:41-49` `_default_stop` SIGTERMs itself immediately and hard-exits 5 s later regardless of an in-flight turn. Task: bounded wait for the current turn's activity claim to clear before signalling; rider on the PID-fingerprint change to the same file. Effort S.

**Terminal-native notification signal — receive side, not emit side** (HM14-cli-ops-lifecycle-C15; low; verifier corrected the citation — the file is 74 lines) — origin `hermes_cli/terminal_notify.py:26-67` (`_write_tty` to `/dev/tty`, `osc9` with C0/DEL stripped from the body, `warp_osc777`, a broken-Warp version table). Halbert state (absent, and the port direction inverts): Halbert's terminal is xterm.js inside Tauri, so emitting OSC 9 raises nothing; the useful half is recognising OSC 9 in `streaming/shell_integration.py:69+` (which already runs an OSC 133/7 state machine) so an agent-run command can light the "subtle indicator-light". Task: parse OSC 9 into a `terminal_notify` event; defer until the indicator affordance is specified (DECISIONS standing directive 2026-08-26). Effort S.

#### Theme C — Background process registry, yield-to-background, completion wake (the TERM-1 "agent gets no history" gap)

**ProcessRegistry: `terminal(background=true)` with poll/log/wait/kill/write/submit** (HM07-tools-terminal-files-checkpoints-C3; high, load-bearing) — origin `tools/process_registry.py:1-42` (rolling 200 KB buffer, `FINISHED_TTL_SECONDS=1800`, `MAX_PROCESSES=64` LRU, JSON checkpoint), `:301-330` (`ProcessSession`: id, command, task_id, owner_task_id, pid, `host_start_time`, exit state, `completion_reason`, `termination_source`), `:1478-1773` (`poll`, `read_log(offset, limit)`, `wait(timeout)`, `kill_process`, `write_stdin` vs `submit_stdin` — the documented Enter-vs-raw trap), `:1995-2035` (the `process_manage` tool schema; verifier corrected the reader's 2064-2090). Mechanism: a background spawn returns a session id immediately; the registry owns the handle, a capped output buffer and exit state; completions drain into the agent's next turn as a system-originated message. Halbert state (absent): `executor.py:785` is the only `background` mention under `tools/`. Task: `tools/process_registry.py` (registry + `ProcessSession` carrying Theme A's start-time fingerprint), a `process_manage` tool registered through `ToolExecutor` so the guest floor and RoleGate apply, `_run_command` honouring `background=true`, and a `completion_queue` drained at turn start. Tests: spawn/poll/wait/kill lifecycle; a completion arriving between turns appears in the next observation set; LRU eviction at 64. Effort L. Prerequisites: Theme A kill ladder and fingerprint. Constraint: registry rows are agent-owned; user shells stay watched, never adopted. Directly resolves the memory note "terminals built for one turn only, agent gets NO history".

**Yield-to-background: a mid-turn user message hands off the live foreground process instead of killing it** (HM07-tools-terminal-files-checkpoints-M1; verifier-added; high) — origin `tools/interrupt.py:66-92` (`request_yield`/`is_thread_yield_requested`/`consume_yield`, keyed by thread ident), `agent/interrupt_control.py:252-261` (a redirect during `_executing_tools` degrades to steer AND requests yield on every tool-worker thread), `tools/environments/base.py:384-397` (the poll loop hands the still-running Popen to the registry via `yield_handler` and returns a partial result; on handler failure it restarts the drain and keeps waiting), test `tests/tools/test_terminal_yield_to_background.py:67-87`. Mechanism: three distinct bits — kill, steer, yield — so guidance is delivered without killing work. Halbert state (partial): `agents/steering.py:18` promises "yield, never kill" in prose; grep for `yield` in `steering.py`/`turn_activity.py` returns only that docstring. Until this lands, a REDIRECT during a tool call parks the correction behind the command for up to `DEFAULT_TIMEOUT`. Task: an `asyncio.Event` yield flag on the executor's `ExecutionContext`; `request_steer`/redirect sets it for the running execution; `_run_command`'s wait loop checks it, registers the live handle with the registry and returns `{status: 'backgrounded', session_id}`; test that a steer during `sleep 30` returns within 1 s and the process is still alive in the registry. Effort M. Prerequisites: C3. This is audit A07-interrupt-algebra-G8 made concrete.

**Foreground-command guardrail: nudge nohup/`&`/dev-server commands toward tracked background mode** (HM07-tools-terminal-files-checkpoints-C8) — origin `tools/terminal_tool_guards.py:65-141` (`_strip_quotes` masks heredocs first, then quoted spans; `_LONG_LIVED_FOREGROUND_PATTERNS` for npm/pnpm/yarn/bun dev|start|serve|watch, docker compose up, vite, nodemon, uvicorn, gunicorn, http.server; ordered guidance, first hit wins, `--help`/`--version` exempt; guidance not a block). Mechanism: the enforcement half of the registry — without it `&` and `nohup` bypass tracking silently. Halbert state (absent). Task: a pure `foreground_guidance(command) -> str | None` in `tools/safety.py`'s neighbourhood returned as the tool result; test each of the three anti-patterns and the help exemption. Effort S. Has standalone value before C3 — today `npm run dev` burns the full `DEFAULT_TIMEOUT`.

**Watch-pattern notification throttle: per-source strikes plus a global circuit breaker** (HM07-tools-terminal-files-checkpoints-C4) — origin `tools/process_registry.py:44-66` (`WATCH_MIN_INTERVAL_SECONDS=15`, `WATCH_STRIKE_LIMIT=3`, `WATCH_LIFETIME_MAX_HITS=8`, global 15 per 10 s window with a 30 s cooldown), `:448-538` (`_check_watch_patterns`: one match per window, a match inside the window is a strike, enough strike windows or the lifetime cap disables watching and demotes to one on-exit notification, with one explanatory event), `:555-595` (`_global_watch_admit`: exactly one `tripped` and one `released` event). Mechanism: pure counters and windows decide whether autonomous background output may interrupt a turn — no model anywhere in the path. Halbert state (absent; no autonomous mid-process notification path exists yet). Task: lift as a generic `NotificationThrottle` used by the registry's watch patterns and reusable by scheduler run-failure alerts; property-test that N concurrent noisy sources produce at most `WATCH_GLOBAL_MAX_PER_WINDOW` deliveries and exactly one trip event. Effort M. Prerequisites: C3. Constraint: deterministic policy — satisfied by construction.

**Crash-recovery checkpoint for background processes across restarts** (HM07-tools-terminal-files-checkpoints-C6) — origin `tools/process_registry.py:1898-1923` (`_write_checkpoint`: skip exited, redact the command losslessly because recovery never re-runs it, `atomic_json_write`), `:1925-1985` (`recover_from_checkpoint`: skip non-host pids, apply the start-time guard with an explicit refuse-to-adopt log, register survivors `detached=True` — status+kill only — re-arm watchers). Mechanism: a registry restart does not silently drop notification delivery for a process that outlived it. Halbert state (absent). Task: checkpoint under the Halbert data dir on every session transition, read at state-machine startup, adopt only on fingerprint match; test a simulated restart with one live and one recycled pid. Effort M. Prerequisites: C3, Theme A fingerprint.

**Terminal "blocked" outcome forced by a progress-only-text detector** (OC09-tasks-system-agent-flows-boards-claws-C2; extends the prior OpenClaw review §5 item that was never implemented) — origin `src/tasks/task-completion-contract.ts:11-19, 40-52, 64-95` (three regexes; a real second sentence rescues it; empty final text -> "did not produce a final deliverable"), `task-executor-policy.ts:79-98` (a subagent success is framed "ready for review", a blocked outcome gets its own follow-up). Mechanism: a completion report that ends in "I'll now check…" is `blocked`, never `succeeded`. Halbert state (partial, verifier-corrected): `scheduler/run_receipts.py:43` already has `blocked_config` distinct from `error`; the classifier is absent. Caution from the verifier: `conversation_sqlite.py:2810`'s `'lost'` is a PTY session state, not a task ladder — do not cite it as one. Task: `agents/completion_contract.py` with the three regexes, applied to subagent completion and scheduled-task final text before the receipt status is written; test the rescue-sentence rule. Effort S.

**Wake-into-conversation for background completions, with a record-only lane for anything behind a pending gate** (HM11-gateway-core-C11; verifier lowered high -> medium and founder-gated the wake lane) — origin `gateway/wake.py:37-53` (`deliver_wake` self-posts a synthetic turn), `:71-101` (`persist_delegation_delivery`: a durable `display_kind='async_delegation_complete'` row and NO agent turn, because a self-posted user prompt "can cross a pending human-confirmation gate"). Mechanism: two lanes — react-to-it vs record-it — and the record lane is mandatory whenever a confirmation is pending. Halbert state (partial): the record-only lane exists as the proactive bus (`proactive/events.py:26-40`, `proactive/gate.py:1-45`, `state_machine.py:1946-1978` publishes background block completions without a turn) but nothing persists such a completion into the transcript, and no wake lane exists. Task now: persist registry completions as inert transcript rows (visible on reload). Task later, founder-gated: the wake lane behind the proactivity dial, hard-refused while `AWAITING_CONFIRMATION` is held. Effort M. Constraint: "commands staged, never executed" and the proactivity posture — see Open founder decisions.

**Resettable idle deadline: progress keeps a timeout alive, silence kills it** (OC08-daemon-process-worker-hooks-audit-C17; low) — origin `src/node-host/with-timeout.ts:6-43` (`resetTimeout` re-arms; the abort names the stage pending at the deadline; the finally clears listeners so a finished work item cannot retain the pending rejection). Halbert state (absent): `executor.py:886` is one fixed `wait_for` by deliberate design (`:879-885`). Task: only when the registry needs it — a `progress_timeout` helper for `wait()` on a background session; keep the foreground executor fixed. Effort S. The reader's own risk note governs: reset only on genuine progress, never on every log line. Related: audit A04 bug "an in-flight tool call counts as inactivity" is the same idea on the execute_code monitor.

#### Theme D — Command classifier v2 (`tools/safety.py`, `routes/terminal.py`, `injection_check.py`, the confirmation gate)

**Normalize and de-obfuscate before any pattern match; scan every command start** (HM07-tools-terminal-files-checkpoints-M4; verifier-added; high) — origin `tools/approval_detection.py:423-444` (`_normalize_command_for_detection`, documented order: strip ANSI+NUL, NFKC, collapse backslash-newline, fold `$HOME` back to `~`, strip backslash escapes and empty-string literals, collapse `$IFS`), `:1009-1017` (`_deobfuscate_shell_word_for_detection`, iterated to a fixed point, non-executing), `:1020-1042` (`_iter_shell_command_starts`: after `(`, `{`, `;`, newline, `&`, `&&`, `|`, `||`, inside `$(…)` and backticks), `:33-35` (fold the macOS `/private/{etc,var,tmp,home}` mirror). Mechanism: every dangerous-command rule runs against a normalized string and against each real command start, so quoting tricks and subshells cannot hide the verb. Halbert state (absent, verified live by the verifier): `_classify_command` (`safety.py:642-720`) matches the raw string; `rm -rf ~/Documents` -> HIGH/confirm, but `r\m -rf ~/Documents`, `rm${IFS}-rf${IFS}~/Documents` and `$(echo rm) -rf ~/Documents` -> MEDIUM, allowed, no confirmation. Task: `tools/command_norm.py` with `normalize_for_classification()` and `iter_command_starts()`; `_classify_command` classifies the max over normalized starts; the same pass feeds `routes/terminal.py`'s patterns and `injection_check.py`; pin the three bypasses as tests. Effort M. Constraint: deterministic, no LLM — squarely the "one policy pipeline" directive. Prerequisite for every other Theme D item being worth anything.

**Fail closed when the command cannot be parsed, with a saved-payload recovery path** (HM06-tools-approval-guards-M1; verifier-added; high) — origin `tools/approval_detection.py:174-179` (block on parser-limit or malformed quoting), `:569-578` (ceilings: length > 128 000, separator-free run > 4 096, >= 25 000 separators; "crossing the ceiling fails closed rather than letting an uninspected suffix execute"), `:585-600` (lexing stops at the end of the simple command so nested `$(grep … | cut …)` is not unbalanced quoting — 546 false blocks in one run taught that), `tools/approval_floors.py:54-110` (`_save_blocked_payload` writes the refused command under `cache/blocked-scripts/` with 7-day GC and the block message points the model at it, because a parser-limit block is almost always a legitimate oversized script). Mechanism: "I could not parse this" is a block, never a fall-through; and the block is recoverable in one turn. Halbert state (absent): `_shell_segments` returning `None` (`safety.py:567-619`) only suppresses the SAFE tier (`:679-683`); CRITICAL/HIGH/MEDIUM rules still regex the raw string, so an unsegmentable command can land on MEDIUM "File deletion" and execute after a generic confirmation; `injection_check.py:85-95` has no parse step at all. Task: ceilings evaluated before classification; `segments is None` -> BLOCK with the payload saved under the Halbert home and a one-line pointer in the tool error; test an unbalanced-quote `rm` is blocked, not MEDIUM. Effort S. Constraint: staged-not-executed posture — the saved payload is a stage, not an execution.

**Founder-authored deny list that outranks every bypass, matched against de-obfuscated variants** (HM06-tools-approval-guards-M3; verifier-added; medium) — origin `tools/approval_floors.py:1-9, 23-51` (`approvals.deny` runs BEFORE yolo/mode-off/cron-approve; `_match_user_deny_rule` iterates `_command_detection_variants`, "so quoting tricks (r\m, git st""atus) can't sidestep a rule"; the refusal names the rule and tells the model not to rephrase), `approval.py:988-996`. Mechanism: a config line the human wrote is a floor below every other decision. Halbert state (partial): only a skill-level `blocked_commands` fnmatch (`safety.py:377-390`, "can only tighten") — nothing the founder owns, and no word de-obfuscation anywhere. Task: `security.deny_commands` (0600 config under the config dir) consulted first in `_classify_command` against normalized variants; refusal text carries the anti-rephrase instruction; test `git st""atus` against a `git status` deny rule. Effort S once M4 exists. Constraint: deterministic, human-authored policy — the cleanest expression of the founder's posture.

**One combined approval request when independent detectors both fire; remembered approvals keyed per finding** (HM06-tools-approval-guards-M5; verifier-added; medium) — origin `tools/approval.py:978-984, 1010-1039` (pattern findings and content-scanner findings are joined into ONE prompt with the full key list, "so a gateway force=True replay cannot bypass one check when only the other was shown"; the permanent "Always" choice is offered only for pattern keys; scanner findings stay session-scoped). Halbert state (absent): `safety.py:678-700` returns on the first rule; `routes/terminal.py:182-207` early-returns at each layer; a `confirmed` replay (`state_machine.py:1514`) is scoped to the action id, not to the findings that justified it. Task: `_classify_command` collects all findings and the confirmation text lists them; if a remembered-approval store is ever added it is keyed per finding, scanner-class findings never permanent. Test: a command tripping a route pattern AND an injection pattern shows both reasons. Effort S. Makes "one policy pipeline across MCP and internal tools" enforceable.

**Unattended-origin approval policy: nobody present means a configured verdict, never a parked prompt; "silence is not consent"** (HM06-tools-approval-guards-M2; verifier-added; high) — origin `tools/approval.py:466-482` (`_unattended_contexts` enumerates single-query, cron, unattended platforms, each with its own config key and refusal wording), `:487-518` (`_unattended_deny`: patterns, then content scan, and if the scanner cannot even import, block "since nobody can approve" unless fail-open is explicitly configured), `:549-558, 585-590` ("Silence is not consent." on every timeout; "Do NOT retry, do NOT rephrase, do NOT attempt the same outcome via a different command" on every denial), `:1057-1060` (two real incidents). Mechanism: presence of a human is an explicit input to the gate. Halbert state (absent): `executor.py:604-611` returns `requires_confirmation` unconditionally; `_handle_awaiting_confirmation` (`state_machine.py:4464-4473`) is a no-op; the state's only exits are `confirm_action` or eviction by a new message (`:834-856`); a HIGH-risk tool from the scheduler, an MCP-originated turn, or a voice turn with no dashboard open pins the session (`:994-1000`). Task: per-origin `approval_policy: deny|ask` resolved from the channel registry and consulted BEFORE staging; a bounded wait with deny-on-timeout written to the run receipt; Hermes's refusal wording. Touches `state_machine.py` (AWAITING_CONFIRMATION), `executor.py:604`, `agents/channels.py`. Test: a scheduler-originated HIGH action produces a `denied_unattended` receipt within the bound and the session lock is released. Effort M. Founder decision: the per-origin defaults (see below). Related audit: A11-permission-lattice-G7 (the Attentive/Present line is data only, QUIET never computed) and A07-G9/G10 (queued turns cannot be stopped; no turn-liveness watchdog).

**Self-repo worktree-mutation rule** (HM06-tools-approval-guards-C2, HM07-tools-terminal-files-checkpoints-M6 verifier-added; merged; verifier lowered high -> medium, and M6 says do not port the parser) — origin `tools/self_repo_guard.py:19-52` (mutation verbs: checkout, switch, rebase, merge, pull, restore, clean, cherry-pick, revert, bisect; reset `--hard/--merge/--keep`; `worktree move/remove`; wrapper and alias tables; `gh pr checkout`), `:71-77` (`get_running_source_root`), `:437-441` (`guard_active` is Windows-only — on POSIX open handles keep the old inode alive, so the origin itself does not enforce this on Halbert's platform), `:445-467`. Mechanism: refuse git operations whose effective target is the checkout backing the running process. Halbert state (absent): `safety.py` has no git rule; `git checkout main` classifies MEDIUM / "Unrecognized command pattern" with no confirmation. Why it still matters here: the founder runs concurrent sessions in this checkout, so an agent-initiated `reset --hard`/`clean -fd` destroys another session's uncommitted work. Task: one HIGH/confirm rule in `_classify_command` for the worktree-mutating verbs when the resolved cwd is inside Halbert's own source root, using M4's normalizer so `g\it checkout` is caught; do not port the 467-line alias/wrapper/heredoc parser. Test: `git reset --hard` in the repo root -> HIGH/confirm; in `/tmp/x` -> unchanged. Effort S. Demoted from the reader's high per the verifier: `execute_code` cannot shell out (`execute_code.py:202` bans `subprocess`), so `run_command` is the only agent path.

**Trust-tier x scan-verdict install policy for skills, with an agent-authored row** (HM06-tools-approval-guards-M4; verifier-added; medium; cross-listed to the skills workstream) — origin `tools/skills_guard.py:24-35` (`TRUSTED_REPOS`; `INSTALL_POLICY` 4x3: builtin never scanned; trusted allows caution, blocks dangerous; community blocks on any finding unless forced; agent-created maps dangerous -> `ask` = hand the finding back to the author to retry without it), `:2-8` (an honest known-gap note about dynamic write destinations). Halbert state (partial): `skills/parser.py:56-58` STATES draft/trusted/archived, `catalog.py:124` excludes non-trusted; no content scan at all — every skill's verdict is implicitly safe; the only signal is the author's own `destructive_requires_approval`. Task: a deterministic scan sharing Theme D's pattern library, a policy table keyed on (provenance sidecar) x (verdict) with a dedicated row for skills the assistant wrote; `ask` semantics for that row. Effort M. Dispatch under the skills packet, not here; listed because the scan library is Theme D's.

#### Theme E — Tool-loop guardrails, malformed-call recovery, abort detachment (the `state_machine.py` dispatch path)

**Refuse, do not coerce, unparseable tool arguments; then a 3-strike repair budget with a graceful partial exit** (HM01-agent-turn-family-C13; verifier raised medium -> high) — origin `agent/turn_tool_validation.py:1-8` (three-way design, role-alternation invariant, #48879), `:23-34` (`ToolValidationVerdict` ok/continue/return with `mixed_invalid_batch`), `:42-51` (one tool-role result per call so every `tool_call` keeps a matching result), `:53-65` (`_partial_exit` closes the interrupted sequence then persists), `:68-95` (strikes advance only when a turn has NO valid call; `_repair_tool_call` fuzzy-fixes near-miss names). Mechanism: a weak model's malformed call is corrected or refused with a clear message, bounded, and never silently executed with wrong arguments. Halbert state (partial, and worse than the reader said): `model/client.py:457-465` swallows the parse failure and dispatches the tool with `{}`; a local model emitting truncated JSON gets `terminal {}` / `write_file {}` run, burning one of five loops, and `_already_called` may then refuse the corrected retry as a duplicate; `executor.py:527-531` returns a bare `Unknown tool` after the peer-proxy attempt. Task: in `client.py` mark the call `args_invalid` instead of `{}`; the dispatcher returns a structured "arguments did not parse; resend" result without executing; close-name repair for unknown tools (single candidate within edit distance 2 of a registered name); strike budget 3 ending in a partial response. Tests: truncated JSON never reaches a handler; `writ_file` is repaired to `write_file`. Effort M. Constraint: local models are Halbert's primary target — the verifier calls this the most likely real-world failure in the set.

**Signature-hashed per-tool guardrail: failing-repeat vs no-progress vs identical-streak, with tolerant tools and progress resets** (HM01-agent-turn-family-C5, OC14-agents-second-pass-A-C6, OC14-agents-second-pass-A-C7, OC15-agents-second-pass-B-C15; merged — four descriptions of one detector family; confirmed twice) — origin `agent/tool_guardrails.py:19-77, 106-120, 160-166, 205-270` (IDEMPOTENT/MUTATING sets, `ToolCallSignature.from_call` SHA-256 over canonical JSON, graduated warn/block/halt, `_LOOP_CAPS`, attended-platform hard-stop default) and the two the reader missed and the verifier ranks higher: `FAILURE_TOLERANT_TOOL_NAMES` (`:43-48` — terminal/execute_code failures are normal work, never halt on a same-tool streak) and `PROGRESS_RESET_TOOL_NAMES` (`:50-57` — a successful mutating call clears every failing signature, so edit-then-rerun is a new experiment); OpenClaw `src/agents/tool-loop-detection.ts:27-51` (six named detector kinds, warning/critical tiers, 30-call rolling history), `src/agents/tool-loop-argument-churn.ts:5-55` (argument churn fires only when >1 variant each repeated >= 3 times sharing one result hash; a novel argument RESETS liveness), `agent-tools.before-tool-call.diagnostics.ts:62-63, 551-566` (warn once per decade bucket, 256-key cap). Mechanism: distinct stuck patterns get distinct, bounded detectors; a retry after a fix is not a replay. Halbert state (partial, verifier-corrected): `_already_called` (`state_machine.py:2764-2776`) is applied on the GENERAL dispatch path (`:2718-2729`) and refuses any (name, args) already settled this turn, forcing REFLECTING — exactly the blunt rule `PROGRESS_RESET_TOOL_NAMES` exists to avoid: after a failing `terminal <test>`, an edit, and the same command again, Halbert refuses the retry and ends the turn. `max_loops=5` (`:232`) is the only ceiling; no argument-churn or ping-pong detector exists. Task, in this order: (1) an intervening successful mutating call (write_config, a `run_command` that exited 0 with a write-plane verb) resets the signature set — test the edit-then-rerun sequence is allowed; (2) signature controller with failure classification and per-tool caps; (3) argument-churn and ping-pong as the two signals exact-args equality cannot see, warning on decade buckets. Effort M. Verifier demoted OC15-C15 and OC14-C7 to low on their own (with `max_loops=5` an uncaught churn costs at most five calls) — folded here as phase 3, not standalone. Note: the reader's proposal leaned on a non-existent "non-interactive detection" in `autonomy/guardrails.py`; the attended/unattended split is Theme D's M2.

**Per-run tool-call ceiling on the interactive path, captured at construction** (OC14-agents-second-pass-A-C20; verifier corrected absent -> partial) — origin `src/agents/agent-tool-source-execution-guard.ts:12-62` (`admit()` throws at the cap at "the last synchronous source boundary, after approval/steering awaits"; `captureAgentToolExecutionBudget` at tool construction so retained tools cannot borrow a later budget). Halbert state: scripts already have it (`script_rpc_host.py:43-46, 105-121`; `execute_code.py:56` `DEFAULT_TOOL_CALL_BUDGET=25`); the interactive path bounds iterations only (`react_agent.py:167`) and loops over an unbounded `tool_calls` list per iteration (`:230`). The parallel-siblings race framing degenerates — Halbert dispatches sequentially (`state_machine.py:2668-2669` takes `tool_calls[0]`). Task: a per-turn call cap on `ExecutionContext` captured when the turn starts; test that a model returning 40 calls in one message is cut at the cap with a structured refusal. Effort S.

**Detach-not-cancel: a late tool result cannot land in a newer generation's context** (OC14-agents-second-pass-A-C4) — origin `src/agents/agent-tools.abort.ts:20-69` (race `execute()` against the combined abort signal; a straggler is discarded; the tool's own turn-handoff abort is exempt), `:72-101` (`AbortSignal.any` per call). Mechanism: since a coroutine cannot truly be cancelled, its result is detached from the aborted turn rather than delivered late. Halbert state (absent): `agents/turn_activity.py:24-70` gives generation claims for the abort itself; `tools/executor.py` has no abort parameter; nothing detaches a late settlement. Task: `ExecutionContext.generation` stamped at dispatch; the observation writer drops (and logs) any result whose generation is stale; test by cancelling mid-`sleep`. Effort M. Constraint: complements "yield, never kill" — the tool keeps running, its result just cannot land in the wrong turn. Related audit: A07-G5 (a stop cannot abort an in-flight model request).

**Persist an UNKNOWN-effect marker when a turn dies after a side-effecting tool ran** (HM02-agent-context-prompt-family-C9; verifier lowered high -> medium and re-scoped) — origin `agent/replay_cleanup.py:19-26, 56-92, 95-119` (read-only orphaned tool blocks are dropped; side-effecting ones are KEPT with the result rewritten to "may have executed; effect UNKNOWN — inspect before retrying", `effect_disposition='unknown'`; #49201 reboot loop). Halbert state (partial): the reboot-loop shape cannot occur — Halbert never persists structured `tool_calls` (results become observation strings, `state_machine.py:153-163`; history is plain role/content, `assembler.py:1349-1358`); a boot sweep marks in-progress rows `interrupted` (`threads.py:512-513`, `conversation_sqlite.py:1402-1413`); the side-effect classifier exists (`tools/base.py:33` `side_effects`, `tools/role_gate.py`). Real gap: observations die with `ctx`, so the next turn is silent about a possibly-completed destructive action. Task: on the interrupted row, persist a fixed-marker row listing side-effecting tools that had started (model it on `_record_superseded_turn`, `state_machine.py:857-917`); the assembler renders it as "this may have run; verify before retrying". Test: kill a turn after `write_config` started, reload, assert the marker is in context. Effort S. Touches `conversation_sqlite.py`. Related audit: A16-session-tree-compaction-G5 (interrupted turn leaves a dangling question with no persisted fact) — same row, write both facts in one change.

#### Theme F — Subagent manager correctness (`agents/subagent.py`, `state_machine.py:2007-2050`)

Founder gate on the whole theme: `SubagentManager` is constructed only in tests and `spawn_subagent` is a no-op in production (`state_machine.py:2007`). The two wedge bugs below are worth fixing because the tests pin the manager and the interrupt algebra assumes it; wiring spawn into a real consumer is a separate decision (see Open founder decisions).

**Liveness by progress, not by a stopwatch: replace the 300 s wall-clock deadline with a staleness monitor** (HM05-delegation-subagents-kanban-M1; verifier-added; high) — origin `tools/delegate_tool_child_run.py:205-268` (`_Heartbeat` on the SHARED periodic scheduler thread, every 30 s; progress = api_call_count advanced OR current_tool changed OR last_activity_ts advanced; the parent's activity clock is touched only while one advances; 15 idle cycles vs 40 in-tool cycles before the heartbeat stops and the parent's watchdog is allowed to fire), `:632-644`, `tools/delegate_tool_config.py:130-138` ("Failures should come from what the child does, not a stopwatch"). Halbert state (absent): `await_subagent_completion` polls every 0.1 s against `timeout=300.0` and on expiry simply breaks; `SubagentHandle` (`subagent.py:38-51`) exposes no progress signal. Task: `last_activity_epoch` + `current_step` on the handle touched by the `_emit` path (`subagent.py:103-122`); a staleness counter reset on advance; terminal-by-staleness with distinct idle vs in-work thresholds; drop the fixed deadline. Test: a handle that emits every 20 s for 10 minutes is not declared over; one silent for the idle threshold is. Effort M. Related audit: A07-G10 (no turn-liveness watchdog) — same activity-stamp primitive.

**Abandonment accounting: an unfinished child gets an explicit terminal entry, a cooperative stop, and never wedges the slot** (HM05-delegation-subagents-kanban-M2; verifier-added; high) — origin `tools/delegate_tool_dispatch.py:97-146` (`_cf_wait(..., FIRST_COMPLETED)` never `as_completed`, "so a wedged child cannot block the parent forever"; every still-pending future becomes a fabricated `interrupted` entry in input order), `tools/delegate_tool_child_run.py:76-81` (`_signal_child_stop` cooperative), `:333-360` (`_defer_close_after_timeout` — never close SQLite from the abandoning thread; shut down pooled sockets so an OpenSSL read settles with EOF), `:776-816` (ordered idempotent teardown). Halbert state (absent, and a live leak): on deadline `await_subagent_completion` clears `current_subagent_handle_id` but the handle stays in `SubagentManager._active` forever (only `complete()`/`cancel()` remove it, `subagent.py:184/203`); with `max_concurrent=2` two abandoned subagents wedge the manager and `_promote_next()` never drains; a completion after `cancel()` returns `False` and drops the result; `cancel()` frees a slot without signalling the work (no cancellation token). Task: a cooperative stop flag on the handle polled by the runner; abandon marks the handle terminal (`abandoned`) so the slot is released; a cancelled-then-completed handle keeps its result in the store with `late=True`. Tests pin all three. Effort S-M. Related audit: A07-G7 (refuted as written, but the cancel-on-stop propagation belongs in this same change).

**Steer into a running subagent, with the steer/completion race closed** (HM05-delegation-subagents-kanban-C16, HM05-delegation-subagents-kanban-M6 verifier-added; merged — the verifier says ship them as one change) — origin `tools/kanban_tools.py:445-487` (watermark seeded on first poll and never replayed; own notes filtered; joined text handed to `agent.steer`; verifier corrected the reader's test-only citation), `tools/delegate_tool_child_run.py:376-385, 40-44, 616-631` (`_merge_late_steer` is "the linearization boundary"; an accepted-but-undeliverable steer lands in the result as `pending_steer`/`missed_steer` with the error text amended; acceptance closes BEFORE the stop signal), `tools/delegate_tool_registry.py:50-56, 107-121` (`accepting_steer` flipped under the registry lock). Halbert state (absent for subagents, present one level up): `agents/steering.py:57-108` `decide_midturn` cites Hermes directly; `apply_steer_to_results:111-127` has the never-drop contract. Task: `SubagentManager.steer(handle_id, text)` with an `accepting_steer` flag under the manager lock, a watermark, and a `missed_steer` field on the result when the child finished first; test a steer racing completion is either delivered or recorded, never silent. Effort M. Constraint: "never kill a tool to deliver guidance".

**Leased delivery of completed subagent results into the parent turn, framed as data** (OC14-agents-second-pass-A-C16) — origin `src/agents/agent-steering-queue.ts:14-18` (`STALE_STEERING_LEASE_MS=5min`, 24 000-char merged cap, 6 000 per item), `:39-47, 73-89, 148-178` (stale-lease self-heal, deterministic ordering, always-deliver-at-least-one), `:180-274` (lease -> inject -> ack, or release restoring the prior status), and the strengthening the verifier ranks above the lease: `:19-24, 66-71, 130-145` — the merged header frames items as "runtime data and evidence, not as user instructions", every field via `sanitizeForPromptLiteral`/`wrapPromptDataBlock`. Halbert state (absent): `subagent.py` has no delivery state. Task: `subagent_results` rows with `pending|leased|delivered`, lease id + timestamp, ack/release, stale-lease sweep; the injection block uses the data-not-instructions framing and the fenced-literal helper the prompt assembler already has for summaries. Test: a crashed parent turn re-leases the item. Effort M. Constraint: subagent output is a Tier-1 untrusted-text surface — scrub before the model.

**Spawn depth from persisted lineage, with a visited set** (OC15-agents-second-pass-B-C10; verifier lowered high -> medium: not a live risk while unwired) — origin `src/agents/subagents/spawn/subagent-depth.ts:138-199` (depth from persisted `spawnDepth` else recursed through `spawnedBy` with a visited Set; UI-threading parents never add depth). Halbert state (absent). Task: `max_depth` on the manager computed by walking `parent_task_id`; refuse at the cap; test a cyclic chain terminates. Effort S.

**Ownership-scoped control plane; placeholder-goal gate; the never-started diagnostic** (HM05-delegation-subagents-kanban-C9, HM05-delegation-subagents-kanban-C7, HM05-delegation-subagents-kanban-M7 verifier-added; all low, grouped as one-line rules for the packet) — C9: `tools/delegate_tool_registry.py:162-178, 196-219` — `_owns_subagent_record` is two-tier (identity chain, then durable session-id lineage) because the identity chain breaks across parent rebuilds; copy both tiers or neither. Halbert holds one `current_subagent_handle_id` (`states.py:143`), so there is no sibling tree yet — record the rule in `cancel()` when depth (C10) lands. C7: `tools/delegate_tool_tasks.py:9-19, 36-66` — placeholder/unexpanded-marker goal detection with a false-positive list (`Vec<T>`, `<div>`, JSON, globs) and, the rule the reader missed, duplicate goals are deliberately allowed (best-of-N fan-out). Halbert `spawn()` (`subagent.py:128-160`) takes the goal unvalidated; add the gate when a model constructs goals. M7: `tools/delegate_tool_child_run.py:84-200, 678-706` — exactly one blind failure (timeout with zero API calls) gets a capped diagnostic file with prompt SIZES not contents and a three-way differentiated error. Halbert: when M1's staleness path lands, write one file for the zero-progress case with the frozen `agent_config_snapshot` (`subagent.py:48`), goal, scoped sources, prompt size; route through the redaction choke point before disk (Tier-2 rule: never the assembled prompt). Effort S each.

**Per-child live, tail-able, redacted transcript with manifest and retention** (HM05-delegation-subagents-kanban-C5; verifier lowered high -> medium) — origin `tools/delegation_live_log.py:1-9, 25, 63-70, 104-117, 227-289` (pre-created header so `tail -f` attaches immediately; one `_redact` choke point every writer funnels through; per-line budgets; `manifest.json`; 7-day prune). Halbert state (partial): the redaction seam exists — `agents/turn_event_tee.py:1-40` fans reduced events through the echo guard and redaction registry, and subagent lifecycle events already reach SSE/proactive (`subagent.py:250-283`); absent is the on-disk artifact, manifest, retention, and tool-level (not lifecycle-level) progress. Task: subscribe a file writer to the tee for subagent events under the cache dir; manifest; prune. Effort M. Lower value than the reader claimed: the founder can already see lifecycle events live.

**Optimistic-concurrency (expected-revision) writes for multi-step records** (OC09-tasks-system-agent-flows-boards-claws-C4) — origin `src/tasks/task-flow-registry.ts:535-566` (`updateFlowRecordByIdExpectedRevision` returns `not_found|revision_conflict|persist_failed`), the same CAS in `session-cards/progress-card-store.ts:126-148` (house rule, not a one-off). Halbert state (absent). Task: an integer `revision` column and `update_expected_revision()` on the subagent-results table above (and any future plan record); test a stale writer gets `revision_conflict`. Effort S. Touches `conversation_sqlite.py` if the results live there.

#### Theme G — Child-process boundary: what a spawned process may see, and what the parent may trust (the security seam shared with the permissions workstream)

**Minimal-allowlist environment for every stdio MCP server Halbert launches** (HM03-agent-providers-credentials-usage-C13, OC01-infra-approvals-devices-backup-C16; merged — both verifiers retargeted to the same site; confirmed twice) — origin `agent/secret_sources/base.py:223-255` (`run_cli`: argv list never a shell, `stdin=DEVNULL`; `run_secret_cli`: `base_keep` = PATH/HOME/USERPROFILE/SYSTEMROOT/TMPDIR/TEMP/LANG/LC_ALL/XDG_CONFIG_HOME/XDG_DATA_HOME plus an explicit `allow_env`, `NO_COLOR=1`, ANSI-scrubbed stderr; `ErrorKind` at `:65-77`, `classify_cli_error` at `:85-91` — the reader's 150-180 was wrong), `src/infra/container-env-file.ts:11-84` (a 0600 file in a 0700 dir instead of `-e` on argv; CR/LF/NUL rejection; try/finally cleanup). Mechanism: a child never receives the whole parent environment; it receives PATH/HOME/locale plus exactly the names its config asks for. Halbert state (absent — the reader's `script_rpc_host.py` contains no subprocess call; the real site is `mcp/client.py:236-239` `self.env = {**os.environ, **(env or {})}` handed to `create_subprocess_exec` at `:262-275`): every third-party MCP server inherits `HALBERT_API_TOKEN`, `HALBERT_MCP_TOKEN`, every other server's token and any API key — on a process the founder's own memory classes as a cloud pipeline. `mcp/config.py:176-207` already does the right thing at the other end (`token_env` names a variable). Task: `StdioTransport` builds env from the allowlist merged with the server's declared env; test that a server sees `PATH` but not `HALBERT_MCP_TOKEN`. Effort S. This is audit A17-mcp-boundary-G1 (high) — one fix closes both. The env-FILE transport has no consumer (Halbert runs no containers); record it for the Docker distribution track.

**A positive denial marker that survives exec, plus an explicit allow-list on every PTY the agent spawns** (HM05-delegation-subagents-kanban-M4 verifier-added, OC24-docs-qa-security-skills-C11 the "now" half; merged) — origin `agent/delegation_context.py:1-7, 71-74, 76-92, 101-112` (ONE predicate every identity gate uses; `scrub_kanban_env` removes the worker keys AND sets `HERMES_DELEGATED_CHILD_CONTEXT=1` because "absence alone would promote a descendant to an orchestrator"; the marker is carried into real subprocesses and survives later execs; ContextVar not `os.environ` because concurrent readers share the environment; honest about being cooperative scoping, not confinement), `skills/coding-agent/SKILL.md:128-150, 185-186` (`env -u CODEX_API_KEY -u OPENAI_API_KEY CODEX_HOME=…` — ambient credentials stripped per worker; verifier corrected the reader's 150-175). Mechanism: a descendant is fenced by a value that is PRESENT, never by a key that is missing, and no exec can shed it. Halbert state (absent): `streaming/pty.py:289-293` `child_env = dict(os.environ)` then `execvpe("/bin/sh")` for every PTY; `session_manager.py:99` passes `env=None` for the agent's own pool shells (`agent_pool.py:83-88`); so every shell the machine runs for itself inherits `HALBERT_API_TOKEN` (`dashboard/auth.py:62`) and `ANTHROPIC_API_KEY` (`agents/llm_client.py:322`) verbatim — for a guest-persona session or a secure-context turn that is the exact ambient-credential inheritance the origin strips. Task: (1) `security/child_env.py` `build_child_env(kind, persona)` — allowlist plus deny-list of `HALBERT_*_TOKEN`/`*_API_KEY` for agent-pool and guest shells (user shells keep the founder's environment); (2) `HALBERT_CHILD_CONTEXT=<kind>` positive marker; (3) one predicate `is_fenced_child()` used by any gate that asks. Tests: an agent-pool shell cannot `echo $HALBERT_API_TOKEN`; the marker survives `exec env -i sh`. Effort M. Constraint: scope by capability boundary; state the cooperative limits in the docstring as the origin does. The git trust/provenance protocol half of C11 (`SKILL.md:38-90`: classify ref trust, fetch-before-branch, `merge-base --is-ancestor` before push) waits for an orchestrator that exists.

**Background offloads run in an EMPTY Context — `asyncio.to_thread` copies the caller's security ContextVars** (HM05-delegation-subagents-kanban-M3; verifier-added; high) — origin `gateway/kanban_watchers_common.py:21-34` (`_run_in_fresh_context`: every blocking offload is `Context().run(func, *args)` inside the thread; "a lingering delegate_task child marker would make write_txn false-trip"; the fix is an empty Context at the boundary, not clearing the var). Mechanism: once a ContextVar carries identity or a permission decision, a thread offload silently re-attributes the work to whoever was on the loop. Halbert state (absent): Halbert deliberately relies on context copying — `tools/executor.py:42-50` documents `current_turn_claim` riding the same copy-down mechanism as `current_turn_digest`, consumed by the RoleGate — and 20+ bare `asyncio.to_thread` sites inherit it: `home/cognitive_loop.py:289` (the autonomous tick), `dashboard/routes/approvals.py:278, :367` (proposal application), `mcp/client.py:682/711/807`, `state_machine.py:3656, :3700`; each sees whatever `current_turn_claim` / `current_turn_digest` (`security/turn_digest.py:52`) / `current_speaker_role` / `current_turn_channel` (`agents/channels.py:246`) / `current_turn` (`continuity/provenance.py:59`) was bound. Task: one `run_detached(fn, *args)` helper (`contextvars.Context().run` inside the thread) and route every process-owned background offload through it, cognitive-loop tick first; test that a claim bound on the loop is `None` inside the offload. Effort S. Constraint: deterministic-policy hygiene — autonomous work is never attributed to, or exempted by, a user turn's identity.

**Ownership and writability check on the security-load-bearing binaries Halbert shells out to** (OC12-state-sessions-secrets-security-shared-C4, OC19-extensions-identity-security-ops-C7; merged; confirmed twice within OpenClaw; both verifiers retargeted) — origin `src/secrets/trusted-plan-path.ts:32-102` (lstat before AND after each permission read, symlink or dev/ino change across the pair rejects; owner must be current user or root; group/world-writable ancestors rejected except sticky; a SECOND pass from root back to target — the actual TOCTOU close), `:104-136` (recursive shebang with cycle set, `env` indirection rejected), `extensions/onepassword/onepassword-op-path.js:1-42` (an unsafe CONFIGURED path throws; missing candidates are skipped). Halbert state (absent, and the reader aimed at the wrong sites — `execute_code` exec()s in-process and `script_rpc_host` is a dispatch gate): the real targets are `crypto/storage.py:227` (`security`) and `:271` (`secret-tool`) — a PATH-hijacked binary there receives the body's private signing key — and `streaming/sandbox.py:98-100` (`bwrap`/`sandbox-exec`) — a hijacked sandbox binary silently defeats the sandbox. Task: `security/trusted_exec.py` `assert_trusted_executable(path)` (absolute, regular file, owner in {root, uid}, no other-writable component, dev/ino stable across the check) applied at those three `shutil.which` sites; skip the recursive-shebang machinery (system binaries). Test: a world-writable dir on PATH ahead of `/usr/bin` is refused. Effort S-M. Medium: it defends against an attacker who can already write a PATH directory.

#### Theme H — File tools: atomic writes, approve-then-execute identity, reads, cleanup, and the founder-gated edit tool

**Atomic write in the one agent-facing file-write tool, with read-back verification** (HM07-tools-terminal-files-checkpoints-M2 verifier-added, OC19-extensions-identity-security-ops-C14; merged — the verifier rewrote C14 into this task; confirmed twice) — origin `tools/file_operations.py:360-415` (`_atomic_write`: mktemp in the TARGET's own directory, symlink target resolved first, existing mode copied, new files chmod'ed after so umask applies, `trap EXIT` cleanup; "cross-device mv is copy+unlink, NOT atomic"), `:1189-1215` (`_verify_written_hash` sha256s the file on disk and returns a hard error rather than success; the documented order deny-list -> surrogate refusal -> syntax gate -> probe -> CRLF/BOM -> atomic write -> verify -> lint delta), `extensions/openshell/src/mirror.ts:58-64` (unlink before write so a pre-planted hardlink cannot redirect content). Mechanism: `applied: true` means the bytes are on disk, in the intended inode, and a crash mid-write cannot truncate a live system file. Halbert state (partial): six internal stores already do temp-in-same-dir + `os.replace` (`config/being_config.py:870`, `scheduler/executor.py:314`, `scheduler/run_receipts.py:121`, `scheduler/monitor_hash.py:146`, `scheduler/engine.py:46-58`, `mcp/config.py:652,782`; `persona/store.py:96-106` adds mkstemp + chmod 0600); `tools/write_config.py` — the tool that edits launchd/systemd units and host config under approval — writes with plain `open(path, "w")` at `:93/:205/:229/:265` behind a `.bak`, writing THROUGH any hardlink/symlink and non-atomically. Task: one `utils/atomic_write.py` (temp in target dir, mode copied, fsync, `os.replace`); route `_apply_yaml/_apply_json/_apply_ini` and the rollback path through it; compare the re-read the provenance ledger already does against the intended bytes before reporting applied. Tests: a hardlink at the target keeps its old content; a simulated crash between temp and replace leaves the original intact. Effort S. Related audit: A11-permission-lattice-G9 (no primitive binds an approval to the artefact digest and re-validates at apply).

**(device, inode) binding across Halbert's approve-then-execute gap** (OC19-extensions-identity-security-ops-C9; verifier narrowed and demoted high -> medium) — origin `extensions/file-transfer/src/shared/path-binding.ts:4-58` (identity captured at grant, re-verified at use: `node-host/file-fetch.ts:172`, `dir-fetch.ts:134`), `policy.ts:1-46` (default deny, deny wins, realpath BEFORE I/O, `SYMLINK_REDIRECT` refused). Halbert state (partial, understated by the reader): `consent/selfmod.py:72-150` `governed_roots` + `_resolve` ("one absolute string, used for both check and open (F16)") + `is_governed_path` is already a realpath-resolved Class-1 DENY set; `safety.py:392-404` classifies cwd against `protected_paths`. Genuinely absent: `findings/proposal_generator.py` stages a proposal, the operator approves later, and execution re-opens the path STRING with nothing verifying it still names the same inode. Task: capture `(st_dev, st_ino, sha256)` on the staged proposal; refuse at apply if any differ; test by swapping the file between stage and apply. Effort S. No parallel policy engine — Halbert does not need one.

**Darwin TCC-protected home directories are excluded from any walk rooted at an ancestor of `$HOME`** (HM07-tools-terminal-files-checkpoints-M3; verifier-added; medium) — origin `tools/file_operations_search.py:22-50` (Desktop, Documents, Downloads, Library, Movies, Music, Pictures; exclusions computed RELATIVE to the search root; "only an ANCESTOR search gets exclusions … a search rooted inside a protected dir stays allowed"). Halbert state (absent): `dashboard/routes/development.py:494-521` defaults project discovery to `$HOME` plus nine subdirectories and globs `*/.git`, `*/*/.git`, `*/*/*/.git` — descending into `~/Documents`/`~/Downloads`/`~/Library` on the founder's machine and triggering privacy dialogs nobody asked for; the only TCC reference (`discovery/scanners/macos/security.py:13`) reads permission state. Task: `utils/darwin_paths.py` `protected_home_exclusions(root)`, applied in `development.py` discovery and any discovery scanner walking from `$HOME`/`/Users`; test that a walk from `$HOME` skips `Documents` while a walk from `~/Documents` does not. Effort S. Constraint: an unattended sweep of `~/Documents` is not something a host steward does unasked — a privacy alignment point.

**stdlib docx/xlsx/ipynb extraction in `read_file`** (HM08-tools-browser-web-media-voice-C10; verifier raised low -> medium) — origin `tools/read_extract.py:21-37` (zipfile + ElementTree only for the three formats; 50 MB ceilings; row/col caps), `:55-60` (300 s cooldown after a failed lazy install — skip the `anydoc` tier). Halbert state (absent, understated): `executor.py:971-1004` `_read_file` opens every path as utf-8 text (`:1003`), so a `.docx` raises `UnicodeDecodeError` today. Task: dispatch on extension before the text open; three stdlib extractors with the size caps; test one fixture each. Effort M. Reading the user's own documents on their own machine is the steward's job; no new dependency.

**Read ledger: the parent is told when work it delegated (or another session) wrote a file it had already read** (HM05-delegation-subagents-kanban-M5; verifier-added; medium) — origin `tools/delegate_tool_child_run.py:723-775` (`append_sibling_write_reminder` annotates the result with "[NOTE: subagent modified files the parent previously read — re-read before editing: …]", capped at 8 paths; checks writes by ANY non-parent task, so nested chains are covered; `files_read`/`files_written` ride the completion event). Halbert state (absent — write half only): `continuity/provenance.py:131` `record_file_change` is called from `write_config.py:95/:160`, `executor.py:1093`, `config/watcher.py:164`, keyed by the `current_turn` ContextVar; nothing records reads. Task: `record_file_read` from `_read_file` and `write_config`'s pre-read into the same ledger; at turn start emit a stale-read observation for any path this conversation read that has since been written by another turn, the watcher, or a subagent — in the conversation, not as a dashboard row. Test: read, external write, next turn carries the note. Effort M. Matters more here than in the origin: the founder runs concurrent sessions and `config/watcher.py` already sees external edits.

**Affirmative-proof-before-discard: retarget from subagent worktrees to the live chroma orphan cleanup** (HM05-delegation-subagents-kanban-C15; verifier raised low -> medium and retargeted) — origin `tools/subagent_worktree.py:1-7, 108-125, 135-186` (prune only when BOTH probes succeed and report zero commits AND clean; any missing base, non-zero probe exit, or raised exception KEEPS the worktree with `inspection_failed` — "Destructive cleanup requires affirmative proof; defaults prove nothing"). Halbert state: no worktrees — but `storage/chromadb_manager.py` `_get_active_segment_ids` returns an EMPTY set on any sqlite failure, so a locked/corrupt `chroma.sqlite3` makes `list_orphans` (`:601-633`) classify EVERY uuid directory as an orphan and `cleanup_orphans` (`:579-650`) `shutil.rmtree`s each (`:638`); the in-loop double-check (`:633-636`) compares segment ids against collection ids — a category mismatch that can never protect. Task: `_get_active_segment_ids` raises or returns a sentinel on failure; `cleanup_orphans` refuses to delete anything unless the active set was affirmatively read; fix the double-check to compare like with like; test with an unreadable sqlite that nothing is removed. Effort S. Constraint: "leave old data unread, never delete" — this is data loss in shipped code.

**Ephemeral-file lifecycle tracker — propose-and-stage form only** (HM17-plugins-acp-providers-C2; verifier lowered high -> medium and founder-gated) — origin `plugins/disk-cleanup/disk_cleanup.py:31-37` (`is_safe_path` under one owned root or `/tmp/hermes-*`), `:49-77` (atomic ledger with `.bak` restore), `:84-113` (protected top-level and control-plane paths, never widened), `:223-252` (`quick()` re-validates the stored category against `guess_category()` and DROPS a misclassified entry rather than deleting), `__init__.py:29-80` (post-tool-call auto-track). Halbert state (absent, narrower than the reader said): `execute_code.py:423-424/:521` already mkdtemps and rmtrees its own run dir; the gap is files left through `_write_file` and the terminal; `discovery/scanners/disk_usage.py` is read-only. Task: track paths under a Halbert-owned scratch root only; on session end stage a cleanup proposal (never execute); never infer deletability from a filename pattern. Effort M. Constraint: DECISIONS standing directives 2026-08-26 (staged, never executed) and the no-users "never delete" rule.

**Targeted file-edit tool and its bookkeeping — founder-gated** (HM07-tools-terminal-files-checkpoints-C12 verifier demoted high -> medium; HM07-tools-terminal-files-checkpoints-C13, HM07-tools-terminal-files-checkpoints-C14, HM07-tools-terminal-files-checkpoints-C23, HM07-tools-terminal-files-checkpoints-C22 all low and dependent on C12; HM07-tools-terminal-files-checkpoints-M5 verifier-added low) — origin `tools/fuzzy_match.py:2-33, 175-306, 337-537` (nine strategies exact -> line-trimmed -> whitespace -> indentation -> escape -> boundary -> unicode -> block-anchor -> context-aware; `SIMILARITY_STRATEGIES` refuse `replace_all`; escape-drift detection; reindent to the matched region; `find_closest_lines` on failure), `tools/patch_parser.py:1-50` (V4A multi-file envelope, markers must own a whole line at column 0 — reimplement the mechanism, do not port the codex/cline parser), `tools/file_operations_lint.py:17-45, 172-192` (post-write lint reporting only errors absent from the pre-write content; "pre-existing errors — this edit didn't introduce new ones but the file is still broken"), `tools/file_tools_read_tracking.py:1-11, 34-66` (read dedup keyed to mtime surviving compaction, 60 s not-found negative cache, escalating patch-failure hints), `tools/file_state.py:1-27` (per-path read stamps with `partial`, last writer, per-path lock — for concurrent subagents only), `tools/file_operations.py:906-968` (NFC/NFD filename resolution that refuses on ambiguity — "several candidates = homoglyph collision"; scored similar-file suggestions). Halbert state (absent): `write_config.py` is whole-content replace by extension and refuses everything else; no `old_string`/patch surface anywhere. Verifier's framing: an excellent implementation of a capability Halbert has not decided to have — whether a host steward gets a general file-edit tool is a scope question, and it must not land before Theme D's gates. Task if approved: `patch(old_string, new_string, replace_all)` through `write_guard`'s read-before-write digest, the atomic writer above, `py_compile` delta lint for `.py`, NFC-canonical basename resolution refusing ambiguity, read dedup and the not-found cache; C13 (multi-file) and C22 (cross-subagent file state) stay deferred behind real need. Effort M (C12) + S each. See Open founder decisions.

**Durable checkpoint/undo of agent host changes — narrow the shadow-store idea** (HM07-tools-terminal-files-checkpoints-C1; verifier lowered high -> medium) — origin `tools/checkpoint_manager.py:1-11` (one bare git store, per-project refs, private index per project), `:162-179` (agent-write ledger `{abs_path: {sha256, ts}}`), `:599-621` (`safe_restore_plan` restores only files whose current hash matches the agent's last write — the human's hand edits are never clobbered), `:623-644` (one snapshot per dir per turn; refuses `/` and `$HOME`), `:716-767` ("take a pre-rollback snapshot so you can undo the undo"). Halbert state (partial): `somatic/checkpoints.py` — 50-deep in-memory byte stack, "pattern stolen from OCC's CheckpointManager", used only from `somatic/lifecycle.py`; plus `.bak` and the provenance write ledger. Fit caveat: write_config's targets are `/etc`-style paths outside any git-snapshotable workdir, so the git-per-workdir shape is a coding-agent shape. Task: the ledger + safe-restore idea only — persist the sha256 of every agent write (provenance already has the event), keep the pre-write bytes in a content-addressed store under the data dir, expose "undo last change" that refuses when the current hash is not the agent's. Do NOT port `legacy-<ts>/` handling (a back-compat shim). Effort L as written, M narrowed. Founder-gated with C12.

#### Theme I — Entry-point side effects and config posture (cross-listed with the dashboard/onboarding section)

**`halbert --version` constructs the whole FastAPI app: argv fast path plus a lazy module-scope app** (OC07-cli-wizard-tui-entry-C1, OC07-cli-wizard-tui-entry-C14; merged — the verifier says they are one defect from two directions; C1 raised medium -> high, C14 the reader had talked itself out of and the verifier reinstated) — origin `src/entry.ts:108-121` (the `isMainModule` guard and its documented failure: a duplicate gateway crashing on the held port), `:207-209` + `src/entry.version-fast-path.ts:5-63` (`--version` imports only `version.js` + `git-commit.js`), `src/cli/argv.ts:32-83`. Halbert state (absent, measured by the verifier): `Halbert/main.py:28-56` imports the dashboard app at `:50`; `dashboard/app.py:1744` runs `app = create_app()` at MODULE scope; `arch -arm64 .venv/bin/python Halbert/main.py --version` takes ~1.1 s and logs "Turn-event tee bridged", "Indexing state initialized (fresh start)" and "Halbert Dashboard API created" before printing the version — a side-effect problem, not a latency nicety. Task: (1) a pure argv check before the heavy block for `--version`/`-V`/`license` importing only `halbert_core.halbert_core`; (2) make `app.py`'s module-scope instance lazy (`uvicorn --factory`, or a module `__getattr__` building on first access) so importing the module builds nothing. Test: `--version` produces exactly one line on stdout and no log lines. Effort S. Touches `dashboard/app.py` (shared hot file).

**Read-only mode so a diagnostic read cannot rewrite `models.yml`** (OC07-cli-wizard-tui-entry-M3; verifier-added; medium) — origin `src/entry.ts:97-104, 158-161` (`secrets audit` on argv sets `OPENCLAW_AUTH_STORE_READONLY=1` before any subsystem loads), `src/agents/auth-profiles/store.ts:1255-1266` (the store skips its write-on-load sync under the flag). Halbert state (absent — the exact anti-case): `model/llm_config.py:656-686` `load_global_file` WRITES (`_backup_before_rewrite()` then `_write_raw` at `:683-684`) whenever `needs_rewrite(raw)`, reachable from every reader including `halbert info`, GET settings routes and MCP read tools. Task: a process flag set at diagnostic entry points plus a `read_only=` parameter; `load_global_file` returns the normalised view without rewriting; test that `models.yml` mtime is unchanged across a read from a read-only surface. Effort S.

**Invalid config fails visibly, never silently serves defaults** (OC07-cli-wizard-tui-entry-M4; verifier-added; medium) — origin `src/cli/program/config-guard.ts:26-39, 219, 321-336` (`ALLOWED_INVALID_COMMANDS` = audit, doctor, logs, health, help, status; every other command is blocked with located issue lines and repair hints). Halbert state (partial): the write side is right (`llm_config.py:768-770` raises `ConfigUnreadableError`; routes surface it at `settings.py:361/:413`, `llm.py:265/:295`); the read side "serves defaults for the session and is left untouched" (`:656-663`) — a corrupt `models.yml` silently reverts pinned slots to defaults with one log line, and since SEC-21 decides locality by tag, presenting defaults as configured is the wrong failure. Task: keep the read fail-open (the assistant must still answer) but surface "config unreadable, defaults in force" on a Settings banner and a startup notice naming the file and the parse error; slot readers report the degraded state. Effort S.

**Onboarding rerun must not silently re-identify the machine** (OC07-cli-wizard-tui-entry-M2; verifier-added; high) — origin `src/wizard/setup.workspace.ts:10-48` (a rerun names both the stored and requested values, defaults the confirm to false, and on decline the existing value wins — "without silently remapping an existing fleet"), `setup.shared.ts:131-155` (persisted marker checked before anything is asked). Halbert state (absent): `dashboard/routes/settings.py:1056-1120` `POST /onboarding/complete` re-runs the deep scan (`:1071-1072`), overwrites `ai_name`/`user_name`/`user_type` (`:1102-1104`), rewrites `preferences.yml` (`:1110-1111`) and mirrors the name onto the being (`:1116`) with no check of the `onboarding_complete` marker it writes at `:1089-1090`; the only guard is client-side (`App.tsx:67`), so a stale tab, a scripted call, or the MCP surface renames the machine. Task: read the marker first; if complete and the requested name differs, return the conflict and require an explicit rename confirmation, keeping the current name on decline; give the `preferences.yml` write the advisory flock `being_config.py:506-614` already uses. Test: a second POST with a different name is refused. Effort S. Constraint: standing directive — named by the onboarding name; the entity name is user-chosen (open call `C1-02`).

**Sanitize untrusted text at the display boundary, isolate RTL rather than delete, drop-don't-clean for the value that would run** (OC07-cli-wizard-tui-entry-M1; verifier-added; high) — origin `src/tui/tui-formatters.ts:130-138` (a completion containing a C0/C1 or bidi control is DROPPED, not cleaned), `:262-299` (`sanitizeRenderableLine` strips controls then wraps RTL runs in isolate marks), `:140-166` (URLs/paths/identifiers exempt from display chunking), `src/tui/tui-autocomplete.ts:9-54` (sanitize only label/description; the ORIGINAL item is stashed behind a Symbol so display can never change what is applied), `packages/terminal-core/src/safe-text.ts:12-16` (strip escapes FIRST so removed bytes cannot recombine into a new sequence). Mechanism: what the human approves is byte-for-byte what would run, and it is rendered so that a bidi override cannot reverse it on screen. Halbert state (partial — right scrubber, wrong surface): `integrations/observation_text.py:62-101` `_structural_scrub` (NFC, Cc/Zl/Zp -> space, Cf dropped with the Trojan-Source rationale) is used only for observation titles and entity ids; `dashboard/frontend/src/components/agent/ConfirmationDialog.tsx:26-41` says this is "the one screen where a person authorises a privileged action" and `renderDescription` (`:52-58`) only HTML-escapes before `dangerouslySetInnerHTML` (`:90`); the @mention popup (`AgentChat.tsx:307-313`) renders host-derived names unscrubbed. Task: promote `_structural_scrub` to `security/text_hygiene.py` (with Theme A's stripper) plus a small TS twin; apply it inside `renderDescription` BEFORE `escapeHtml`; isolate RTL runs; a staged command containing U+202E is refused at staging. Test: the dialog never displays a reversed command. Effort S. Related audit: A05-G10 (no chat-input sanitizer).

#### Theme J — Outbound fetch (cross-listed with permissions/security; listed here because `tools/web_search.py` is a tool)

**One SSRF-safe HTTP client, connect-time re-validation, and the two fail-open holes in the existing helper** (HM06-tools-approval-guards-C3; verifier corrected absent -> partial; HM06-tools-approval-guards-C6 folded in as a predicate) — origin `tools/url_safety.py:93-124` (metadata hostnames and IPs incl. IPv4-mapped forms, link-local, CGNAT), `:127-158` (allow-private toggle; profile-scoped turns bypass the process cache), `:318-345` (`_resolved_http_connect_ips` re-resolves and validates EVERY answer at connect time — the DNS-rebinding close), `:348-399` (guarded backends; Unix-socket connect refused; Host/SNI preserved), `:470-495` (`create_ssrf_safe_client`, `redirect_target_from_response`); `tools/website_policy.py:23-58` (a founder domain blocklist with a 30 s TTL cache; only the shared-file path degrades gracefully — a malformed config section raises). Halbert state (partial — the reader's grep was provably wrong): `dashboard/routes/llm.py:62-128` `is_safe_url()` exists with a `# SSRF Protection` banner but (a) is private to the LLM-provider route — `tools/web_search.py`, `web/search.py`, `rag/scrapers/*` have no call site; (b) is pre-flight only, so the rebinding TOCTOU is open; (c) `:113-114` returns `True` unconditionally for `ollama`/`lm-studio`/`apple-foundation` — an attacker-supplied base_url labelled local skips every check; (d) `:125-126` swallows `gaierror` and falls through to `return True` — DNS failure fails OPEN (Hermes raises). Task: `security/http_client.py` with a connect-time-validating transport; replace `llm.py`'s helper; close (c) and (d); route web_search/web/rag scrapers through it; add `security.website_blocklist` as one more predicate on the same client rather than a separate packet. Tests: a hostname resolving to 127.0.0.1 on the second lookup is refused; DNS failure is refused. Effort M. Constraint: local models pinned, `:cloud` tags are cloud — same posture at the HTTP layer.

**Harden the existing `web_search` tool: provider failure is not "no results", fallback only in auto mode, cancellation wins** (OC11-talk-tts-acp-web-routing-context-C13; verifier corrected absent -> partial — the reader claimed no web_search tool exists; `tools/web_search.py` is 283 lines) — origin `src/web-search/runtime-execution.ts:18-24, 32, 50-56, 63` (`throwIfAborted` before AND after each attempt; a structured `missing_*_api_key` result is fallback-worthy ONLY when the provider was auto-selected, never when explicitly requested). Halbert state: `_search_duckduckgo` swallows timeout and every exception into `return []` (`web_search.py:111-116`) so failure reads as "No results found" (`:270-272`); backend choice `:82-87` is a hard if/else with no fallback to the configured SearXNG instance; no cancellation on the path. Task: structured `SearchUnavailable` distinct from empty results; auto-mode fallback to SearXNG only; check the turn's cancel flag around each attempt. Effort S. Constraint: web search stays off by default behind the visible switch (open call `C3-08`).

### Related confirmed audit gaps

- A04-execute-code-G1 [high]: the monitor loop has no deadline when the worker survives KeyboardInterrupt — the execute_code twin of Theme A's kill ladder (in-process, so "abandon the daemon thread and retire the host").
- A04-execute-code-G2 [high]: no post-settle disarm of the dispatch hook; a thread the script left behind dispatches under the old session/role — pairs with Theme G's fence (`ScriptRpcHost.retire()`).
- A04-execute-code-G3 [high]: unbounded spill and output-as-activity — Theme A cap-plus-spill applies (`MAX_SPILL_BYTES`).
- A04-execute-code-G4, G8 [medium]: stdout/traceback not redacted before context; stderr not captured — Theme A spill/redaction and the ANSI strip.
- A04-execute-code-G9 [medium]: a user stop does not interrupt a running script — the stop predicate belongs with Theme C's yield/abort work (A07-G2).
- A04-execute-code-G5, G6, G7, G10, G11 [low]: `__name__` mismatch, partial stdout discarded on raise, schema never lists stubs, SystemExit as error, non-atomic budget — dispatch as the execute_code follow-up inside the shell-executor packet.
- A04 confirmed bugs: AST gate misses obvious spellings of banned affordances (Theme D's normalizer idea applied to the AST scan); the monitor keeps injecting KeyboardInterrupt during unwind; an in-flight tool call counts as inactivity (OC08-C17's idea); process-wide `sys.stdout`/`sys.modules`/`sys.path` mutation with no serialization; `handle()` raises on a non-ASCII token; `max_tool_calls=0` treated as unset.
- A07-interrupt-algebra-G2 [high]: `/stop` during a tool is demoted to a steer — Theme C's yield primitive restores the distinction (kill vs steer vs yield are three bits).
- A07-G5 [medium]: a stop cannot abort an in-flight model request — Theme E detach-not-cancel.
- A07-G8 [low]: no yield primitive — this IS HM07-M1.
- A07-G9, G10 [medium]: queued turns cannot be stopped; no turn-liveness watchdog — the activity stamp shared with Theme F's staleness monitor.
- A07-G13 [medium]: the dashboard stop button bypasses the generation claim via `/api/agent/cancel` — touches `routes/agent.py`.
- A07-G11 [low, founder]: auto-resume of a crash-interrupted turn — pairs with Theme E's UNKNOWN-effect marker; the marker is what makes any auto-resume safe to even consider.
- A07 confirmed bugs: empty steer accepted; `request_stop` mirrors only half of `cancel_session`'s paused rule; a steered voice arrival leaves its relay receipt unspent; `test_stop_and_redirect_share_safety` pins nothing.
- A17-mcp-boundary-G1 [high]: stdio MCP servers inherit the whole environment — identical to Theme G's merged env-allowlist item; one fix.
- A17-G10 [medium]: closing a stdio server signals only the direct child — Theme A's group-kill shape.
- A17-G2 [high]: the stdio reader dies on any line over 64 KiB — same family as Theme A's bounded framer.
- A17-G5 [medium]: remote tool descriptions enter the prompt without tag stripping — Theme A's `strip_unicode_tags`.
- A17-G7 [medium]: MCP results carry no untrusted provenance — the framing rule from OC14-C16 (data, not instructions) is the same discipline.
- A05-echo-display-digest-G2 [medium]: timeline reload serves raw persisted tool blocks — Theme A's redact-before-cap fix must cover the durable row too.
- A05-G4 [medium]: a non-zero-exit `run_command` counts as a successful effect — fix with Theme A's exit-code table.
- A05-G7, G8 [low]: no structured truncated fact; cap cuts mid-line — Theme A line-boundary window.
- A05-G10 [low]: no chat-input sanitizer — Theme I's display-boundary hygiene module.
- A11-permission-lattice-G7 [medium]: the Attentive/Present line is data only, QUIET never computed — Theme D's unattended policy is its mechanism.
- A11-G9 [medium]: no primitive binds an approval to the artefact digest and re-validates at apply — Theme H's inode binding.
- A11-G3 [medium]: nothing re-checks policy between evaluation and use — same approve-then-execute gap.
- A06-scheduler-G6 [refuted as a gap, origin verified]: pid-only owner liveness — Theme A's fingerprint helper is the drop-in.
- A06 confirmed bug: after a wall-clock timeout the task thread keeps running with live side effects while the receipt says error — the same abandonment-accounting problem as HM05-M2, one seam over.
- A16-session-tree-compaction-G5 [medium]: an interrupted turn leaves no persisted fact it was cut short — write it in the same row as Theme E's UNKNOWN-effect marker.

### Do not lift

- HM02-agent-context-prompt-family-C14 (CJK-aware markdown table realignment): no fixed-width markdown renderer for assistant output exists in the tree (the terminal talk channel carries turns typed at the terminal), so this means building the renderer plus a `wcwidth` dependency for a surface the dashboard already handles. Verifier agrees: low.
- HM05-delegation-subagents-kanban-C6 (output_schema with one bounded retry): no LLM-driven subagent produces a result to validate; the origin's `MAX_SCHEMA_RETRIES=1` constant sits in a "plugin-compat, internal code must not use" block — the real bound is structural at `delegate_tool_child_run.py:393-427`. Record the one-retry-never-re-paste lesson in the subagent packet's notes; do not build.
- HM05-delegation-subagents-kanban-C8 (summary budget from parent headroom): subagent results are not LLM text in Halbert today; keep only the verifier's lesson for whoever builds it — use the LAST call's `prompt_tokens`, never the running session sum (1 393 summaries collapsed to the floor in one Hermes run), and never treat "no usage yet" as zero.
- HM05-delegation-subagents-kanban-C10 (narration-instead-of-action nudge): Halbert turns end at a user, not a dispatcher reading an exit code; the failure mode is already addressed prompt-side (`prompts/agent_prompts.py:616-623`).
- HM07-tools-terminal-files-checkpoints-C20 (sudo password cache and headless path): no non-PTY path needs sudo; the PTY handles the prompt (`routes/terminal.py:14-16`). If one ever does, surface it into the SAME watched terminal — never a second prompt channel — per the reader's own anti-pattern.
- HM12-gateway-rooms-platforms-hooks-C4 and HM12-gateway-rooms-platforms-hooks-C5 (adapter capability flags; ADDING-A-CHANNEL checklist): conditional on an iMessage adapter that is not scheduled; fail-closed defaults are already house style in `agents/channels.py:53-131`. Belongs to the channel workstream when C6 is dispatched, not here.
- HM15-cli-model-config-ux-C17 (bang-shell passthrough): conditional on a composer affordance nobody has asked for; its important half — the subprocess environment rebuilt without provider keys (`hermes_cli/bang_shell.py:89-99`) — is lifted in Theme G. If ever built, it must reuse the terminal tool's exact guard chain (the verifier's warning).
- HM19-evals-tests-meta-C8 (tool-schema-diet A/B harness): a method to reach for when a schema change is proposed; none is pending, the harness needs real model calls, and building it speculatively runs against "complete and test current features first" (DECISIONS 2026-09-01). Recorded, not dispatched.
- OC07-cli-wizard-tui-entry-C2 (progress-reporter ladder): the dashboard is the primary surface; adoption sites are RAG scrapers and index rebuilds only. The two portable ideas (single-owner guard, raw-stdin no-op) are one paragraph of notes, not a packet.
- OC08-daemon-process-worker-hooks-audit-C2 (one-shot secret via anonymous pipe): no call site hands a live credential to a child; the reader's own prerequisite is the gate. Build when one exists.
- OC11-talk-tts-acp-web-routing-context-C14 (ordered extractor chain): no web_fetch tool; attach the rule (per-extractor isolation, return the winner) to a future fetch tool's design note.
- OC12-state-sessions-secrets-security-shared-C2 (per-secret egress proxy): the task proposal cannot work as written — `execute_code` exec()s in-process with default builtins (`execute_code.py:446-448`, an accepted "scan, not a sandbox" posture), so a per-secret host allowlist has no choke point; egress control must live outside the executing process. Architectural lesson only; presupposes a secret store and an egress boundary, neither of which exists.

### Proposed packets

**TT-01 Shell executor hardening** — Theme A entire: OC13-C5 + OC08-C1, HM11-C18 + HM07-C5, HM07-C15 + HM08-C5 + OC08-C18, HM18-C9, HM06-C5, HM07-C7; plus the file-write riders HM07-M2 + OC19-C14 and OC19-C9; plus audit A04-G1/G2/G3/G4/G8 and A05-G4 as the execute_code phase. Prerequisites: none (first packet to dispatch). Hot files: `tools/executor.py` (shared hot file — group spawn, pump cap, hints, redaction), `streaming/agent_pool.py`, `streaming/pty.py`, `tools/execute_code.py`, `tools/script_rpc_host.py`, `tools/write_config.py`, `dashboard/parent_watchdog.py`, new `tools/spill.py`, `utils/process_identity.py`, `utils/atomic_write.py`, `security/text_hygiene.py`. Founder decision: none.

**TT-02 Child-process boundary fence** — Theme G entire: HM03-C13 + OC01-C16 (= A17-G1), HM05-M4 + OC24-C11 now-half, HM05-M3, OC12-C4 + OC19-C7; plus A17-G10 (stdio killpg) since TT-01's helper lands first. Prerequisites: TT-01 (kill helper). Hot files: `mcp/client.py`, `streaming/pty.py`, `streaming/session_manager.py`, `streaming/agent_pool.py`, `home/cognitive_loop.py`, `dashboard/routes/approvals.py`, `crypto/storage.py`, `streaming/sandbox.py`, new `security/child_env.py`, `security/trusted_exec.py`, `utils/run_detached.py`; touches `agents/state_machine.py` at the two `to_thread` sites (`:3656`, `:3700`). Founder decision: none — this is the "one policy pipeline" directive applied to propagation; coordinate with the permissions/security section, which will list A17-G1 too.

**TT-03 Command classifier v2 and the confirmation gate** — Theme D: HM07-M4, HM06-M1, HM06-M3, HM06-M5, HM06-C2 + HM07-M6, HM06-M2; HM06-M4 handed to the skills packet with a pointer to the shared scan library. Prerequisites: TT-01's `text_hygiene` (ANSI/NUL strip is step one of normalization). Hot files: `tools/safety.py`, `dashboard/routes/terminal.py`, `streaming/injection_check.py`, `tools/executor.py:604-611` (shared hot file), `agents/state_machine.py` (AWAITING_CONFIRMATION, `:834-856`, `:994-1000`, `:1514`, `:3468-3480`, `:4464-4473` — shared hot file), `agents/channels.py`, `scheduler/run_receipts.py`, new `tools/command_norm.py`. Founder decision: YES — the per-origin approval defaults and the bounded-wait length (see below), and the founder deny-list file location.

**TT-04 Background registry, yield, wake, and the watched-terminal surface** — Themes C and B: HM07-C3, HM07-M1 (= A07-G8), HM07-C8, HM07-C4, HM07-C6, OC09-C2, HM11-C11 record-lane, OC08-C17; HM18-C18, OC24-C10, OC18-C6, OC03-C12, OC19-C13, HM18-C13, HM14-C15 receive-side stub; plus A07-G2 (stop must kill, steer must not) and A04-G9. Prerequisites: TT-01 (kill ladder, fingerprint, spill), ROADMAP TERM-1 open call `R04-POOL`/`R04-F2` (watched shell wiring). Hot files: `tools/executor.py` (shared), `agents/state_machine.py` (`:1946-1978` completion publish, tool wait loop — shared), `dashboard/routes/agent.py` (shared — steer/redirect entry), `agents/steering.py`, `agents/turn_activity.py`, `proactive/events.py`, `dashboard/routes/websocket.py`, `dashboard/routes/terminal.py`, `streaming/session_manager.py`, `streaming/shell_integration.py`, `dashboard/frontend/src/hooks/useTerminalSessions.ts`, `hooks/useTasks.ts`, `components/agent/StatusStrip.tsx`, new `tools/process_registry.py`, `tools/notification_throttle.py`, `agents/completion_contract.py`, `agents/status_controller.py`. Founder decision: YES for the wake lane only (record-only lane needs none). Largest packet; split C from B if two executors are available (B has no Python dependency on C beyond the status-controller event source).

**TT-05 Tool-loop guardrails and malformed-call recovery** — Theme E: HM01-C13 first, then HM01-C5 + OC14-C6 + OC14-C7 + OC15-C15, OC14-C20, OC14-C4, HM02-C9 (+ A16-G5 in the same row); plus A07-G5. Prerequisites: none technically; sequence after TT-03 if both touch `state_machine.py` in the same tree (different call sites: dispatch loop `:2668-2776` vs confirmation `:3468+`/`:4464+`). Hot files: `agents/state_machine.py` (shared), `model/client.py:457-465`, `tools/executor.py:500-531` (shared), `agents/react_agent.py`, `agents/conversation_sqlite.py` (shared — the UNKNOWN-effect / interrupted marker row), `agents/threads.py`, new `agents/tool_guardrails.py`. Founder decision: none (A07-G11 auto-resume is explicitly NOT in scope).

**TT-06 Subagent manager correctness** — Theme F: HM05-M1, HM05-M2, HM05-C16 + HM05-M6, OC14-C16, OC15-C10, HM05-C9, HM05-C7, HM05-M7, HM05-C5, OC09-C4; plus A07-G7's cancel-propagation half and A07-G10's activity stamp shared with M1. Prerequisites: TT-05 (generation/detach primitive for late results). Hot files: `agents/subagent.py`, `agents/state_machine.py:2007-2050` (shared), `agents/states.py:143`, `agents/turn_event_tee.py`, `agents/conversation_sqlite.py` (shared — results/lease table if persisted there), `prompts/` assembler for the data-not-instructions block. Founder decision: YES — whether `SubagentManager` is wired to a production consumer at all; M1/M2 are worth fixing regardless because tests pin the manager.

Not packeted here (routed): Theme I items OC07-C1 + OC07-C14, OC07-M3, OC07-M4, OC07-M2, OC07-M1 go to the dashboard/onboarding/diagnostics packet writer (shared hot file `dashboard/app.py:1744`); Theme J items HM06-C3 (+ HM06-C6), OC11-C13 go to the permissions/security packet writer; Theme H standalones HM07-M3, HM08-C10, HM05-M5, HM05-C15 (the chroma orphan fix — dispatch this one immediately as a single-file defect), HM17-C2 (founder-gated) are single-file changes dispatchable without a packet; HM07-C12/C13/C14/C23/C22/M5 and HM07-C1 wait on the founder's file-edit-tool decision.

### Open founder decisions

1. "When a HIGH-risk tool is proposed by an origin with no human present — the scheduler, an MCP-originated turn, a voice turn with no dashboard attached — what is the verdict, and how long may a turn wait for a human before that verdict is applied?" Default: `deny` for scheduler and MCP origins, `ask` with a 10-minute bound for voice and dashboard, deny-on-timeout written to the receipt with Hermes's "silence is not consent" and anti-rephrase wording. (TT-03, HM06-M2, A11-G7.)
2. "May a background completion start an agent turn on its own when no turn is in flight?" Default: no — completions are persisted as inert transcript rows and published on the proactive bus; the wake lane is built behind the proactivity dial, off, and structurally refused while a confirmation is pending. (TT-04, HM11-C11.)
3. "Does the host steward get a general targeted file-edit tool (`patch(old_string, new_string)`) on arbitrary files, or does `write_config` on config-shaped files stay the only agent file write?" Default: `write_config` stays the only write until a concrete steward task needs more; if approved later, it lands behind TT-03's gates with the atomic writer, delta lint and NFC ambiguity refusal. (HM07-C12 family, HM07-C1.)
4. "May the machine delete files it created in its own scratch root, or only propose?" Default: propose-and-stage only, scratch root only, never by filename pattern. (HM17-C2.)
5. "Is `SubagentManager` to be wired to a production consumer now, and which one?" Default: fix the wedge and staleness bugs (tests pin the manager), leave `spawn_subagent` unwired until a consumer is named; ownership/depth rules land with the wiring. (TT-06.)
6. "Where does the founder-authored command deny list live?" Default: a `security` section in a 0600 file under the config dir (not `being.yml`, whose split is pending under `A2-05`), consulted before every other decision and matched against normalized variants. (TT-03, HM06-M3.)
7. "Should agent-run git worktree mutations inside Halbert's own source root require confirmation?" Default: yes — one HIGH/confirm rule for checkout/switch/reset --hard/clean/rebase/merge/pull/worktree when cwd resolves inside the repo root, because concurrent sessions edit this checkout; no rule elsewhere. (TT-03, HM06-C2 + HM07-M6.)
8. "On an onboarding rerun with a different computer name, refuse or confirm?" Default: refuse silently renaming; return the conflict and require an explicit confirmation, current name wins on decline. (OC07-M2, standing directive 2026-08-25 and open call `C1-02`.)
