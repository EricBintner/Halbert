## models-providers-cost — 54 verified mechanisms from HM01, HM03, HM04, HM06, HM09, HM12, HM14, HM15, OC03, OC04, OC05, OC07, OC13, OC14, OC20, OC21, OC22, OCC01 (audit unit A14; A10's refutation bears on the utility slot)

Scope: the model picker and its five connection slots, the provider adapters, the utility slot, local-model handling (Ollama and OpenAI-compatible endpoints), the retry/fallback/rate-limit layer, credentials on the model path, and usage/cost accounting as a mechanism only. Model names never appear on any surface this section proposes; every notice, log line and breakdown keys on the connection slot and its locality.

Origin roots: OpenClaw paths are relative to `/Volumes/Thunderbolt/AI/openclaw`, Hermes paths to `/Volumes/Thunderbolt/AI/OSS/hermes-agent`, open-claude-code (OCC) to `/Volumes/Thunderbolt/AI/OSS/open-claude-code`. Halbert paths are relative to `/Volumes/4TB-BAD/Halbert/halbert_core/halbert_core` unless they start with `packages/`, `dashboard/` or `tests/`.

Spot-checks I ran in the Halbert tree while writing this (all on main, 2026-09-09): `model/rate_limiter.py` clamps an explicit Retry-After to `max_backoff=60.0` (`wait = min(retry_after, self._max_backoff)`); `model/utility_slot.py` contains no `is_local_model`/cloud/secure reference; `agents/error_recovery.py`'s `is_circuit_open` and `record_success` have zero callers (the only production caller of the module is `model/tier_router.py:735` `record_failure`); `tier_router.py` writes `self._model_health[cache_key] = False` on fallback without stamping `_last_health_check`; `agents/llm_client.py` uses `aiohttp.ClientTimeout(total=self.timeout)` at 149/165/225/385/438 (line 246 is a log line); `tools/system_tools.py:157-162` runs `subprocess.run` with no `env=`; `model/client.py:1466-1474` lists `/v1/models` with no Authorization header and `is_model_loaded` treats a listing as loaded. All eight verifier claims hold. One verifier worry does not: `model/llm_config.py:966-974` `_endpoints_matching` compares `_normalise_url(url)` (strip, rstrip `/`, lowercase) for whole-URL equality, so a saved credential cannot attach to a different host today; HM15-M6 is demoted accordingly (see theme G).

### What Halbert has here today

Halbert routes through five connection slots (`chat_model`, `specialist_model`, `secure_model`, `vision_model`, `utility_model`) declared in `model/llm_config.py` and picked in `packages/model-picker` (chat/specialist/vision slots per the 2026-08-26 decision; the utility slot landed as D-7 on 2026-09-07 and spoken summarization 04-C2 consumes it through `integrations/speech_summarizer.py`). Model locality has one choke point, `llm_config.is_local_model()` (SEC-21, 2026-09-08: `:cloud` tag is the primary signal, loopback is necessary never sufficient), applied at `normalise` to the `secure_model` slot only (`llm_config.py:504-518`) and at the turn gate. `model/tier_router.py` is the live routing layer: a candidate list (`:481-522`), a 429/529 retry loop (`:685-740`) over a reactive `RateLimiter` (`model/rate_limiter.py`, in-memory, per process, 60 s ceiling on both the backoff and an explicit Retry-After), a single re-route fallback (`:745-757`) that marks the failed `provider:model` unhealthy on any `GenerationError`, and a 5-minute health cache (`:425-432`). `agents/error_recovery.py` holds a nine-bucket `ErrorType` classifier and a circuit breaker whose open/closed state is written and never read. The Ollama adapter already reads `prompt_eval_count`/`eval_count` into `ModelResponse.metadata` (`model/providers/ollama.py:185-187, 255-257`) and `LLMResponse.usage` (`agents/llm_client.py:279-280`), but `context/tokens.py:16-80` and `model/client.py:716` estimate from characters and drive `num_ctx` (`client.py:731-746`), logging rather than acting when the prompt exceeds the window; `_MODEL_MAX_CACHE` (`client.py:909-912`) is keyed by model name and only ever grows. `agents/llm_client.py` speaks to Ollama over aiohttp with a 120 s total timeout on both the chat and the streaming path; `model/providers/*.py` and `client.py` use `requests` with library-default redirects and a literal `Authorization: Bearer` header. Tool calls are read only from a provider's structured `tool_calls` field (`client.py:440-469`); there is no plain-text repair, no argument coercion, no output-quality check. Credentials live inline in `models.yml` (0600) and are looked up by normalised URL (`llm_config.py:966-991`); the body's signing key has a full custody ladder in `crypto/storage.py` (Secure Enclave/TPM, then Keychain via `security`, then a 0600 file with refusal on loose permissions) that provider keys do not use; `mcp/config.py:180-209` already resolves a `token_env` reference for MCP servers. The prompt carries a literal `CACHE_BOUNDARY_MARKER` with a stable-prefix key (`prompts/agent_prompts.py:20-60`, SK-2) that no provider adapter consumes. Fallback is announced through `StreamEvent.model_selected.fallback_from` (`agents/events.py:135-167`, `dashboard/routes/agent.py:755-777`, `useAgentStream.ts:592-604`), re-announced on every fallback turn, and raw exception text reaches the SSE stream at `routes/agent.py:1861,1927`. There is no cost concept anywhere in the tree, no usage columns in `agents/conversation_sqlite.py` beyond compaction pre/post token counts, and no self-diagnostic command. Already lifted on this seam: the utility slot ladder (D-7), 04-C2 spoken summarization, SEC-21 locality, STUB-01 (the `TierRouter` OpenAI stub and the cost-cascade router deleted), the cache-boundary marker (SK-2), and the eval harness + probe battery (09) that a packet here should add probes to.

One structural fact from the A10 refutation reshapes the utility-slot findings: the engine's `VoiceRiskPolicy` word cap (12-35 words) is enforced upstream of the speech summarizer, whose deterministic gate needs more than 600 characters (`speech_summarizer.py:98`), so the summarizer, and therefore today's only caller of `resolve_aux_model`, is unreachable. The locality hole in the utility ladder (A14-G4, OC20-C18) is a latent structural bypass rather than a live leak. It stays high because the ladder is the designed home for every future cheap internal task (card generation, auto-review, summarization when the cap is lifted) and because the bypass exists at exactly the choke point SEC-21 declared unique.

### Lift themes, ranked

Effort keys: S under a day, M a few days, L a week or more. Verifier-added items carry my estimate. "Confirmed twice" marks a mechanism both origins have.

#### Theme A — One locality choke point for every resolution path (high)

**Locality gate on the resolved default and on every utility-ladder rung, not only on explicit overrides** (OC20-extensions-memory-knowledge-automation-C18; audit A14-utility-slot-model-G4; confirmed twice: Hermes `agent/auxiliary_client.py:757-766` re-checks policy-blind rungs against provider policy, OpenClaw `src/agents/utility-model.ts:102-110` makes the derived default inherit the primary's isolation) — origin `extensions/llm-task/README.md:64-68` (prose), enforced at `src/plugins/runtime/runtime-llm.runtime.ts:344-365` (`resolveAuthorityModelPolicy`) and `:386-410` (`assertModelAllowed` taking kind `override | completion`, so the completion allowlist covers the resolved default). Mechanism: an admin allowlist is applied at the one place a model is resolved, so an unconfigured default cannot bypass a policy that explicit overrides must pass; the invariant is "no resolution path exists that the policy does not see". Halbert state: partial (verifier corrected). `llm_config.py:504-518` applies `is_local_model()` to `secure_model` alone; `model/utility_slot.py` (`:14-49` documented ladder, `:93-147`, `:170-207`) has no locality reference on any rung, and its catalog rung probes the anchor's provider catalog and takes the smallest same-family entry, which can be a `:cloud` sibling or the operator's OpenAI-compatible endpoint; `agents/state_machine.py:4143-4163` invokes the summarizer without consulting `secure_context` (tracked at `:2565-2571`, used at `:2625/3606/3943/3968`). Task: add `require_local: bool` to `resolve_aux_model`/`_resolve_aux`; when set (always when the anchor turn is `secure_context`, and by default for any caller that sends conversation text), every rung's candidate passes `llm_config.is_local_model(model, url, provider)` or is dropped and the ladder continues; if no rung passes, return no pick (the caller degrades to screen-only, mirroring the secure-slot fail-closed precedent); the summarizer call site passes `require_local=ctx.secure_context`. Tests: the A14-G4 proposed test in `tests/test_utility_slot.py` (a smaller `:cloud`-tagged sibling in the catalog is skipped in favour of the local 8b; a declared utility slot on a non-local endpoint is refused for a secure anchor), plus a state_machine test that a secure turn never reaches `call_llm_chat` with a non-local URL. Effort S. Prerequisites: none (SEC-21 is merged). Constraint: this is the SEC-21 directive applied to the one path that escaped it; the decision "drop the rung vs refuse the whole summary" is founder decision 1 below, default drop-and-continue, no summary if nothing local answers.

#### Theme B — Measured tokens, context windows and loaded state (high)

**Usage-anchored token accounting: estimate only the delta since the provider's last authoritative count** (HM03-agent-providers-credentials-usage-C5) — origin `agent/usage_anchor.py:1-93` (verifier: cite 1-93, not 1-70; `message_fingerprint` 34-43, `capture_usage_anchor` 46-62, `_anchor_matches` 65-76, `anchored_context_tokens` 79-93, persist/restore 110-151). Mechanism: after each call, remember the provider's real `prompt_tokens` and a content fingerprint of the transcript position it priced; on the next turn estimate only the messages added since, so drift is bounded to one turn instead of the whole conversation; a fingerprint mismatch fails closed to full estimation. Halbert state: partial (verifier corrected from absent). The signal is already read (`model/providers/ollama.py:185-187, 255-257`; `agents/llm_client.py:279-280`); nothing feeds it back, and the pure estimate drives `num_ctx` at `client.py:731-746`, where an undercount makes Ollama truncate the head of the prompt. Task: add a per-thread anchor (fingerprint over message content, not object identity, so a resumed thread still matches) captured in `client.py`'s Ollama response handling and in `llm_client.py`, and make `context/tokens.py`'s budget use the anchored count as the floor for already-priced messages; pin with a test that a 40-message thread whose anchor prices 38 messages estimates only two, and that a mutated message invalidates the anchor. Effort M. Prerequisites: none. Constraint: none; the anchor keys on the slot, not the model.

**Learn the real window from the provider's error text; never confuse "prompt too long" with "max_tokens too large"** (HM03-agent-providers-credentials-usage-M2, verifier-added) — origin `agent/model_metadata.py:1053-1056` (`get_next_probe_tier`), `:1058-1077` (`parse_context_limit_from_error`, with a 1024..10,000,000 plausibility clamp), `:1080-1085` (adopt a parsed limit only when lower than the current window), `:1088-1105` (`parse_available_output_tokens_from_error`, a distinct class whose remedy is a smaller `max_tokens` for this call only). Mechanism: two overflow errors that look alike get two remedies, both by regex over the error string, and a provider-quoted window is adopted only downward. Halbert state: absent. `client.py:735-746` detects overflow, logs "Ollama will truncate the head of the prompt", and sends anyway; the correction machinery exists (`compute_num_ctx` `:1133-1140`, `_NUM_CTX_CACHE` `:1184-1199`, `model_context_limit`). Task: a small error-text parser beside the overflow handling that (a) adopts a lower quoted window through the same path `model_context_limit` uses, (b) retries the output-cap class with a smaller `num_predict` leaving `num_ctx` alone, (c) compresses rather than guessing when nothing parses; tests with three canned error bodies (vLLM `max_model_len`, "N tokens > M maximum", an output-cap message) asserting which knob moved. Effort S. Prerequisites: C5 above makes the estimate honest first. Constraint: no model tables; pure regex.

**Route-keyed, monotonic-within-key context-length cache** (HM15-cli-model-config-ux-M2, verifier-added) — origin `hermes_cli/route_identity.py:9-44` (`normalize_route_base_url`: only provably-equivalent components canonicalised, unparseable input returned raw), `:47-59` (`should_clear_context_pin`, fail-closed: any comparison error drops the pin). Mechanism: a cached context verdict describes a route, not a name; when the endpoint changes the pin is invalidated rather than merged. Halbert state: partial. `client.py:909-912` `remember_model_context_limit` writes `_MODEL_MAX_CACHE[model]` only when larger; the same model name at a local daemon and at a saved remote endpoint collapse into one process-lifetime maximum, and the larger wins for both. Task: key the cache by `(provider, normalised_base_url, model)` with a `normalize_route_base_url` equivalent (reuse or extend `llm_config._normalise_url`, which today only lowercases and strips the trailing slash, so add default-port dropping and keep every other port), keep the raise-only rule within a key; test that the same name at two URLs holds two limits and that `http://localhost:11434` and `http://localhost:11434/` share one. Effort S. Prerequisites: none.

**Loaded state and the loaded context window come from LM Studio's own API, never from the OpenAI-compatible listing** (HM15-cli-model-config-ux-M5, verifier-added) — origin `hermes_cli/models_local.py:360-372` (server root), `:465-483` (`_lmstudio_loaded_context` walks `loaded_instances[*].config.context_length`), `:484-520` (`ensure_lmstudio_model_loaded`: existing loaded-instance context is authoritative, a target above `max_context_length` is rejected not clamped, typed result distinguishes not-loaded / load-attempted / rejected). Mechanism: "loaded" is measured from the runtime's loaded-instance state; the listing says only "installed". Halbert state: partial. `client.py:1466-1474` GETs `{endpoint}/v1/models` for `provider == "openai"` with no Authorization header and maps every entry to a name; `is_model_loaded` (`:1487-1497`) reports True for anything listed, which for LM Studio means installed, not loaded, so any "the model is loaded" surface states something Halbert did not measure. Task: probe `/api/v1/models` when the endpoint answers it and read `loaded_instances` for the verdict, returning `unknown` rather than True when it does not; send the saved endpoint's bearer on the listing call; feed `loaded_instances[*].config.context_length` into the context ladder as a stronger signal than a listing (`client.py:915-935` `remember_listing_context_length` already documents that distinction). Tests: a fake LM Studio server with two installed and one loaded model. Effort S. Prerequisites: M2's route key so the loaded window is stored per route. Constraint: grounded-in-measured-data; the picker's provider list already names LM Studio as a product, which is fine, the rule bars model names.

**Mid-conversation slot switch: state the consequence before the next turn** (HM15-cli-model-config-ux-M1, verifier-added) — origin `hermes_cli/context_switch_guard.py:18-19, 50-112` (resolve the incoming model's real window, estimate the live session's tokens, compute the new compression threshold, append one plain warning; silent when numbers are unknown, when compression is off, or after two ineffective compressions). Mechanism: warn about a consequence once, from numbers already computed, never nag. Halbert state: absent on the user-facing half (`client.py:731-751` logs at call time, after the message was sent). Task: a deterministic post-save check on the endpoint/model save path (`dashboard/routes/settings.py` save route and the `packages/model-picker` save flow) comparing the outgoing slot's effective window with the incoming one via `architecture_context_length()`/`_MODEL_MAX_CACHE`, estimating the live thread's tokens (C5's anchor makes this cheap), and returning one sentence for the picker to render; suppressed when either number is unknown. Test: a save that shrinks the window returns the sentence, a save that grows it returns nothing. Effort S. Prerequisites: C5, M2. Constraint: one continuous conversation is the design this protects; the sentence names the slot, never the model.

**Set Ollama `keep_alive` from the conversation's own liveness** (OC22-extensions-local-providers-apps-C9) — origin `extensions/ollama/src/stream.runtime.ts:188` (`keep_alive` is one of four pass-through top-level params; the verifier notes the origin sets no policy, it only exposes the lever). Mechanism: the caller decides how long the daemon keeps the model resident instead of Ollama's blind default timer. Halbert state: absent (`agents/llm_client.py:129-139` builds the payload without it; the only `keep_alive` hits are launchd plist keys). Task: add `keep_alive` to the Ollama payload in `llm_client.py` and `providers/ollama.py`, set from a single residency policy (keep the active chat slot's model loaded while the thread is live, release after an idle window) that also consults `gpu_tools.py`'s memory budget so a shared GPU is not starved. Effort S once the policy is decided. Prerequisites: founder decision 6 on the idle window. Priority low as the verifier ruled; the design is the cost, not the key.

#### Theme C — Transport liveness: idle-gap timeouts, stop that stops the wire, a breaker that is read (high)

**Idle-gap stream timeout scaled by context size with a capability-keyed reasoning floor** (HM03-agent-providers-credentials-usage-C7; verifier raised medium to high, a live defect) — origin `agent/chat_completion_helpers.py:586-590` (configured base, default 180 s), `:592-599` (`_scale_stale_timeout_for_context`: >100k tokens gives max(base, 300), >50k gives max(base, 240)), `:601-613` (`_cloud_stale_timeout` applies the scale then the floor); `agent/reasoning_timeouts.py:19-43` is a model-name floor table and is the anti-pattern, `:60-72` the floor accessor. Mechanism: the stale detector measures the gap between chunks, not total wall time, widens with the prompt, and gets a floor when the endpoint is a reasoning model. Halbert state: absent, worse than the reader wrote: `agents/llm_client.py:149/165/225/385/438` are `aiohttp.ClientTimeout(total=self.timeout)` with `self.timeout` defaulting to 120 (`:83, :94`); line 225 is inside `stream()`, so a healthy stream still emitting tokens is killed at 120 s. Task: switch the streaming path to `ClientTimeout(sock_read=idle_gap, total=None)` with an explicit idle-gap detector, keep a generous total only on the non-streaming path, scale the gap by C5's anchored prompt tokens, and apply a floor when `utils/reasoning.py:97-130` `is_reasoning_model()` is True (capability from `/api/show`, never a name table); test with a fake server that emits a chunk every 10 s for 200 s (must survive) and one that goes silent for the gap (must be killed with a typed stale error). Effort S. Prerequisites: none; pair with M3 because it touches the same two call sites.

**A consecutive-stale streak gate: give up before the next attempt** (HM03-agent-providers-credentials-usage-M1, verifier-added) — origin `agent/chat_completion_helpers.py:518-533` (streak bump/reset), `:538-547` (an interrupted 30 s wait with no output counts as stale), `:573-583` (`_check_stale_giveup` raises before any network attempt once the streak hits the default 5), `:960` (reset on success). Mechanism: staleness is remembered across attempts; the gate is consulted before the request is issued, so an unresponsive backend costs a bounded number of stale waits and then a message telling the user to switch connections. Halbert state: partial, and the shape is inverted: `agents/error_recovery.py:105-107` keeps `_failure_counts` with `circuit_breaker_threshold=5`, `record_failure` (`:205-211`) opens the breaker, and `is_circuit_open` (`:219-233`) and `record_success` (`:213-217`) have zero callers (spot-checked). Task: consult `is_circuit_open` at the top of `tier_router.generate()` and in `_check_model_health`, call `record_success` at `tier_router.py:711` where the rate limiter is reset, count stale kills from C7 as failures, and surface the refusal as a typed turn exit ("this connection has not answered N times") rather than another timeout; the missing test: a model that fails threshold times is not attempted again until the reset window. Effort S. Prerequisites: C7 for the stale signal; theme D's typed status so the refusal has a name.

**Stop stops the wire: abort the in-flight request, shutdown() sockets from foreign threads, never close()** (HM03-agent-providers-credentials-usage-M3, verifier-added) — origin `agent/client_lifecycle.py:217-268` (`_drain_transports_after_abandonment`: a worker blocked in an OpenSSL read must not have its FDs hard-closed from another thread; shutdown() so the read sees EOF), `:271-295` (retire a replaced shared client, do not close it), `:391-420` (abort plus poison the reuse slot). Mechanism: cancellation from a different thread than the one blocked on the socket has one safe answer, and the abandoned client is retired with its pool slot poisoned. Halbert state: partial: the stop/steer/redirect verbs are race-safe (`agents/steering.py:1-26`, `agents/turn_activity.py:24-58`, `state_machine.py:1564-1614`) but nothing touches the HTTP request; after a stop the model keeps generating until the total timeout, up to 120 s on the non-streaming path, and billable on a peer or cloud slot. Task: give the turn an abort hook that closes the aiohttp response/session for the cancelled generation, claimed through `TurnActivity` so a late abort declines instead of killing the next turn; on the `requests`-based paths in `model/providers/*.py` follow the shutdown-not-close rule; test that a stop during a slow fake stream closes the connection within one idle gap. Effort M. Prerequisites: C7 (same call sites); the interrupt algebra (07) is merged.

**Interruptible, activity-touching backoff sleep in the live retry loop** (HM01-agent-turn-family-C8) — origin `agent/turn_recovery.py:938-963` (200 ms slices, interrupt check each slice with the redirect-preserving restart branch at 951-957, activity touch every 30 s at 960-962). Mechanism: a backoff wait is decomposed so a stop lands within 200 ms and an inactivity monitor sees the turn as alive. Halbert state: absent. The verifier's correction: the reader pointed at `utils/retry.py:116/:197`, whose only consumer is `scheduler/executor.py:572`; the interactive one is `model/tier_router.py:736`, a bare `time.sleep(wait)` inside the live 429/529 loop, where a `/stop` cannot land for up to 60 s (more once theme D honours Retry-After). Task: replace `tier_router.py:736` with a sliced wait that checks the turn's stop flag and touches `TurnActivity`; do the same in `utils/retry.py` for background jobs; test that a stop during a 30 s wait returns within 0.5 s. Effort S. Prerequisites: none; do it in the same packet as theme D's Retry-After change, or the longer waits arrive without an exit.

**Leak-safe cross-thread coroutine scheduling** (HM01-agent-turn-family-M5, verifier-added, low) — origin `agent/async_utils.py:20-41` (`safe_schedule_threadsafe`: every failure path calls `coro.close()` before returning None; once the loop accepts the coroutine its lifecycle belongs to the loop), `:44-50` (`consume_detached_task_result`). Mechanism: a coroutine the loop refuses is closed, so shutdown does not emit "never awaited" warnings and leak frames. Halbert state: absent at two sites: `tools/execute_code.py:398-405` (RPC host thread schedules `executor.execute`) and `integrations/wyoming_agent.py:187-192` (checks `is_closed()` then schedules unguarded). Task: one helper under `utils/`, both sites routed through it, a test that a closed loop yields None and a closed coroutine. Effort S. Prerequisites: none. Listed here because it is transport lifecycle; it can ride with any packet that touches `execute_code.py`.

#### Theme D — Provider failure semantics: taxonomy, Retry-After, cooldowns, backend identity, slot status (medium, with two live defects)

**Honour Retry-After in full; cap only the exponential branch; make `blocked_until` load-bearing** (HM01-agent-turn-family-C7 and HM03-agent-providers-credentials-usage-M4, merged: same defect, two origins) — origins `agent/turn_recovery.py:973-1025` (`compute_error_backoff`: Retry-After from headers then from a nested `error.retry_after` body field at 991-999, `min(retry_after, 600)` with the recorded incident "Tier 1 input-token buckets reset in ~171 s, so a 120 s cap caused us to retry before the actual reset window and re-trip the limit" at 1001-1004, zero/expired clamp-to-absent at 1005-1009), `agent/fallback_cooldown.py:1-27` (60 s to 4 h exponential cooldown armed only when leaving the primary), `agent/credential_pool.py:127-133, 309-336` (`_exhausted_ttl`: the cooldown length is a decision with a reason, sized by what failed). Mechanism: a provider-stated reset window is authoritative and normally honoured in full; the ceiling belongs on the guessed exponential branch, not on an explicit instruction. Halbert state: present and wrong (verifier marked M4 `present`): `rate_limiter.py:107` `wait = min(retry_after, self._max_backoff)` with `max_backoff=60.0` (`:59-63`), so `Retry-After: 3600` becomes five 60 s retries (`should_retry` `:78-90`, `max_retries=5`), each guaranteed to fail, and `RetryState.blocked_until` (`:127`) is only read by `status()` (`:133-146`) which nothing calls. Task: honour Retry-After up to a sane ceiling (the turn's own deadline; 600 s is Hermes's number), keep `max_backoff` for the jittered branch at `:110-112`, and consult `blocked_until` in `should_retry`/`get_wait_time` before the attempt; when the window exceeds the turn deadline, fall back immediately with a typed reason rather than waiting; tests: a 171 s header waits 171 s, a 3600 s header on an interactive turn falls back at once with reason `rate_limited_until`. Hold the primary-scoped 4 h cooldown half (Halbert's fallback is a single re-route, not a chain) until HM15-C3 exists. Effort S. Prerequisites: C8 (interruptible wait) in the same change. Constraint: none.

**Priority-ordered provider-error taxonomy with billing / rate-limit / overloaded / context-overflow / auth-permanent classes and a billing-unverified hedge** (HM01-agent-turn-family-C6) — origin `agent/error_classifier.py:25-56` (`FailoverReason` enum with per-member recovery comments), `:59-83` (`ClassifiedError`: retryable, should_compress, should_rotate_credential, should_fallback; `billing_unverified` at 76-79), `:87-127` (ordered pattern tables; the "same 'out of extra usage' body is also returned for a content-filter rejection" rationale at 98-101; "overloaded: credential fine, back off on the same key, never rotate" at 123-127). Mechanism: a raw exception maps to exactly one reason with pre-computed recovery hints, and an ambiguous billing body is hedged rather than asserted. Halbert state: partial. `agents/error_recovery.py:18-28` has nine generic buckets; `classify_error` is at `:110` (not `:122`); the api bucket at `:124` carries `401/403/500/502/503` and 429 reaches `LLM_RATE_LIMIT` at `:121`; the manager is effectively unwired (only its own singleton at `:285` and `tier_router.py:735`), and the turn loop counts `ctx.error_recovery_attempts` by hand at `state_machine.py:4446`. Task: put the taxonomy where the retry lives, `model/tier_router.py` plus `model/rate_limiter.py`, as a small `classify_provider_error(exc, status, body, headers) -> ClassifiedError`; drive the 429/529 loop, the fallback decision and the health mark from its hints; carry the reason on `GenerationError` so themes C and E can name it; tests over canned bodies for each class. Effort M. Prerequisites: none. Constraint: provider-error pattern matching is not a model-name surface.

**BackendIdentity + FailureScope: skip a fallback candidate only along the axis the failure invalidated; stamp the unhealthy mark with its own time** (HM03-agent-providers-credentials-usage-C1 and HM03-agent-providers-credentials-usage-M6, merged: the same twenty lines of `tier_router.py`) — origin `agent/backend_identity.py:23-32` (`FailureScope` MODEL/CREDENTIAL/ENDPOINT), `:39-58` (frozen `BackendIdentity` + `build()`), `:77-118` (`same_credential_surface`, `same_endpoint`, `same_deployment`), `:127-133` (`should_skip_candidate`), consumed at `agent/chat_completion_helpers.py:1824-1842` (`try_activate_fallback` keeps a per-turn `_unavailable_fallback_keys` set, no TTL, and consults the predicate before every candidate; `:1860-1863` a candidate whose client cannot be built joins that set). Mechanism: "is this candidate the same failure surface as the one that just failed, along the axis that failed?" is one normalised identity and three predicates, and per-turn unavailability is a different fact from TTL'd health. Halbert state: absent for the predicate, partial with a bug for the health mark. `tier_router.py:744-749` writes `_model_health['{provider}:{model_id}'] = False` on any `GenerationError` (401, DNS and timeout alike, the exact axis conflation Hermes's docstring warns about) and does not write `_last_health_check`, so the False expires against the timestamp of the original successful probe (`:430-432`, 300 s window): a model failing 290 s after its last check is eligible again 10 s later, and `provider.health_check()` (`/api/tags` at `providers/ollama.py:61`) says healthy for a model that cannot generate. Task: `model/backend_identity.py` with `BackendIdentity(saved_endpoint_id, provider, model, normalised_base_url)` (key on the saved-endpoint id, not a named-provider registry) and `should_skip_candidate(candidate, failed, scope)`; use it in the candidate walk at `:481-522` and the fallback recursion at `:746-756`; stamp `_last_health_check` wherever `_model_health` is written; keep a per-turn failed set separate from the TTL cache. Tests: a 401 on endpoint A skips every candidate sharing A's credential and none on B; a timeout marks the deployment, not the model name; the unhealthy mark lasts its full window. Effort S. Prerequisites: C6 supplies the scope from the classified reason.

**Slot status vocabulary: terminal (needs re-auth) versus transient (cooldown)** (HM03-agent-providers-credentials-usage-C17, low, rider on C1) — origin `agent/credential_pool.py:1072-1089` (`_is_terminal_auth_failure`: only a 401 whose reason is in a closed permanent set; a reason-less 401 and `token_expired` stay transient), `:1091-1122` (`_mark_exhausted` DEAD vs EXHAUSTED, rationale at 1102-1104: a revoked credential re-entering rotation hourly fails forever). Mechanism: a closed set of permanent reasons short-circuits to "needs explicit user action" instead of the retry schedule. Halbert state: absent (`rate_limiter.py:39-46` `RetryState` carries attempts/status/retry_after/blocked_until only). Task: a `SlotStatus` (ok / cooling_until / needs_reauth / not_local_refused / unreachable) on the routing layer, set from C6's reason, read by the dashboard slot card; a `needs_reauth` slot is not retried on the transient schedule. Effort S as part of the C1 change. Prerequisites: C6, C1.

**Cross-process rate-limit guard file for the cloud slot, with the genuine-exhaustion guard** (HM03-agent-providers-credentials-usage-C2) — origin `agent/nous_rate_guard.py:31-38` (state path), `:50-78` (`record_nous_rate_limit`, atomic write), `:80-93` (remaining window, expired state unlinked), `:96-104` (clear on success); strengthening the reader missed, `:26` `_MIN_RESET_FOR_BREAKER_SECONDS = 60.0` and `:106-129` `is_genuine_nous_rate_limit` (only `remaining == 0` with `reset >= 60 s` trips the shared breaker). Mechanism: a 429 discovered by one process is visible to every other before it fires. Halbert state: absent (`rate_limiter.py:68` in-memory per `TierRouter`, constructed at `tier_router.py:270`; the dashboard/FastAPI process, the scheduler and the MCP server can each hold one). Task: `record_retry` writes `{slot_id, blocked_until, reason}` atomically to a file under the config home and `should_retry`/`get_wait_time` read it first; lift the genuine-exhaustion guard with the file, not after; test with two `RateLimiter` instances sharing a temp dir. Effort S. Prerequisites: the Retry-After fix above (a 60 s-clamped window is not worth sharing). Single-host, single-user: this degenerates to one file and stays correct.

**Pre-emptive, self-calibrating token-bucket pacing** (HM12-gateway-rooms-platforms-hooks-C14, extends-lifted, low) — origin `gateway/platforms/signal_rate_limit.py:100-172` (`acquire(n)` before the send releasing the lock across the sleep, `report_rpc_duration` deducting after the RPC with no refill credit for the in-flight window, `feedback(retry_after)` emptying the bucket and recalibrating `refill_rate` from the server's own signal). Mechanism: pace before the 429 and learn the rate from the provider rather than a constant. Halbert state: partial (`rate_limiter.py:75-115` reactive only). Task: a small `TokenBucket` in `rate_limiter.py` fronting the cloud slot and `web/search.py`, calibrated by `feedback()` from observed Retry-After. Effort S. Prerequisites: C2 so the bucket state is shared. Low because local slots are the default path.

**Deterministic-empty-completion detection** (HM01-agent-turn-family-C10, low, verifier demoted from medium) — origin `agent/empty_response_guard.py:1-56` (two guards: two consecutive same-signature zero-output completions skip remaining retries; a costly empty attempt shrinks the retry budget from 3 to 1). Mechanism: notice identical blank completions and stop paying for them. Halbert state: absent, and the motivation does not transfer: Halbert has no empty-completion retry loop, so there is no budget to cut. Task: one line in the C6 change: two identical zero-output completions from the same backend classify as `empty_completion`, which fails over or says so instead of handing the user a blank reply. Effort S. Prerequisites: C6.

#### Theme E — Failover made visible and honest: chain, handoff, transition notice, capability invalidation (medium)

**Provider-scoped fallback chain per slot with snapshot/restore around the picker, re-gated by locality** (HM15-cli-model-config-ux-C3) — origin `hermes_cli/fallback_config.py:12-28` (`resolve_entry_api_key` through the secret scope, not `os.getenv`), `:49-74` (`_entry_identity`, `get_fallback_chain` deduped by (provider, model, base_url); the file is 74 lines, the reader's "1-419" was wrong), `hermes_cli/fallback_cmd.py:141-193` (snapshot before the picker runs, `except BaseException` with `add_note` on partial restore, restore on the success path too; `_restore_primary_route` at 82-91), `:130-138` (list prints "Primary / Fallback chain"). Mechanism: an ordered list of alternate endpoints walked on primary failure, edited through the same picker under a guaranteed rollback. Halbert state: absent (`client.py:234-236` `fallback_url`/`fallback_provider` are the chat slot's own values). Task: an optional `fallback: [saved_endpoint_id, ...]` per slot in `llm_config.py`, walked in `tier_router.py` after the C1 skip predicate, and the picker's chain editor reusing the endpoint picker with snapshot/restore; every chain entry re-runs `is_local_model()` before a secure turn walks it (the verifier's addition, the SEC-21 hole otherwise reopens), and running-on-fallback is visible on the slot card. Tests: a secure turn never walks to a non-local entry; a picker crash mid-edit restores both stores. Effort M. Prerequisites: theme A, C1. Constraint: the chain lists slots and endpoints, never model recommendations.

**Failover handoff briefing injected as a user-role turn** (OC03-auto-reply-C7) — origin `src/auto-reply/handoff-summarizer.ts:16-18, 54-58` (`role: 'user'` so the new model treats the briefing as input rather than its own prior output), `:26-42` (team versus solo framing by active subagents), wired at `src/agents/embedded-agent-runner/run/attempt-history-prepare.ts:114-118` when a session resumes from quota suspension; test pins at `handoff-summarizer.test.ts:20-60`. Mechanism: the fallback model's first turn is primed with a synthetic user-role state summary, never an assistant-role one. Halbert state: absent; the reader's `model/router.py` citation is false (no fallback logic there). The real fallback is `tier_router.py:480-527` (`ModelSelection.fallback_used/fallback_from`) and `federation/compute_router.py:187-188, 370-405`; `model/context_handoff.py:249-303` injects its summary as a SYSTEM message at `:285-291` and only for tier handoff. Task: on a mid-conversation fallback in `tier_router.py`/`compute_router.py`, prepend one user-role briefing built from the thread's open leaf and any active subagent work; pin both bodies in a test. Effort S. Prerequisites: none.

**Fallback transition state and a double-gated raw-error surface** (OC03-auto-reply-C9) — origin `src/auto-reply/fallback-state.ts:12-19` (`TRANSIENT_FALLBACK_REASONS` closed set), `:20-21` (`TRANSIENT_ERROR_DETAIL_HINT_RE`), `:30-40` (`formatFallbackAttemptErrorPreview`: raw text only when the reason is in the set AND the string matches the hint), `:145-214` (`resolveFallbackTransition`: active/transitioned/cleared from persisted previous state), `:92-110` (notice and cleared-notice builders). Mechanism: raw provider text reaches a user only through two gates; notices fire on transitions, not every turn. Halbert state: partial (verifier corrected). The notice exists (`agents/events.py:135-167`, `routes/agent.py:755-777`, `useAgentStream.ts:592-604`) but re-announces every fallback turn with no cleared notice, and `routes/agent.py:1861, 1927` yield `StreamEvent.error(session_id, str(e))` verbatim (`events.py:337-346`). Task: persist previous fallback state per thread, emit `fallback_active` once and `fallback_cleared` once; route `routes/agent.py`'s error yields through a `classified reason in transient set AND hint regex` gate, otherwise a fixed sentence keyed by C6's reason; tests for both surfaces. Effort S. Prerequisites: C6. Constraint: redaction at the response choke point; the gate sits on `routes/agent.py`, a shared hot file.

**Reroute notice with three independent guards and bounded, redacted text, naming slot and locality only** (OC14-agents-second-pass-A-C22, low) — origin `src/agents/agent-run-terminal-receipt.ts:53-69` (emit only when run id matches AND rerouted AND terminal disposition is visible), `:36-41` (route text through redaction and a 128-char cap), `:44-50` (320-char cap on the producer fact). Mechanism: a user-facing notice is derived from three checks, not opportunistically, and bounded before display. Halbert state: absent; the tool-schema fallback at `client.py:426-431` is logged silently. Task: the one formatter the theme's notices share, with the three guards, rendering "the chat connection fell back to its secondary" / "this turn ran on a cloud connection" and never a provider/model identifier. Effort S. Prerequisites: C9. Constraint: founder rule on model names, which the origin's text violates and this version does not.

**Invalidate a cached capability on an observed failure, not a TTL** (OC21-extensions-device-media-desktop-C9) — origin `extensions/imessage/src/private-api-status.ts:59-88` (a successful probe is cached without expiry; `invalidate` deletes the entry), sole caller `client.ts:320-337` ("this is the one place that learns the bridge went away"). Mechanism: trust a verified-present capability indefinitely and bust it the instant a live call proves it dead. Halbert state: absent (`capabilities.py:412-450` `probe()` runs the seven `_PROBES` once; `has()` `:457-461` never re-probes; `reset_registry()` `:504-507` is test-only). Task: `CapabilityRegistry.invalidate(cap)` plus calls from the HA client, the local-LLM/secure-model provider failure paths (C6's `unreachable` reason) and the audio/web probes; distinguish capability-gone from one transient failure by requiring C1's endpoint-scoped failure, not a model-scoped one. Effort S. Prerequisites: C1/C6 for the signal.

#### Theme F — Local-model output robustness: tool-call repair, argument coercion, degenerate output (high for the first item)

**Plain-text tool-call repair for local models that emit prose-encoded calls** (OC13-packages-C3) — origin `packages/tool-call-repair/src/grammar.ts:1-321` (Harmony markers 3-8, `<function=`/`<parameter=` at 121-123, `scanXmlishToolCall` 157, incremental helpers 21-119), `payload.ts:1-740` (256,000-byte payload cap at 44, 120-char name cap at 45, `allowedToolNames` gating at 527/592, `parseStandalonePlainTextToolCallBlocks` 645, `stripPlainTextToolCallBlocks` 670), `protection-fast-path.ts:18-85` (the fence tracker that "must decline rather than miss a fence boundary"), `promote.ts:36, 50, 132` (rewrite into the provider-native tool-call shape). Mechanism: a streaming parser recognises bracket, XML-ish and Harmony tool-call syntaxes, validates against the allowed tool names and byte caps, refuses anything inside a Markdown fence, and promotes the span into the same structured shape the pipeline already consumes. Halbert state: absent (`client.py:440-469` `_normalise_tool_calls` reads only structured fields; `_call_ollama` `:694-720` never inspects assistant text). Task: a Python port of grammar + payload + fence tracker as a post-processing step in `client.py`'s Ollama/llama.cpp path producing the `{id, name, arguments}` shape `_normalise_tool_calls` returns, stripping the span from the shown text; the executor's existing policy pipeline sees the repaired call exactly as a structured one (no new dispatch path). Tests: each grammar form, a fenced example that must not promote, an unknown name that must not promote, the byte cap. Effort L. Prerequisites: none. Constraint: generic across whatever local model is configured; no "supported family" list.

**Schema-guided tool-argument coercion, with the key-unrename half of the sanitizer** (HM06-tools-approval-guards-C8 and HM06-tools-approval-guards-C9, merged: C8's first call is C9's `unrename_tool_args`) — origins `tools/arg_coercion.py:1-8` (contract: schema-guided, conservative, originals kept when ambiguous), `:20-36` (`coerce_tool_args` pulls the registered schema and first calls `schema_sanitizer.unrename_tool_args`), `:38-60` (array branch: JSON-encoded arrays parse, `'null'` becomes None, a bare scalar is wrapped with a warning); `tools/schema_sanitizer.py:17-20` (`_PROP_KEY_RE` `^[a-zA-Z0-9_.-]{1,64}$`, one bad key 400s the whole tools array), `:41-55` (deterministic insertion-ordered rename with a reverse map). Mechanism: `'42'` to int, `'true'` to bool, JSON-encoded containers parsed, all keyed by the tool's declared schema, never a guess; and sanitised keys map back to the registry's wire names before dispatch. Halbert state: absent for coercion (only `rate_limiter.py:165-169` `_coerce_retry_after`); partial for the sanitizer (`client.py:625` passes `parameters` straight into `input_schema`). The verifier demoted C9 to low because Halbert writes its own schemas and controls both ends of the wire; only the unrename half earns its place. Task: `coerce_tool_args(name, args)` at the single dispatch choke point in `tools/executor.py`, after policy classification and before execute, applied for every provider (cheap and idempotent); a test matrix of string/int/bool/array/nested repairs and one ambiguous case that stays untouched. Effort S. Prerequisites: none. Constraint: a parsing repair, not a policy decision; the policy pipeline still sees the final args.

**Garbled-output detection for local responses, scoped by locality, never by model family** (OC22-extensions-local-providers-apps-C5) — origin `extensions/ollama/src/stream.runtime.ts:80-82` (a model-family regex trigger and an 80-char minimum), `:131-165` (`countMatches`, `maxCharacterFrequency`, `isLikelyGarbledVisibleText`: letter-or-digit ratio < 0.08 AND symbol ratio > 0.6 AND (dominant character > 0.22 OR a 12+ symbol run)), throw sites `:1333-1340, 1345-1349`. Mechanism: a character-frequency heuristic turns degenerate quantised output into an explicit retryable error instead of delivering it. Halbert state: absent (`llm_client.py:129-155` has only the tool-schema retry on 400/404/422/501). Task: the numeric heuristic on the local-provider response path in `llm_client.py` and `client.py`, always on for any response from an endpoint `is_local_model()` accepts, raising C6's `degenerate_output` reason which retries once then surfaces a slot-and-locality notice; tests with a symbol loop, a JSON blob and a code block (the last two must pass). Effort S. Prerequisites: C6. Constraint: the origin's trigger is a hardcoded model-family regex, which the founder rule forbids; locality scoping replaces it and the thresholds are model-independent.

**Reasoning-effort ladder: clamp down, never escalate, omit rather than substitute** (HM03-agent-providers-credentials-usage-C10, low) — origin `agent/reasoning_effort.py:25` (`EFFORT_LADDER` including `none`), `:96-127` (`clamp_effort`: pass-through when supported or unknown, else nearest weaker, never an enabled ask down to `none`), `agent/lmstudio_reasoning.py:23-40` (return None to omit the field when the allowed set cannot honour it). Mechanism: vocabulary math decoupling a requested effort from what a wire accepts. Halbert state: absent; the first half (sending a `think`/effort parameter at all) does not exist either (`utils/reasoning.py:51` strips `<think>`, `:97-130` detects capability). Task: only bundled with a feature that sends Ollama's `think` parameter keyed to `/api/show` capabilities; then the ladder is one small file. Effort S once that exists. Prerequisites: a reasoning toggle in the picker (not proposed here).

#### Theme G — Credential custody at the process and wire boundaries (high for the first item)

**Provider credentials are scrubbed from the environment of every subprocess** (HM15-cli-model-config-ux-M8, verifier-added) — origin `hermes_cli/bang_shell.py:89-99` (`_bang_env`: the CLI holds every provider key and a user-typed command may run a third-party script, so reuse the one sanitizer the terminal backend uses, `build_subprocess_env()`), `:116-121` (the sanitized mapping passed as `env=` at Popen). Mechanism: one function builds every child environment, shared by the human shortcut and the model's tool calls, so there is one policy. Halbert state: absent (spot-checked): `tools/system_tools.py:157-162` `subprocess.run(cmd, capture_output=True, text=True, timeout=timeout)` with no `env=`; no `os.environ.copy()`, `env=` or sanitizer under `tools/` (the only `env=` is `streaming/session_manager.py:99` passing through to `PTYSession`), and `streaming/pty.py:289-293` copies the environment wholesale into every user shell. Every subprocess (system_tools, accelerator_tools, gpu_tools, schedule_cron, anything reached through `execute_code`'s RPC host) exports whatever the parent holds. Task: `halbert_core/tools/subprocess_env.py` `build_subprocess_env()` stripping credential-shaped names (a suffix list `_KEY/_TOKEN/_SECRET/_PASSWORD/API_KEY` plus the concrete names `models.yml` loads) and made the only way a child environment is constructed, including the PTY path; a test walking every `subprocess.`/`Popen`/`PTYSession` call site and asserting none passes the bare environment. Effort S. Prerequisites: none, and it is a prerequisite of OC05-C6 below. Constraint: Tier-2 secrets posture applied at the process boundary, deterministic. Touches `streaming/pty.py`, which TERM-1 (watched shells) owns.

**A credential can be a reference resolved at use time, through the custody ladder Halbert already owns** (HM03-agent-providers-credentials-usage-M5 verifier-added, OC05-config-C6, OC04-commands-C15, merged: three routes to "persisted config never holds the raw key") — origins Hermes `agent/secret_sources/registry.py:1-11` (registry owns scheme uniqueness, the wall-clock timeout around fetch, precedence and provenance), `:39-44` (built-in sources: two vault CLIs and a generic `command` source), `:105-124` (a duplicate URI scheme is refused), `:220-235` (each fetch on its own single-worker executor under a budget), `agent/secret_sources/base.py:180-190` (protected env vars: a vault may never overwrite the credential used to reach it; fetch timeout); OpenClaw `src/config/env-substitution.ts:28` (name pattern), `:31-39` (`MissingEnvVarError` carrying the config path), `:44-78` (`$${VAR}` escape parsed before `${VAR}`), `:92-142` (pending/resolved secret-ref hooks); OpenClaw `src/commands/doctor-model-catalog-credentials.ts:17, 43-55, 129-238` (a plaintext copy is dropped only after an equivalent credential is confirmed in the locked store; the migration half is dropped per the no-migrations directive). Mechanism: the stored artefact is a reference plus a source; the secret is fetched at use time under a timeout with a typed failure, never written back, never logged, and the bootstrap credential is protected from the vault it reaches. Halbert state: partial (verifier corrected C6 and C15 from absent): `llm_config.py:136` `api_key` literal, `:977-984` `api_key_for(url)` returns the stored string, round-tripped at `:465/625/904`, file 0600 (`:273-276, 425, 445`); `mcp/config.py:180-209` already resolves `token_env` for MCP servers with a once-per-name missing-var warning and the stated intent at `:28, 656-661` ("a secret typed into a web form is not persisted"); `crypto/storage.py:1-27, 89-230` is the custody ladder (Keychain via `security`, Secret Service via `secret-tool`, 0600 file with refusal) scoped to the signing key. Task: let a saved endpoint's `api_key` be a literal, a `token_env` reference (the MCP grammar, extended to `saved_endpoints`), or a keychain reference resolved through `crypto/storage.py`'s `KeychainKeyStore` at `api_key_for` time with a short timeout and a typed failure; the literal stays the default and nothing is migrated; the vault-bootstrap protection rule copied verbatim. The verifier's caution binds: env indirection puts the secret in the process environment, which `streaming/pty.py:289-293` copies into every shell, so M8 lands first or this trades a 0600 file for a `ps`-visible environment. Effort M. Prerequisites: M8; founder decision 3. Constraint: no migrations; single-user degenerate case is one literal key.

**Credential headers never cross an origin on redirect** (HM15-cli-model-config-ux-M3 verifier-added and HM14-cli-ops-lifecycle-C13, merged: same file, the reader's line numbers for C13 were impossible, the file is 169 lines) — origin `hermes_cli/urllib_security.py:19-20` (`_CROSS_ORIGIN_SAFE_HEADERS = {accept, user-agent}`; an allowlist because custom provider headers carry credentials under arbitrary names), `:25-32` (`url_origin` lets a malformed port fail the request closed), `:41-61` (`SafeCredentialRedirectHandler`: let urllib settle 307/308 semantics first, then strip on origin change), `:64-82` (`_CrossOriginRequestSanitizer` at `handler_order = inf` so a cookie/auth processor cannot re-add a secret), installed `:137`. Mechanism: on any cross-origin redirect every header not on a tiny allowlist is dropped, at the last point in the handler chain. Halbert state: absent. `client.py:526-528` sets `Authorization: Bearer` and posts at `:534/575/668` via `requests` with no session and no redirect policy; `requests` strips only the literal `Authorization` header on a cross-host redirect, which covers today's shape by luck. Task: one shared `requests.Session` for credentialed model calls with `allow_redirects=False` (a model endpoint that redirects is a misconfiguration to surface), or an explicit origin hook dropping non-allowlisted headers; test with a fake endpoint that 302s to another host and asserts no Authorization arrives. Effort S. Prerequisites: none; mandatory before HM15-C14 (custom headers) is ever built. Priority low per both verifiers because the exposure is theoretical today.

**Configured credentials apply only to the origin they were configured for; explicit keys replace, never append** (HM15-cli-model-config-ux-M6, verifier-added; demoted from medium to low after my spot-check) — origin `hermes_cli/models_local.py:150-163` (`_get_ollama_native_headers` returns `{}` when the requested base URL is not the configured root), `:264-283` (`_same_ollama_native_root`), `:127-131` (`_drop_authorization`, case-insensitive), `:133-148` (never overwrite an operator-supplied Authorization with a derived Bearer). Mechanism: a credential travels only to the host it was configured for, and two Authorization variants never race. Halbert state: partial, better than the verifier feared. `llm_config.py:966-974` `_endpoints_matching` compares `_normalise_url` whole-URL equality (spot-checked), so a saved key cannot attach to a different host today; the remaining pieces are the case-insensitive replace-not-append rule and using the M2 route normaliser so a default-port variant does not miss its key. Task: a five-line header-merge helper used by every credentialed call, plus a test that `authorization` in any case is replaced. Effort S. Prerequisites: M2's normaliser. Becomes mandatory if HM15-C14 lands.

#### Theme H — Self-diagnosis and setup verification (high for the doctor; cross-listed to the dashboard-app-onboarding-diagnostics workstream, which should own the packet)

**`doctor`: a structured self-diagnostic with opt-in repair, warn-only drift detection and parallel probes that print in submission order** (HM15-cli-model-config-ux-M4, verifier-added) — origin `hermes_cli/doctor_report.py:55-65` (`Finding` separates issues, manual_issues and fixed, with `merge()`), `:68-85` (`doctor_check` decorator: a crashing check still returns its partial Finding), `hermes_cli/doctor_config.py:22-39` (deprecation tables, "warn with the modern replacement, never auto-migrated"; env deprecations read from the `.env` file, not process env, so config-to-env bridges do not false-positive), `:288-410` (drift checks distinct from validation), `hermes_cli/doctor_connectivity.py:1-6, 279-292` (every probe is a pure function returning a `ProbeResult`; no printing in workers; the caller prints in submission order so a parallel sweep is deterministic and diffable). Mechanism: one decorator makes every check a uniform unit, repair is opt-in per run, drift is reported and never fixed silently. Halbert state: absent (only per-subsystem ad-hoc health in `mcp/health.py`, `dashboard/routes/services.py`, `gpu.py`). Task: `halbert_core/diagnostics/` with `Finding` + `doctor_check`, `--fix` opt-in, a parallel probe runner; first checks: every enabled slot answers one real round trip (OC07-C6 below), `is_local_model()` verdict per slot, saved-endpoint reachability, the C11 close-match check, the credential-in-environment check from M8, the symlink pre-flight from M7; surfaced as a CLI command and a dashboard panel. Effort M. Prerequisites: none; the diagnostics workstream should own the frame, this workstream contributes the model checks. Constraint: first-person from measured data; drift stays warn-only, matching no-migrations.

**Onboarding verifies the configured slot with one real call and offers fix-or-continue** (OC07-cli-wizard-tui-entry-C6) — origin `src/wizard/setup.inference-verification.ts:29-38` (verification becomes required when the candidate points at a local service), `:58-101` (real inference call under a spinner, latency on success, error on failure), `:110-165` (the retry loop, non-interactive short-circuit at 133-135, fix-vs-continue at 136-149 returning `verified: false` rather than pretending). Mechanism: onboarding never completes with an unverified connection without telling the user. Halbert state: partial: `model/config_wizard.py:415-432` `_test_compute_peer` probes a health route (1.5 s) and saves either way; `dashboard/frontend/src/components/llm/ProbeButton.tsx:70-88` fires a burst probe against `POST /compute/endpoint-probe` with p50/p90/p99; both are compute-peer surfaces. `Onboarding.tsx` (welcome/configure/scanning/scan_results/complete) never round-trips the chat or secure slot. Task: a "test this connection" step after configure that makes one real chat call against the chosen slot through `tier_router` (so the SEC-21 gate and the C6 taxonomy both apply), shows latency or the classified reason, and offers fix or continue-anyway with the slot marked unverified; a test that onboarding cannot complete silently with an unreachable slot. Effort M. Prerequisites: C6 for the reason text. Constraint: latency and reason, never a model name.

**Close-match feedback for a hand-edited `models.yml`** (HM15-cli-model-config-ux-C11, low) — origin `hermes_cli/models_validate.py:1-9` (verdict ladder, "the ladder ORDER is behavior"), `:32-51` (five verdict builders), `:101-104` (`get_close_matches` auto-correct at cutoff 0.9, three suggestions otherwise), `:547-577` (`validate_requested_model`). Mechanism: graduated verdicts instead of valid/invalid. Halbert state: absent; the picker is selection-based so only the hand-edit path benefits. Task: at config load, `difflib.get_close_matches` of each slot's model against the endpoint's discovered list, reported as a doctor finding, never auto-corrected. Effort S. Prerequisites: the doctor frame.

**Symlink-aware home initialisation: never materialise a missing mount's target** (HM15-cli-model-config-ux-M7, verifier-added; belongs to CFG-1, listed here because it rode in this unit) — origin `hermes_cli/config_home.py:11-13` (collect every symlinked component, parents first), `:15-41` (check each link resolves before any mkdir; "Never materialize a missing mount's target on the underlying disk"; `HomeInitializationError` carries the link chain and promises nothing was replaced or created), `:30-32` (no chmod through a link). Mechanism: directory creation refuses to create through an unavailable link and raises a typed error. Halbert state: absent for directories (single-file symlink handling exists at `config/being_config.py:861` and `persona/store.py:113,157`; no `mkdir(parents=True)` guard in `utils/paths.py` or `model/config_locator.py`). This repo itself lives on an external volume; a config home symlinked into an unmounted disk would produce an empty stub, a blank-memory assistant reporting itself healthy, and a divergent tree at the next mount. Task: a pre-flight in the path helper walking link components, refusing to create through an unavailable one, raising a typed error naming the chain, deleting nothing; test with a dangling symlink in a temp dir. Effort S. Prerequisites: none. Constraint: leave old data unread, never delete, satisfied by construction.

#### Theme I — Usage and cost accounting, mechanism only (low; instrumentation first)

**Prompt-prefix caching on the static prefix of the system prompt, with the two cache usage fields recorded** (OCC01-v2-second-look-C8; novelty corrected to extends-lifted) — origin `v2/src/core/system-prompt.mjs:57-107` (`buildSystemPrompt` returns an explicit `{staticPrefix, dynamicSuffix}`), `:109-135` (`toCacheBlocks` marks the static prefix only), `v2/src/core/cache.mjs:29-57` (`applyCacheControl`), `:62-75` (`updateStats` reads `cache_creation_input_tokens`/`cache_read_input_tokens`). Mechanism: a boundary-plus-consumer pair: the prefix above the boundary is marked cacheable, the suffix is not, and the real hit rate comes from usage. Halbert state: partial: the boundary half exists (`prompts/agent_prompts.py:20-31` `CACHE_BOUNDARY_MARKER`, `:45-60` `stable_prefix_key`, assembled at `state_machine.py:2331`; lifted by SK-2); the adapter half does not: `model/providers/anthropic.py:255-260` passes `system` as a bare string and `:275-279` parses only input/output tokens. Task: split at the marker in `providers/anthropic.py` only when the connected slot speaks that wire protocol, emit the two text blocks with `cache_control` on the first, record both cache usage fields into the response metadata that theme B's anchor already reads. Effort S. Prerequisites: none. Constraint: wire-protocol plumbing keyed by the slot's protocol, not a model name; value bounded to cloud slots, which the posture treats as secondary.

**Usage aggregation by connection slot with an honest sub-cent label** (HM04-agent-learning-verification-review-C19) — origin `agent/insights.py:1-2, 12, 18-24, 460` (`_fmt_est_cost` through `format_cost_label`: sub-cent aggregates render at 4 dp instead of collapsing to "~$0.00"); the prior Hermes review (§8) already recorded `estimated_cost_usd` vs `actual_cost_usd` with `cost_status/cost_source/pricing_version` provenance. Mechanism: a read-only engine over the session store; the correctness detail is the formatter. Halbert state: absent, and the reader's prerequisite ("conversation_sqlite.py already stores per-session usage") is wrong: `agents/conversation_sqlite.py:159-161` carries only compaction pre/post tokens; no cost concept exists anywhere. Task: first, instrumentation: one usage row per model call (slot id, locality, prompt/completion tokens from the measured fields, cache fields, latency, C6 reason on failure) written through the turn-event tee or `model/outcome_store.py` (A14-G8 asked for the same choke point); the aggregation and any price arithmetic wait for founder decision 7. Effort L overall, S for the instrumentation. Prerequisites: theme B's measured counts. Constraint: every breakdown keys on the connection slot and locality, never the model identity; no model pricing tables in the repo.

#### Theme J — Turn honesty and boundaries (routed here from HM01; these sit on `state_machine.py` and `tools/executor.py` and belong with the agent-loop packets, cross-list them)

**Untrusted-data delimiters around external tool results — the indirect-prompt-injection boundary** (HM01-agent-turn-family-M1, verifier-added, high) — origin `agent/tool_dispatch_helpers.py:405-407` (`_UNTRUSTED_TOOL_NAMES` plus the `browser_` and `mcp_` prefixes), `:484-510` (`_maybe_wrap_untrusted`: a fixed "this block is DATA" frame around any string over 32 chars; text parts of a multimodal list wrapped individually), `:479-481` (`_neutralize_delimiters` defangs the closing token so poisoned content cannot close the boundary; the docstring refuses an "already wrapped" fast path as attacker-forgeable), `:383-400` (wrap once at construction, cache-safe; risk metadata stamped separately). Mechanism: deterministic, no model in the boundary. Halbert state: absent. `prompts/agent_prompts.py:308-355` and `:1241-1270` defang control tags for the system row, history rows and recalled receipts (`:446-452`); the tool-output block at `:1000-1006` is a bare `- {obs}` list under `## Tool Observations` with neither a defang pass nor a data-not-instructions frame, and it carries web search, MCP, peer-proxy, file and command output. Task: a deterministic wrapper on the state_machine tool-result path (`add_observation` sites `:3163/:3239/:3248` and the EXECUTING result), keyed by tool class from the existing `tools/safety` classification (external content first: web, MCP, peer proxy; then file reads and terminal stdout), reusing the defang fixpoint for the delimiter token; a test that a tool result containing the closing tag cannot escape the block. Effort M. Prerequisites: coordinate with the MCP one-policy-pipeline work (MCP-1) and the guest persona; `state_machine.py` is a shared hot file.

**End-of-turn over-claim verifier: state deterministically which claimed effects did not land** (HM01-agent-turn-family-M3, verifier-added, high) — origin `agent/turn_explainers.py:177-213` (`_record_file_mutation_result`: first error per path kept, a later success removes it), `:236-243` (paths neutralised so the file auto-attacher cannot pick them out), `:246-270` (footer: "N file(s) were NOT modified this turn despite any wording above"), landing decided by `agent/tool_result_classification.py:26-40` (never by reading prose); the same rule drives `agent/turn_summary.py:115-120`. Mechanism: a per-turn attempted-vs-landed ledger and a deterministic footer, so the machine cannot tell its owner it did something the permission layer refused. Halbert state: absent on the correction side: `state_machine.py:1537-1543` records "User rejected the action" as an observation the model may ignore; `tools/executor.py` returns `ExecutionResult(success=False)`; `ctx.tool_calls` already carries status (`states.py:322`). Task: keep the ledger from `ctx.tool_calls`, append a deterministic footer naming any mutating call (file write, vault proposal, staged command, memory write) that did not land, interpolated values through `security/display_transport` redaction; tests for a refused write and a write that later succeeded. Effort S. Prerequisites: composes with the consent ledger and typed denials (D3-P2/P3, merged). Constraint: deterministic, first-person, measured.

**Typed `turn_exit_reason` with a static cause-and-remedy table** (HM01-agent-turn-family-M2, verifier-added, medium) — origin `agent/turn_explainers.py:20-56` (exact reasons to one sentence of cause plus one of remedy), `:58-73` (prefix-matched parameterised reasons), `:76-143` (persistence causes refined by class: lock contention, rotated session, full disk each with their own instruction), `:226-232` (a config flag; no model consulted). Halbert state: absent (`state_machine.py:1367-1369, 1377-1380, 1410, 1490` are ad hoc strings through `StreamEvent.error`, `events.py:337-345`). Task: a small typed set (max_iterations, oscillation, tool_loop_blocked, model_unreachable, rate_limited_until, context_overflow, degenerate_output, interrupted, store_write_failed, awaiting_confirmation_abandoned) stamped on the terminal event beside `recoverable`, with one static table; this is where themes C and D's typed refusals get their sentence. Effort S. Prerequisites: C6 supplies half the vocabulary.

**Pre-mutation workspace checkpoint, including before a destructive terminal command** (HM01-agent-turn-family-M4, verifier-added, medium) — origin `agent/tool_executor.py:77-89` (`_ensure_file_checkpoint` resolves the path as the file tools will and checkpoints the containing directory), `:922-933` (terminal: checkpoint the command's cwd only when `_is_destructive_command` says so, labelled "before terminal: <first 60 chars>"; inside `contextlib.suppress`, so failure never blocks), with the agent-write ledger at `turn_explainers.py:196-204` telling agent-authored content from later hand edits. Halbert state: absent for filesystem and shell effects (rollback exists only for config proposals, `continuity/vault.py:200`, `continuity/provenance.py:103`). Task: a non-blocking checkpoint hook at the dispatch choke point in `tools/executor.py` on the final args, for file-writing tools and destructive-predicate terminal commands, scoped by capability boundary; schedule with M3 since both want the attempted-vs-landed ledger. Effort M. Prerequisites: M3; TERM-1 for the destructive predicate. Constraint: staging plus consent stays the primary defence; this covers the residual case classified below the confirmation bar.

**Iteration budget with refunds, and a ceiling that is not five** (HM01-agent-turn-family-M6, verifier-added, medium) — origin `agent/iteration_budget.py:1-22` (lock-guarded consume/refund, cap 500 parent / 50 per subagent; clean exits in `turn_preflight_gate.py:55-56` refund before ending). Halbert state: partial: `ctx.loop_count` against `max_loops=5` (`state_machine.py:254`, enforced `:1361`), every increment site (`:3101, :3193, :3419`) charges whether or not work happened, inline meta-tools escape by not counting at all behind a second ceiling (`:234-241`). Task: one budget object with consume/refund, refund iterations spent on inline meta-tools and validation refusals, revisit the cap; bookkeeping, to land with the tool-call repair (theme F) that makes a local model's iterations count. Effort S. Prerequisites: none.

### Related confirmed audit gaps

All from A14-utility-slot-model unless noted; G6 (unauthenticated Ollama probe) and G8 (side-task usage not recorded) were refuted and are not listed, though G8's ask reappears as theme I's instrumentation choke point.

- A14-G4 [high]: the utility pick bypasses locality; a `:cloud` catalog sibling or a cloud-provider utility slot can receive the spoken copy of a secure turn. Same mechanism as OC20-C18 (theme A). Latent today per the A10 refutation (the summarizer is unreachable under the engine word cap), structural regardless.
- A14-G3 [medium]: only `embed` is disqualified; reasoning/thinking, vision and other non-chat siblings can be picked. Fix: reuse `utils.reasoning.is_reasoning_model` and `capabilities._VISION_TOKEN_RE` in the catalog rung.
- A14-G9 [medium]: the catalog rung cannot rank OpenAI-wire catalogs (family split on `:`, size regex requiring `:`); Ollama-only despite the provider allow-list. Fix: strip a generic size/variant tail and the org prefix.
- A14-G1 [low]: no operator control over the catalog rung; no "utility = my chat model, no heuristics" state. Founder decision 2.
- A14-G2 [low]: the catalog probe is uncached with no negative cache; a dead endpoint is re-dialled on every resolution. Fix: per-(url, provider) memo with success and failure TTLs.
- A14-G5 [low]: no size floor, no context screening, tie order unpinned in the smallest-sibling rule.
- A14-G7 [low]: a failing utility model is never rescued by the next rung at request time. Fix: `_resolve_aux(exclude=...)` and one bounded retry.
- A14 bug: the legacy `small_model` rung is dead after the first store read in the process (`utility_slot.py:115-117, 287-297`; `llm_config.py:656-695` rewrites the file on first load). Delete the rung; no migrations.
- A14 bug: `task` provenance is documented and never logged (`utility_slot.py:93-106`); one log line at resolution naming task, rung, provider, url.
- A14 bug: synchronous probe and summary run inside `async _handle_responding` (`state_machine.py:3835, 4160-4163`; `speech_summarizer.py:93-146`) and stall the event loop, blocking SSE consumers, the scheduler tick and the stop endpoint. Run in a thread.
- A14 bug: raw file read plus YAML parse on every resolution even when the declared slot answers (`utility_slot.py:117, 297-305`).
- A14 bug: `model/loader.py:9-16` uses `yaml` without importing it (reproduced: `NameError`); no live caller.
- A14 bug: `_same_family` prefix rule fuses distinct families sharing a 3-char prefix (`utility_slot.py:257-265`); executed: `_same_family('abcx-coder','abc')` is True.
- A10 refutation (context, not a gap): the speech summarizer's `>600` char gate can never be reached because the engine's word cap runs first; `tests/test_speech_summarizer.py::test_short_reply_never_invokes_the_summarizer` is vacuous. The master plan's "spoken summarization once a utility slot exists" is the wrong next step; the slot exists and the seam is unreachable.
- A17-mcp-boundary-G20 was refuted (bare provider-prefixed credentials in a peer's error text); noted only because theme E's error gate on `routes/agent.py` is where such text would be stopped if the verdict changes.

### Do not lift

- **HM09-tools-mcp-C15** MCP sampling (server-initiated completions through Halbert's slot): no-verdict, no MCP server Halbert uses needs it, and it opens a path where a server steers Halbert's own model; if ever needed it goes through the MCP-1 policy pipeline with trust tier and consent, and the reply carries content only, never provider or model identity.
- **HM15-cli-model-config-ux-C1** selection-time guard registry: a shape with no content; Hermes's two guards are a cost threshold from a pricing catalog and a vendor data-training tier, neither of which exists for Halbert's local-first set. Land only when a real first guard appears (a candidate: "this slot's endpoint is not local" as a picker-time warning, which SEC-21's `normalise` already does for the secure slot).
- **HM01-agent-turn-family-C9** repetition-dominated-fragment guard: gates a `finish_reason=length` continuation feature Halbert does not have; watch item against whatever packet adds continuation.
- **HM12-gateway-rooms-platforms-hooks-C15** httpx keepalive limits: the task does not transfer; Halbert's long-lived clients are aiohttp (`integrations/home_assistant/ha_client.py:39`, `ha_event_stream.py:173`, `integrations/frigate/frigate_client.py:53`), and a shared `TCPConnector(limit, keepalive_timeout)` is worth doing only if fd growth is observed.
- **HM15-cli-model-config-ux-C14** per-endpoint custom headers, TLS-verify toggle, capability override: conditional on a self-hosted-behind-a-proxy scenario that has not appeared; if it ever lands, a `tls_verify=false` endpoint must be structurally ineligible for the secure slot, and theme G's redirect stripping and replace-not-append rule become mandatory first.

Demotions made in this section, with reasons: HM15-M6 medium to low (spot-check shows `_endpoints_matching` already compares normalised whole URLs); HM06-C9 folded into C8 as its unrename half (verifier: Halbert writes its own schemas); HM01-C10 medium to low (no retry budget exists to cut, verifier); OC04-C15 migration half dropped and custody half merged into M5 (no-migrations directive); HM03-C7 raised medium to high (verifier: a live 120 s kill on healthy streams); OC20-C18 kept high although the only caller is unreachable today (structural bypass of the one choke point).

### Proposed packets

1. **MP-1 Locality everywhere and utility-slot solidity** — items OC20-C18, A14-G4/G3/G9/G1/G2/G5/G7, the six A14 bugs, OC14-C22's slot-and-locality notice text. Prerequisites: none (SEC-21 merged); founder decisions 1 and 2. Hot files: `model/utility_slot.py`, `model/llm_config.py` (`normalise`, `is_local_model` callers), `integrations/speech_summarizer.py`, **`agents/state_machine.py`** (`_handle_responding` to_thread fix at `:4160-4163` and `require_local=ctx.secure_context`), `tests/test_utility_slot.py`. Founder decision gates the fail-closed rule; the rest is dispatchable now. Smallest and highest-leverage packet here.

2. **MP-2 Provider failure semantics** — items HM01-C6, HM01-C7 + HM03-M4, HM01-C8 (the `tier_router.py:736` sleep), HM03-C1 + HM03-M6, HM03-C17, HM03-C2, HM12-C14, HM01-C10, OC03-C9, OC03-C7, OC21-C9. Prerequisites: none; founder decision 4 for the Retry-After ceiling. Hot files: `model/tier_router.py`, `model/rate_limiter.py`, `agents/error_recovery.py` (retire or rewire), `model/backend_identity.py` (new), `federation/compute_router.py`, `capabilities.py`, **`dashboard/routes/agent.py`** (the error-yield gate at `:1861, 1927`), `agents/events.py`. Order inside: C6 first (everything else reads its reason), then C7/M4 with C8 in the same commit, then C1/M6/C17, then C2, then the E items.

3. **MP-3 Transport liveness** — items HM03-C7, HM03-M1, HM03-M3, HM01-M5. Prerequisites: MP-2's C6 for the typed reason, and the merged interrupt algebra (07). Hot files: `agents/llm_client.py`, `model/client.py`, `model/providers/*.py`, `agents/turn_activity.py`, **`agents/state_machine.py`** (the abort hook in `cancel_session`), `tools/execute_code.py`, `integrations/wyoming_agent.py`. Tests need a fake slow/silent streaming server fixture, which MP-5 reuses.

4. **MP-4 Measured context and residency** — items HM03-C5, HM03-M2, HM15-M2, HM15-M5, HM15-M1, OC22-C9, OCC01-C8 (the adapter half), theme I's usage-row instrumentation. Prerequisites: none for C5/M2/M5; founder decision 6 for keep_alive; founder decision 7 before any cost arithmetic. Hot files: `model/client.py` (num_ctx, `_MODEL_MAX_CACHE`), `model/providers/ollama.py`, `model/providers/anthropic.py`, `context/tokens.py`, `agents/llm_client.py`, `dashboard/routes/settings.py` (save path), `packages/model-picker` (switch-consequence sentence), `model/outcome_store.py` or the turn-event tee for usage rows. Touches no shared hot file except the settings route.

5. **MP-5 Local-model output robustness** — items OC13-C3, HM06-C8 + HM06-C9 (unrename half), OC22-C5, HM01-M6 (iteration refunds), HM03-C10 held until a think toggle exists. Prerequisites: MP-2's C6 for `degenerate_output`. Hot files: `model/client.py` (`_normalise_tool_calls`, `_call_ollama`), new `model/tool_call_repair/` (grammar, payload, fence tracker, promote), **`tools/executor.py`** (coercion at the dispatch choke point; shared with 04/06 seams), `agents/llm_client.py`, **`agents/state_machine.py`** (loop budget). Largest packet (C3 is L); the fence tracker ships with the grammar or the packet does not merge.

6. **MP-6 Credential custody at the process and wire boundaries** — items HM15-M8, HM03-M5 + OC05-C6 + OC04-C15, HM15-M3 + HM14-C13, HM15-M6. Prerequisites: founder decision 3; M8 lands before any env indirection. Hot files: new `tools/subprocess_env.py`, `tools/system_tools.py`, `tools/accelerator_tools.py`, `tools/gpu_tools.py`, `scheduler/schedule_cron.py`, `streaming/pty.py` and `streaming/session_manager.py` (TERM-1 owns these; coordinate), `model/llm_config.py` (`api_key_for`), `crypto/storage.py`, `model/client.py` (shared session, `allow_redirects=False`), `mcp/config.py` (shared `token_env` resolver). Security-shaped: send through the security-review skill before merge.

Cross-listed, not packeted here: theme H (HM15-M4 doctor, OC07-C6 onboarding verify, HM15-C11, HM15-M7) belongs to the dashboard-app-onboarding-diagnostics workstream's packet, with MP-2's C6 and MP-1's locality verdict as the model checks it runs; theme J (HM01-M1/M3/M2/M4/M6) belongs with the agent-loop/terminal packets on `state_machine.py` and `tools/executor.py`, with M2's exit vocabulary consumed by MP-2 and MP-3.

### Open founder decisions

1. "When the utility ladder's only candidates are non-local and the anchor turn is secure (or the caller sends conversation text), do we drop the rung and continue, or refuse the whole side task?" Default: drop the candidate and continue down the ladder; if nothing local answers, no summary, screen-only, mirroring the secure slot's fail-closed precedent. Never a cloud pick for conversation text.

2. "Is the utility slot's live catalog probe an operator opt-in (config knob, default off, so 'utility = my declared slot, else my chat model' is the default), or on by default as D-7 built it?" Default: opt-in, default off; the probe re-dials an endpoint every resolution today and picks siblings the operator never declared.

3. "May a saved endpoint's API key be a reference (a Keychain item resolved through the existing custody ladder, or a `token_env` name like MCP servers already use) instead of a literal, with the literal remaining the default?" Default: yes, both forms, no migration, and only after the subprocess environment is scrubbed (MP-6's first item), since env indirection otherwise makes the key visible to every shell Halbert spawns.

4. "How long may an interactive turn wait on a provider's Retry-After before falling back with a typed reason?" Default: honour it in full up to the turn's own deadline (600 s is Hermes's ceiling); beyond that fall back immediately and show "the cloud connection is rate-limited until T" on the slot card; background jobs may wait the full window.

5. "Is a fallback to a cloud connection on a non-secure turn ever silent?" Default: never; one transition notice when it becomes active and one when it clears, naming the slot and locality ("this turn ran on a cloud connection"), never a model.

6. "What is the model-residency policy for the local chat slot: keep the model loaded while the conversation is live and release after what idle window, bounded by the GPU budget?" Default: `keep_alive` equal to a 30-minute idle window while the dashboard or a voice session is connected, Ollama's default otherwise, and never when `gpu_tools.py` reports another role's reservation.

7. "Do we want any cost figure in the product, and if so keyed how?" Default: instrument usage rows now (slot, locality, measured tokens, latency, reason), show token counts by connection slot on the slot card, and hold all price arithmetic; if a cost figure is ever shown it keys on the slot with the sub-cent-honest formatter, and no pricing table for any vendor's models enters the repo.

8. "Does the `doctor` self-diagnostic live in this workstream or in dashboard-app-onboarding-diagnostics, and is it both a CLI command and a dashboard panel?" Default: the diagnostics workstream owns the frame, this workstream supplies the slot/locality/reachability checks, and it ships as both surfaces with `--fix` opt-in and drift warn-only.
