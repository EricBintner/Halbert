## testing-evals-qa-ops — 25 verified mechanisms from HM04, HM13, HM19, HM20, OC09, OC11, OC21, OC22, OC24, OCC02

ROADMAP row: `CI-1` (CI green: Python suite green in CI with the `vision` extra; ratchet green; SPDX test green; census green — 26 local failures in SONNET-03/04/05 scope at last snapshot). Every item below was read by a discovery unit and re-read by an independent verifier; where the verifier corrected the reader (state, priority, line numbers, prerequisites) this section follows the verifier and says so. Origin paths are relative to `/Volumes/Thunderbolt/AI/OSS/hermes-agent` for `HM*` ids and `/Volumes/Thunderbolt/AI/openclaw` for `OC*` ids; `OCC*` is `/Volumes/Thunderbolt/AI/OSS/open-claude-code`. Spot-checks made while writing this section are listed at the end of "What Halbert has here today".

### What Halbert has here today

The backend suite is 411 `test_*.py` files under `halbert_core/tests`, run in one interpreter by `.github/workflows/ci.yml` (`test` job, `pytest tests/ -q` from `halbert_core`; the `suite-census` job at `ci.yml:14-116` is a meta-gate that fails when a test file exists that no job runs). `halbert_core/pyproject.toml:153-154` is the entire pytest configuration (`asyncio_mode = "auto"`): no registered markers, no `addopts`, no ruff section, and there is no root `pyproject.toml`, `ruff.toml` or `.ruff.toml` anywhere. The only marks in use are `asyncio` (492), `parametrize` (169), `skipif` (31), `skip` (14), `xfail` (1) — there is no `integration` marker convention despite two reader proposals assuming one. `halbert_core/tests/conftest.py` (232 lines) carries four autouse fixtures, all env-level: `_isolated_config_canon_store` (:9-47, the one import-time-constant re-pin, whose docstring records the canon DB "holding nothing but junk records"), `_reset_capability_registry` (:50), `_no_declared_workspace_layer` (:76, two `delenv`s), and `_isolated_data_and_log_dirs` (:145-167, `setenv` of `HALBERT_DATA_DIR`/`HALBERT_LOG_DIR` only — its own docstring says pollution here "appends to a hash chain that is meant to be evidence"). Nothing scrubs credential-shaped env vars, redirects `HOME`, pins `TZ`/locale/hashseed, blocks non-loopback sockets, or intercepts `os.kill`/`subprocess` — and the audit pass confirmed two consequences already: the conversation store's default path ignores `HALBERT_DATA_DIR` so pytest can open the production `conversations.db` (A08-G12 + own bug), and skills tests read the developer's real `~/.config/halbert/skills` (A13 own bug). The suite's known failure mode is cross-file interpreter-state leakage: the wave-4 merge log (`.handoff/OPENCLAW-LIFT-MASTER-PLAN-2026-09-07.md:131`) records 205 failures that reproduce identically on the baseline and pass in isolation. Evals: `halbert_core/evals/consolidation/` holds a README, `SCORECARD-2026-09-07.md` and one jsonl of rows — the packet-09 harness (`continuity/eval_corpus.py`, `eval_consolidation.py`, `continuity/verdict.py`) with no orchestrator; the A02 audit confirmed 18 gaps and 7 bugs in it (listed under "Related confirmed audit gaps"). Static guards that exist: `tests/test_no_model_names_in_user_facing_source.py` (whole-word regex over 21 model families, `dashboard/routes/**.py` + `dashboard/frontend/src/**.{ts,tsx}` only, >100-file coverage floor, self-tests) and `tests/test_model_locality.py` for SEC-21. Wire-level assertions exist only for the sync Ollama path (`tests/test_num_ctx.py:124-170` asserts `post.call_args.kwargs['json']['options']['num_ctx']`) and via a fake `aiohttp.ClientSession` in `tests/test_tools_supported.py:112-184`; `agents/llm_client.py`'s async payloads (built at :128-137 and :207-216) have no body assertion anywhere. Doctor/health: `utils/health.py` has a `HealthCheck(name, status, message, details, timestamp)` dataclass and five fixed `check_*` functions with no registry, rank, check id or fix hint; there is no `doctor` module (grep of ROADMAP/DECISIONS for "doctor" returns nothing), while `findings/store.py` holds the richer Four-Whys `Finding` shape separately. Frontend: vitest 112 files / 1,029 tests, `e2e/{plan-b,continuity}.smoke.mjs` Playwright walks outside CI, Tauri v2 shell with `tauri.conf.json:22-24` `"security": {"csp": null}` and `:30-33` `"plugins": {"shell": {"open": true}}`. No `.npmrc`, no `allowScripts`/`ignore-scripts` anywhere. Scripts: ~40 under `scripts/` including real gates (`corpus_license_gate.py`, `corpus_quality_gate.py`, `check_contrast.py`, `check_literal_colors.py`, `check-dco.sh`, `boot_smoke.py`); worktrees run tests through `wt_pytest.py` at the repo root; the venv is universal2 and must be invoked as `arch -arm64` (memory `halbert-venv-arch-arm64-fix`). Per-turn effect tracking already exists: `security/turn_digest.py:155-172 record_effect` (ContextVar-bound) fed from `tools/executor.py:645-647` for `WRITE_PLANE_TOOLS`.

Already lifted on this seam (waves 1–6): the packet-09 eval harness + probe battery (question-bank invariance, ceiling arm, closed-book exam, scorecards, red-seam probes P-01..P-08, CRAG verdict contract). Not previously packeted: `OSS-REVIEW-OPENCLAW-2026-09-07.md:156-160` recorded golden wire traces, coverage-ID test profiles, contract suites as installers, policy/mechanism split and boundary tests as un-packeted observations; the master plan's deep-pass agenda (:60-68) has no testing row. Everything in this section is therefore new, except HM19-C1 (extends the "golden wire traces" observation) and HM19-C2 / OC24-C8 (extend the packet-09 eval methodology).

Spot-checks made for this section, all consistent with the records: `grep tool.ruff halbert_core/pyproject.toml` empty; `dashboard/routes/services.py:246` `async def diagnose_service` calling `subprocess.run` at :261; `tools/system_info.py:75-107` opens `/proc/meminfo` unguarded with the broad `except` at :106-107; the four autouse fixtures above; `csp: null`; 411 test files; `evals/` contents; `turn_digest.record_effect` at :155 and its executor call at :645-647; `ci.yml` test job; hermes `agent/turn_stop_gates.py:116` appends the nudge as a `"role": "user"` row (the verifier's correction to HM04-C1); hermes `agent/coding_context.py:466 detect_project_facts` / `:502 project_facts_for` (the verifier's cheaper prerequisite for HM04-C2).

### Lift themes, ranked

#### Theme 1 — Test-run blast radius: a test run must be structurally unable to touch the live host, the live stores, or a `:cloud` endpoint (four mechanisms, one packet)

This is the highest-value theme in the workstream. Halbert's product surface is host administration, the founder's live model slots were both `:cloud` (SEC-21), and the audit pass already found two live-store leaks. Each mechanism below bounds one axis; together they make the suite hermetic.

**One hermetic-environment autouse fixture** (HM19-evals-tests-meta-M3; verifier-added) — origin `tests/conftest.py:240-275, 455-535`; companion audio-primitive guard at `:1044-1075`. A single autouse fixture (1) `delenv`s every credential-shaped name matched by `_looks_like_credential` (:248-253, :464-466); (2) `delenv`s a frozenset of behavioural flags annotated with their damage (:257-275 — the voice/TTS entries note the gateway reads them at call time and would produce "real synthesis, real playback, out of the developer's speakers"); (3) redirects `HOME`/profile home to per-test tempdirs; (4) re-pins the import-time `DEFAULT_DB_PATH` constant (:505-515) because argless constructors would otherwise open the developer's real state DB; (5) pins `TZ=UTC`, `LANG/LC_ALL=C.UTF-8`, `PYTHONHASHSEED=0` (:521-524); (6) disables AWS IMDS probing (:531-533). Invariant: local and CI match, and no test can reach production state or credentials by inheriting the developer's environment. Halbert state: partial — `conftest.py:9-47` implements facet (4) for exactly one constant family (`CANON_DIR/SNAP_DIR/RAW_DIR` from `config/snapshot.py`); the only `delenv`s are the two config-locator vars at :85 and :97; nothing else. Task: replace the four env-only fixtures' shared concerns with one `_hermetic_environment` autouse fixture in `halbert_core/tests/conftest.py`: scrub credential-shaped names (start with `HALBERT_API_TOKEN`, `HALBERT_MCP_TOKEN`, `*_API_KEY`, `*_TOKEN`, `*_SECRET`), `delenv` Halbert's behavioural flags (inventory what `modality_wiring.py`, the TTS path and the terminal-home code read off `os.environ` at call time), redirect `HOME`, pin TZ/locale/hashseed, and inventory every other import-time path constant the way `snapshot.py`'s were (the conversation store's default path — A08 own bug — is the first). Pin with a test that constructs each store argless under the fixture and asserts the resolved path is under `tmp_path`, plus a test that `os.environ` contains no credential-shaped key during a test. Effort: M (estimate). Prerequisites: none; this is the packet's first task and the direct prerequisite of C4. Constraint note: SEC-21 (`:cloud` is cloud) is the reason this is high — a scrubbed environment plus C4's connect guard is what makes reaching a cloud endpoint with the founder's own key impossible from a test.

**Autouse egress-safety net: block any non-loopback `socket.connect`** (HM19-evals-tests-meta-C4) — origin `evals/provider_fallback/probe_104120.py:22-30` ("Egress safety boundary, not a patched production predicate."; the `local_connect` wrapper refuses anything outside 127.0.0.1/::1/localhost) and `probe_104360.py:17-24` (`raise RuntimeError('nonloopback blocked')` at :24 — the reader's cited 16-22 clipped the raise); `issue_104359.py:20-22` carries the same guard "even during production imports". Mechanism: a process-wide monkeypatch of `socket.socket.connect` that raises for any non-loopback address, independent of fixture correctness — it catches what a per-test mock cannot, including off-thread calls. Halbert state: absent — `conftest.py` has no socket guard; the only network-adjacent code is the `TestClient.websocket_connect` wrapper at :207-228 which appends `?token=`. The case that makes it matter here: `model/client.py`'s context probe spawns background threads and `tests/test_num_ctx.py:29-38` warns that an unjoined probe thread "outlives the patch and reaches a real daemon". Task: add an autouse fixture in `conftest.py` that patches `socket.socket.connect` (and `socket.create_connection`) to refuse non-loopback targets with a `RuntimeError` naming the address, register a `pytest.mark.allow_network` opt-out marker in `[tool.pytest.ini_options]` (the packet must define it — the reader's "mirror the existing integration marker" refers to a convention that does not exist), and pin it with a test that a `requests.get('http://203.0.113.1')` inside a test raises the guard's error before any DNS/transport work. Effort: S. Prerequisites: M3 (shares the packet). Priority: verifier raised medium → high ("cheapest high-leverage item in the unit"); this section keeps high. Constraint note: SEC-21.

**Fail-closed live-database test-isolation guard (env + process ancestry)** (HM13-state-root-files-C7; same seam as audit gap A08-store-hardening-G12) — origin, with the verifier's citation correction the packet must carry: `_ensure_test_isolation` is `hermes_state.py:184-214` (raises `RuntimeError` naming the resolved path, the real root, the pytest-marker bypass and the child-process env bypass) and is called at `hermes_state.py:422` as the first statement of `SessionDB.__init__` ("before any connection/pragma/mkdir"); `hermes_state_guard.py:1-145` holds the predicates — `:25-38 _real_platform_state_root` deliberately avoiding `Path.home()` because tests monkeypatch it, `:55-62 _running_under_pytest`, `:65-108` the basename-matched launcher names plus memoised `_has_pytest_ancestor` via `psutil.Process().parents()`, `:111-113 _in_test_context`, `:116-125 _is_production_state_db`; `:17-18` records the field incident ("pytest fixture rows landed in the production state.db and a pytest-spawned child flipped the journal mode under the live WAL writer", #82770). Mechanism: at the one point a store resolves its default DB path, check env markers first and a memoised process-ancestry walk as the backstop, and raise before any connection/mkdir/pragma when a test-context process would open a production path. Invariant: an env-rebuilt subprocess (which loses `PYTEST_*` and the redirect together) still cannot write to the live store. Halbert state: absent — `conftest.py:145-167` is `setenv`-only with no raise; A08 confirmed the conversation store's default path ignores `HALBERT_DATA_DIR` outright. What it protects: `obs/audit.py`'s tamper-evident hash chain, `continuity/state_store.py`'s change ledger, `agents/conversation_sqlite.py`. Task: add `halbert_core/halbert_core/testing_guard.py` (stdlib + optional psutil) with the two predicates and `ensure_test_isolation(resolved_path)`, call it as the first statement of `ConversationStore.__init__` in `agents/conversation_sqlite.py`, `StateStore.__init__` in `continuity/state_store.py`, and the audit-log opener in `obs/audit.py`; pin with (a) a test that a production-shaped path under pytest raises, (b) a subprocess test that spawns `python -c` with a rebuilt env (no `PYTEST_*`, no `HALBERT_DATA_DIR`) and asserts the child still raises via ancestry. Effort: M. Prerequisites: M3 (so the default path is redirected first; the guard is the backstop, not the primary). Constraint note: deterministic, no model in the path; touches `conversation_sqlite.py` (shared hot file — coordinate with any session-tree work).

**Live-system guard intercepting the destructive primitives, with its own argv regression suite** (HM19-evals-tests-meta-M4; verifier-added) — origin `tests/test_live_system_guard.py:1-40` (only argv[0] of a list command is an executable because "arguments are data: a file named `skill` is not the `skill` binary" :1-8, :15-23; wrapper forms `bash -c 'pkill -f ...'` :29-33 and `env GUARD_TEST=1 pkill -f ...` :36-40 still raise `RuntimeError('live-system guard')`); `conftest.py:766, :1003` the `live_system_guard_bypass` marker registered at :1152; `conftest.py:1070` "intercepts os.kill rather than trusting every caller to mock it". Mechanism: conftest patches `os.kill` and the `subprocess` spawn entry points so no test can send a killer command or service-mutating verb at the developer's live processes; every blocked-case fixture command matches no real process so a guard regression cannot kill anything while proving the guard. Halbert state: absent, with a larger blast radius than the origin — 28 test files import `subprocess` and many run it for real (`test_audit_continuity.py:326, :383`, `test_audit_verify_cli.py:128`, `test_consent_cli.py:51`, `test_corpus_license_gate.py:308+`, `test_dashboard_main.py:47`, `test_crypto_storage.py:98-102`), while the code under test calls `os.kill(self._pid, SIGTERM/SIGKILL)` at `streaming/pty.py:362, :432` (exercised by `test_session_manager.py` and `test_terminal_route.py`), `dashboard/parent_watchdog.py:46` sends SIGTERM to `os.getpid()`, `dashboard/routes/services.py` shells `systemctl` with `stop/disable/restart` (the verbs `tools/safety.py:410` calls dangerous), and `discovery/scanners/system_profile.py:820, :1475` run `launchctl list`. Task: add a conftest-level guard wrapping `os.kill`, `os.killpg`, `subprocess.Popen` (and thereby `run`/`check_output`) that refuses a denylist of killer executables (`kill`, `pkill`, `killall`, `launchctl` with `stop/unload/kickstart -k`, `systemctl` with `stop/disable/restart/kill`) using the argv[0]-vs-wrapper distinction verbatim; permit `os.kill(pid, 0)` and signals to pids the test itself forked (track pids returned by the patched `Popen` and `pty.fork` wrappers); register `pytest.mark.live_system_guard_bypass`; ship `tests/test_live_system_guard.py` with blocked cases that match no real process. Pin: the argv cases above plus a case that `pty.py`'s reaper path on a test-forked child is allowed. Effort: M (estimate). Prerequisites: M3/C4 (same packet). Constraint note: user shells are watched by the AI and terminals are agent-owned — the guard must not make the PTY reaper tests bypass-everything; the forked-pid allowance is the design point.

#### Theme 2 — Suite integrity: per-file isolation, the async-blocking lint gate, and a shadowed-definition tripwire (one packet, after Theme 1)

**Per-file process isolation as the test-runner design** (HM19-evals-tests-meta-M2; verifier-added) — origin `scripts/run_tests_parallel.py:1-40` (one `python -m pytest <file>` subprocess per test file, bounded by `os.cpu_count()`, xdist rejected by name because "xdist's persistent workers accumulate state across files, which is exactly the leakage we wanted to fix"; the ~250 ms spawn × ~850 files ≈ 3.5 min tradeoff argued in the docstring) and `tests/conftest.py:783-800` where the historic manual module-state-reset autouse fixture is deleted in favour of the runner (the running example: approvals carried between tests made `test_command_guards` fail 12/15 CI runs). Mechanism: the process boundary is the isolation boundary; intra-file state is the author's responsibility, cross-file state cannot leak. Halbert state: absent — no runner, no isolation strategy, and the consequence is already in the merge log: `.handoff/OPENCLAW-LIFT-MASTER-PLAN-2026-09-07.md:131` reports 205 failures ("async def functions are not natively supported" across voice/TTS/terminal-stream/compute suites) that reproduce on the baseline and pass in isolation (`test_terminal_stream_bridge.py` 23/23 alone) — diagnosed as a suite-run plugin interaction and lived with. The defensive autouse resets in `conftest.py` (`_isolated_config_canon_store`, `_reset_capability_registry`) exist for the same reason. Task: write `scripts/run_tests_isolated.py` (~60 lines: `subprocess.Popen` per file under `halbert_core/tests`, semaphore-bounded, pytest args passed through, exit 0 only if every file exits 0, per-file stdout captured to a report), carrying the two Halbert-specific constraints — invoke `arch -arm64 .venv/bin/python` and pin the child's cwd to `halbert_core` with `PYTHONPATH` set deliberately (memory `halbert-core-namespace-import-gotcha`: pytest-from-root resolves `halbert_core` as an outer namespace package) — run the 205-failure set through it, and record which failures survive real isolation in the packet's handoff. Add it as a second CI job beside the existing single-process run until the two failure sets are reconciled (see founder decision 1). Effort: M (estimate; the script is S, the reconciliation is the work). Prerequisites: Theme 1 first — 411 fresh interpreters each resolving default paths is exactly when the live-store guard must already exist. Constraint note: CI-1's definition of done is "Python suite green in CI"; this is the mechanism that makes "green in isolation" and "green in CI" the same statement.

**The ruff gate is five rules plus a per-file-ignores ratchet, and Halbert has live blocking calls inside async route handlers** (HM19-evals-tests-meta-M1; verifier-added) — origin `pyproject.toml:604-658`: `:616-629` documents ASYNC210/220/221/251 by incident ("blocking calls inside `async def` freeze the whole gateway/uvicorn event loop — every adapter, timer, and health check stops until the call returns"; a 17-minute `getaddrinfo` hang; a `time.sleep` in `start_gateway`), `:630 select = ['PLW1514','ASYNC210','ASYNC220','ASYNC221','ASYNC251']`, `:632-658` the ratchet: intentional exemptions (tests; plugins get PLW1514 relaxed but "the ASYNC gate applies to them with full force") plus a frozen legacy baseline under "Each entry is an EXISTING violation being fixed in its own PR; do NOT add new files here." Mechanism: turn the rule on today against a codebase with pre-existing violations by freezing them file-by-file, so the gate blocks new violations immediately and the baseline only shrinks. Halbert state: absent — no ruff config anywhere (verified), and confirmed hot sites read at the line: `dashboard/routes/services.py:246 async def diagnose_service` runs `subprocess.run(['systemctl','status',...], timeout=10)` at :261 (a ten-second freeze of the uvicorn loop, with more at :276, :285, :448); `dashboard/routes/discovery.py:481 async def _fetch_backup_history` runs `subprocess.run(['journalctl',...])` at :501 (further sites :262, :276, :668); `dashboard/routes/rag.py:408`. Every one stalls the heartbeat, the WS pumps and the scheduler tick for its duration. Task: add `[tool.ruff]`/`[tool.ruff.lint]` to `halbert_core/pyproject.toml` selecting the five rules, generate the per-file-ignores baseline from a first run under the same "do NOT add new files here" banner, add a `ruff check` step to `ci.yml`'s `test` job, then fix the confirmed sites with `await asyncio.to_thread(...)` / `asyncio.create_subprocess_exec` in their own commits and delete their baseline entries. Pin: a test in `tests/test_lint_ratchet.py` that the baseline list only shrinks (parse the pyproject, compare to a checked-in count). Effort: M (estimate: config S, the three route files M). Prerequisites: none (independent of Theme 1; shares the packet with M2 because both are CI-1 scope). Constraint note: the ROADMAP CI-1 row's "ratchet green" refers to an existing ratchet — name this one distinctly (`async-blocking ratchet`) so the two are not confused.

**AST guard at collection time: no test module may define the same name twice** (HM19-evals-tests-meta-M7; verifier-added) — origin `tests/test_no_shadowed_test_definitions.py:1-32`: a plain `ast` walk over every module under `tests/`, flagging a `FunctionDef`/`AsyncFunctionDef`/`ClassDef` name defined twice in one scope; the docstring names both in-tree incidents (a second `_clean_env` autouse fixture silently replaced the original for all 158 tests in a file, dropping the credential stripping; a duplicated test whose survivor omitted the case its name describes). Mechanism: Python keeps only the last definition, so a duplicate deletes coverage or swaps a module's isolation rules with no error and no skip. Halbert state: absent, and the verifier ran the scan — zero hits today, so this is a preventive tripwire priced as such. Why still worth ~30 lines: Halbert's recorded working pattern is multiple concurrent sessions editing the same test files (memories `fable-handoff-2026-08-25-outcomes`, `halbert-core-namespace-import-gotcha`), and a duplicate autouse fixture in a file would silently disarm the canon-store or capability-registry isolation for that whole file. Task: add `halbert_core/tests/test_no_shadowed_test_definitions.py` scanning `halbert_core/tests/**/test_*.py` and `conftest.py` files. Effort: S (estimate). Prerequisites: none. Priority: low (verifier and reader agree).

#### Theme 3 — Verification before "done": an evidence ledger, a stop-time nudge, and the honest-completion metric (one packet; touches `state_machine.py`)

The founder's own session notes record repeated premature-success episodes; these mechanisms are the deterministic answer. None is a security or permission decision — they are prompt-level and passive — so the "no LLM in policy" directive is not touched, and the nudge never blocks a turn.

**Verification evidence ledger: what was actually proven, going stale on the next edit** (HM04-agent-learning-verification-review-C2) — origin `agent/verification_evidence.py:1-576`: `:44-79` three tables (meta / verification_events / verification_state); `:108-129` WAL with fallback and busy_timeout; `:132-147` the transaction contextmanager whose docstring names the WAL/SHM fd-leak class; `:160-215` quote-aware `_split_shell_segments`; `:220-238 _exit_status_is_attributable` (backgrounding, `|`, `|&`, `||` disqualify; an `&&` chain proves each member only when exit 0); `:255-268` npm/pnpm/yarn/bun and pytest-spelling equivalence classes; `:417-441` ad-hoc scripts count only when no canonical commands exist; `:480-499 _insert_evidence` repoints `last_event_id` and clears `last_edit_at`; `:502-534 mark_workspace_edited` stamps `last_edit_at` and merges changed paths (bounded to 200) without touching `last_event_id`; `:551-576 verification_status` reports `stale` by timestamp comparison. Mechanism: terminal commands are shell-tokenised, matched against a workspace's canonical verify commands, scoped targeted/full, and only attributable exit statuses count; an edit after the last evidence makes the status `stale` by timestamp comparison, not by a flag someone must remember to clear. Halbert state: absent (grep for `verification_evidence|verify_on_stop|verification_status|mark_workspace_edited` over core and tests: nothing). Task: port as `halbert_core/halbert_core/agents/verification_evidence.py` with one small SQLite table set under `HALBERT_DATA_DIR`, fed from two seams: terminal-command results (the executor's command tool result path in `tools/executor.py`) call `record_terminal_result`, and the existing `security/turn_digest.py:155 record_effect` (already fed for `WRITE_PLANE_TOOLS` from `executor.py:645-647`) calls `mark_workspace_edited` with the written paths. Keep the attributability parser and the canonical-vs-ad-hoc distinction verbatim. The canonical-command source is not an open problem — the verifier's correction: `agent/coding_context.py:466-492 detect_project_facts` / `:502-514 project_facts_for` is ~30 lines of static detection; port that (and only that) from HM04-C3. Pin: parser table tests (each `&&`/`||`/`|`/`&` case), a staleness test (record pass, edit, assert `stale`), and a self-prune test. Effort: M. Prerequisites: none beyond the two seams named. Constraint note: passive and additive; the ledger stores command strings — route them through the Tier-2 scrub before persisting so a secret typed into a shell never lands in a ledger row.

**Verify-on-stop nudge: a policy-only guard against a false "done" after code edits** (HM04-agent-learning-verification-review-C1) — origin `agent/verification_stop.py:1-228`: `:13 _MAX_CHANGED_PATHS_IN_NUDGE = 8`; `:17-23` the non-code extension/filename allowlist (README/SKILL.md/LICENSE edits never nudge); `:50-76 verify_on_stop_enabled` precedence; `:158-165` the `attempts >= max_attempts` bound (default 2); `:167-222` the three-way instruction (canonical command / `hermes verify --json` / tempfile-prefixed ad-hoc script) always ending "if verification is not possible, explain the concrete blocker instead of claiming the work is fully verified." Two verifier corrections to the reader's text that the packet must carry: (a) the default is OFF/opt-in (`:55-57`; "auto" is the legacy surface-aware mode, not the default); (b) the nudge is appended as a `"role": "user"` row (`agent/turn_stop_gates.py:116`), with the "[System: ...]" text a literal inside the user turn — that is what keeps role alternation legal. Mechanism: never runs checks itself; at turn end, if any changed path this turn is code and `verification_status()` for the workspace is not `passed`, synthesise one bounded follow-up naming the exact status and the changed paths and asking for a real command or an honest blocker. Halbert state: absent, and with an unlisted prerequisite the verifier found — there is no stop-gate framework: grep `stop_gate|pending_verification|finish_reason` over `agents/*.py` hits only `llm_client.py:42/277/489`, a response dataclass field with no consumer. The state machine has no place to hang "the model stopped with text; run the gates". Task: (1) add a minimal stop-gate seam in `agents/state_machine.py` at the point a turn is about to commit its final text — an ordered list of pure gate callables returning `continue(nudge)` or `pass`, with a per-turn attempt counter; (2) add `agents/verification_stop.py` mirroring the origin, reading the C2 ledger and the turn digest's written paths; (3) config key under the existing agent config with the origin's env > config > default precedence. Pin: a gate-order test, a prose-only-turn test (no nudge), a code-edit-without-evidence test (exactly one user-role nudge, then the turn proceeds on attempt 2 regardless), and a test that a `passed` status yields no nudge. Effort: M (the seam is the cost; the module is a port). Prerequisites: C2. Constraint note: touches `state_machine.py` (shared hot file; merge last in its wave). Founder decision 2 governs the default.

**Invented-completion-claim rate as an eval metric** (OC24-docs-qa-security-skills-C8) — origin `qa/frontier-harness-plan.md:83-91` (verifier-corrected lines; the reader cited 97-115): the scoring rubric's "empty-promise rate" at :86 and "scope drift: unrelated scenario updates, grand wrap-ups, or invented completion tallies" at :90; the tuning loop at :74-81. Mechanism: score responses for dishonest completion claims as a first-class dimension, not only for task success. Halbert state: absent (grep `empty.promise|scope.drift`: nothing). Verifier narrowing followed here: the "cheapest model first, fix the provider overlay" half does not transfer — Halbert's slots are local-first with `is_local_model` (`model/llm_config.py:181-190`) as the locality choke point and the founder forbids naming model families, so "run GPT then Claude" is out. Task: add an `invented_completion` dimension to the packet-09 verdict/scorecard rows (`continuity/verdict.py` claims vocabulary and the `evals/consolidation` row shape): a deterministic check that a "done" claim is accompanied by at least one attributable verification event from the C2 ledger, scored per turn. Effort: S. Prerequisites: C2. Priority: low.

**Two-layer verification discipline for UI work: state-read proof is not rendered-UI proof** (OC24-docs-qa-security-skills-C20) — origin `skills/control-ui/SKILL.md:122-128` (verifier-corrected; the reader cited 95-108): "An HTTP 200 ... is transport proof, not visual proof ... If no browser-control or connected-client inspection is available, report the dashboard as published but visually unverified instead of claiming success." Mechanism: a named anti-pattern check requiring a rendered-client observation before a UI success claim. Halbert state: absent — grep for "transport proof"/"visual proof" across CLAUDE.md, ROADMAP.md, DECISIONS.md, `.handoff/*.md`, `documentation/*.md` returns nothing. Task: one paragraph in the repo's dev conventions (the natural home is `halbert_core/halbert_core/dashboard/frontend/e2e/README.md` next to the Playwright smokes, and a line in the project CLAUDE.md) stating the rule and the "visually unverified" wording; no code. Effort: S. Prerequisites: none. Priority: low; carried because it costs a paragraph and names a failure mode the founder has hit.

#### Theme 4 — Wire-contract tests and surface-exhaustive constraint evals (one packet, extends packet 09)

**Wire-level contract tests for the LLM client: assert the real request JSON against a loopback fixture** (HM19-evals-tests-meta-C1; extends the "golden wire traces" observation at `OSS-REVIEW-OPENCLAW-2026-09-07.md:156`) — origin `evals/provider_wire/issue_103944.py:1-40` (`os.environ.clear()` then `HOME`/`HERMES_HOME`/`PATH`/`NO_PROXY=*` rebuilt, tempfile HOME, `ThreadingHTTPServer` capturing the POST body at `/chat/completions`), `issue_104282.py:1-25`, `issue_104359.py:1-18` ("Offline production-path wire probe; only loopback HTTP is permitted"), `provider_fallback/probe_104120.py:1-30` — a house convention across 6+ independently numbered probes. Mechanism: run the real production HTTP client against a loopback server after scrubbing the environment, and assert the bytes on the wire rather than a mocked client object. Halbert state: corrected by the verifier from absent to partial — `tests/test_num_ctx.py:124-170` asserts the serialised outbound body for `model/client.py`'s sync Ollama path, `tests/test_tools_supported.py:112-184` drives the real `OllamaClient` through a fake `aiohttp.ClientSession`, and `tests/test_model_locality.py` covers the SEC-21 predicate. Genuinely absent: (1) `agents/llm_client.py`'s async aiohttp payloads (:128-137, :207-216) have no body assertion; (2) nothing runs a real transport against a loopback server, so header/encoding/proxy behaviour is untested; (3) no test scrubs `os.environ` before constructing a client. Task: add `halbert_core/tests/wire/` with a loopback `ThreadingHTTPServer` fixture that records request path, headers and JSON body for the Ollama chat/generate shapes and the Anthropic messages shape; drive `agents/llm_client.py`'s real async client at both build sites and assert the body (model tag, `options.num_ctx`, `tools` presence/absence, `stream` flag) and headers (no `Authorization` on a local target); add one SEC-21 regression at the wire: a `:cloud`-tagged slot never produces a request from the local-model code path and a secure turn produces no request at all. Effort: M. Prerequisites: Theme 1's M3 + C4 (the env scrub and loopback-only guard are what make this suite safe to run on the founder's machine). Priority: verifier lowered high → medium because C4 is the cheaper egress boundary; kept medium here. Constraint note: never bake model names — fixtures use slot labels and a placeholder tag, with `:cloud` as the only literal suffix under test.

**Surface-exhaustive constraint-removal eval for the "never a model name" directive, with a boundary doc** (HM19-evals-tests-meta-C2) — origin `evals/output_caps_scope.md:1-72` (run recipe, `LOCAL_CAPTURE_ONLY` fixture, a "Boundary" section splitting Removed :19-25 from Preserved :27-52 with named reasons, an 8-row before/after table :54-64) and `evals/output_caps_surfaces.py:12-50` (drives `agent.run_conversation`, delegate child agents, `MoAClient`, `auxiliary_client.call_llm`, `curator._run_llm_review`, each asserting the fixture string; PTY arm at :112). Mechanism: to prove a cross-cutting constraint holds everywhere, enumerate every production surface, drive each through a local-capture fixture, and publish the allowed/forbidden boundary. Halbert state: corrected by the verifier from absent to partial — the reader's evidence line was wrong: `tests/test_no_model_names_in_user_facing_source.py` already enforces the directive statically (regex over 21 families, named exceptions :33-49, two directories :64-72, self-tests :90-98, >100-file floor :76-78). The strengthening is still real: it is (a) a source-text match over two directories only — `model/attribution.py`, `model/config_wizard.py`, `model/hardware_detector.py`, `packages/model-picker/**`, `packages/design-system/**` and every CLI/TTS string path are outside `_sources()`; (b) a fixed family list its own docstring calls "a tripwire for drift, not a proof of absence" (:17-19); (c) never a runtime surface, so a name assembled at runtime is invisible. Task: build `halbert_core/evals/model_name_surface/` — a surfaces table (ModelSettings and the connection-slot picker via vitest render, chat responses and TTS confirmation via the Theme-4 loopback fixture returning a sentinel model tag, `config_wizard`, `doctor`/health output, CLI `halbert` status strings) each asserted to render only slot labels when the configured tag is the sentinel; extend the static guard's `_sources()` to the three `model/*.py` files and the two packages; publish `evals/model_name_surface/BOUNDARY.md` listing where a raw tag is allowed (config file, `models.yml`, logs at DEBUG) and forbidden (every user-facing string). Effort: M. Prerequisites: C1's fixture. Priority: verifier lowered high → medium (the tripwire already catches the drift class that bit twice); kept medium. Constraint note: this is the enforcement half of the founder directive "never bake or recommend model names".

**Large-batch eval orchestrator discipline: exit-3-never-scored, resume by jsonl cell, N-infra-abort fuse** (HM19-evals-tests-meta-C9) — origin `evals/core_tool_deferral/README.md:19-27` (worker runs one cell in an isolated subprocess with a hermetic env; "Exit 3 = infra/config error (never scored)"; orchestrator "resume-safe, per-task wall timeouts, parallel cells, errored-record retry, 3-infra-abort fuse") and `evals/browser_use/README.md:23-24` ("Completed cells in results/*.jsonl are skipped on rerun (same pattern as scripts/toolperf_abeval)"). Mechanism: infra failures are never scored, a small fixed infra-error count aborts the run, and completed cells persist so a rerun skips them. Halbert state: absent — `evals/consolidation` has no orchestrator to carry it; the session memory `workflow-refute-fanout-sizing` records exactly the budget blowout this prevents. Task: a design note, not a build — when `halbert_core/evals/` grows a second battery (the model-name surface eval above is the likely first), its runner must carry the three rules from the first commit. Record it in `halbert_core/evals/README.md`. Effort: S. Prerequisites: none. Priority: low (reader and verifier agree; nothing changes today).

#### Theme 5 — Doctor, diagnostics and host-truth: one findings shape, a check registry, a coverage table, a redacted support bundle, and one live defect (one packet)

**Fix Darwin breakage in the agent-facing `get_memory_info` tool** (OC22-extensions-local-providers-apps-C7) — origin `extensions/llama-cpp/src/hardware.ts:60-83` (verifier-corrected from 58-77): `readAvailableMemory` branches linux → `/proc/meminfo` `MemAvailable`, darwin → `vm_stat` page size + Pages free + Pages inactive ("Inactive pages are reclaimable; os.freemem alone mistakes file cache for pressure"), `os.freemem()` last. Mechanism: platform-branched available-memory probing so the same tool is correct on both hosts. Halbert state: partial, and a live defect on the host Halbert runs on — `tools/system_info.py:75-107 get_memory_info()` opens `/proc/meminfo` unconditionally; on Darwin the `FileNotFoundError` is caught by the broad `except` at :106-107 and returned as "Error getting memory info: ..." straight to the model, while the correct darwin logic already exists at `discovery/scanners/system_profile.py:443-460` (vm_stat, page-size regex, free + inactive + speculative, "matches Activity Monitor"). Note the two implementations disagree (origin: free+inactive; system_profile: free+inactive+speculative) — pick one and say which. Task: factor the vm_stat parse out of `system_profile.py` into a shared helper and call it from `get_memory_info` when `platform.system() == 'Darwin'`, keeping `/proc/meminfo` for Linux; pin with a test that feeds a canned `vm_stat` output and asserts the rendered totals, and a test that the Darwin path never opens `/proc/meminfo`. Effort: S. Prerequisites: none. Constraint note: "first-person as the machine, grounded in measured data" — today the machine reports an error string about its own memory.

**Doctor/health findings unified to a structured, severity-ranked shape with fix hints** (OC09-tasks-system-agent-flows-boards-claws-C16) — origin `src/flows/health-checks.ts:6-12` (severity + rank map), `:14-31` threshold filtering, `:33-45 HealthFinding` (checkId, severity, message, source, path, line, column, target, requirement, fixHint), `:47-67` the three-mode `doctor|lint|fix` context with `dryRun`/`diff`. Mechanism: one structured finding record instead of a free-text status, so the same check serves report, lint and fix modes. Halbert state: partial — `utils/health.py:26-33 HealthCheck(name, status, message, details, timestamp)` and `SystemHealth` :36-48 with no rank/id/fix hint, while `findings/store.py:5-12, :33-45` already has the Four-Whys `Finding` + `FindingStatus`. Two incompatible vocabularies. Task (the verifier's preferred option): route `utils/health.py`'s checks through `findings/store.py`'s `Finding` shape (severity, `why_trust` as `path:line` provenance, fix hint in the existing fields) rather than growing a second parallel shape; keep `get_system_health()` as a thin projection for the existing dashboard consumer. Pin: each `check_*` returns a `Finding` with a stable check id. Effort: S. Prerequisites: none; do it first in the packet because OC21-C2 and OC11-C11 register into it. Priority: low.

**SHA-256-pinned native-artifact verification with symlink rejection, registered as a health check** (OC21-extensions-device-media-desktop-C2) — origin `extensions/cua-computer/src/driver-artifact-verification.ts` (244 lines; the reader's "1-260" overshoots): `:22-27` five typed diagnostic codes, `:120-122` sha256 over `readFileSync`, `:183-190` package name AND version compared against the manifest's accepted version, `:199-215` `lstatSync` then reject `!stat.isFile() || stat.isSymbolicLink()` before hashing, `:227-235` reject on digest mismatch, every failure carrying a `fixHint`; `doctor.ts:10-31` registers it as a `HealthCheck {id, kind, description, source, detect -> [{checkId, severity, ..., fixHint}]}`. Mechanism: verify locally installed native binaries by content hash against a manifest shipped with the trusted package before granting any capability backed by them. Halbert state: absent — grep `sha256|digest|checksum` across `mcp/*.py` returns only `hmac.compare_digest` token checks (`camera_gate.py:452-461`, `server.py:1485-1488`). The verifier's relevance correction: Halbert already has a live native-artifact trust surface — `mcp/client.py:229-284` spawns external MCP servers by a config-supplied `command` (`config.command` at :829) — so the check has a real target today, and audit gap A17-G3 (a `write_file` to `mcp_config.yml` classifies MEDIUM on macOS and the health monitor launches the new command within one tick) is the adjacent hole. Task: add `utils/artifact_verify.py` verifying a `{path: sha256}` manifest with symlink/non-regular rejection and typed reasons; register a health check that, for every MCP server command resolving to a file under the user's control, compares against a pinned digest recorded at first approval (a missing pin is a `warn` finding with the hint to pin, a mismatch is `critical`). Pin: symlink, missing, mismatch, ok. Effort: S. Prerequisites: C16's registry. Priority: medium. Constraint note: deterministic; one policy pipeline across MCP and internal tools — the digest pin is data the admission gate can consult later, not a second gate.

**Coverage registry: a static inventory of which seams the redaction choke point actually sees** (OC11-talk-tts-acp-web-routing-context-C11) — origin `src/proxy-capture/coverage.ts:9-16` (status `'captured'|'proxy-only'|'uncovered'`), `:25-140 DEBUG_PROXY_COVERAGE_ENTRIES`, and `:168-195 maybeWarnAboutDebugProxyCoverage` (outside the reader's cited range) warning once per (sessionId, proxyUrl) with the non-captured ids. Mechanism: a hand-maintained table that is both documentation and a runtime warning source, so an operator relying on blanket coverage knows the gaps. Halbert state: absent, and the reader's evidence was wrong (no doctor module exists); the verifier's rewrite is followed: the honest sink is `findings/store.py`. Task: add `security/redaction_coverage.py` — a static table of egress seams (MCP response choke point, dashboard WS, TTS/Wyoming path — A10-G5 says the quality rules run on only one of the two speech egress paths, terminal stream, turn-event tee, scheduler receipts) with `status` and a note — and a health check that emits one `Finding` per `uncovered` seam. Effort: S. Prerequisites: C16. Priority: low. Constraint note: redaction at the response choke point is the directive; this is the table that says where the choke point is not.

**Redacted diagnostic support bundle for post-mortem debugging** (OC09-tasks-system-agent-flows-boards-claws-C13) — origin `src/trajectory/metadata.ts:1-19` (imports `redactConfigObject`, `resolveCommitHash`, `resolveOsSummary`, `redactPathForSupport`/`sanitizeSupportSnapshotValue`, plugin and skill snapshots), `:22-46` the params, `src/trajectory/types.ts:34-57 TrajectoryBundleManifest` and `:59-73` the closed warning-code set with counts and sample rows. Mechanism: one function builds a redacted snapshot (config, active plugins/skills, git commit, OS summary) bundled with the run's event trace into a versioned manifest. Halbert state: absent — and it has every ingredient: `obs/audit.py` (chained, verifiable, before/after digests), `security/result_redaction.py` plus the redaction variant registry, the discovery scanners for the OS/hardware summary, the turn-event tee. Task: add a `halbert diagnostics export` CLI command (and a dashboard settings button that stages, never sends) writing a versioned zip under the data dir: redacted config snapshot (through the Tier-2 scrub), active skills catalog snapshot, git commit, hardware summary from the scanners, the last N turn-event-tee entries and health findings, with a manifest listing the warning taxonomy. Pin: a test that the bundle contains no string matching the redaction registry's secret variants, and that `models.yml` tags appear only as slot labels. Effort: M. Prerequisites: C16 (findings in the bundle); Theme 5 packet. Priority: medium. Constraint note: Tier-2 secrets by deterministic template, scrubbed before any model — the bundle is a file on disk that a human may paste anywhere, so the scrub is the whole value; founder decision 4 covers what it may contain.

#### Theme 6 — Posture contracts: desktop security, npm install scripts, and a security-regression rulepack (one packet; each is a test that pins a decision)

**Declarative desktop-security contract tests: window-open policy and entitlements/Info.plist alignment** (HM19-evals-tests-meta-M5; verifier-added) — origin `tests-js/window-open-policy.test.ts:1-31` (a named-CVE regression, GHSA-9f4c-93c8-jc8g / CVE-2026-70608; imports the real policy module `main.ts` wires in and asserts every scheme — https, http:8080, `file:///etc/passwd`, `javascript:`, a custom proto, the empty string — returns `{action:'deny'}` with only a sanitised origin reported) and `tests-js/desktop-mac-entitlements.test.ts:1-45` (parses the actual plists and asserts every `com.apple.security.device.*` entitlement granted to the main app is also granted to the inherited helper, because under the hardened runtime TCC silently refuses instead of prompting — the log line is quoted at :15-17; its constants at :38-41 already cover a Tauri target's `tauri.conf.json`, `entitlements.plist` and `Info.plist`). Mechanism: pin desktop-security posture as a test rather than a review habit. Halbert state: absent, with a real gap behind each: `dashboard/frontend/src-tauri/tauri.conf.json:22-24` `"csp": null` and `:30-33` `"shell": {"open": true}`, `src-tauri/capabilities/default.json` grants `opener:default` to the main window and the voice HUD, and the webview renders agent- and RAG-influenced data through `<a href={doc.url} target="_blank">` (`components/settings/tabs/KnowledgeTab.tsx:467, :649`; `CloudDisclosureModal.tsx:123`) — so there is no deny-by-default window-open policy anywhere; separately, no `entitlements.plist`, no `Info.plist`, no `NSCameraUsageDescription`/`NSMicrophoneUsageDescription` exist, a gap `.handoff/security-audit-findings-2026-09-06.json:1474, :1781` already documents (AVFoundation terminating the caller while `webcam_capture.py:102` blames the hardware). Task: two vitest contract tests under `dashboard/frontend/src/__tests__/posture/`: (1) parse `tauri.conf.json` and assert `security.csp` is non-null and that outbound navigation goes through one `openExternal(url)` policy module with an explicit scheme/host allowlist (add the module and route the three `<a target="_blank">` sites through it); (2) once the audit's plist fix lands, assert every device entitlement, its Info.plist usage string and the `tauri.conf` bundle reference agree. Effort: M (estimate; the policy module and CSP are the work, the tests are S). Prerequisites: founder decision 5 on the CSP (it can break the dev server and inline styles; scope it before writing); the plist fix from the security audit (SEC-N rows) for test (2). Priority: medium. Constraint note: the security audit's posture bar ("reviewer-defensible"); DIST-1/APPLE-1 rows own the bundle.

**npm install-script allowlist pinned to the lockfile it gates** (HM19-evals-tests-meta-M6; verifier-added) — origin `tests-js/allow-scripts-sync.test.ts:1-28` (two contracts: every versioned pin names a version the lockfile resolves; every package the lockfile marks as having an install script is covered by a decision — allowed at exact version or denied by bare name; the docstring records it biting twice — `get-windows` without an entry so `node-pre-gyp` never fetched the binding; an Electron CVE bump leaving the old pin behind) and the companion `package-json-lazy-deps.test.ts:1-25` (nothing eager in the root manifest). Mechanism: keep the install-script allowlist honest against dependency bumps, because npm downgrades a now-blocked postinstall to a buried warning. Halbert state: absent — no `.npmrc`, and `allowScripts`/`ignore-scripts` appear in none of the four `package.json` files (root, `packages/model-picker`, `packages/design-system`, `dashboard/frontend`); every transitive postinstall runs unrestricted as the founder on the host Halbert administers, and the design-system workspace pulls a full Storybook 8 tree. Task: decide the posture (founder decision 6; default `ignore-scripts=true` in a root `.npmrc` plus a short explicit allowlist for packages that genuinely need a native build — esbuild, the Tauri CLI, sharp if present), record it in the root `package.json`, and add a vitest test that reads `package-lock.json`, fails when an allowlist pin names a version the lockfile no longer resolves, and fails when a package with `hasInstallScript` has no decision. Effort: S (estimate). Prerequisites: founder decision 6. Priority: medium. Constraint note: single-user single-host — the threat is not an abstract hostile registry, it is that `npm install` on this repo runs arbitrary code as the founder.

**A precise security-regression rulepack: one rule per recurring closed finding, metadata-bound to its review** (OC24-docs-qa-security-skills-C2) — origin `security/README.md:1-80` (compile step into a checked-in super-config; the four-step preference ladder variant detector > scoped behavioural regression > exact regression canary > no rule; the quality contract "catch the vulnerable behavior, be silent on the fix, keep findings limited to verified regressions or variants") and `security/opengrep/README.md:1-60` (id rewritten to `<source-id>.<original-id>`; the metadata table `ghsa/advisory-id/advisory-url/detector-bucket/source-rule-id/source-file` enforced by `check-rule-metadata.mjs`); `security/opengrep/precise.yml` holds exactly 145 rules (verified by grep). Mechanism: every confirmed vulnerability that can recur becomes a durable, low-noise static rule tied by metadata to its finding, run on every diff. Halbert state: absent — no semgrep/opengrep/ast-grep anywhere; `.github/workflows` holds only `ci.yml` and `dco.yml`. Priority: verifier lowered high → medium and this section agrees — the reader's task ("one rule per closed SEC-N finding" across 186 findings) is the wholesale copy the origin's own contract warns against. Task: adopt the methodology with a starter set of five to ten rules anchored on findings that actually recurred (the query-credential shape from the security audit's C1, the `:cloud` tag in a locality predicate (SEC-21), a `subprocess.run` inside `async def` in `dashboard/routes/` — which overlaps M1's ruff gate and should defer to it, a raw `Path.home()` in a store's default-path resolution), each carrying `finding-id` + review-doc path metadata as the non-negotiable part; a `check_rule_metadata.py` under `scripts/`; a CI step on the diff. Effort: M. Prerequisites: pick the engine (opengrep/semgrep are the origin's; `ast-grep` is lighter — founder decision 7). Constraint note: deterministic policy; the rules never touch runtime.

#### Theme 7 — Ops conventions (low; record, do not packet)

**Upstream-tracking loop over the three origin repos, narrowed to the primitive that exists** (OCC02-archive-bundle-and-tracking-pipeline-C4) — origin `docs/adr/ADR-003-nightly-verified-release-pipeline.md` (219 lines: four phases; hard-gate table :103-110; "AI analysis ... must not block releases when the API key is unavailable" :26; failure-issue dedupe :137-140), `scripts/check-claude-release.sh:1-49` (registry poll, 8-byte tracker file, exit 0/1/2), `scripts/generate-release-notes.sh:1-60`, `.github/workflows/nightly.yml` (code-backed). Mechanism: separate deterministic hard gates from optional never-blocking enrichment; keep state in the smallest durable artifact; route any failure to one deduped tracking issue. Halbert state: corrected by the verifier from absent to partial, and the reader's evidence was wrong on both halves — `scripts/` holds ~40 scripts including real gates; `ci.yml`'s `suite-census` job is a meta-gate stronger than anything in ADR-003; the polling primitive exists at `scheduler/monitor_hash.py:1-55` (persisted source hash, `NOT_GATED/BASELINE/SUPPRESS/ALERT_DIFF/SOURCE_ERROR`, hash persisted before the agent runs, "a source failure is an error, never a change"). Genuinely absent: a scheduled job pointing at the OSS repos, a cron trigger on `ci.yml`, failure-issue dedupe. Task (narrowed): register one `detector_sweep`-class job in `DEFAULT_MONITOR_HASH_JOBS` whose probe is `git ls-remote <origin> HEAD` for each of the three repos, with an `ALERT_DIFF` writing a `.handoff/REVIEW-NEEDED-<repo>-<date>.md` stub — never auto-patching. Effort: S after narrowing (the reader's L was for the whole pipeline). Prerequisites: none. Priority: low (no users, no release cadence; the founder tracks state in `.handoff/` by choice). Constraint note: touches `scheduler/` config only, not the shared hot files.

### Related confirmed audit gaps

On the test-isolation seam (Theme 1):
- A08-store-hardening-G12 [confirmed/medium]: no live-DB test-isolation guard; the conversation store's default path ignores `HALBERT_DATA_DIR` and pytest can open the production `conversations.db` — the same origin (`hermes_state.py:184-215`) as HM13-C7; fix together.
- A08 own bug [confirmed]: the conversation store's default path ignores `HALBERT_DATA_DIR` while the state ledger honours it (CFG-1 applied to one store only) — the import-time-constant inventory in M3 must start here.
- A13 own bug [confirmed]: tests read the developer's real `~/.config/halbert/skills` through `daemon_skill_dirs()`, so their outcome depends on the machine's home directory — M3's `HOME` redirect closes it.
- A17-mcp-boundary-G3 [confirmed/medium]: `mcp_config.yml` is an unconfirmed arbitrary-command path (write at MEDIUM on macOS; the health monitor launches the new command within one tick) — OC21-C2's digest pin is the adjacent check; the classification fix belongs to permissions-consent-security.

On the eval-harness seam (Theme 4; the packet-09 lift this workstream owns):
- A02-eval-harness-G1 [confirmed/high]: `run_exam` hands the answerer the gold answer — the closed-book claim is only true for the built-in regex answerer.
- A02-G2 [confirmed/medium]: `judge_score` has no hedging check; any longer/negated/punctuated correct answer scores partial; the packet's hedging-marker spec was dropped (`_HEDGES` is dead code).
- A02-G3 [confirmed/medium]: no RECOVERY arm (retrieval over the destroyed region), which is Halbert's real production answer to consolidation loss.
- A02-G4 [confirmed/medium]: the gate continuation keeps the head of gate output; the origin keeps the tail where the diagnostic is.
- A02-G5 [confirmed/medium]: a gate that raises crashes `verdict()`; the origin converts it into a failed gate.
- A02-G6..G18 [confirmed/low]: no retry exhaustion or turn budget; no judge timeout; WAIT accepts pid <= 0 / seconds == 0; breakers reset only on a clean verdict; breakers inert unless the caller keeps the `JudgeCircuit`; per-question verdicts discarded; region messages shared-mutable across arms; no region token count on rows; `FAILED-RETRYABLE` never retries; no unchanged-workspace fingerprint; judge input unbounded; region-scoping tripwire does not prove head/tail survive; no age bound on a pid wait.
- A02 own bugs [confirmed]: `MatrixReport.render()`/`main()` crash when an OK arm scored zero questions; the `[gate output truncated]` marker never survives; exact-match is punctuation/whitespace-fragile; `_run_arm` catches only `PolicyFailed`; `run_matrix` derives region/turns from `threads[0]` only; `ParseCircuitOpen`/`TransportCircuitOpen` are exported and imported by tests but never raised; `_HEDGES` is dead code with a docstring claiming the judges agree.

Vacuous-test bugs the audits confirmed across other units — the class a "test pins nothing" review should sweep once Theme 2's runner makes per-file results trustworthy:
- A01 own bug: the "utility not confidence" test passes vacuously (avg_score ignored by the ranker).
- A07 own bug: `test_stop_and_redirect_share_safety` pins nothing.
- A10 own bug: `tests/test_speech_summarizer.py::test_short_reply_never_invokes_the_summarizer` is vacuous — the summarizer IS invoked.
- A13 own bug: six of nine bundled `SKILL.md` descriptions exceed the 60-character discipline and no test pins it.
- A06 own bug: monitor probe output with undecodable filenames raises inside `evaluate()`, so the gate never persists a baseline and the catch-up runs ungated — relevant to OCC02-C4's use of `monitor_hash.py`.

On the async-blocking seam (Theme 2, M1): A10-tts-G2 was refuted as a gap but its finding stands as an own bug — "the 12 s summary timeout is not the real bound: the GPU advisory lock can add 30 s before the request starts, on the event loop" — a sixth blocking site the ASYNC ratchet baseline will list.

### Do not lift

- **HM20-skill-library-C10 — adversarial-persona UX test with RED/YELLOW/WHITE/GREEN triage.** Verifier lowered medium → low; this section drops it. Single-user product: the founder is the only user, so a roleplayed non-technical persona is "a user who does not exist"; the driver it needs (`dashboard/frontend/e2e/*.smoke.mjs`) already exists for real walks; and the reader's sink was wrong — writing simulated complaints into `findings/store.py` would put fiction in a store whose whole value is that every row is measured. Nothing here is worth a packet. (Origin `optional-skills/dogfood/adversarial-ux-test/SKILL.md:111-126` remains a good read for the triage questions.)
- **HM04-agent-learning-verification-review-C3 — `hermes verify` recipe detection + bootstrap/build/test/start/readiness-poll runner.** Low priority, L effort, and the verifier's provenance correction matters: both files state they are a near-1:1 port from superagent-ai/grok-cli (`recipes.py:3-4`, `environment.py:3`), so it is not Hermes IP and an executor should read grok-cli. Halbert's canonical commands are known (pytest via `arch -arm64`, vitest, `tsc --noEmit`, `cargo`), which is why only the ~30-line `detect_project_facts` moves into HM04-C2 above; the runner, recipes and readiness poll do not. The verifier notes the two load-bearing bits of `runner.py` (refuse-to-verify when the check would destroy live state; process-group teardown) surfaced as separate items in another workstream; they belong with terminal-tools-subagents, not here.

Every other id in the digest appears in exactly one theme above. Priority demotions made in this section beyond the verifier's: none. Verifier demotions carried: HM19-C1 high → medium, HM19-C2 high → medium, OC24-C2 high → medium, HM20-C10 medium → low, OCC02-C4 medium → low. Verifier promotion carried: HM19-C4 medium → high.

### Proposed packets

**T1 — Test-run blast radius** (HM19-M3, HM19-C4, HM13-C7 with A08-G12 and the A08/A13 own bugs, HM19-M4). Prerequisites: none; this is the first packet in the workstream and a prerequisite of T2 and T4. Founder decision: 3 (raise vs redirect for the live-DB guard; default raise). Hot files: `agents/conversation_sqlite.py` (guard call in `__init__` — shared hot file; merge after any session-tree change in the same wave), `continuity/state_store.py`, `obs/audit.py`, `tests/conftest.py`, `halbert_core/pyproject.toml` (marker registration). Verification gate: full suite green under the new fixtures; the ancestry subprocess test green; a deliberate `requests.get` to a non-loopback host inside a test raises.

**T2 — Suite integrity and the async ratchet** (HM19-M2, HM19-M1, HM19-M7, plus the live defect OC22-C7 because it is a one-file fix in the same lane). Prerequisites: T1 merged (411 fresh interpreters need the live-store guard). Founder decision: 1 (whether the isolated runner replaces or sits beside `pytest tests/` in CI). Hot files: `dashboard/routes/services.py`, `dashboard/routes/discovery.py`, `dashboard/routes/rag.py` (the three async-blocking fixes; none of the five shared hot files, but `dashboard/app.py` is adjacent if a lifespan helper is introduced), `ci.yml`, `halbert_core/pyproject.toml`, `tools/system_info.py`, `discovery/scanners/system_profile.py`. Verification gate: `ruff check` green against the frozen baseline; the isolated runner's surviving-failure list recorded in the handoff; CI-1 row updated with the reconciled count.

**T3 — Verification before "done"** (HM04-C2, HM04-C1, OC24-C8 metric, OC24-C20 convention; ports `detect_project_facts` from the dropped HM04-C3). Prerequisites: T1 (the ledger's tests need the hermetic env); founder decision 2 (nudge default). Hot files: `agents/state_machine.py` (the new stop-gate seam — shared hot file, merge last in its wave), `tools/executor.py` (the `record_terminal_result` call beside the existing `record_effect` at :645-647 — shared hot file), `security/turn_digest.py`, `continuity/verdict.py`. Verification gate: gate-order and nudge-count tests; a full-suite run proving typed turns without edits are byte-identical.

**T4 — Wire contracts and the model-name surface eval** (HM19-C1, HM19-C2, HM19-C9 as a README note; the A02 gaps G1–G5 and the seven A02 own bugs as the strengthening half, G6–G18 as a follow-on list). Prerequisites: T1 (env scrub + loopback guard). Founder decision: none. Hot files: `agents/llm_client.py` (read-only under test; no change intended), `continuity/eval_consolidation.py`, `continuity/verdict.py`, `tests/test_no_model_names_in_user_facing_source.py` (extend `_sources()`), new `tests/wire/` and `evals/model_name_surface/`. Verification gate: the wire suite green with the socket guard active; `BOUNDARY.md` committed; the A02 G1 fix (`answer_fn(question)` without gold) pinned.

**T5 — Doctor, diagnostics and host-truth** (OC09-C16 first, then OC21-C2, OC11-C11, OC09-C13). Prerequisites: none hard; T1 recommended so the bundle test's redaction check runs hermetically. Founder decision: 4 (bundle contents and destination; default local file, never sent). Hot files: `utils/health.py`, `findings/store.py`, `mcp/client.py` (read the configured command path for the digest check; no behaviour change), `mcp/health.py`, `security/result_redaction.py`, a new CLI subcommand in `Halbert/main.py` (entry point) and a staged-only dashboard action (UI commands staged, never executed). Verification gate: every `check_*` returns a `Finding`; the bundle test proves no secret-variant string survives.

**T6 — Posture contracts** (HM19-M5, HM19-M6, OC24-C2, OCC02-C4 as a small scheduler-config task). Prerequisites: founder decisions 5, 6, 7; the security audit's plist fix (SEC-N rows) for M5's second test; T2's ruff gate before OC24-C2 so the async rule is not duplicated. Hot files: `dashboard/frontend/src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`, `KnowledgeTab.tsx`, `CloudDisclosureModal.tsx`, root `.npmrc` and `package.json`, new `security/rules/` + `scripts/check_rule_metadata.py`, `ci.yml`, `scheduler/` job defaults (`DEFAULT_MONITOR_HASH_JOBS`). None of the five shared hot files. Verification gate: vitest posture tests green; `npm ci` on a clean checkout with the allowlist; the rulepack fires on a planted regression fixture and is silent on main.

### Open founder decisions

1. "Should the per-file isolated runner replace `pytest tests/` as the CI backend job, or run beside it until the two failure sets agree?" — Recommended default: beside it, as a second required job, until the surviving-failure list from T2 is reconciled to zero; then it replaces the single-process run and the defensive autouse resets are deleted. Rationale: the 205-failure set must be explained before the runner is allowed to hide it.
2. "Should the verify-on-stop nudge be on by default for turns that edit code, or opt-in as Hermes ships it?" — Recommended default: on, bounded to one nudge per turn (max_attempts=2), with a config switch. Hermes defaults off; Halbert's own session notes record the premature-success episodes the nudge exists for, and it never blocks a turn — the cost is one extra local model call on unverified code edits.
3. "When a test-context process resolves a production store path, should Halbert raise (fail closed, as Hermes) or silently redirect to a temp path?" — Recommended default: raise, naming the path and the bypass marker. A silent redirect hides the misconfiguration that would corrupt the audit hash chain the next time the redirect is missing.
4. "What may the diagnostics bundle contain, and where does it go?" — Recommended default: a local zip under the data dir only, never sent anywhere by Halbert; contents = redacted config, skills catalog snapshot, git commit, hardware summary, last 200 turn-event-tee entries, health findings; model tags rendered as slot labels; conversation text excluded by default with an explicit opt-in flag.
5. "Adopt a non-null Content-Security-Policy and a deny-by-default external-link policy for the Tauri webview?" — Recommended default: yes; CSP scoped to self + the local API origin + `fonts.googleapis.com`/`fonts.gstatic.com` if fonts are remote, and an `openExternal` allowlist of `https:` only. Expect the dev server to need a dev-only relaxation; the test pins the production config.
6. "npm install-script posture: blanket `ignore-scripts` with a short allowlist, or npm's `allowScripts` map?" — Recommended default: `ignore-scripts=true` in a root `.npmrc` plus an explicit allowlist of the packages that genuinely need a native build, verified against `package-lock.json` by the T6 test. Rationale: on a single host the standing denial should be the default and the exceptions the visible thing.
7. "Which engine for the security-regression rulepack, and how many rules to start?" — Recommended default: `ast-grep` (single binary, YAML rules, Python + TypeScript) with five to ten rules anchored on findings that recurred, each carrying `finding-id` and review-doc metadata; never "one rule per closed SEC-N finding".
