## other — 28 verified mechanisms from OC01, OC02, OC08, OC14, OC16, OC21, OC23, OCC01, HM01, HM08, HM13, HM14, HM17, HM18, HM19, HM20

Origin path legend: `openclaw/...` = /Volumes/Thunderbolt/AI/openclaw, `hermes/...` = /Volumes/Thunderbolt/AI/OSS/hermes-agent, `occ/...` = /Volumes/Thunderbolt/AI/OSS/open-claude-code. Halbert paths are relative to /Volumes/4TB-BAD/Halbert/halbert_core/halbert_core unless they start with `Halbert/`, `dashboard/frontend/` or `.handoff/`.

None of the 28 ids is verifier-added (no `-M<n>` ids landed in this bucket) and none is a "confirmed twice" pair in the strict sense (each mechanism was read in one origin). Where the audit pass independently found the Hermes twin of an OpenClaw mechanism, the entry says so. Nothing here appears in `.handoff/OSS-REVIEW-OPENCLAW-2026-09-07.md`, `OSS-REVIEW-HERMES-2026-09-07.md` or the master plan except OCC01-C10, whose in-memory half was lifted as C1c (`somatic/checkpoints.py:15` credits OCC by name); everything else is new to this pass.

Classification of the 28 "unplaced" items into ROADMAP rows (the section header the packet writer should file each under):

| Row | Items |
|---|---|
| CFG-1 (one config and data story) — candidate new row `BACKUP-1` | OC01-C5, OC01-C6, OC01-C17, OC16-C3 |
| TRUST-1 (redaction at the choke point) / obs | OC08-C8, OC08-C9, HM13-C10 |
| STATE-1 ("macOS says when a sensor is unavailable") | OC08-C16 |
| MIND-1 / scheduler seam | HM01-C4, OC02-C12 |
| agent turn (no row; ws terminal-tools-subagents) | OC14-C12, OC14-C10 |
| ID-1 / LD-1 (peer identity) | HM14-C20 |
| KNOW-1 (scrapers) + TRUST-1 (web search) | OC21-C6, HM08-C6 |
| SHELL-1 / SURF-1 (dashboard) | OC23-C23, HM18-C14, HM18-C6 |
| CI-1 / CLI-1 (tree and process integrity) | HM19-C11, HM13-C17 |
| LEDGER-1 (tamper-evident stores, undo) | OC08-C10, OCC01-C10 |
| VOICE-1 | HM08-C12 |
| DIST-1 (Docker track R0.9/R0.10) | HM20-C7 |
| SKILL-1 | HM20-C9, HM17-C4 |
| utils (no row) | HM17-C5 |
| no row, founder decision first | HM08-C7 |

### What Halbert has here today

Halbert never backs up its own state: `discovery/scanners/backup.py` (946 lines) and `dashboard/frontend/src/pages/Backups.tsx` only discover third-party backup tools, and the single tar producer in the tree is `persona/memory_purge.py:294-297`, a bare `tarfile.open(...,'w:gz'); tar.add(source_dir)` with no volatile filter, no vanished-file tolerance and no symlink policy (spot-checked). The logging layer is `obs/logging.py`, 30 lines of `JsonFormatter` + `get_logger`; a repo-wide grep for `logging.Filter`, `addFilter`, `setLogRecordFactory`, `QueueHandler` and `FileHandler` returns nothing, so redaction today lives only at deliberate choke points (`security/result_redaction.py`, `ingestion/redaction.py`) and every log record is synchronous stderr. Bounded execution exists three times over: `utils/retry.py:292-334 retry_with_timeout` uses process-global `SIGALRM` and has zero callers (spot-checked), `scheduler/executor.py:62-86 _call_with_timeout` is a thread-join bound that leaves the worker running, and nothing bounds an async model call. `agents/error_recovery.py:110-135 classify_error` classifies by substring on `str(error)` and never walks `__cause__`. Live vitals reach the prompt from `context/extra_adapters.py:321-356` with `load_avg = os.getloadavg()[0] if hasattr(os,'getloadavg') else 0.0` (:328) formatted straight into the model's context, and `dashboard/routes/system.py:37-79` has the same shape. Peer identity is `federation/peer_discovery.py:295 node_id = HALBERT_PERSONA_ID + '-' + socket.gethostname()`, broadcast in the mDNS TXT record. The only settings hot-reload is the personality block at `dashboard/routes/settings.py:3201-3209`; no `restart_required` concept exists. Sixty-six outbound `requests.get`/`urlopen` sites have no byte cap and no staged rename, although `os.replace` is idiomatic for local state (`scheduler/run_receipts.py:121`, `monitor_hash.py:146`, `consent/store.py:599`, `persona/store.py:103`, `model/llm_config.py:424`). The dashboard guards `localStorage` in `lib/apiBase.ts:33-55` and nowhere else; `contexts/DebugContext.tsx:37` reads it inside a `useState` initializer of a provider that wraps the whole app. `Halbert/main.py:13-15` is an insert-if-absent `sys.path` prepend. `somatic/checkpoints.py` holds the OCC checkpoint stack in memory only. Already lifted on adjacent seams (waves 1-6): redaction variant registry + echo guard, store hardening (darwin fsync, `user_version` ladder), scheduler run receipts and restart budget, execute_code host RPC, interrupt algebra, five-axis permission modules + consent ledger.

### Lift themes, ranked

#### Theme 1 — Self-backup of Halbert's own state (CFG-1; candidate row BACKUP-1)

One packet: a create-only archiver for the state directory with the three invariants below, plus the SQLite snapshot it cannot work without. This is the highest-value feature gap in the bucket for a machine that holds all of its own memory locally with no cloud sync.

**Self-backup engine: volatile-path filter + tar-EOF retry + symlink containment** (OC01-infra-approvals-devices-backup-C5) — origin `openclaw/src/infra/backup-volatile-filter.ts:15-33, 101-171`, `backup-tar-retry.ts:3-24`, `backup-archive-path-policy.ts:22-37, 52-84`, `backup-create.ts:317-356`. A `backup create` command tars the state dir while (a) skipping paths known to be live-mutated (`.sock/.pid/.tmp`, Chromium `Singleton*`, the sqlite generation-lock/reindex/backup-uuid transient pattern; `normalizePosix` collapses `..` before the ancestry check so the filter cannot be bypassed), (b) retrying only the tar EOF/grow-shrink/`TAR_BAD_ARCHIVE` error class, 3 attempts with 10 s/20 s backoff and a fresh temp path per attempt, and (c) refusing any symlink entry whose resolved target leaves the archive root or a declared asset (`normalizeArchivePath` also rejects absolute paths, backslashes and `.`/`..` segments). Invariant: an archive of a live process is complete for everything that matters, never torn by a file that was mid-write, and never a tar-slip vector on restore. Halbert: absent (verifier confirmed, "worse than the reader said" — `persona/memory_purge.py:294-297` follows and archives any symlink planted under a persona memory dir). Task: new module `backup/create.py` exposing `create_state_backup(state_dir, out_path) -> BackupResult(skipped_volatile, entries, retries)`; a `filter=` callback on `TarFile.add` implements the volatile rules against Halbert's real layout (enumerate `utils/paths.py:53 data_dir()` / `:63 state_dir()` contents first — that enumeration is the packet's first deliverable); retry wraps only `tarfile.ReadError`/`OSError` with EOF-class messages; a `_assert_symlink_contained(entry, root)` check runs before every symlink add. CLI verb under `Halbert/main.py`; optional dashboard button on `Backups.tsx` later. Tests: `tests/backup/test_create.py` pins (1) a `.pid` and a `*.tmp` are skipped and counted, (2) a symlink to `/etc/passwd` inside the state dir raises, (3) a file deleted between listing and add does not fail the archive (see C17), (4) a live sqlite with `-wal` is archived via C6, not raw. Effort L. Prerequisites: state-directory enumeration; C6. Constraint note: create-only — no restore, no migration of old backups (standing directive: no migrations, leave old data unread). Whether secret-bearing files (`being.yml peer_token`, `.env`, credential stores) go into the archive at rest is a founder call (Open decision 1); default exclude.

**Verified SQLite snapshot for live stores** (OC01-infra-approvals-devices-backup-C6, split by the verifier) — origin `openclaw/src/infra/backup-sqlite-snapshot.ts:133-247` (discovers every owned `.sqlite` under the state dir and each agent root, skips the lock dir and volatile paths, and for a symlinked canonical DB at :175-191/:220-240 raises unless it resolves to a regular file) and `backup-create.ts:364-433` (capture-twice-and-diff, 3 attempts, each in its own dir). The verifier split this candidate: the capture-twice-and-diff half exists only to survive a concurrent legacy-audit *migration*, which Halbert must not have — dropped. The half that stands is the snapshot plan: a live `.sqlite` plus `-wal/-shm` cannot be tar'd consistently, so C5 is not implementable without it. Invariant: every SQLite file in the archive is a self-consistent database image. Halbert: absent (no `VACUUM INTO`, `sqlite3_backup` or `.backup(` anywhere; spot-checked). Task: `backup/sqlite_snapshot.py: snapshot_sqlite(src, dst)` using `sqlite3.Connection.backup()` (online backup API, no lock of the writer) followed by a `PRAGMA integrity_check` on the copy and a size/page-count sanity check; the C5 filter routes `*.sqlite`/`*.db` through it and excludes `-wal/-shm`. Discovery of owned DBs is a declared list (`continuity/state_store.py`, `agents/conversation_sqlite.py`, `consent/store.py`, `attunement/store.py`, the Haloysius home) rather than a glob, and a symlinked DB that does not resolve to a regular file fails loudly. Test: write rows in one thread while snapshotting in another, open the copy, assert row count is monotone and `integrity_check == ok`. Effort M. Prerequisites: none technical; ships inside the C5 packet. Constraint note: touches `conversation_sqlite.py` only as a read-only source path, not its code.

**Vanished-file tolerance, no hardlink dedup** (OC01-infra-approvals-devices-backup-C17) — origin `openclaw/src/infra/backup-volatile-stat-cache.ts:5-29` (synthetic all-false `Stats` for a volatile path so node-tar treats a mid-backup vanish as a non-event) and `:31-41` (`BackupLinkCache` no-ops so every entry is independently restorable). Two invariants: a file that disappears mid-backup must not fail the archive; no hardlink entries. Halbert: absent (nothing to apply it to yet). Task: fold into C5 — the `filter=` callback returns `None` for a path that raised `FileNotFoundError` on `lstat` and is volatile-matched (counted in `skipped_volatile`), and the archiver never sets `dereference`/hardlink detection. Python's `tarfile` expresses both intents differently from node-tar; port the invariants, not the code. Effort S (inside C5). Priority low as recorded; kept only as two test cases in C5's suite.

#### Theme 2 — Redaction and correlation on the logging layer itself (TRUST-1 / obs)

One packet on `obs/logging.py` (an atlas hub file — run `prep_impact` first) and the root handler in `dashboard/__main__.py`. This is the seam where audit gap A03-G9 and this pass's C8 meet: the dispatcher logs the raw exception before the redacted error is returned.

**Structural redaction on every log emit path** (OC08-daemon-process-worker-hooks-audit-C8; the Hermes twin is `agent/redact*.RedactingFormatter`, cited by audit A03-G9) — origin `openclaw/src/logging/subsystem.ts:246-273` (`createConsoleLineFormatter` calls `redactSensitiveText` before colorizing), `:275-295` (`writeConsoleLine` redacts again unless the caller passes `opts.redacted` — the verifier corrected the reader: redaction is not "structurally unavoidable", the bypass is explicit and documented at :281-284 for raw writes), `:297-320` (probe-line suppression). Invariant: a secret that reaches a log call never reaches a handler in the clear, regardless of which module made the call. Halbert: absent (obs/logging.py has no redaction; zero `logging.Filter`/`addFilter` hits; spot-checked). Task — follow the verifier's implementation correction, not the reader's: do NOT add a `get_subsystem_logger(name)` factory (every module already calls `logging.getLogger(__name__)` directly — `parent_watchdog.py:24`, `dashboard/app.py:27`, `skills/telemetry.py:34` — and would never adopt one; that is opt-in redaction with extra steps). Install a `RedactingFilter(logging.Filter)` on the root handler in `dashboard/__main__.py` (and on the CLI root in `Halbert/main.py`) whose `filter()` rewrites `record.msg`/`record.args` after formatting via `security/result_redaction.redact_error_text` (the A03-G5 helper: registry pass first per A03-G2, then `ingestion.redaction.redact_text(prose=True)`); give `JsonFormatter.format()` the same pass over string fields as a second line of defence for handlers added elsewhere. Tests: `tests/obs/test_log_redaction.py` logs a registered secret value and a pattern-shaped token through `logging.getLogger('anything')` and asserts neither appears in the captured stderr; a second test asserts the MCP dispatcher's catch-all log line (A03-G9's reproduction) is redacted. Effort M. Prerequisites: A03-G5's `redact_error_text` helper (build it in this packet if the redaction packet has not). Constraint note: reinforces "redact at the response choke point" by making the choke point universal; deterministic, no model involved.

**First-character pre-check before the redaction matcher** (OC08-daemon-process-worker-hooks-audit-C9) — origin `openclaw/src/logging/secret-redaction-registry.ts:57-72` (early return when the registry is empty; lazily build the set of first characters of every registered value; `text.includes(firstChar)` scan; unchanged text when nothing could match, all before the bucketed prefix matcher is compiled at :74+). Invariant: unconditional per-record redaction stays cheap on the hot path. Halbert: absent; verifier scoping note — `redact_text` is already line-oriented and bounded (`_NESTED_JSON_WHOLE_MAX` 1 MB at :1276, depth 8 at :1279, `_B64_MAX_TOKEN_CHARS` 8192 at :1389), so the cliff is smaller than in OpenClaw. Task: a sub-bullet of C8 — `security/result_redaction` registry gets a `could_match(text)` fast path built from registered values' first characters, invalidated on every `register()`; a test pins that registering a new value invalidates the probe set. Effort S. Prerequisites: C8. Not a standalone packet.

**Process-wide correlation id via LogRecordFactory** (HM13-state-root-files-C10; narrowed by the verifier) — origin `hermes/hermes_logging.py:108-133` (`_install_session_record_factory` wraps the current factory, stamps `record.session_tag` — empty string when unset so `%(session_tag)s` never KeyErrors — idempotent via a marker attribute; also stamps the home path because the listener formats on another thread after the ContextVar is gone), `:429-440` (`_NonFormattingQueueHandler.prepare` returns `copy.copy(record)`), `:464-484` (`SimpleQueue` + root `QueueHandler` with atexit stop), `:488-502` (`flush_log_queue` vs `drain_log_queue` for hard-exit paths). Invariant: every record, including third-party and propagated ones, carries the current turn/thread id at zero per-callsite cost. Halbert: verifier corrected absent → partial and medium → low: the QueueListener half solves a problem Halbert does not have (no file handlers anywhere; stderr only, no rotation lock). Task: only the factory — in `obs/logging.py` add `install_record_factory()` reading a `ContextVar` (`turn_id`, `thread_id`, `channel`) set by the agent route / channel registry and stamping it on every record; `JsonFormatter` emits the fields when present. Drop the queue half until Halbert logs to files. Test: a record logged from a worker thread inside a turn context carries the turn id; a record outside carries empty strings, not a KeyError. Effort S (M as recorded, narrowed). Prerequisites: none; rides in the C8 packet because both touch the same 30-line hub.

#### Theme 3 — Measured, not assumed: what the process reports must be what it measured (STATE-1, CI-1, CLI-1)

Three small items sharing one invariant — the machine asserts a fact only if it measured it: the vitals in its self-report, the tree it actually imported, the module it actually loaded. One S/M packet.

**Host-stat clamping and "unavailable" instead of false zero** (OC08-daemon-process-worker-hooks-audit-C16) — origin `openclaw/src/node-host/host-stats.ts:5-7` (`clampFinite` returns 0 for non-finite and clamps to `[0,max]`), `:10-17` (every memory/load sample passes through it; freemem clamped to totalmem), `:23` (cpuCount to `[1,4096]`), `:24` (`loadAverage` omitted entirely when all-zero — an unsupported-platform reading is not reported as a real zero), `:30` (disk available clamped to total). Invariant: a first-person self-report never contains a number the host did not produce. Halbert: verifier corrected partial → absent and low → medium with a concrete defect: `context/extra_adapters.py:328` `load_avg = ... else 0.0` formatted into the prompt at :331 as `- Load average: 0.00`; nothing clamps `mem.percent`/`disk.percent`; `dashboard/routes/system.py:37-79` has the same shape (spot-checked: exact). This is the STATE-1 acceptance clause "macOS says when a sensor is unavailable" applied to load average. Task: `system/vitals.py: sample_vitals() -> Vitals` (typed, every numeric clamped finite and to its natural bound, `load_average: Optional[tuple]` = None when `os.getloadavg` is missing or all-zero, `unavailable: list[str]` naming the omitted sensors); `extra_adapters._get_live_telemetry` and `routes/system.py` both read it and render `Load average: unavailable on this platform` rather than a number. Test: monkeypatch `os` without `getloadavg` and assert the rendered telemetry block contains no `0.00` and lists the sensor as unavailable; a NaN/inf psutil value renders as unavailable. Effort S. Prerequisites: none. Constraint note: "grounded in measured data" standing directive; this is the machine claiming a measurement it did not take.

**Foreign-module assertion for worktree runs, then a navigability bench** (HM19-evals-tests-meta-C11; re-ordered by the verifier) — origin `hermes/evals/codebase_navigability/runtime_bench.py:14` (child pinned with `PYTHONPATH=TREE`), `:53-54` (`foreign = [m for m,mod in sys.modules.items() if mod.__file__ and 'hermes-agent' in mod.__file__ and not mod.__file__.startswith(tree) and 'site-packages' not in mod.__file__]`, flagged as `FOREIGN_MODULES`), plus `README.md:1-45`, `bench.py:9-16` (~19k real locate-X tasks resolved through re-exports) and `lookup_sim.py:18-23` (simulated `grep -n` + 200-line `read_file` + 2000-line paging policy charged in tool calls and tokens over a paired sample). Invariant: the process under test imported the tree it was pointed at; and a refactor is measured by what it costs an agent to navigate, not only CPU. Halbert: partial (SourcePrep gives hub files and 30 circular deps but no agent lookup cost; no bench equivalent). Verifier raised low → medium because the two-line foreign-module assertion is exactly Halbert's most expensive recorded footgun (memory `halbert-worktree-venv-gotchas`: the venv's editable install silently resolves `halbert_core` to the MAIN tree from a worktree; the current mitigation is a hand-rolled meta-path-stripping wrapper). Task, in that order: (1) `tests/conftest.py` session-start hook that computes the foreign set against `Path(__file__).parents[1]` and fails the session with the offending module list — this replaces the wrapper for pytest runs; a `scripts/assert_tree.py` for ad-hoc runs; (2) optional, tied to the next large decomposition: `scripts/nav_bench.py` sampling real import statements from `halbert_core/tests`, resolving through SourcePrep's atlas, reporting tokens-to-defining-file before/after. Test for (1): run pytest from a worktree with the editable install pointing at main and assert the session aborts naming `halbert_core`. Effort S for (1), M for (2). Prerequisites: none.

**sys.path hardening in the CLI entry** (HM13-state-root-files-C17) — origin `hermes/hermes_bootstrap.py:77-96` (`harden_import_path`: strip `''`/`'.'` entries, remove any entry whose abspath equals the root, `insert(0, root)` — relocate, not insert-if-absent; docstring :78-86 gives the two launch forms it covers). Invariant: a same-named module in the launch directory cannot shadow the first-party package. Halbert: absent (`Halbert/main.py:13-15` insert-if-absent; spot-checked). Priority low: Halbert's importable surface is the namespaced `halbert_core.halbert_core`, far less shadowable than Hermes's bare `utils`. Task: three lines in `Halbert/main.py` (and `dashboard/__main__.py` if it has its own prepend), plus a unit test that builds a fake `sys.path` list and asserts the result. The packet must state that this is adjacent to but does NOT fix the namespace-package resolution gotcha (memory `halbert-core-namespace-import-gotcha`) — that one is (1) above. Effort S. Prerequisites: none.

#### Theme 4 — One bounded-execution helper and typed error outcomes (MIND-1 / agent turn)

One packet on `scheduler/executor.py` (shared hot file), `utils/retry.py` and `agents/error_recovery.py`. It closes the confirmed A06 bug (timeout leaves the worker running with live side effects) and gives `execute_with_retry` a classification that survives exception wrapping.

**Unify the timeout mechanisms; delete the SIGALRM path** (HM01-agent-turn-family-C4; priority high → medium per verifier) — origin `hermes/agent/deadline.py:1-13` (exports), `:36-37` (`MAX_SAFE_TIMEOUT_S = 31_536_000.0`, macOS `time_t` rationale), `:98-110` (`clamp_timeout` normalises None/NaN/negative/overflow uniformly), `:213-248` (`run_bounded_async` driven by a daemon `threading.Timer` so a wedged event loop cannot disable its own deadline, with a second timer that `faulthandler.dump_traceback(all_threads=True)` when the loop is blocked), `:282-304` (`run_bounded_sync` under `contextvars.copy_context()`), `:342` (`kill_process_tree` with psutil handling). Invariant: one clamp, one deadline primitive, a deadline that fires even when the thing it bounds has wedged the loop. Halbert: partial — `utils/retry.py:315-324` `signal.signal(SIGALRM)+signal.alarm()` is process-global and main-thread-only, and `scheduler/executor.py:62-86 _call_with_timeout` is a thread-join bound with no async flavour and no clamp. The verifier's correction must reach the founder as written: `retry_with_timeout` has ZERO callers (spot-checked — only its own `def` at :292), so the "live concurrency race" framing is wrong; this is "delete dead unsafe code, keep one bounded-execution helper". Task: `utils/deadline.py` with `clamp_timeout`, `run_bounded_sync`, `run_bounded_async`, `kill_process_tree`; migrate `executor._call_with_timeout` to it, and on timeout record `status='timeout'` on the run receipt (not `'error'`) and mark the worker abandoned so the retry decorator does not start a second copy (A06 own-bug); delete `retry_with_timeout`. Tests: `tests/utils/test_deadline.py` pins the clamp table, a blocked-loop async call returns `TimeoutError` at the deadline, and `tests/scheduler/test_executor_timeout.py` pins that a timed-out job's receipt says `timeout` and the retry path does not re-dispatch. Effort M. Prerequisites: none. Hot file: `executor.py`.

**Typed outcome through generic catch boundaries via the cause chain** (OC14-agents-second-pass-A-C12; priority low → medium per verifier) — origin `openclaw/src/agents/agent-run-terminal-error.ts:5-13` (`AgentRunTerminalOutcomeError` carries a typed `terminalOutcome` and passes the original as `cause`), `:16-26` (`findAgentRunTerminalOutcome` walks `candidate.cause` with a `Set` cycle guard). Invariant: a typed classification survives any number of generic `raise X from e` wrappers. Halbert: absent, with a concrete matching defect — `agents/error_recovery.py:110-135 classify_error` substring-matches `str(error)` and `type(error).__name__` and never consults `__cause__` (spot-checked); there are 29 `raise ... from e` sites across the core, so a wrapped `TimeoutError` or `ConnectionError` falls to `ErrorType.UNKNOWN` and gets the wrong backoff. Task: add `ClassifiedError(Exception)` carrying `error_type`, and `_walk_causes(exc) -> Iterator[BaseException]` (`__cause__` then `__context__`, seen-set); `classify_error` first returns a `ClassifiedError.error_type` found anywhere in the chain, then applies the existing substring ladder to every exception in the chain, not only the outermost. Blast radius bounded to `execute_with_retry` (:151-204). Test: `RuntimeError('wrapped') from asyncio.TimeoutError()` classifies as `LLM_TIMEOUT`; a self-referential cause chain terminates. Effort S. Prerequisites: none.

**Fatal / transient / config-error taxonomy for the top-level exception handler** (OC02-infra-rest-C12; design note, low) — origin `openclaw/src/infra/unhandled-rejections.ts:42-48` (`FATAL_ERROR_CODES`: OOM, script timeout, worker crash), `:50-56` (`CONFIG_ERROR_CODES` + `EXIT_CONFIG_ERROR = 78`, sysexits `EX_CONFIG`, used at :406), `:58-65` (`TRANSIENT_SQLITE_CODES` {BUSY, CANTOPEN, IOERR, LOCKED} / errcodes {5,6,10,14}), `:67-82` (benign `EPIPE`/`EIO` and the network set), `:19-39` (handlers registered on a `Symbol.for` global so a double module copy shares one registry). Invariant: an escaped exception is classified before the process decides to die, log, or exit with a scriptable code. Halbert: absent — no `sys.excepthook`, `loop.set_exception_handler` or `threading.excepthook` anywhere (spot-checked), so an exception escaping a background task or thread is a default traceback with no classification; SQLite contention is handled locally (`continuity/state_store.py:308-311, 398-410`, `agents/conversation_sqlite.py:516-530`, `attunement/store.py:110`). Task: not dispatchable alone — the reader's prerequisite (a top-level handler) does not exist. When the deadline packet installs `loop.set_exception_handler` in `dashboard/app.py`'s lifespan, give it this taxonomy with `sqlite3.OperationalError` busy/locked as log-and-continue and a config error as exit 78. Carry the `EX_CONFIG=78` convention regardless. Effort M when the handler exists. Hot file: `dashboard/app.py`.

#### Theme 5 — Atomic, durable files: a minted install identity and bounded network fetches (ID-1 / LD-1, KNOW-1, TRUST-1)

Two items that share the tmp + fsync + `os.replace` + directory-fsync recipe Halbert already uses for local state but not for identity or for anything fetched from the network. Pairs naturally with the confirmed A06 ledger fsync bug and A12-G8.

**Stable, atomically minted per-install identity** (HM14-cli-ops-lifecycle-C20; priority low → medium per verifier) — origin `hermes/hermes_cli/install_identity.py:77-110` (`read_or_create_install_id`: re-read under the lock before minting at :86-88; `mkstemp` + `fsync` + `os.replace` + `_fsync_directory` at :94-104; in-process lock plus cross-process file lock with the Windows byte-range rationale at :84-85; returns `None` rather than an ephemeral id when nothing is persistable). Invariant: exactly one opaque id per install, minted once, surviving a rename of the machine. Halbert: absent; the verifier found what the reader lacked — `federation/peer_discovery.py:295` builds `node_id` from `HALBERT_PERSONA_ID + '-' + socket.gethostname()` and returns it at :316 for the mDNS TXT record (spot-checked), so the LAN-broadcast peer identity is hostname-derived and changes on rename, while the comment at :296-298 is careful that the display name is "never the raw hostname"; `federation/telemetry_agent.py:98-107` has the same anchor. Task: `identity/install_id.py: read_or_create_install_id(state_dir) -> Optional[str]` with the same recipe (fcntl lock file next to it; thread lock; 32-hex uuid4); `peer_discovery.py` uses it for `node_id` (display name unchanged); `telemetry_agent.py` likewise. Old hostname-derived ids are left unread — no migration, no alias table. Tests: two processes racing first mint produce one file with one id (multiprocessing test), and a simulated crash between `mkstemp` and `os.replace` leaves no id (the next call mints). Effort S. Prerequisites: none. Constraint note: "never the raw hostname" (standing directive 2026-08-25) and `C1-02` (hostname is a body fact, not the entity) — this removes a raw-hostname broadcast; "no migrations" — old peers re-pair, they are not remapped. Founder decision 4 covers whether existing pairings are allowed to break.

**Bounded, atomic, single-flight download for large fetched files** (OC21-extensions-device-media-desktop-C6; single-flight coalescing also appears in Hermes `tools/web_result_cache.py:118-133` and `tui_gateway/git_probe.py:42-43`) — origin `openclaw/extensions/geolocation/src/database-store.ts:20-43` (`readBoundedBody` streams via `getReader()` and throws once the running total passes the cap — "rejected mid-flight instead of after it has already been allocated in full"), `:48` + `:92-94` (`MAX_COMPRESSED_BYTES`, `gunzipAsync(..., {maxOutputLength})` bomb guard), `:95-103` (parse-before-publish, write to `${target}.partial`, `rename` into place), `:133-141` (refresh failure logs and keeps serving the cached copy), `:151-158` (`inFlight ??= openFromDisk().finally(...)`). Invariant: no network response can allocate more than a declared bound, and a cached file on disk is always a complete one. Halbert: absent — `rag/scrapers/homebrew.py:76 requests.get(url, timeout=...)`, and the verifier's broader check: 66 outbound `requests.get`/`urlopen` sites, zero `stream=True`/`iter_content`/`Content-Length` handling outbound (the only hit is `mcp/server.py:1571-1581`, inbound); runtime relevance beyond build-time scrapers: `tools/web_search.py:100-103, 187-190` fetch third-party search endpoints with only an aiohttp total timeout and no response size bound. Task: `utils/bounded_download.py: download(url, dest, max_bytes, max_decompressed=None) -> Path` (streamed cap via `iter_content`, `.partial` + `os.replace`, per-dest single-flight lock, stale-serves on refresh failure) and an async `read_bounded(resp, max_bytes)` for aiohttp; adopt in `homebrew.py` first, then `web_search.py`'s two fetches with a modest cap (search JSON, e.g. 2 MiB). Tests: a fake server sending `max_bytes+1` raises before the body is held; a crash mid-write leaves only `.partial`; two concurrent callers produce one fetch. Effort M. Prerequisites: none. Constraint note: web search is a switch, off by default (`C3-08`) — bounding its responses is TRUST-1 work, not a new capability.

#### Theme 6 — Settings reload plan and dashboard storage hygiene (CFG-1, SHELL-1)

One dashboard-owned packet: a declarative reload classifier on the backend settings route and two small frontend/route hygiene items.

**Declarative settings-path → reload-action classifier** (OC16-gateway-second-pass-A-C3; priority high → medium per verifier) — origin `openclaw/src/gateway/config-reload-plan.ts:20-39` (`GatewayReloadPlan`), `:41-62` (`RELOAD_ACTIONS` + `isNoopGatewayReloadPlan`), `:135-200` (the declarative table itself — the verifier corrects the citation: `18-90` misses the table; auth token/password, `mcp.apps`, `secrets.egressProxy`, `plugins.*` = restart; a long hot list; per-prefix action lists), `config-reload-recovery.ts:25-31` (`PROVIDER_AUTH_RELEVANT_CONFIG_ROOTS` {auth, env, models, plugins, secrets}), `:32-46` (per-agent-subfield allowlist), `:48-69` (heartbeat/subagents/compaction only matter when the changed leaf is a model/provider selector); plus one mechanism the reader read but did not report, `config-diff.ts:53-77` (`diffGatewayReloadPaths` also diffs a projected boundary view because an effective owner can change with no authored key change). Invariant: a settings save produces the minimal correct recovery — hot action, restart-required flag, or nothing — never a blanket restart or an unsafe silent hot-patch. Halbert: absent (only the personality hot-reload at `dashboard/routes/settings.py:3201-3209`; spot-checked); and `halbert_core/config/` is about the HOST's config files (manifest/snapshot/drift/watcher), so the new module belongs under `dashboard/` or `settings/`, not `config/`. Halbert needs ~6 of OpenClaw's ~65 prefixes. Task: `dashboard/settings_reload_plan.py: plan_reload(changed_paths) -> ReloadPlan(restart_required: bool, hot_actions: set[str], reasons)` over a table like {`personality.*` → hot `reload_personality`; `models.slots.*`, `models.secure_model` → hot `rebuild_router`; `dashboard.bind`, `dashboard.token`, `mcp.servers.*` → restart; `web_search.enabled` → hot `toggle_web_search`}, plus a projected-view diff: compute the resolved model per slot before and after the save (the `models.yml` analogue of `config-diff.ts`), and treat a resolved change as a `models.slots` change even when the authored key is unchanged. The settings route returns the plan to the UI, which shows "restart required" instead of pretending. Test: table-driven, one row per prefix, plus the projected-diff case. Effort M. Prerequisites: none. Constraint note: no model names on any surface — the plan reports slot names, never resolved model ids.

**Guarded localStorage accessor** (OC23-control-ui-C23; priority low → medium per verifier) — origin `openclaw/ui/src/local-storage.ts:1-35` (`isStorage` duck-check, `window[name]` access wrapped in try/catch returning null, descriptor path `descriptor && !descriptor.get` to refuse a test harness's getter; a VITEST branch Halbert does not need). Invariant: storage unavailability degrades one feature, never the mount. Halbert: partial, with the exact split verified — guarded: `lib/apiBase.ts:33-41, 50-55`; unguarded: `contexts/DebugContext.tsx:37, 48`, `contexts/ShellModeContext.tsx:67, 109, 123, 142, 161`, `lib/peerApi.ts:146, 151, 156`, `pages/Services.tsx:153-164`, `pages/Storage.tsx:154-164`, `pages/Network.tsx:155-166`, `components/legal/CloudDisclosureModal.tsx:65`. Raised because `DebugContext.tsx:37` is inside a `useState` initializer of a provider that wraps the app (spot-checked), so a throwing `localStorage` (Tauri webview with storage disabled, private mode) takes the whole dashboard down at mount. Task: `dashboard/frontend/src/lib/safeStorage.ts: getSafeLocalStorage(): Storage | null` plus `readKey/writeKey` helpers; migrate the eleven sites; `apiBase.ts` keeps its own guard or adopts the helper. Test (vitest): a `localStorage` getter that throws leaves `DebugProvider` mounting with debug off. Effort S. Prerequisites: none.

**Single-flight + negative-TTL cache for repeated subprocess probes** (HM18-tui-gateway-ui-desktop-docs-C14) — origin `hermes/tui_gateway/git_probe.py:1-5` (design docstring), `:19` (`_NEG_TTL = 30.0`, "a fresh `git init` shows within seconds"), `:42-43` (class docstring: positives live for the process, negatives for the TTL, followers wait on the leader), `:82`, `:96/:101` (`repo_root`/`common_repo_root` cached entry points), `:131` (`warm_roots`). The verifier corrects the attribution: `run_git` itself (:21-34) is uncached; the cache is one layer up. Invariant: neither stale-forever caching nor a subprocess-spawn storm from a route handler. Halbert: absent — `dashboard/routes/development.py:30 subprocess.run(...)` with no memoization. Priority low; the verifier's guidance stands: pick it up only if `development.py` is touched for another reason, do not dispatch alone. Task: a ~30-line `utils/probe_cache.py` and its use in `development.py`; test pins that a negative result is re-probed after the TTL and a positive is not. Effort S. Prerequisites: none.

#### Theme 7 — Ledger-adjacent design tasks (LEDGER-1; engine-side implications)

Two items that are design work, not ports: one because the origin mechanism conflicts with a property Halbert deliberately built, one because the naive port re-opens a security finding already on record.

**Bounded audit log without losing tamper-evidence** (OC08-daemon-process-worker-hooks-audit-C10) — origin `openclaw/src/audit/audit-event-store.ts:49-51` (`AUDIT_EVENT_RETENTION_MS` = 30 d, `AUDIT_EVENT_MAX_ROWS` = 100 000, `AUDIT_EVENT_PRUNE_BATCH_ROWS` = 1 024), `:52-54` (WeakMap row-count cache invalidated only on out-of-band maintenance so the hot path avoids a 100k-row scan per message). Invariant in the origin: the audit store never grows unboundedly. Halbert: absent, and the mechanisms CONFLICT — `obs/audit.py:6-16` says the per-file chain was replaced by `haloysius.integrity.EventLog` (:158) precisely because "deleting a day's file, or trimming records off the end of one, left a log that still verified perfectly"; a batch prune reintroduces the failure the module exists to catch. Task: a DESIGN packet, not a port — checkpoint-and-archive: verify the chain, write an anchored checkpoint hash, archive the segment (into the backup of Theme 1), start a new chain anchored to the checkpoint, preserving cross-boundary verifiability; align the window with the `CD-5` 90-day timeline retention already decided. Engine-side implication the verifier flagged: the bound belongs in Haloysius's `EventLog`, so this is a Halbert-side proposal to the Haloysius repo, not Halbert code. Effort L (design + engine proposal). Prerequisites: founder/design review (Open decision 3); `INTEG-05` wording ("no tampering detected since this log began") must stay true across the checkpoint boundary. Constraint note: memory engine = external Haloysius, Halbert-side proposals only.

**Disk-spilled checkpoints and a founder-facing undo** (OCC01-v2-second-look-C10; extends-lifted; present) — origin `occ/v2/src/core/checkpoints.mjs:29-65` (`save()` writes each checkpoint as its own JSON file — id, filePath, relativePath, content, timestamp, size — bounded LIFO with disk eviction past `maxCheckpoints=50`), `:71-92` (`undo()` pops the newest, rewrites the original, unlinks the file), and the user-facing verb the reader missed, `occ/v2/src/ui/commands.mjs:413-421` (`/undo — Undo last file edit`). Invariant: undo survives a crash mid-proposal. Halbert: present — `somatic/checkpoints.py:15` credits OCC by name, `:30-31 self._stacks: Dict[str, List[Optional[bytes]]]` purely in-process, FIFO trim at :48-49, no file write, no dashboard action calls `rollback()` (spot-checked). Two cautions the packet must carry (verifier): (1) spilling checkpoint CONTENT writes the bytes of exactly the files that should not be read freely (a checkpointed `/etc/wireguard/wg0.conf`, an `~/.aws/credentials` edit); `.handoff/SECURITY-AUDIT-FINDINGS-2026-09-06.md:340` already records this failure mode for `write_config`'s backup directory (0644 backups of escalated plaintext) — any spill must be 0600 inside the governed config dir with a retention bound, or it re-opens that finding; (2) it overlaps `write_config`'s per-write `.bak` and the continuity digest ledger behind `continuity/write_guard.py`, so the packet must say why a third store is warranted rather than extending one of those. Task, only if Open decision 5 says build: `somatic/checkpoints.py` gains `spill_dir: Optional[Path]`; each checkpoint written 0600 as `<sha256(path)>-<n>.bin` with a bounded count; `rollback()` reads from disk when the in-memory stack is empty; a dashboard action "undo last change" staged through the existing approvals path (UI commands are staged, never executed). Test: kill the process between `checkpoint()` and `rollback()` and assert the file is restored on the next call; assert mode 0600. Effort S. Priority low as recorded. Constraint note: LEDGER-1 wants digests before/after, not content — a content spill is a different artefact from the ledger and must not be confused with it.

#### Theme 8 — Filed against future features (do not dispatch now)

Kept because the mechanism is verified and the future feature is on the roadmap; each is prerequisite-shaped and should be attached to that feature's packet when it opens.

**Subprocess isolation for occasional heavy local models** (HM08-tools-browser-web-media-voice-C12; VOICE-1) — origin `hermes/tools/neutts_synth.py:1-8` ("called by tts_tool via subprocess so the ~500 MB TTS model lives in a process that exits after synthesis"; literal `python -m tools.neutts_synth --text ... --out ...`), `:16-20` (no-dependency struct WAV writer), `:48-70` (load, infer, write, device-string comment). Invariant: a model used once an hour does not stay resident in the daemon. Halbert: absent — sherpa ASR and openWakeWord load in-process and stay resident; the only unload surface is the LLM provider API (`model/providers/base.py:98-106`, `llamacpp.py:72`, `router.py:569/603`), which does not reach audio models. Task: apply to the next occasional-use heavy model (larger STT/TTS voice, OCR); for the models already resident the better remedy is an idle-unload watcher, which lives in the voice workstream's verifier-added list, not here. Effort S when it applies. Priority low.

**Attachment staging: magic-byte sniff, hard caps, workspace-vs-outside copy-in** (HM18-tui-gateway-ui-desktop-docs-C6; SHELL-1 file drop; verifier corrected partial → absent) — origin `hermes/tui_gateway/prompt_attachments.py:16-18` (25 MB image / 50 MB PDF / 25 pages), `:20-23` (`_IMAGE_MAGIC` PNG/JPEG/GIF87a/GIF89a/BMP for filename-less uploads), `:25-26` (ref-quoting regex), `:111-127` (`_format_ref_value`, `_attachment_ref_path`), `:136-180` (`_stage_session_file_attachment` three-way split — in-workspace as-is, on-gateway-outside copy-in, off-gateway base64 decode-in; collision-safe naming; the load-bearing comment that `attachments/` is bind-mounted into container backends so `@file:` resolves in the sandbox). Invariant: anything dropped into chat is size-capped, type-sniffed and inside the boundary before a tool can see it. Halbert: absent — `dashboard/routes/vision.py:7` "returned as base64 JPEG. Nothing is stored to disk" is a camera path sharing no mechanism; no `UploadFile`/multipart route exists under `dashboard/routes/`. Task: file against the chat file-drop feature when it opens; the sniff table, caps and copy-in split lift directly. Effort S then. Priority low. Constraint note: the redaction variant registry and camera gate already lifted must run on the staged file's path and content.

**Docker diagnose-before-destroy discipline and Dockerfile hardening checklist** (HM20-skill-library-C7; DIST-1 Docker track R0.9/R0.10) — origin `hermes/optional-skills/devops/docker-management/SKILL.md:227` ("always start with a diagnostic before cleaning"), `:229-244` (targeted-vs-aggressive prune ladder), `:246` (warn gate on `docker system prune -a --volumes`), `:261-270` (verification checklist), `:272-282` (7-item Dockerfile list: multi-stage, layer order, combined RUN, `.dockerignore`, pinned base, non-root USER, slim base). Halbert: absent — the nine builtin skills are config/discovery/frigate/home/network/security/service/storage-ops plus the `understated` lens. Task: seed a `docker-ops` builtin SKILL.md when the Docker track lands (ghcr.io/ericbintner/halbert-core); the two rules that matter for this product (non-root USER, pinned base) go into the Dockerfile review regardless. Effort S. Priority low; do not dispatch ahead of the track.

### Related confirmed audit gaps

- A03-redaction-G9 [confirmed/low] — log lines are not redacted; the MCP dispatcher logs the raw exception before returning the redacted error. Fix: `JsonFormatter.format()` applies `redact_error_text` to the rendered message and string fields; Hermes's `RedactingFormatter` is the twin. Same seam as OC08-C8/C9 — one packet.
- A03-redaction-G5 [confirmed/medium] — dispatcher catch-all redacts with patterns only, registry skipped; introduces `security/result_redaction.redact_error_text(msg)`. OC08-C8's filter must call this helper, not `redact_text` alone.
- A03-redaction-G2 [confirmed/medium] — registry pass runs after the pattern pass, so a pattern rewrite inside a registered value leaks the remainder; order must be registry first. Governs the order inside the logging filter.
- A06-scheduler own-bug [confirmed] — after a wall-clock timeout the task thread keeps running with live side effects while the receipt says `error` and the retry decorator starts a second copy (`executor.py:62-86`, "the task thread is a daemon and is left to finish on its own"). Closed by HM01-C4's migration.
- A04-execute-code-G1 [confirmed/high] — the monitor loop has no deadline when the worker survives KeyboardInterrupt injection. A consumer of `utils/deadline.py` once it exists.
- A02-eval-harness-G7 [confirmed/low] — no timeout around the judge call; a hung local model stalls the turn. Second consumer of `run_bounded_async`.
- A06-scheduler own-bug [confirmed] — `_persist_restart_ledger` keeps future-dated stamps and fsyncs the file but not its directory; `_load_restart_ledger` silently drops a malformed ledger (`executor.py:299-306`). Same atomic-write recipe as HM14-C20; fix in the Theme 5 packet.
- A12-claims-admission-G8 [confirmed/low] — `guest_homes.yml` renamed into place without fsync. Same recipe; same packet.
- A06-scheduler-G5 [confirmed/medium] — `receipts.json` has no retention and every write rewrites and fsyncs the whole file (Hermes `cron/executions.py:27 MAX_TERMINAL_EXECUTIONS = 1000`, `:128-136` prunes only terminal rows). Retention on a non-tamper-evident store — the easy case; contrast with OC08-C10, where the bound must not break the chain.
- A01-promotion own-bug [confirmed] — the promotion store singleton pins the data dir at first use, unlike `continuity/recall.py:48-51` ("never cache one: the path is resolved at call time so `HALBERT_DATA_DIR` is honoured per call"). The reason HM17-C5's `SingletonSlot` is the wrong primitive for Halbert stores: CFG-1 wants per-call path resolution, not a first-call-wins singleton.
- A05-echo-display-digest-G3 [confirmed/low] — display seam runs the exact-value registry only, no forced pattern pass. Same registry-then-pattern composition the logging filter needs.

### Do not lift

- **HM08-tools-browser-web-media-voice-C7** (CDP browser supervisor, dialog watchdogs, three-layer SSRF net; `hermes/tools/browser_supervisor.py:36-46, 328-364`, `browser_supervisor_dialogs.py:46-107, 174-201`, `browser_tool_eval_policy.py:15-24, 31-41, 108-151` — three of the reader's ranges were wrong addresses, corrected by the verifier). Not applicable: no browsing surface exists and `grep browser|playwright|puppeteer|cdp` over ROADMAP.md and DECISIONS.md returns zero lines. Keep as a blueprint (the CDP-URL redaction discipline and the layered SSRF guard) if Open decision 6 ever says yes.
- **HM08-tools-browser-web-media-voice-C6** (TTL + single-flight disk cache for web search; `hermes/tools/web_result_cache.py:78-137, 160-281`). Demoted medium → low by the verifier and to do-not-lift here: backends are DuckDuckGo and self-hosted SearXNG (`tools/web_search.py:6-8`) — unpaid, un-rate-limited, one user, no subagent fan-out — so the coalescer defends a cost that does not exist on this host. If it is ever built, carry Hermes's invariant at `web_tools_rescue.py:1-7` ("callers must never cache a rescue-served response") — Halbert's DuckDuckGo → SearXNG fallback has the same shape.
- **HM17-plugins-acp-providers-C4** (per-extension storage convention; `hermes/plugins/plugin_storage.py:1-44`). No consumer: Halbert has no third-party Python extension ecosystem (its "plugins" are SKILL.md text), and `utils/paths.py:53 data_dir()` / `:63 state_dir()` and `utils/platform.py:343` already exist, leaving only a name regex and a WAL one-liner. Revisit only if a Python extension surface is decided.
- **HM17-plugins-acp-providers-C5** (thread-safe lazy-singleton slot; `hermes/plugins/plugin_utils.py:19-64`). Demoted here: the verifier found exactly one naive site, `storage/chromadb_manager.py:938 if _manager is None` (spot-checked) plus a module global at `model/outcome_store.py:224`; `mcp/client.py` (asyncio.Lock at :551, per-server locks at :897-902/:986) and `model/client.py` (file advisory lock :102-116) do not have the bug; DECISIONS.md 2026-08-23 confines ChromaDB to eval only. And per the A01 singleton-pin bug above, Halbert's stores want per-call path resolution, not first-call-wins. Fix the chromadb site with a `threading.Lock` when that file is next touched.
- **OC14-agents-second-pass-A-C10** (rank-table precedence for merging concurrent terminal observations; `openclaw/src/agents/agent-run-terminal-outcome.ts:94-119, 205-310`). Demoted medium → low by the verifier and to do-not-lift here: `agents/conversation_status.py:25-45` already guards transitions with an explicit allowed-transition table, and a Halbert turn has one owner with no attempt-level runner producing two independent terminal observations, so the algebra degenerates. Reference only if compaction/timeout/abort ever race (A16-G6 is where that would surface).
- **HM20-skill-library-C9** (interview-the-send questionnaire skill; `hermes/optional-skills/productivity/decision-questionnaire/SKILL.md:30-52`). Demoted here: on a single-user host the founder is both the user and the person who would carry the questionnaire, and the do-not-use gate at :30-31 ("when the answer is discoverable from the environment, find it yourself first") is already Halbert's default posture. The one reusable line — write the draft to a file and report its absolute path — belongs in any skill that produces a document.

### Proposed packets

1. **OTHER-P1 State backup, create-only** — items OC01-C5, OC01-C6 (snapshot half only), OC01-C17; related A06-G5 (receipt retention, so the archive is not bloated by unbounded receipts). Prerequisites: Open decision 1 (secrets at rest) and 2 (create-only scope); CFG-1's one path resolver (the archiver must read `state_dir()` at call time); an enumeration of the real state layout as the first deliverable. Hot files: none of the five shared hot files are edited — `conversation_sqlite.py`'s database is a snapshot *source* only; new `backup/create.py`, `backup/sqlite_snapshot.py`, a verb in `Halbert/main.py`, optional `Backups.tsx` button later. Founder decision gates it: yes (decision 1).
2. **OTHER-P2 Redacting, correlated logging** — items OC08-C8, OC08-C9, HM13-C10 (factory half); closes A03-G9, uses A03-G5's helper in A03-G2's order. Prerequisites: none beyond `prep_impact` on `obs/logging.py` (hub file); coordinate with the permissions-consent-security section's redaction packet if it also builds `redact_error_text` — one owner. Hot files: `dashboard/app.py` only if the root logger is configured there rather than in `dashboard/__main__.py`; `routes/agent.py` sets the turn ContextVar for correlation. Founder decision gates it: no (decision 7 is a default the packet can assume).
3. **OTHER-P3 Measured, not assumed** — items OC08-C16, HM19-C11 (foreign-module assertion first; bench optional), HM13-C17. Prerequisites: none; STATE-1's "sensor unavailable" acceptance clause is the spec for C16. Hot files: none of the shared five; `context/extra_adapters.py`, `dashboard/routes/system.py`, `tests/conftest.py`, `Halbert/main.py`. Founder decision gates it: no.
4. **OTHER-P4 One bounded-execution helper, typed error outcomes** — items HM01-C4, OC14-C12, OC02-C12 (design note only; the taxonomy lands when the handler does); closes the A06 timeout own-bug, and is the primitive A04-G1 and A02-G7 consume. Prerequisites: none. Hot files: `executor.py` (shared hot file — migration of `_call_with_timeout` and the receipt status), `dashboard/app.py` if the packet installs `loop.set_exception_handler`; `state_machine.py` untouched. Founder decision gates it: no. The packet must carry the verifier's correction that `retry_with_timeout` has zero callers — this is deletion of dead unsafe code, not a live race.
5. **OTHER-P5 Atomic durable files and a minted identity** — items HM14-C20, OC21-C6; closes the A06 ledger fsync own-bug and A12-G8 with the same recipe. Prerequisites: Open decision 4 (existing pairings may break; old node_ids left unread). Hot files: `executor.py` (the restart-ledger fsync fix — coordinate with P4 so the two edits land in one branch or in sequence); `tools/web_search.py`, `federation/peer_discovery.py`, `federation/telemetry_agent.py`, `rag/scrapers/homebrew.py`. Founder decision gates it: partly (decision 4 gates the node_id switch; the rest can proceed).
6. **OTHER-P6 Settings reload plan and dashboard storage hygiene** — items OC16-C3, OC23-C23, HM18-C14 (opportunistic, only if `development.py` is open anyway). Prerequisites: none; the reload table must name slots, never models (standing directive). Hot files: `dashboard/routes/settings.py` (3,200+ lines, not in the shared five but heavily edited by concurrent sessions — rebase often); `dashboard/app.py` if the "restart required" flag is surfaced through the lifespan; frontend contexts and `lib/`. Founder decision gates it: no.

Not packetised: OC08-C10 (a Halbert-side design proposal to the Haloysius repo, gated by decision 3), OCC01-C10 (gated by decision 5; S if approved), and the three Theme 8 items (attach to VOICE-1, the chat file-drop feature, and the Docker track respectively).

### Open founder decisions

1. "Does `halbert backup create` include secret-bearing files (`being.yml peer_token`, `.env`, credential stores) in the archive at rest?" — Recommended default: exclude them by a declared deny list that reuses the Tier-2 secret paths, report the count of excluded files in the result, and defer encryption entirely; an `--include-secrets` flag only after an encryption decision exists.
2. "Is v1 of self-backup create-only, with restore deferred?" — Recommended default: yes; restore implies reading old archives, which is where the no-migration directive bites, so decide it separately when the archive format has shipped.
3. "May the audit log be bounded by a checkpoint-and-archive scheme (chain verified, anchored checkpoint hash, segment archived, new chain anchored to the checkpoint), implemented in Haloysius's EventLog, with the window aligned to the CD-5 90-day retention?" — Recommended default: yes as a design proposal to Haloysius; Halbert ships no prune of its own, ever, and the INTEG-05 copy stays "since this log began" until the engine supports the boundary.
4. "May the mDNS `node_id` switch from `persona-hostname` to a minted opaque install id, accepting that already-paired peers must re-pair and old ids are left unread?" — Recommended default: yes; there are no users, and the current value broadcasts the raw hostname that the naming directive keeps off every surface.
5. "Should somatic checkpoints spill content to disk (0600, bounded) and expose a staged 'undo last change' action, or is `write_config`'s `.bak` plus the continuity digest ledger enough?" — Recommended default: do not build a third store; add the staged undo action over the existing `rollback()` only if the in-memory stack proves insufficient in practice.
6. "Is browser automation (a CDP-driven browsing tool) ever in scope for Halbert?" — Recommended default: no; HM08-C7 stays a blueprint.
7. "Is a redaction pass on every log record (with the first-character fast path) acceptable as an unconditional cost?" — Recommended default: yes; the pass is deterministic, bounded, and the alternative is the A03-G9 leak class recurring at every new call site.
