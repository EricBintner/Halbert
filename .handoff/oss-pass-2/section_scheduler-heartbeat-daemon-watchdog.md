## scheduler-heartbeat-daemon-watchdog — 83 verified mechanisms from HM01, HM02, HM04, HM05, HM06, HM07, HM10, HM11, HM13, HM14, HM15, HM16, HM19, HM20, OC02, OC04, OC06, OC08, OC09, OC14, OC15, OC16, OC17, OC18, OC20, OC22, OC23, OC24

Origin repos: Hermes at `/Volumes/Thunderbolt/AI/OSS/hermes-agent`, OpenClaw at `/Volumes/Thunderbolt/AI/openclaw`. Halbert paths below are relative to `/Volumes/4TB-BAD/Halbert/halbert_core/halbert_core` unless they start with `tests/` or `.handoff/`. Every id from `ws_scheduler-heartbeat-daemon-watchdog.digest.md` appears exactly once, either under a theme or in the do-not-lift list. Where the verifier corrected the reader's state or priority I follow the verifier and say so. Six Halbert-side claims were spot-checked in the repo on 2026-09-09 and all hold (listed in the summary).

### What Halbert has here today

Background work is an APScheduler instance with a `MemoryJobStore` (jobs re-register at boot, `DECISIONS.md` `C4-01`) driven by `AutonomousExecutor` (`scheduler/executor.py`), which runs registry-named tasks (`Job.task: str`, `scheduler/job.py:15-27`, no prompt field) on a helper thread under one flat wall-clock deadline (`_call_with_timeout`, `executor.py:62-86`: `worker.join(timeout_s)`, then `TimeoutError` while the daemon thread keeps running). Per-job records are JSON files under `data_subdir("scheduler")` owned by `SchedulerEngine` (`scheduler/engine.py:18-63`), loaded once in `__init__` and never re-read; three separate engine instances coexist by construction (`dashboard/app.py:1249`, `dashboard/routes/jobs.py:31` and `:103-110`, `executor.py:114`). The wave-1 lift (packet 03) added `scheduler/catchup.py` (cadence-scaled grace, run/skip modes, a retirement-diagnostic writer with no production caller), `scheduler/restart_budget.py` (fixed sliding window, cooldown, a clock-rollback hold), `scheduler/run_receipts.py` (started-before-side-effects receipts, the closed status set `{ok, error, delivery_failed, blocked_config}` with `delivered_to_user()`, occurrence-level idempotency keyed on `(job_id, scheduled_instant)` — unwired, and `recover_on_boot` marking dead-owner receipts `interrupted` on a bare `os.kill(pid, 0)` probe whose `PermissionError` branch reads as alive, `:57-68`), and `scheduler/monitor_hash.py` (a maintainer-named set `{'detector_sweep'}`, applied only in the boot catch-up closure at `dashboard/app.py:615-648`). The two production jobs are the detector sweep and the morning report (`scheduler/autonomous_tasks.py:527-560`; `proactive/morning_report.py` is a fixed-shape deterministic digest behind `ProactiveGate`), and `autonomous_tasks.py:84-110` still asks a model for `requires_approval` with a `confidence_threshold` dial. Guardrails are `autonomy/guardrails.py:227-280` (a CWD-relative `data/safe_mode_active.flag` checked at `executor.py:588-593` and `proactive/gate.py:146` only) fed by `autonomy/anomaly_detector.py:120-155`, whose `failure_streak` is global across jobs, so one permanently broken job puts every job into safe mode. Liveness today is `dashboard/parent_watchdog.py` (parent-pid only, exits 0), a 60 s idle heartbeat calling `ThreadManager.tick()` when the turn lock is free (`MIND-1`), `model/client.py:109-160` `llm_advisory_lock` (mutual exclusion on the local GPU, no priority), and `dashboard/app.py:1623-1724` `shutdown_event` — ten awaited `.stop()` calls, each try/except-wrapped, none bounded (zero `wait_for` in the file). The dashboard exposes list/get/cancel (`dashboard/routes/jobs.py:13-110`) and `lib/tauri.ts:209-218` exports `getScheduledJobs()`/`cancelScheduledJob()` that nothing calls; there is no scheduler page, no create endpoint, no schedule parser, no delivery destination on a job, no launchd registration, no faulthandler anywhere. Already lifted and merged as of 2026-09-08 (context list): scheduler durability (catch-up, restart budget, run receipts, monitor hash), interrupt algebra, session-tree schema, channel registry, policy lattice and admission gates, utility slot. The F-4 memo (`.handoff/DECISION-MEMO-F4-PROMPTED-HEARTBEAT-2026-09-07.md` §6) is decided: no prompted heartbeat; Option A names Hermes's ticker epoch markers as the liveness follow-up, still unbuilt. The master plan (`.handoff/OPENCLAW-LIFT-MASTER-PLAN-2026-09-07.md:67`) deliberately left the watchdog/daemon workstream un-packeted "until that workstream is scheduled" and named Hermes's startup watchdog as the template — this section is that packet's input.

### Lift themes, ranked

Priorities are the verifier's effective priority. "confirmed twice" = OpenClaw and Hermes both ship the mechanism. "verifier-added" = ids ending `-M<n>`, effort estimated here.

#### Theme 1 — Run ownership identity and the authority of the job record (highest value per line; strengthens the packet-03 lift)

- **The fire path re-reads the durable record and refuses a cancelled/paused job — the dashboard cancel button does not stop a scheduled job today** (HM16-cron-second-pass-M1; verifier-added) — Hermes `cron/jobs.py:477-486` (`_has_pause_marker`, `is_job_runnable`), `:488-501` (`effective_job_state`), callers `cron/scheduler_provider.py:261`, `tools/cronjob_tools.py:180-201`. Every dispatch path re-reads the record immediately before firing and asks `enabled and not paused`; the pause marker is a deliberate second gate so a half-paused record never fires before self-heal; display makes `enabled=true` authoritative so a list can never show paused while the job fires. Invariant: the durable record, not an in-memory copy, is the authority at fire time. Halbert: absent, live defect spot-checked — `dashboard/routes/jobs.py:103-110` builds its own `SchedulerEngine()` and calls `engine.cancel_job`, which writes `state='cancelled'` to that JSON file and never touches APScheduler; `AutonomousExecutor.cancel_job` (`executor.py:528-544`) is never called by the route; `_wrap_task`'s `wrapped()` (`executor.py:581-620`) reads the record only for a confidence float; `Job.is_terminal()` has one caller, `engine.cancel_job` itself. Task: route the cancel endpoint through the live executor (expose it on `app.state` or have the route call `AutonomousExecutor.cancel_job`), and add an `is_runnable` re-read at the top of `wrapped()` that returns without running (and without a receipt-free return — see OC18-M7) when the record is cancelled/paused/terminal. Test: cancel via the route, advance the trigger, assert no receipt was started. Effort S. Prerequisites: none. Hot files: `executor.py`, `dashboard/routes/jobs.py`, `dashboard/app.py`.
- **Reentrant in-process lock plus cross-process flock around load→modify→save, failing open for the store and closed for the per-job fire fence** (HM16-cron-second-pass-M5; verifier-added) — Hermes `cron/jobs.py:198-236`, `238-291` (`_jobs_lock`, depth-counted, flock on `<cron dir>/.jobs.lock`, proceeds in-process-only on timeout because "a briefly torn cross-process write beats a dead scheduler"), `292-345` (`_fire_job_lock`, fails CLOSED). Two opposite failure postures on purpose: availability wins for the store, correctness wins for never-double-fire. Halbert: partial — `engine.py:44-63` `_persist_job` atomically replaces per job file but there is no lock and no reload (`engine.py:22, 27-40`); three engines write from possibly-stale copies. Task: one shared engine on `app.state` plus reload-before-mutate in `update_job_state`/`cancel_job`, and a per-job fire fence (an `fcntl.flock` on `<persist_dir>/<job_id>.fire.lock`, fail closed) taken inside `wrapped()`. Test: two engines over one dir, cancel through one, assert the other's next `update_job_state` does not resurrect `running`. Effort M. Constraint: single-user single-host — the cross-process half exists only because the route and the executor already are separate instances; do not build a lock server.
- **PID-recycling-proof owner identity: pid + process start time (+ per-process token), with a lease cap and unknown-liveness-holds** (OC06-plugins-sdk-C13, OC18-channels-cron-second-pass-M4 verifier-added, HM16-cron-second-pass-C17, HM14-cli-ops-lifecycle-C21; confirmed twice, four units) — OpenClaw `src/plugin-sdk/file-lock.ts:28-29, 77-87, 193-235` (`staleRecovery: 'fail-closed' | 'remove-if-unchanged'`; payload embeds pid AND starttime; reclaim re-verifies `(dev, ino)` immediately before unlink; rule at `src/infra/stale-lock-file.ts:33-59`: "Only a mismatched process start time proves PID reuse while the PID is alive"), OpenClaw `src/channels/message/ingress-claim-owner.ts:1-14, 16-29, 30-70` (`pid:startToken:uuid`; darwin `ps -o lstart=` with `LC_ALL=C TZ=UTC` and a 2 s timeout, linux `/proc/<pid>/stat` field 22 parsed after the last `)`; `INGRESS_CLAIM_LEASE_MS` 30 min; unreadable start time = unknown = HOLD), Hermes `cron/executions.py:107-124` (`_owner_is_live` requires `current == started_at`, fails safe when it cannot prove death; reused at `cron/delivery_queue.py:24, 200, 264`), Hermes `hermes_cli/process_identity.py:28-31, 56-63, 70-83` and `update_abort_recovery.py:22-28` ("Identity is the incarnation (pid, create_time), never the PID alone"). Halbert: absent, spot-checked — `scheduler/run_receipts.py:57-68` `_default_pid_alive` is a bare `os.kill(pid, 0)` with `PermissionError` → alive; `recover_on_boot` (`:180-198`) marks a receipt interrupted only when that probe says dead. The verifier raised OC06-C13 to high because `recover_on_boot` runs at BOOT, comparing a pid recorded in the previous boot against a pid namespace that was reassigned from scratch: collision is ordinary, and the consequence is a receipt stuck `running` forever, that job's interrupted run never recovered. HM16-C17 stays low on its own (occurrence idempotency already prevents a double fire); HM14-C21 was raised to medium; the four are one fix. Task: record `owner_start_time` (`psutil.Process(pid).create_time()`, psutil is already a dependency) and a per-process uuid in `mark_started` (`run_receipts.py:137-155`); `recover_on_boot` treats a receipt live only when pid exists AND start time matches within ±2 s, HOLDS when the start time is unreadable, and reclaims any receipt older than a bounded lease regardless. Test: a receipt whose pid is `os.getpid()` but whose start time is stamped one hour earlier must be marked interrupted. Effort S. Constraint note: packet 03's out-of-scope guard (`.handoff/OPENCLAW-LIFT-PACKET-03-SCHEDULER-DURABILITY-2026-09-07.md:34`) says do NOT port `getFileLockProcessStartTime`; A06-G6 was refuted on that recorded decision. The verifier's boot-namespace argument is new evidence the packet did not weigh. Founder decision 1 below; recommended default is to lift the one field and keep the multi-instance matrix out. Drop `REAPABLE_PURPOSES` — Halbert reaps nothing.
- **Every abandonment path must terminalize the run receipt** (OC18-channels-cron-second-pass-M7; verifier-added) — OpenClaw `src/cron/service/run-admission.ts:106-135, 495-540` (a reservation fenced by a concurrent mutation is finished `skipped` with reason "cron reservation fenced by concurrent mutation", because "abandoning it here would self-fence the job forever"). Invariant: a receipt is a claim; a path that gives up the claim closes it. Halbert: absent; today nothing refuses to start on a running receipt so the self-fence is unreachable, but A06 own-bug 5 shows the abandonment: the safe-mode skip (`executor.py:588-595`) and the guardrail rejection (`:626-633`) return `None` with no receipt at all, and the timeout path leaves the worker writing after a terminal status. Task: write the invariant into the `run_receipts` module docstring; make `wrapped()` open the receipt before the guardrail/safe-mode/runnable checks and `mark_finished` with `skipped`/`rejected` + reason on every early return; test with a parametrized fixture over each early-return branch asserting no receipt is left `running` after the call returns. Effort S. Prerequisite: HM16-M1 (adds another early return) lands in the same change.
- **Owner-only permissions on the job store, chmod inside the atomic-replace helper** (HM16-cron-second-pass-M8; verifier-added) — Hermes `cron/jobs.py:525-537` (`_secure_dir` 0700, `_secure_file` 0600), `538-560` (`_preserve_file_ownership` after `os.replace`). Halbert: absent — no `chmod|0o600|0o700` under `scheduler/*.py` or `utils/paths.py`; job records carry task inputs and receipts.json is written at the umask. Task: 0700 on the scheduler dir at creation, 0600 on each file inside `_persist_job` and the receipts writer (the ownership-restore half is not needed on a single-user host but is the reason to put the chmod in the replace helper). Test: stat after write. Effort S.

#### Theme 2 — Honest timeouts: inactivity, phases, streams, process groups

- **Inactivity-based job watchdog with a run-claim heartbeat while the job is alive** (HM16-cron-second-pass-C16; packet 03 addendum item 7, packeted and unimplemented) — Hermes `cron/scheduler.py:1719-1737` (`_raise_inactivity_timeout` pulling `last_activity_desc`/`seconds_since_activity`, `request_hard_interrupt`, then `TimeoutError`), `:1739-1770` (`_run_agent_with_watchdog`: default 600 s inactivity, `0` = unlimited, `_POLL_INTERVAL` 5 s, claim heartbeat for #62002), `:954-966`. Only genuine idleness triggers the interrupt; the claim is refreshed while the job runs so a dead-owner TTL never reaps a slow-but-alive run. Halbert: absent, spot-checked — `_call_with_timeout` is a flat `worker.join(timeout_s)`. Kept high by the verifier as the one item naming a live behavioural defect in shipped code (A06 own-bug 3: after a timeout the thread keeps running with side effects while the receipt says error and the retry decorator starts a second run). Task: give tasks a `progress(desc)` callback that stamps a monotonic activity clock on the run; the watchdog thread polls every 5 s and raises only when `now - last_activity > inactivity_s`; refresh the receipt's `heartbeat_at` on each stamp; keep an optional hard ceiling. Test: a task that stamps every second for 3× the inactivity budget completes; a task that sleeps silently past it is interrupted with reason `inactivity`. Effort M. Hot file: `executor.py`, `scheduler/run_receipts.py`.
- **Phase-aware watchdog: setup / pre-execution / execution budgets, queue wait excluded** (OC18-channels-cron-second-pass-M1 verifier-added; OC18-channels-cron-second-pass-C12 payload-shape ceiling folded in per the verifier's "dispatch together") — OpenClaw `src/cron/service/agent-watchdog.ts:15-17, 20-46, 76-127, 143-176` (three deadlines with three reasons; a phase vocabulary decides which timer owns a moment; `noteLaneWait`/`noteLaneAdmitted` restart the setup budget after queue contention — "Queue contention is not runner setup"), `src/cron/service/timeout-policy.ts:10, 16, 20-30` (10 min generic, 60 min agent-turn, explicit override wins, `<=0` = none). Halbert: absent — one `timeout_s=600` on `Job` (`job.py:23`) and one message `Job {id} exceeded {n}s timeout`, so "blocked on Ollama model load", "queued behind the APScheduler pool" and "ran long" are indistinguishable in the receipt. Task: a small phase enum on the run (`queued, admitted, model_resolution, context_assembled, executing`) reported through the same `progress` callback as C16; budget `queued` not at all, `model_resolution`/`context_assembled` at ~60 s with their own reasons, `timeout_s` for execution; record the phase current at fire time in `mark_finished(error=...)`; resolve the ceiling by job kind when an agent-turn job kind exists. Test: a task that reports `model_resolution` and stalls is killed at 60 s with reason `model_resolution_timeout`, not at 600 s. Effort M (M1) + S (C12). Prerequisite: C16's activity clock.
- **Idle-stall watchdog for a stream — Halbert's streaming client uses a TOTAL timeout** (OC18-channels-cron-second-pass-M2; verifier-added) — OpenClaw `src/channels/transport/stall-watchdog.ts:29-49, 51-66, 88-122` (arm/touch per chunk; poll at `min(5s, max(250ms, timeout/6))` rather than re-timer per chunk; disarm BEFORE `onTimeout` so retries cannot double-fire; every host callback contained, even `String(error)` coercion). Halbert: absent, live defect spot-checked — `agents/llm_client.py:225` passes `aiohttp.ClientTimeout(total=self.timeout)` (120 s) to the streaming `/api/chat` and iterates `resp.content`; same at `:149, :165, :385, :438`. A healthy long local generation is aborted at 120 s wall clock and a mid-generation stall goes unnoticed until the same 120 s, with one log line for both. Task: `ClientTimeout(sock_read=<idle>, total=None)` on the streaming paths plus a small armable idle watchdog around the `async for`, reporting "the model stopped producing output after N s" distinctly from "ran longer than allowed"; the callback must never raise into the streaming task. Test: a fake server that emits a chunk every 2 s for 5 min completes; one that emits two chunks then hangs is reported as a stall at the idle limit. Effort S. Not strictly a scheduler file, but the scheduled morning report calls this client (`autonomous_tasks.py:156`), so it belongs in this packet. Constraint: local models pinned — the fix must not change endpoint selection.
- **A timed-out subprocess must be killed as a process GROUP** (HM16-cron-second-pass-M4; verifier-added) — Hermes `cron/scheduler_script.py:150-168, 170-186, 188-214` (tree kill, then `killpg` TERM → verify with signal 0 → KILL; the stated failure is not the orphan but the caller blocking on EOF because a grandchild inherited the pipe write ends). Halbert: absent — `grep start_new_session|setsid|killpg|preexec_fn` hits only `utils/ollama.py:136`, `streaming/pty.py:269`, `dashboard/__main__.py:90`; `tools/executor.py:903-910` kills the direct child only, as do `tools/system_info.py:43`, `tools/applescript_tools.py:85`, `mcp/client.py:331`. Task: spawn tool subprocesses with `start_new_session=True` and on timeout `killpg(TERM)`, wait, verify, `killpg(KILL)` in one helper under `utils/`; adopt it at the four sites. Test: `sh -c 'sleep 300 & wait'` with a 1 s timeout returns within 2 s and leaves no `sleep` child. Effort S. Hot file: `tools/executor.py`.
- **Hand teardown of a per-run resource to the still-running worker's own Future** (HM16-cron-second-pass-C10) — Hermes `cron/scheduler_detached_worker.py:1-34` (`defer_teardown_to_running_worker` via `add_done_callback`, exactly-once even if the worker finished between `done()` and registration). Halbert: absent — the abandoned thread in `_call_with_timeout` can still write to `scheduler_engine`/receipts after the timeout path recorded a terminal status. Task: when C16 lands, give the worker a `Future`; the timeout path marks the receipt `error` and registers a done-callback that records `late_completion=True` rather than letting the worker overwrite the terminal state. Effort S. Low priority, folded into C16.
- **DaemonThreadPoolExecutor: never blocks interpreter exit, propagates contextvars** (HM07-tools-terminal-files-checkpoints-C19) — Hermes `tools/daemon_pool.py:1-56` (daemon workers not registered in `_threads_queues`; `submit()` wraps in `copy_context().run` because stdlib only does that from 3.14). Halbert: absent — stock `ThreadPoolExecutor` at `scheduler/executor.py`, `vision/sources.py`, `dashboard/routes/llm.py`, `dashboard/routes/compute.py`. The contextvar half matters more here: persona/permission scope and session ids ride ContextVars and silently reset on a pool worker. Task: add `utils/daemon_pool.py`, swap in at the scheduler job pool; keep the stock class where a durable write must block exit. Test: a ContextVar set on the caller is visible inside the submitted fn. Effort S. Low.
- **Exclude verified human-wait time from any inactivity deadline, measured at the wait's own blocking call** (HM06-tools-approval-guards-C15) — Hermes `tools/approval_human_wait.py:1-13, 30-55` (per-session accounting, `HUMAN_WAIT_MARGIN_S` 60 shared by every clamp, ceiling = approvals timeout + margin; #79719). Halbert: absent; the verifier corrected the evidence — Halbert does have an inactivity deadline of this shape at `tools/execute_code.py:60-63, 465-473` (30 s + 10 s grace), but approval is non-blocking (`tools/executor.py:604-611` unwinds with `requires_confirmation=True`), so no human wait sits inside a timer yet. Task: bank the rule for the moment a blocking clarify/consent prompt becomes reachable from inside a script or a scheduled job; nothing to build now. Effort S when needed. Low.

#### Theme 3 — Failure containment, admission and the kill switch

- **Per-job auto-disable after a consecutive-failure streak / auto-pause when proven unrunnable, with an auditable reason and a system-job carve-out** (OC18-channels-cron-second-pass-C11, HM16-cron-second-pass-C14; confirmed twice) — OpenClaw `src/cron/service/auto-disable.ts:11-15, 32-34, 41-45, 50-54, 75-80` (10 consecutive run failures; `MAX_SCHEDULE_ERRORS=3` at `src/cron/service/jobs-scheduling.ts:370, 387-395`; persisted `state.autoDisabled {reason, atMs, consecutiveErrors}`; one notification carrying the exact re-enable command; gateway-owned system jobs are never auto-disabled because nothing else re-creates them), Hermes `cron/scheduler_prompt.py:323-343` (`_block_and_pause_job` → `pause_job(job_id, 'Auto-paused by scheduler: <reason>')`; "an unrunnable job left enabled re-fires every tick forever"), supporting predicates `cron/jobs.py:477-501`. Halbert: the verifier corrected C11 to partial and C14 to absent (it ran the grep the reader did not: no pause concept anywhere; `engine.py` has only terminal `cancel_job`), and the true state is worse than "retries forever": `anomaly_detector.py:120-155` keeps a GLOBAL `failure_streak`, `guardrails.py:89` `auto_pause_on_anomaly=True` writes the safe-mode flag, and `executor.py:588-593` then skips every job (spot-checked). One broken job halts the scheduler. Task: add `enabled: bool`, `paused_at`, `paused_reason`, `consecutive_failures` to `Job`; count per job in `_finish_receipt`; trip at 10 run failures / 3 schedule errors into `enabled=False` with the reason; exempt the registry's system jobs (detector sweep, morning report, timeline retention) which instead keep retrying; deliver the trip through `ProactiveGate`; make the anomaly detector's streak per-job so safe mode is reserved for cross-job anomalies. Test: one always-failing user job trips after 10 runs and a sibling job keeps firing; the sweep never trips. Effort M. No emoji in the notification (origin leads with one). Hot files: `executor.py`, `job.py`, `engine.py`, `autonomy/anomaly_detector.py`. Founder decision 5.
- **Preflight the local model endpoint before admitting a scheduled run; skip, never substitute** (OC18-channels-cron-second-pass-M3; verifier-added) — OpenClaw `src/cron/isolated-agent/model-preflight.runtime.ts:12-14, 19-29, 148-164, 201-206` (2.5 s probe, cached 5 min per endpoint, structured `unavailable` with `retryAfterMs`; only local base URLs are probed), `src/cron/isolated-agent/run-fallback-policy.ts:85-140` (`{ok:false, reason}` → occurrence skipped with an honest reason, not started and failed). Halbert: absent — `/api/tags` is probed only for context windows (`model/client.py:982-1030`, cached at `:851`); `autonomous_tasks.py:156` calls `model_manager.generate` blind. Combined with C11: a laptop that suspended overnight wakes, fails sweeps against an unstarted Ollama, and safe-modes the scheduler. Task: cached reachability preflight in the executor's admission path keyed by the configured local slot's endpoint; unreachable → `mark_finished(status='skipped', error='provider unavailable, retry after N')` that neither counts toward a streak nor consumes the occurrence key. Test: endpoint refused → receipt `skipped`, `consecutive_failures` unchanged, occurrence not recorded. Effort S. Constraint: lift preflight-and-skip only; do NOT lift the fallback-chain substitution — the pinned local slot and SEC-21 (`:cloud` is cloud) forbid a silent candidate switch.
- **Fail-before-spend preflight: three concrete fail-open checks setting `blocked_config`** (HM16-cron-second-pass-C11; extends the packet-03 addendum item 3 taxonomy) — Hermes `cron/scheduler_preflight.py:84-124, 217-339` (provider key, delivery target, skill readiness; each check fails OPEN on its own error and blocks only on an affirmative misconfiguration verdict; `_preflight_job_config` try/except-continues per check). Halbert: partial — `run_receipts.py:21-22, 43` carry `blocked_config` "refused in preflight, before any LLM spend" with no producer anywhere (A06-G8: dead vocabulary). Verifier lowered to medium because only model-resolvability has a subject today. Task: implement `preflight(job) -> Optional[reason]` with the resolvable-slot check now (M3 is its first member) and the delivery-target/skill checks as the job model grows; `wrapped()` marks `blocked_config` and returns. Test: a job whose slot is unset gets a `blocked_config` receipt and no `generate` call. Effort S now, M later. Note: `dashboard/app.py:1251` constructs the executor with `enable_llm=False`, yet the morning report still calls `generate` — the preflight is not vacuous.
- **Failure-incident dedup by (job_id, normalized error signature), closed-until-the-error-changes** (HM16-cron-second-pass-C8; packet 03 addendum item 5, unimplemented) — Hermes `cron/incidents.py:1-9, 90-172` (`_normalize_error` whitespace-collapse + lowercase, `_redact_error` before truncation, `_error_signature = sha256(job_id + normalized)[:12]`, `upsert_incident` refreshes `last_seen_at` without reopening; a closed incident mints a NEW id only when the text changes; keyword failure-type table; the addendum adds "3 consecutive failures appends a worth-a-review-or-pause-it nudge"). Halbert: partial — `findings/store.py:271-283` dedups on exact `(detector, title)` with SNOOZED/DISMISSED, no signature hash, no scheduler wiring. Task: a scheduler-aware wrapper that turns each failed receipt into a finding keyed by signature, reopening on change; redact through the response choke point before hashing. Test: three identical failures → one finding with `last_seen` advanced; a different error → a second finding. Effort M. Constraint: Tier-2 secrets — the error text is scrubbed deterministically before it is stored or shown.
- **Filesystem-sentinel ESTOP covering every turn-admission surface** (HM01-agent-turn-family-C1) — Hermes `agent/estop.py:31-33, 36-49, 52-61, 64-74, 122-138` (sentinel at `$HERMES_HOME/ESTOP`; `is_engaged()` re-stats and fails SAFE on `OSError`; `engage()` writes `{engaged_at, reason}`; `check_paused()` logs once per engagement per component and re-arms on resume), consumers `cron/scheduler.py:3767-3770`, `gateway/kanban_watchers_common.py:107-110`, `gateway/run_inbound.py:236-239`. Halbert: partial, with two live bugs the verifier found: `Path("data/safe_mode_active.flag")` (`guardrails.py:227-249`) resolves against the process CWD, so a pause from one working directory is invisible to a process started from another and a stray `./data` flag pauses a fresh run; `is_safe_mode_active()` syncs file→memory only in the True direction (`:276-278`), so a flag removed out of band never lifts. A control surface exists (`dashboard/routes/settings.py:2496-2522`); zero references in `agents/state_machine.py` or `tools/executor.py`, so interactive turns and MCP/internal dispatch are uncovered. A07-G12 was refuted on "safe mode exists"; this is the strengthening that survives. Task: anchor the sentinel under the Halbert data dir, write reason + engaged_at JSON, sync both directions, once-per-engagement logging, and check it at `state_machine.py` turn admission and at `tools/executor.py` dispatch (the one policy pipeline MCP also flows through) — engaged means no new autonomous work, in-flight work finishes. Test: engage from a different CWD, assert a new turn is refused and a scheduled fire is skipped with a receipt. Effort S. Hot files: `state_machine.py`, `tools/executor.py`. Constraint: deterministic policy — two `os.stat` calls, no model.
- **Standing-order documents: declarative scope / triggers / approval gates / escalation, plus Execute-Verify-Report with 3-attempts-then-escalate** (OC24-docs-qa-security-skills-C19) — OpenClaw `docs/automation/standing-orders.md:18-32` (WHAT-vs-WHEN split, four-field program shape at `:26-29`), `:160-165` (response matrix), `:165-181` (Execute / Verify / Report; "Never retry indefinitely - 3 attempts max, then escalate"). Halbert: the verifier corrected absent→partial and RAISED to medium on a constraint ground: `scheduler/autonomous_tasks.py:25-38` `TaskDecision` already carries `requires_approval`/`approval_reason`/`risk_level`, but `:84-110` `_make_decision` produces them from an LLM call with a `confidence_threshold` dial — a model judging its own approval gate, against the standing directive that no LLM makes a permission decision. Task: one standing-order YAML/markdown per recurring autonomous domain (host health sweep, morning report, retention), loaded as data; `requires_approval` comes from the document's approval-gate field; replace `confidence_threshold` with a per-job attempt counter that escalates (a finding) at 3. Test: with the document saying `approval_gate: always` for config writes, the sweep's proposal is staged regardless of what any model returns. Effort S. Founder decision 3.
- **The agent-facing scheduling tool is denied the model/provider/endpoint fields the human surfaces have** (HM16-cron-second-pass-M9; verifier-added; state present) — Hermes `tools/cronjob_tools.py:986-1002` (`_HANDLER_FORWARDED_ARGS` omits model/provider/base_url "the agent must not point unattended spend at a different model"). Halbert: holds by absence today (no model-setting tool, no create-job route). Nothing to build; record in `DECISIONS.md` as the invariant that any future job-creation tool exposes no inference-slot field — the concrete mechanism behind "connection slots, not model menus". Effort 0.
- **Two-signal ticker liveness markers: heartbeat every loop, last-success only on a clean tick, last-error text, catch-up counter** (HM16-cron-second-pass-M3; verifier-added; = A06-G11, A15-G5, F-4 Option A's named follow-up) — Hermes `cron/jobs.py:1128-1152, 1155-1183, 1185-1215` (`_write_marker` atomic best-effort; `None` means "cannot determine", not dead; "a ticker stuck failing every tick would otherwise keep the plain heartbeat fresh and falsely report healthy", #32612/#32895). Halbert: absent — no way to tell "scheduler thread dead" from "nothing due". Task: four epoch files under the scheduler data dir written by the executor's tick and by the 60 s idle heartbeat; ages surfaced on `/health` and in `dashboard/routes/jobs.py`. Test: simulate a tick that raises → heartbeat advances, success does not, last-error holds the text. Effort S. Deterministic, zero-token; this closes the parked F-4 Option A item.
- **Golden-text eval for user-facing status copy under edge-case configuration** (HM19-evals-tests-meta-C13) — Hermes `evals/gateway_status_render/iteration_ceiling_ab.py:1-35` (real agent, stub transport, assert the literal outbound string). Halbert: the verifier corrected absent→partial: `tests/test_consent_copy_manifest.py` already pins consent copy by sha256 with versioned supersession — a stronger discipline. What is uncovered is the status/heartbeat family (`tests/test_conversation_status.py` tests enum membership, not rendered copy). Task: extend the copy-manifest discipline to the scheduler health line and the terminal-watch notification text (ties to M3's ages), not import Hermes's pattern. Effort S. Low.

#### Theme 4 — Turn liveness and local-model priority (the one conversation's lock)

- **Turn-liveness watchdog: force-abort a turn that stalls silently while holding the lock** (HM01-agent-turn-family-C2; = A07-G10 confirmed medium) — Hermes `agent/turn_liveness.py:42-85, 119-142, 144-151, 162-206` (config resolver rejects NaN/Inf/non-positive; `_sample()` reads `(generation, last_activity_ts)` under the activity lock; two-phase observational-then-committed surfacing; `commit_liveness_abort` revalidates under the same lock at `:124-138`), wiring `agent/turn_facade_lease.py:47-66, 68-78, 80-86` (`_touch_activity('starting new turn')` BEFORE scheduling, "else the watchdog would measure idle from the PREVIOUS turn"). Halbert: absent — `state_machine.py:223-232` `TURN_LOCK_TIMEOUT_S` bounds only a QUEUED waiter (`_acquire_turn_lock` `:946-951`); a wedged holder is never released. Two things the verifier found that make this cheaper: `agents/turn_activity.py:1-65` already has the lock + monotonic generation + single-shot `claim()` (lifted from Hermes `require_generation`), and an approval pause cannot false-trip because `:1351-1358` breaks the loop at AWAITING_CONFIRMATION and the `finally` at `:832` releases the lock. Task: stamp the activity clock at handler entry, each tool start/complete, each stream chunk; a sampler task compares idle against a ceiling and, on trip, claims the generation and calls `request_stop` (the algebra's stop, so the interrupt lands through the lifted path) then force-releases `turn_lock`. Test: a fake model that never yields is aborted at the ceiling and the next turn acquires the lock. Effort M. Hot file: `state_machine.py`. Founder decision 4 (ceiling default).
- **Tool-execution activity heartbeat so a long tool call never trips the watchdog** (HM01-agent-turn-family-C3; strict dependent of C2) — Hermes `agent/tool_executor.py:548-554, 557-596` (30 s heartbeat thread, "must stay far below the turn-inactivity timeout (default 1800s)"; swallows every exception because "a heartbeat must never break the agent loop"; 2 s join). Halbert: absent — `tools/executor.py:800` `await pool.run_block(command, cwd=cwd, timeout=timeout)` has no intermediate signal. Task: wrap tool dispatch in a heartbeat touching the same clock; ship with C2. Effort S. Hot file: `tools/executor.py`.
- **Idle-deferred background inference: post-turn background work must not take the local model slot the next live turn needs** (HM04-agent-learning-verification-review-C12) — Hermes `agent/review_idle_queue.py:1-11, 25-27, 95-104, 123-139, 165-197` (`_IDLE_SETTLE_S` 15 — "two back-to-back prompts must not look idle, a coffee break must"; `_MAX_AGE_DEFAULT_S` 30 min unconditional floor; coalesce by session but KEEP the original enqueue time; enabled re-checked at dispatch). Halbert: the verifier corrected absent→partial and kept high: `model/client.py:109-160` `llm_advisory_lock` (flock, fail-open on timeout) is applied at `:338-346` for `LOCAL_GPU_PROVIDERS` — plain mutual exclusion with no priority, so a background job that takes the lock first delays the live turn for up to the lock timeout, exactly what C12 prevents. The lock's held state is already the idleness signal; no `/slots` probe is needed. Task: a `BackgroundInferenceQueue` in `scheduler/` that any non-live inference (morning-report summarizer, future consolidation) enqueues into; dispatch only when `now - last_turn_end >= 15 s` AND the advisory lock is free, or at the 30 min age-out; a live turn arriving during a background call is the priority holder. Test: enqueue, start a live turn within 15 s, assert the background call ran after the turn. Effort M. Hot file: `model/client.py`. Constraint: `MEM-04` — background memory work registers on Halbert's APScheduler; this queue is where it registers.
- **Thread-scoped stdout/stderr silencing proxy, not process-global reassignment** (HM02-agent-context-prompt-family-C16) — Hermes `agent/thread_scoped_output.py:1-8, 20-26, 29-58` (one routing proxy installed idempotently as `sys.stdout`; per-thread depth counter picks sink or passthrough). Halbert: the verifier removed the reader's caveat — the bug is live and spot-checked: `tools/execute_code.py:447` assigns the PROCESS-GLOBAL `sys.stdout = capture` from the `halbert-script` worker thread (`:458`), restored at `:455/:514`. While a script runs, every other thread's prints (dashboard, scheduler, a concurrent turn) land in the script's capture buffer that is returned to the model, and two concurrent runs can capture each other's "original". Task: install a thread-routing proxy once at process start; `execute_code` silences/captures its own thread only. Test: two concurrent scripts each see only their own output; a print from a third thread reaches the real stdout. Effort S. Medium (verifier raised from low).

#### Theme 5 — Process lifecycle: loop watchdog, unclean-exit sentinel, bounded shutdown, startup, supervision, sleep/wake (the deferred daemon packet's contents)

Prerequisite for the hard-exit items: **HM11-gateway-core-M2** (dashboard workstream digest; Hermes `gateway/restart.py:9-20, 62-68`) — a distinct exit-code vocabulary (restart-me vs stop-restarting-me) and a supervisor probe (`XPC_SERVICE_NAME` or an explicit `HALBERT_SUPERVISED` flag). Today `dashboard/__main__.py:153, 199` exit 1 for everything, `parent_watchdog.py:42-49` exits 0, and the Tauri shell does not respawn the sidecar. Under no supervisor, a hard-exit watchdog converts a wedge into an outage: the rule is log CRITICAL and stay alive for the founder unless supervised.

- **Event-loop liveness watchdog + off-loop heartbeat file (+ loop-tick witness socket later)** (HM11-gateway-core-C1) — Hermes `gateway/shutdown_watchdog.py:79-84, 87-146, 186-208, 242-292, 294-306, 308-320, 323-380` (OS thread pings the loop via `call_soon_threadsafe`; 3 missed → `faulthandler.dump_traceback(all_threads=True)` → `os._exit(RESTART_EXIT_CODE)`; heartbeat written off-loop via awaited `to_thread` so a stalled fsync cannot wedge the loop it proves alive; a unix socket served ON the loop so only an answer proves scheduling; floor timer keeps the selector wait bounded; shutdown watchdog releases the pid file/lock BEFORE log drain then hard-exits). Halbert: absent — `parent_watchdog.py` polls the parent pid only; no `faulthandler` anywhere. Task: `dashboard/loop_watchdog.py` started beside `parent_watchdog` in `app.py` startup; probe every 10 s, 3 strikes → dump to `<data>/forensics/` and, if supervised, exit with the restart code, else log CRITICAL and keep running; heartbeat file `{pid, monotonic, start_time, rss}` every 30 s under the data dir. Witness socket only when an external supervisor consumes the file. Test: block the loop with `time.sleep` in a handler and assert the dump appears. Effort M. Hot file: `dashboard/app.py`. Gated by HM11-M2.
- **Unclean-exit / suspected-OOM lifecycle sentinel with a state-db integrity check only on a suspicious boot** (HM11-gateway-core-C2) — Hermes `gateway/lifecycle_ledger.py:107-122, 125-135, 138-162, 165-183, 205-231, 234-247` (phase=running claimed at boot, phase=exited on every clean path; a boot finding `running` with a dead `pid+start_time` = unclean death; `PRAGMA quick_check(1)` "cheap once per unclean boot, too costly every boot"; `mark_exited` rewrites only a sentinel whose pid is this process), `gateway/memory_status.py:30, 51-61`. Halbert: absent — `app.py:141-172` `run_conversation_boot_hooks` heals turn rows on EVERY boot (`mark_interrupted`), it never classifies the previous death and never inspects the store. Task: `dashboard/lifecycle_sentinel.py`; on an unclean boot log WARNING with the last heartbeat's RSS, run `quick_check(1)` on `conversations.db` and the state ledger, surface `unclean_restart` on `/health`. Portability: `sample_memory` is `/proc`-only; use psutil. Test: write a `running` sentinel with a dead pid, boot, assert the flag and that `quick_check` ran. Effort M. Pairs with HM11-M1 (FTS classifier, dashboard digest) and the A08 store gaps: this is the read-side half of the same story. Hot file: `dashboard/app.py`, touches `conversation_sqlite.py` read-only.
- **Bound the shutdown sequence: every `await stop()` under a timeout, and a cleanup outcome that is `closed | uncertain`, never certified by a timer** (OC16-gateway-second-pass-A-C7, OC15-agents-second-pass-B-C16) — OpenClaw `src/gateway/active-sessions-shutdown-drain.ts:16, 22-24, 54-67` (`allSettled` started BEFORE the race so one slow plugin cannot starve later handlers; unref'd timer; returns `{emitted, timedOut}`), `src/gateway/chat-abort-lifecycle-internal.ts:20-79` (registry removal alone is not drainage: `terminalOwnersSettled()` must hold), `src/agents/run-cleanup-timeout.ts:15-48` (AsyncLocalStorage receipt starts `closed`, flips `uncertain` on any failure, propagates to the parent in `finally`; "The budget bounds reporting, not resource closure"). Halbert: spot-checked — `dashboard/app.py:1623-1724` has ten unbounded awaits (`stop_thread_tick_heartbeat`, `stop_mcp_health_monitor`, `terminal_manager.shutdown()` at `:1673`, HA stream, Frigate MQTT, Wyoming, audio coordinator); one hung websocket close forces SIGKILL, the case the fsync work should not have to survive. `run_receipts.py:43` has no `uncertain`. Task: `asyncio.wait_for(step, timeout=N)` per step with a shared aggregate budget, a `ShutdownOutcome(closed|uncertain, reasons)` written to the lifecycle sentinel (so C2's next boot knows the exit was clean-but-uncertain), and the same primitive on subagent kill. Test: a stop that never returns → shutdown completes within the budget with `uncertain` and the step named. Effort S + S. High. Hot file: `dashboard/app.py`. This also closes A15-G7 (stop does not join the in-flight sweep — add the join under the same budget).
- **Startup-deadlock watchdog armed before heavy imports: phase-owned progress leases, capped CPU fallback, escort-thread exit** (HM13-state-root-files-C9) — Hermes `hermes_startup_watchdog.py:1-26, 70, 75, 228-250, 270-330, 332-340, 361-405` ("IMPORT-LIGHTNESS IS A CORRECTNESS PROPERTY"; the ~30 h field wedge with a live PID; `lease()` clamped to 900 s; CPU extension capped at 3; `_fire` starts the escort FIRST, then faulthandler to stderr AND file, ledger join bounded 5 s, escort hard-exits at 10 s if forensics wedge). Halbert: absent. The verifier lowered high→medium for a process reason: the master plan already named this the template for the deferred packet; fold it in. Task: arm in `dashboard/__main__.py` before importing `halbert_core.dashboard.app`; leases around the RAG index open and the model catalog probe; fire → dump + sentinel record + (if supervised) exit. Effort M. Gated by HM11-M2.
- **Process-boot restart-loop breaker (chained inter-boot gaps, period-agnostic) and exponential respawn-storm breaker** (HM11-gateway-core-C5) — Hermes `gateway/restart_loop_guard.py:41-46, 61-76, 96-114` (trips at 3 chained boots with gaps ≤300 s, handles a backwards clock step, fails open on I/O), `gateway/status.py:57-83` (ring-buffered start log, `min(cap, 5*2**n)` backoff). Halbert: absent; `restart_budget.py` is job-level. Verifier lowered to low: the tripped action ("skip auto-resume") has no Halbert subject, and the storm breaker presumes a `KeepAlive` supervisor. Task: build only once launchd registration exists (founder decision 2); then persist a boot chain in the sentinel and surface `restart_loop` on `/health`. Effort S. Low.
- **Periodic RSS log line for slow-leak detection** (HM11-gateway-core-C10) — Hermes `gateway/memory_monitor.py:30-44, 47-55` (one grep-friendly `[MEMORY] rss= gc= threads= uptime=` line per 300 s; disables itself once if introspection is unavailable). Halbert: absent — `psutil.virtual_memory()` at `utils/health.py:164`, `routes/system.py:44`, `mcp/server.py:128` is system memory, never the backend's own RSS series. Task: ~40 lines beside the loop watchdog using `psutil.Process().memory_info().rss` (not `ru_maxrss`, which is a high-water mark and only ever rises). Effort S. Low; harmless.
- **Event-loop health hysteresis: utilization/CPU warnings need delay co-evidence; a separate persistent-degradation clock** (OC17-gateway-second-pass-B-C4) — OpenClaw `src/gateway/server/event-loop-health.ts:10-17, 66-97, 121-189, 207-213`. Halbert: the verifier corrected absent→partial and lowered to low — `context/extra_adapters.py:326-332` and `tools/system_info.py:125` already take the readings; what is missing is the ~5-line co-evidence rule and a degraded-since clock. Task: add hysteresis to the readings Halbert already takes when C1's probe lands (probe latency is the delay signal); do not build a 20 ms Python sampler. Effort S. Low.
- **Tri-state readiness (starting / draining / downstream) with a grace that expires when the retry ladder does** (OC17-gateway-second-pass-B-C5) — OpenClaw `src/gateway/server/readiness.ts:44-59, 62-84, 87-130` (draining checked before starting; a channel mid-restart-backoff is ignored only while `restartPending && !running`; 1 s TTL cache; state-db failure short-circuits before any channel snapshot). Halbert: absent (`/health` is one boolean handler). Task: when C1/C2 land, make `/health` report `starting|ready|draining|unclean_restart` and treat a mid-backoff reconnect (OC16-C8) as not-failed while its ladder runs — Halbert already owns the ladder half in `restart_budget.py`. Effort S. Low.
- **Verify who holds the expected port before hopping to a fallback; capture listeners before the status snapshot** (OC22-extensions-local-providers-apps-C11) — OpenClaw `apps/macos/Sources/OpenClaw/PortGuardian.swift:62-90` (reap orphans first; capture listeners BEFORE the launchd status snapshot "if its process exits and the PID is reused, the newer status snapshot cannot bless the replacement"; `isExpected` refuses to kill in remote mode). Halbert: absent — `dashboard/__main__.py:48-57` `find_available_port(8000, 8100)` binds the first free port; a crashed backend's orphan silently pushes the next boot to a new port. Task: before falling back, psutil-match the occupant's cmdline against Halbert's own; reap an orphaned prior backend and reclaim 8000; log clearly when the occupant is foreign. Skip the csops/cdhash identity check (disproportionate for one host). Effort S. Low. Ties to HM11-M3 (single-instance lock, dashboard digest).
- **launchd LaunchAgent registration (darwin only), supervisor detection, restart-handoff shell, safe-restart preflight, restart blocker diagnostic, env-var blocklist** (HM14-cli-ops-lifecycle-C19, OC02-infra-rest-C3, OC08-daemon-process-worker-hooks-audit-C3, OC02-infra-rest-C8, OC09-tasks-system-agent-flows-boards-claws-C17, OC04-commands-C17; merged as one gated design unit) — Hermes `hermes_cli/service_manager.py:32-52, 55-77` (Protocol + detection ordered by specificity); OpenClaw `src/infra/supervisor-markers.ts:5-22, 46-53, 57-79` (`LAUNCH_JOB_LABEL`/`XPC_SERVICE_NAME` hints; exit-and-let-launchd-relaunch when supervised, detached self-respawn otherwise, `src/infra/process-respawn.ts:44-77, 86-110`), `src/daemon/launchd-restart-handoff.ts:57-72, 103-135` (`while kill -0 "$wait_pid"; do sleep 0.1; done` then `launchctl kickstart -k`/bootstrap; #110137 ExitTimeOut race), `src/daemon/restart-logs.ts:75-95` (structured lifecycle audit line, never blocks the mutation), `src/infra/restart-coordinator.ts:10-18, 55-95, 97-125` (active-work snapshot; `scheduled|deferred|coalesced` with an operator bypass), `src/tasks/task-restart-blocker.ts:5-25` (formatted list of what a restart would interrupt), `src/commands/daemon-install-helpers.ts:156-185` (dangerous-env-var-name blocklist before persisting into a service definition; the Linux ambient `*_API_KEY` scan at `:187-232` is an anti-pattern for Halbert's consent posture — do not lift it). Halbert: absent on all six — every `launchd|launchctl|LaunchAgents` hit is a read-only discovery scanner or a watched path in `consent/selfmod.py:111`; there is no self-triggered restart anywhere (`app.py:147` only writes a "restarted here" timeline marker after the fact). All six were kept low or medium by the verifier as design input for this deferred packet, not dispatchable work; C19 (medium) is the only one with a decision behind it. Task, if founder decision 2 opens it: a darwin-only `dashboard/launchd.py` (install/uninstall a `~/Library/LaunchAgents/<label>.plist` with `KeepAlive: {SuccessfulExit: false}` keyed to HM11-M2's exit codes, no env secrets without the blocklist), a supervisor probe, and a restart path = exit-with-restart-code when supervised, else refuse with the active-work diagnostic (receipts running, `streaming/session_manager.py` live PTYs, `mcp/client.py` in-flight calls — Halbert already has the substrate the preflight reads). Effort M (C19) + S each for the rest. Constraint: single-host — take only the launchd backend; the Protocol/s6/systemd/Windows matrix is multi-deployment machinery Halbert must not inherit.
- **Proactively reconnect live connections after a host thaw, detected from the heartbeat tick gap, not an OS hook** (OC16-gateway-second-pass-A-C8) — OpenClaw `src/gateway/channel-thaw-restart.ts:16-23, 25-34, 37-38, 52-59, 66-70, 76-85, 98-103` (snapshot running targets, dedupe, skip manually-stopped, `shouldContinue()` checked between stop and start with the unfinished tail returned for retry, two-call recovery for a stop that timed out; recovery defers while admission is closed), detector `src/gateway/host-thaw-recovery.ts:3-5, 91-96` (`HOST_THAW_MIN_FROZEN_MS` 45 s beyond the expected cadence; "shorter gaps are ordinary event-loop load and must not churn channel sockets"). Halbert: absent — `mcp/health.py` is periodic-only; HA stream, Frigate MQTT and Wyoming each reconnect on their own sweep. The verifier removed the NSWorkspace half: detect the gap in the existing 60 s idle heartbeat. Task: in the tick, if `monotonic gap > cadence + 45 s`, fire `on_thaw()` that stop→starts the four reconnectable clients in sequence with cooperative cancellation. Test: monkeypatch monotonic to jump 5 min and assert the reconnect hooks ran once. Effort M. Medium. Related: A06-G3 (slots missed while asleep are dropped by the 60 s misfire grace) — the same gap detector should call `catchup` for the asleep window, not only at boot.
- **Sleep/wake suspension lease with a generation fence and bounded resume retries** (OC22-extensions-local-providers-apps-C4) — OpenClaw `apps/macos/Sources/OpenClaw/GatewaySleepCycleController.swift:47-66, 68-91, 93-108` (`willSleep` bumps `cycleGeneration`, requests a lease, releases immediately if the generation moved; `didWake` refreshes the transport FIRST "after real sleep the transport is usually dead", then resumes with 3 retries at 2 s under a generation guard); the Linux port `apps/linux/src-tauri/src/gateway_sleep.rs:6-7, 26-30` shows the same shape. Halbert: absent — `NSWorkspace` appears only for `frontmostApplication` (`vision/screen_capture.py:202-203`). The verifier's cheaper path: PyObjC in the sidecar (AppKit already imported there), no Rust. Task: subscribe to `NSWorkspace.willSleepNotification`/`didWakeNotification`; on sleep, mark the turn state machine `suspended` (a live generation is stopped through the lifted `request_stop`, with a receipt reason `host_sleep`); on wake, run C8's `on_thaw()` first, then lift the suspension under a generation counter. Test: two rapid sleep/wake cycles → only the newest generation resumes. Effort M. Medium. Constraint: UI commands staged never executed — a turn paused by sleep is not re-run automatically (A07-G11's founder question applies).

#### Theme 6 — The scheduled-work surface: schedule input, job shapes, per-job state, timezone (the job model grows only if the founder opens user-created jobs)

- **Deterministic natural-language schedule parser** (HM16-cron-second-pass-M2; verifier-added) — Hermes `cron/jobs.py:655-677` (`_parse_clock_time`: 9am / 9:30am / 14:00 / bare 24 h / noon / midnight), `680-712` (`_natural_every_to_cron`: weekday keyword or comma/'and' list, optional 'at'), `716-731` (croniter validation, interval), `733-760` (`parse_schedule` → `{kind: once|interval|cron}`; unparseable raises at creation). Halbert: absent — `executor.py:462-468` takes `cron_expr` as a dict of `CronTrigger` kwargs supplied by code; `job.py:15` documents `schedule` as "cron expression or ISO timestamp" with no parser. Task: `scheduler/schedule_parse.py`, pure functions returning the three-kind dict, validated before storage; the one continuous conversation accepts "every weekday at 8" with no model call. Test table of ~20 phrases. Effort M. Medium. Constraint: a template beats a model — this is the directive's concrete form for scheduling.
- **Cron expresses wall-clock intent: schedule edits re-anchor without firing, offset changes fire a never-served occurrence** (HM16-cron-second-pass-M6; verifier-added) — Hermes `cron/jobs.py:807-824, 826-840, 1006-1030, 1039-1062` (`_classify_stale_cron_next_run` separating `expr_edit` from `timezone_migration`; "21:00+10 → 13:00+02"). Halbert: partial — `catchup.py` has no timezone reasoning at all; `AutonomousExecutor` defaults `timezone='UTC'` (`executor.py:131, 153`); only `app.py:1241-1254` resolves the real zone, while `routes/jobs.py:31` and the `executor.py:114` example construct with UTC. The lift is the invariant, not the legacy-row classifier (no-migrations directive: there is no old data). Task: one configured zone everywhere (remove the UTC default), compare wall clock across DST/config changes, and make boot catch-up serve an occurrence whose only change was the offset. Test: a job at 07:00 across a DST boundary fires at 07:00 local both days. Effort M. Medium. Pairs with A06-G10/A15-G4 (schedule-activation guard: a slot computed from an edited time is not a missed run). Founder decision 6.
- **Three job shapes (prompt / script-augmented / `no_agent` script-only) with a script-authored wake gate; per-job monitor field replacing the maintainer-named set** (HM16-cron-second-pass-C15; extends the monitor-hash lift) — Hermes `cron/scheduler_prompt.py:19-33` (`_parse_wake_gate`: last stdout line parsed as JSON; False only on `{"wakeAgent": false}` exactly, #1232), `tools/cronjob_job_args.py:73-90` (`no_agent`: empty stdout sends nothing; non-zero exit or timeout alerts; prompt ignored). Halbert: partial — `monitor_hash.py:34, 91` `DEFAULT_MONITOR_HASH_JOBS = frozenset({'detector_sweep'})`. Task: `Job.gate: {mode: hash|script|none, source}`; the `no_agent` lane is the founder-aligned half — a deterministic check that stays silent with nothing to report and never touches a model. Also apply the gate on every fire, not only in the boot closure (A06-G7). Effort M. Medium.
- **Job-script confinement: resolve-then-`relative_to` one scripts dir; interpreter by extension, shebang not honoured** (HM16-cron-second-pass-M7; verifier-added) — Hermes `cron/scheduler_script.py:256-292, 296-315`. Halbert: partial — `tools/execute_code.py:202` bans `subprocess` in generated scripts (stronger for its lane), but `tools/schedule_cron.py:16-30` accepts a free-text command and writes it into the OS crontab with no path or interpreter discipline. Task: when C15's script lane exists, require the script to resolve inside `<config>/scheduler/scripts` after `resolve()` and pick the interpreter by extension; and retire or fence `schedule_cron.py`'s free-text crontab write behind the same rule now. Effort S. Medium. Constraint note: "scope by capability boundary, not filesystem path" — argue the scripts dir as a containment mechanism, not an identity scope.
- **Per-job durable state slot: KV notepad with byte caps and byte-identical empty render / single scratch row with compare-and-swap** (HM16-cron-second-pass-C4, OC18-channels-cron-second-pass-C13; confirmed twice; packet 03 addendum item 6, unimplemented) — Hermes `cron/notepad.py:1-30, 68-158` (`MAX_VALUE_BYTES` 16 KB, `MAX_KEY_CHARS` 128, `MAX_JOB_TOTAL_BYTES` 64 KB checked by a `SUM(LENGTH(...))` guard before upsert; write path is CLI-only, "no model tool is added"; "An empty notepad MUST return empty string ... byte-identical prompt"), OpenClaw `src/cron/scratch-store.ts:17, 23-24, 34, 176-187, 231-247` (monotonic revision surviving unset/recreate via a tombstone; `revision-conflict` result; guarded delete). Halbert: absent — `job.py:9-30` is a bare dataclass; no cursor/watermark concept. Task: `scheduler/notepad.py` with the Hermes caps and a revision column with CAS semantics from OpenClaw; render into the job's prompt/context assembly; write path is the executor and the CLI, never a model tool. Effort S. Medium (C4) / low (C13, no overlapping runs to protect yet).
- **Consent-first automation suggestion queue: dedup key, `MAX_PENDING` 5, latched dismiss, idempotent accept; starter catalog seeded once; blueprints = skills with a schedule block** (HM16-cron-second-pass-C2, HM16-cron-second-pass-C3, HM10-tools-skills-second-pass-C18; C3 and C18 are sub-tasks of C2) — Hermes `cron/suggestions.py:1-45, 118-225` (dedup across dismissed/accepted/pending so re-seeding is a no-op; `accept_suggestion` marks ACCEPTED even when registration raises AFTER the job is durable, then re-raises — the verifier corrected the reader's ordering), `cron/suggestion_catalog.py:1-40, 100-143` ("Nothing here auto-schedules"), `tools/blueprints.py:1-8, 118-126, 129-158, 161+` (a blueprint is an ordinary SKILL.md with `metadata.hermes.blueprint`; one pure translation to/from the job spec; install registers a dismissable suggestion, never schedules). Halbert: C2 partial (`home/behavior.py:46-58, 207, 289` tracks pattern confidence/dismissed, proposes no job), C3/C18 absent. Verifier lowered C2 to medium: it needs a create-job API that does not exist. Task: `scheduler/suggestions.py` with this exact lifecycle; the `home/behavior.py` confidence-crossing detector proposes; a 3–4 entry starter catalog written from Halbert's own domain (disk, updates, host health — not Hermes's inbox/standup); an optional `schedule:` frontmatter block on the CC-compatible SKILL.md translated by one pure function. Effort M + S + M. Constraint: proposals respect the proactive dial (Off/Quiet/Balanced/Assertive) and are staged, never executed.
- **Batch classifier script: one auxiliary-model call scores a candidate batch, threshold filter, silent-on-nothing, loud-on-failure** (HM16-cron-second-pass-C21) — Hermes `cron/scripts/classify_items.py:1-45` (`_ID_KEYS`/`_VIEW_KEYS`; empty stdout feeds `no_agent` silence; classifier failure exits non-zero). Halbert: absent; `model/utility_slot.py:1-15` is the resolve-never-hardcode ladder it must use. Task: the worked example for C15's `no_agent` lane once it exists; resolve the model through the utility slot. Effort S. Low. Constraint: never bake a model name.
- **Weekly host-state digest: name what was deferred and what could not be checked; read every approved write back before claiming it landed** (HM20-skill-library-C11) — Hermes `skills/productivity/weekly-review-planning/SKILL.md:16, 52, 56, 58-66, 76-81`. Halbert: the verifier corrected absent→partial and raised to medium — `proactive/morning_report.py:55-180` is already this object; missing are exactly the two disciplines: a "coverage gaps / deferred" section (the report lists only what it found) and read-back verification that an approved proposal's write landed (`findings/proposals.py:36-66` records `execution_result`; the report never re-reads the changed record). Task: add both sections to `MorningReportGenerator.generate()`, deterministic; the read-back compares the digest recorded in the change ledger (`LEDGER-1`) to the file now. Effort M. Medium. Constraint: deterministic template; the optional summarizer stays optional.

- **Scheduler dashboard page (list / run history / cancel first; create with client-side validation later) and a run-receipt inspector with a typed failure taxonomy** (OC23-control-ui-C13, OC23-control-ui-C21; C21 is sequenced after C13) — OpenClaw `ui/src/pages/cron/suggestions.ts:1-40` (starter presets WEEKDAY_MORNINGS/EVERY_MORNING/WEEKLY/HOURLY), `ui/src/pages/cron/timezone-suggestions.ts:32-47` (browser tz, UTC, zones already used by jobs, then the full list, deduped in order), `ui/src/lib/cron/index.ts:340-418` (`validateCronForm`: per-kind schedule validation, trigger minimum interval, `^https?://` webhook check, `failureAlertAfter >= 1`, parseable cooldown), `ui/src/pages/activity/run-inspector-model.ts:131-138, 139-160` (`present|not-found|expired|corrupt|ambiguous|unsupported` from structured `reasonCode` values with remediation codes — never from an error string; `mergeDecisionPage` at `:107-129` refuses to merge pages whose execution or context id differ). Halbert: absent, and slightly worse than the reader said — `lib/tauri.ts:209-218` already exports `getScheduledJobs()`/`cancelScheduledJob()` against live endpoints (`dashboard/routes/settings.py:2963-2988`, `dashboard/routes/jobs.py:13-110`) and nothing in the frontend calls them; no page reads `run_receipts`. Two scope corrections from the verifier the packet must carry: Halbert's jobs are registry tasks (`Job.task: str`), so a "starter ideas with ready-made prompts" gallery does not map — the analogue is a picker over `autonomous_tasks.py`'s registered tasks; and there is no create endpoint (`SchedulerEngine.add_job` at `engine.py:67` is not exposed), so "add a create form" is backend work first. I demote the create/edit half from high to medium behind HM16-M2/M5 and founder decision 7; the list + run-history + cancel view stays high because it lands with the cancel fix (HM16-M1) and makes the dead client functions live. Task: a scheduler page under the existing design tokens (no emoji — drop the origin's `emoji` field) showing each job's state, next run, last receipt status and M3's liveness ages, with cancel wired through the fixed route; a receipt drawer classifying a failed lookup as not-found/expired/corrupt/ambiguous from receipt data. Test: frontend vitest for the classifier table; a route test that the page's list call matches `routes/jobs.py`'s shape. Effort L (C13) + S (C21). Constraint: UI commands staged never executed — the create form, when it exists, stages a job for approval, it does not fire one.

#### Theme 7 — Delivery, background-task registry, and side-effect truth

- **Detached background-task registry: execution status and delivery status as two state machines, projected from the agent event stream** (OC09-tasks-system-agent-flows-boards-claws-C1) — OpenClaw `src/tasks/task-registry.types.ts:14-24, 29-37, 127-171` (runtimes subagent/acp/cron/cli; statuses queued..lost; a 7-value `TaskDeliveryStatus`), `src/tasks/detached-task-runtime-contract.ts:19-38`, `src/tasks/task-registry-lifecycle.ts:27, 29-150` (an `onAgentEvent` listener patches status on lifecycle start/end/error, counts tool starts, throttles liveness writes to 60 s, then `maybeDeliver...`), `src/tasks/task-registry.process-state.ts:36-41` (cancel handles are in-process only; records are durable; boot reconciliation follows). Halbert: the verifier corrected absent→partial — `run_receipts.py:15-31, 43, 51-53, 185-198` already separates run success from user delivery and flips dead-pid receipts to `interrupted` (OpenClaw's `lost`). Genuinely absent: one registry across runtimes (scheduled job, subagent, execute_code, terminal block), a per-record delivery state machine, the event-listener projection, and the dashboard's Workloads rail. Task: `tasks/registry.py` on the SQLite state-lease pattern, fed by the turn-event tee (already lifted); deliver through `proactive/gate.py:74-89` `ProactiveGate.should_notify` — do not reinvent `notifyPolicy`. Test: a subagent that dies without reporting becomes `lost`, not `failed`. Effort L. High, but L — the verifier's "the registry is what the Workloads rail needs" is the value case; dispatch after Theme 1 so receipts are trustworthy input.
- **Detect committed outbound side-effects before replaying a crashed turn or run** (OC14-agents-second-pass-A-C18; extends the receipts lift) — OpenClaw `src/agents/agent-command-restart-recovery.ts:19-52` (`resolveCommandRecoveryOptions` restores `disableMessageTool`/`forceRestartSafeTools`/media/`suppressText` from the persisted session entry when `restartRecoveryDeliveryRunId` matches; throws if inconsistent), `:264-268, 279` (`restartUnsafeSideEffectsDetected = hasCommittedOutboundDeliveryEvidence || didSendDeterministicApprovalPrompt` — the verifier corrected the reader's 298-303). Halbert: partial — `run_receipts.py:180-186` concedes "An interrupted run proves nothing about its side effects ... the re-run decision belongs to the wiring layer"; the started-receipt-before-side-effects half exists (`executor.py:646-654`), the evidence the wiring layer needs does not. Task: record `side_effects: {delivered: bool, prompted: bool, writes: [ledger ids]}` on the receipt as they happen (the change ledger already has the writes); boot recovery replays an interrupted run only when the evidence is empty, otherwise marks it `interrupted_with_effects` and surfaces it. Test: an interrupted receipt with `delivered=True` is not re-run at boot. Effort M. High. Also the substrate for A07-G11's founder question (resume a crashed turn?).
- **Notification-level idempotency keys `task-event:{id}:{at}:{kind}`, mirrored into the conversation under the same key** (OC09-tasks-system-agent-flows-boards-claws-C3) — OpenClaw `src/tasks/task-registry-delivery.ts:55-64, 66-70, 298-312`. Halbert: the verifier corrected absent→partial — run-level idempotency exists (`run_receipts.py:200-215`); notification-level does not. Frame as "extend the occurrence-key discipline to the delivery step". Task: key every proactive-event write; dedupe before enqueue; the mirror into the one conversation carries the key. Effort S. Medium.
- **Three-way completion status with `required_delivery`: best-effort succeeds on execution alone; a promised delivery cannot** (OC18-channels-cron-second-pass-C16; extends the status split) — OpenClaw `src/cron/completion-status.ts:25-46, 60`. Halbert: partial. Task: one field beside the status pair; fold into whichever packet touches the task-status shape (C1). Effort S. Low.
- **Refuse a delivery target inherited from a shared last-recipient bucket; express the refusal as not-ok on the single resolution result, before anything durable is enqueued** (OC18-channels-cron-second-pass-M6; verifier-added; prospective) — OpenClaw `src/cron/isolated-agent/delivery-target.ts:293-329`. Halbert: absent; there is no delivery destination on a job yet, but guest personas, sibling homes and federation peers make "announce into the last conversation" ambiguous. Task: record in `DECISIONS.md` now — a scheduled or autonomous message is delivered only to a destination named on the job or carried by its own session, never a globally shared last recipient; a job created while a guest was fronting must not inherit the guest's surface. Effort S (doc). Medium as an invariant.
- **Durable at-most-once delivery outbox — the single-process degenerate form only** (HM16-cron-second-pass-C6) — Hermes `cron/delivery_queue.py:1-8, 76-134, 197-285, 355-382` ("losing a delivery is safer than duplicating a possibly-completed send"; CAS claim; dead owner fenced to `unknown`, never retried). Halbert: absent; one process owns both the executor and the channels (`app.py:1249`), so the cross-process fencing has no subject and the verifier lowered to low. Task: only the reduced form — a delivery row with `pending → attempting → delivered | failed | unknown`, where a crash mid-send is fenced `unknown` and never silently retried. Effort S/M. Low; part of C1's delivery state machine, not a separate queue.
- **Bounded consecutive delivery failures before treating a target as dead; never unsubscribe on a terminal event kind** (HM05-delegation-subagents-kanban-C11) — Hermes `gateway/kanban_watchers_notifier.py:31, 34, 35-49, 395, 527` (`MAX_SEND_FAILURES=12 ≈ 60 s`; subscriptions removed only on the irreversible archived status — the rule the reader missed). Halbert: the verifier corrected to partial: the wake-vs-bookkeeping split already exists and is better placed (`proactive/events.py:96-98`, `routes/being.py:32-40`, `proactive/gate.py:28-49`); residual is `subagent.py:111-122` dropping on the first exception. Effort S. Low (the SSE subscriber is in-process and cannot go transiently dead).
- **Structured delivery routing with a separate failure destination; strict webhook URL normalization** (OC18-channels-cron-second-pass-C14) — OpenClaw `src/cron/delivery-plan.ts:64-132, 160-180` (`resolveFailureDestination`, corrected name), `src/cron/delivery-target-validation.ts:2-9` (present-but-blank rejected at creation), `src/cron/webhook-url.ts:16` (HTTP(S) only, reject embedded userinfo). Halbert: absent. Split per the verifier: the ~20-line webhook normalizer applies now to any outbound URL Halbert accepts from config; the delivery plan waits for user-created jobs. Effort S now, M later. Low.

#### Theme 8 — Hand-offs: verified in this workstream's units, belonging to another seam (listed so no id is dropped)

- **Marker-forgery defense folding lookalike Unicode before scanning** (OC18-channels-cron-second-pass-M5; verifier-added) — OpenClaw `src/security/external-content.ts:24-58, 60-61, 78-112, 114-152, 154-205, 218-270` (fold fullwidth/homoglyph codepoints with an index map back to original offsets; special-token literal removal; `detectSuspiciousPatterns` returns WHICH patterns matched). Halbert: partial and exposed on the side OpenClaw covers — `prompts/agent_prompts.py:284-286` `_CONTROL_TAG_RE` is ASCII-only with `re.IGNORECASE`, and Halbert's own convention (`threads.py:133, 151`; `agent_prompts.py:350, 1246`) replaces its emitted markers with fullwidth lookalikes "so the command stays legible" — so an inbound fullwidth `<continuity>` is not neutralized and reaches the model in a form Halbert asserts is legible. Task: fold-then-match in `_defang_continuity` and `threads.py::_fence`, with code-region awareness. Effort S. Medium. Hand to the prompt/redaction workstream; reached from cron only via `run-external-content.runtime.ts:1-5`.
- **In-flight admission counter with drain-on-retirement — retargeted from the skill registry to `mcp/client.py`** (OC06-plugins-sdk-C7) — OpenClaw `src/plugins/command-execution-lock.ts:33-39, 47-53, 70-73, 82-89`. Halbert: partial — `mcp/client.py:969-988` `disconnect()` already closes the connect race under the per-server lock, but `call_tool` (`:1072-1102`) resolves the connection under the lock at `:1091` and awaits `transport.request` at `:1092-1095` OUTSIDE it, so a reconnect closes the transport under an in-flight `tools/call`. Task: per-server in-flight counter + `asyncio.Event` set at zero, awaited by disconnect/reconnect before close. Effort S. Medium. Hand to the MCP workstream; it matters here because OC16-C8/OC22-C4's thaw reconnect is exactly the reconnect that races.
- **Per-key three-way rollback instead of blind `.bak` restore** (OC20-extensions-memory-knowledge-automation-C16) — OpenClaw `extensions/workboard/src/store-compensation.ts:5-9, 11-26, 28-51, 53-75`. Halbert: absent, confirmed by reading — `tools/write_config.py:72-77, 203, 227, 263` restores a whole-file `.bak` on failure, so a founder's hand edit between apply and rollback is clobbered; `findings/proposal_generator.py:304-306, 384-397` is the caller. Simplify the port to "restore this key only if it still holds the value we wrote". Effort M. Medium (it reverts the founder's own edit on a machine they are using). Hand to the config/ledger seam (`LEDGER-1`); scheduled proposals are its caller.
- **Automated-origin self-trigger guard — retargeted to `config/watcher.py`** (OC20-extensions-memory-knowledge-automation-C17) — OpenClaw `extensions/workboard/src/automation-nudge.ts:10, 57-62, 143-151` (60 s debounce; checks raw `cron:*` and canonicalized `agent:*:cron:*:run:*` forms). Halbert: the verifier found the concrete target — `config/watcher.py:27-38` snapshots the manifest unconditionally on every modified/created/moved/deleted event with no debounce and no self-write suppression, while `write_config.py` writes the file AND a `.bak`, so one agent change fires the handler at least twice, and approved proposals form a write→watch→snapshot loop. The scheduler modules the reader guessed at carry no origin concept. Effort S. Medium. Hand to the config seam.

### Related confirmed audit gaps (same seams; strengthening the packet-03 lift)

A06-scheduler (refuter: 10 of 14 stand; all 7 own-bugs confirmed; two audit failure scenarios need correcting before dispatch — G2's real double-fire is the `:catchup`/`:recovery` id recursion at `executor.py:523` and the catch-up-vs-cron overlap, not the base-job path; G7's real substance is that the daily cron fires get no suppression and the baseline refreshes only at boot):
- A06-G1 [high]: occurrence-level idempotency is never wired — no dispatch path records or checks a scheduled instant (`run_receipts.py:200-215` has zero production callers). Theme 1/7 depend on it.
- A06-G2 [medium]: boot recovery re-run and boot catch-up are uncoordinated (see correction above).
- A06-G3 [high]: slots missed while the process is alive but the machine is asleep are dropped by APScheduler's 60 s misfire grace; catch-up exists only at boot — OC16-C8's gap detector is the fix.
- A06-G4 [medium]: clock rollback is mis-classified as an exhausted restart budget and escalates to persistent safe mode (own-bugs 4 and 6: `restart_budget.py:45-52`, `executor.py:299-306, 349-371` — future-dated stamps retained forever, fsync of file not dir, malformed ledger silently dropped).
- A06-G5 [medium]: `receipts.json` has no retention; every write rewrites and fsyncs the whole file (Hermes `MAX_TERMINAL_EXECUTIONS` 1000, OpenClaw retention 64).
- A06-G7 [low]: monitor-hash gate applied only to boot catch-up, never to the regular fire (Theme 6, C15).
- A06-G8 [low]: the closed status set has producers for `ok`/`error` only — `delivered_to_user`, `delivery_failed`, `blocked_config` are dead vocabulary (Theme 3, C11; Theme 7, C1).
- A06-G9 [medium]: a corrupt `receipts.json` takes the scheduler down at construction (reproduced for a truncated file and a top-level list); the monitor store is contained.
- A06-G10 [low]: no schedule-activation guard (OpenClaw #91944) — Theme 6, M6.
- A06-G11 [low]: no on-disk liveness/lateness markers — Theme 3, M3.
- Refuted, for the record: G6 (pid-only liveness — refuted on packet 03 line 34's recorded decision, now reopened by OC18-M4's boot-namespace argument), G12 (no durable one-shots to retire), G13 (multi-instance matrix — correctly out), G14 (7-day horizon cannot bite a 6-hourly or daily job).
- Own-bugs: (1) registration overwrites the per-job record, erasing last-run facts, and the catch-up completion lands on `<id>:catchup` — reproduced against `SchedulerEngine`; (2) task-level failures are recorded `ok`/`completed` because `autonomous_tasks.py:505-507, 559-561` swallow exceptions into `{'status':'error'}` and `executor.py:660` takes any return; (3) after a wall-clock timeout the thread keeps running with side effects while the receipt says error and the retry decorator starts a second run (Theme 2, C16); (5) guardrail rejection and safe-mode skip leave no receipt (Theme 1, M7); (7) monitor probe output with undecodable filenames raises inside `evaluate()` (`monitor_hash.py:171` strict encode), so the baseline never persists and catch-up runs ungated.

A15-heartbeat-catchup-wiring (9 of 12 stand):
- A15-G1 [medium]: a served catch-up writes only to `<id>:catchup` while `engine.add_job` blanks the parent, so the same slot is re-served on every restart (duplicate morning report per restart inside 12 h); fix with own-bug 1.
- A15-G2 [medium]: occurrence idempotency has zero production callers and no UTC canonicalisation — the mechanism G1/G3 need.
- A15-G3 [low]: recovery and catch-up can arm the same job at boot; NOTE the audit's proposed `_boot_recovery_pending` guard is broken (`executor.py:340` drains that set during registration).
- A15-G4 [medium]: no schedule-change guard although the prior record already carries `str(cron_expr)`.
- A15-G5 [medium]: ticker liveness markers — ruled in by F-4 Option A, unbuilt (Theme 3, M3).
- A15-G6 [low]: a `BaseException` inside a tick silently ends the heartbeat task (Hermes catches `BaseException` at `scheduler_provider.py:412-421`).
- A15-G7 [low]: stopping the heartbeat does not join an in-flight sweep, contradicting `app.py:1626-1628`'s own comment (Theme 5, C7).
- A15-G8 [low]: a failed slot is treated as served forever; no replay after a backoff window.
- A15-G9 [low]: RETIRE never writes the retirement diagnostic (dead code).
- Own-bugs: the 1 s race between delayed scheduler start and proactive registration (`app.py:1234-1315`); satellite guard for catch-up and idle tick read different sources of truth; `<id>:catchup`/`<id>:recovery` records accumulate and inflate `get_status`; retirement diagnostic + occurrence idempotency shipped dead; the monitor-hash probe persists a listing of `~/.ssh` (names, sizes, modes, mtimes) into the data dir (`app.py:437-443, 456`; `monitor_hash.py:179`) — a Tier-2 concern to fix with C15.

A07-interrupt-algebra, on the turn-lock seam: G10 [medium] no turn-liveness watchdog (Theme 4, C2 is the fix); G11 [low] a crash-interrupted turn is healed but never resumed — founder decision first (Theme 7, C18 is the evidence it needs); G12 refuted (safe mode exists) but HM01-C1's CWD and one-way-sync bugs stand; own-bug: `request_stop` mirrors only half of `cancel_session`'s paused rule.

A08-store-hardening, on the unclean-boot seam: G2 [high] reopening over a corrupt index bricks the store; G7 [medium] a store that fails to open degrades silently and permanently; G8 [medium] a zeroed file at the store path bricks every boot — HM11-C2's `quick_check` on a suspicious boot is the detection half these repairs need; HM11-M1 (dashboard digest) is the write-path classifier fix.

### Do not lift

- OC04-commands-C18 (systemd linger escalation): Linux-only; Halbert has no privileged repair path; the three-step shape is a doc note at most.
- HM16-cron-second-pass-C18 (single heap-ordered periodic thread): the origin's justification is 130 subagents × 2–3 threads; 15 idle daemon threads on one host do not repay a 15-subsystem migration. Keep only the exception-isolation idea inside whichever loops Theme 5 touches.
- HM11-gateway-core-C6 (shutdown-signal forensics + supervisor-timeout alignment): everything load-bearing is `/proc`/systemd; on macOS the residual answer is "Tauri or the terminal" and there is no Tauri before-quit grace value to align against. A one-line SIGTERM log with parent pid can ride C7 if wanted.
- HM14-cli-ops-lifecycle-C22 (RLIMIT_NOFILE raise): fails open and is free, but pointless until "too many open files" is observed; port verbatim, config-gated, at that moment.
- HM15-cli-model-config-ux-C10 (content-hash adaptive polling): ~15 lines with no consumer; fold into `utils/retry.py` when a poll-and-react watcher is written.
- OC18-channels-cron-second-pass-C15 (per-run session reaper): no isolated per-run agent sessions exist; the `0`-means-disabled sentinel is the only thing to remember when they do.
- OC17-gateway-second-pass-B-C19 (restart-trace env handoff): nothing to attach to until restart-from-inside exists; the "reject a clock-implausible value" instinct is already in `restart_budget.py:1-12`.
- OC08-daemon-process-worker-hooks-audit-C5 (future-config guard): Halbert's YAML carries no schema-version stamp; attach as a note to whatever packet introduces config versioning. Supports the no-migrations directive when it does.
- HM05-delegation-subagents-kanban-C4 (durable delegation ledger, L): the only production subagent caller is a synchronous deterministic auditor; the pid+start-time pair — the part worth having — is lifted in Theme 1; the registry in Theme 7 (C1) covers the rest when subagents grow.
- HM16-cron-second-pass-C5 (`context_from` output chaining): presupposes per-run output artifacts, which `run_receipts.py:5-8` records as deliberately removed; revisit only if the notepad proves insufficient.

### Proposed packets

1. **SCHED-P1 Receipt truth and job-record authority** — HM16-M1, HM16-M5, OC06-C13 + OC18-M4 + HM16-C17 + HM14-C21 (one change), OC18-M7, HM16-M8, OC14-C18; plus audit A06-G1/G2/G5/G9, own-bugs 1/2/5/6, A15-G1/G2/G3/G4/G8, A15 own-bugs 1 and 3. Prerequisites: founder decision 1 (start-time field); none otherwise. Hot files: `scheduler/executor.py`, `scheduler/engine.py`, `scheduler/run_receipts.py`, `scheduler/catchup.py`, `dashboard/routes/jobs.py`, **`dashboard/app.py`** (boot catch-up closure, shared engine on `app.state`). ROADMAP: MIND-1. Founder decision gates: yes (decision 1, small).
2. **SCHED-P2 Honest timeouts** — HM16-C16, OC18-M1 + OC18-C12, OC18-M2, HM16-M4, HM16-C10, HM07-C19, HM06-C15 (note only); audit A06 own-bug 3. Prerequisites: P1's receipt phase field is convenient but not required. Hot files: `scheduler/executor.py`, `agents/llm_client.py`, **`tools/executor.py`** (process-group kill), `tools/system_info.py`, `mcp/client.py`. Founder decision: none (M2 changes a default from total to idle; note it in the packet).
3. **SCHED-P3 Failure containment, admission and the kill switch** — OC18-C11 + HM16-C14, OC18-M3, HM16-C11, HM16-C8, HM01-C1, OC24-C19, HM16-M9 (record), HM16-M3, HM19-C13; audit A06-G4/G8/G11, A15-G5/G6. Prerequisites: P1 (receipts must be trustworthy before streaks count them). Hot files: `scheduler/executor.py`, `scheduler/job.py`, `autonomy/guardrails.py`, `autonomy/anomaly_detector.py`, `scheduler/autonomous_tasks.py`, **`agents/state_machine.py`** and **`tools/executor.py`** (ESTOP check at admission and dispatch), `findings/store.py`. Founder decision gates: yes (decisions 3 and 5).
4. **SCHED-P4 Turn liveness and local-model priority** — HM01-C2, HM01-C3, HM04-C12, HM02-C16; audit A07-G10. Prerequisites: none (the interrupt algebra and `turn_activity.py` are merged). Hot files: **`agents/state_machine.py`**, **`tools/executor.py`**, `model/client.py`, `tools/execute_code.py`, `agents/turn_activity.py`. Founder decision gates: decision 4 (ceiling default only; not blocking).
5. **SCHED-P5 Process lifecycle — the deferred daemon packet** — in-process, founder-independent core: OC16-C7 + OC15-C16, HM11-C2, HM11-C10, OC22-C11, OC16-C8, OC22-C4, OC17-C4, OC17-C5; gated tail: HM11-C1, HM13-C9, HM11-C5, HM14-C19 + OC02-C3 + OC08-C3 + OC02-C8 + OC09-C17 + OC04-C17; audit A15-G7, A06-G3, A08-G2/G7/G8 (detection half). Prerequisites: HM11-gateway-core-M2 (exit-code vocabulary + supervisor probe, dashboard workstream) before any hard-exit path; HM11-M3 (single-instance lock) pairs with OC22-C11; founder decision 2 for the launchd tail. Hot files: **`dashboard/app.py`** (startup and `shutdown_event`), `dashboard/__main__.py`, `dashboard/parent_watchdog.py`, `mcp/health.py`, `mcp/client.py`, read-only touch of **`conversation_sqlite.py`** for `quick_check`. Founder decision gates: core no; tail yes (decision 2).
6. **SCHED-P6 The scheduled-work surface** — HM16-M2, HM16-M6, HM16-C15 + HM16-M7, HM16-C4 + OC18-C13, HM16-C2 + HM16-C3 + HM10-C18, HM16-C21, HM20-C11, OC23-C13 (list/history/cancel half first), OC23-C21, OC09-C1, OC09-C3, OC18-C16, OC18-M6 (doc), HM16-C6 (reduced), HM05-C11, OC18-C14 (webhook normalizer now); audit A06-G7/G10, A15-G4/G9, A15 own-bug 5 (`~/.ssh` listing). Prerequisites: P1 and P3 (a create endpoint on an engine that cannot cancel is not shippable); founder decisions 6 and 7. Hot files: `scheduler/job.py`, `scheduler/engine.py`, `scheduler/monitor_hash.py`, `proactive/morning_report.py`, `dashboard/routes/jobs.py`, frontend `lib/tauri.ts` and a new scheduler page (shared colour tokens, no emoji), new `tasks/registry.py` with a listener on the turn-event tee (touches **`routes/agent.py`** where the tee is emitted). Founder decision gates: yes (decisions 6, 7; the ESTOP/M9 invariants recorded in DECISIONS.md).

Hand-offs (Theme 8) go to their owning workstreams: OC18-M5 → prompts/redaction; OC06-C7 → MCP; OC20-C16 and OC20-C17 → config/ledger.

### Open founder decisions

1. "Packet 03 ruled out porting the process-start-time check because Halbert is single-instance. The verifier showed boot recovery compares a previous boot's pid against a freshly reassigned pid namespace, so a collision leaves a receipt `running` forever. Do we record `owner_start_time` beside `owner_pid` in run receipts (one field, psutil already present), keeping the multi-instance matrix out?" — Recommended default: yes; amend the packet-03 out-of-scope line to say the matrix stays out and the start-time field is in.
2. "Should Halbert's background work outlive the Tauri app — a LaunchAgent with KeepAlive, restart-on-exit-code, and a self-restart path — or does the scheduler run only while the app is open?" — Recommended default: not yet. Build P5's core with "log CRITICAL and stay alive under no supervisor"; open the launchd tail only when a concrete need (overnight sweeps with the app closed, an FDR-04 self-update restart) is named. Every hard-exit path waits on HM11-M2 either way.
3. "May a standing-order document (scope / triggers / approval gate / escalation, reviewed once) replace the model-produced `requires_approval` and `confidence_threshold` in `scheduler/autonomous_tasks.py`?" — Recommended default: yes; the standing directive that no LLM makes a permission decision already answers it, and the 3-attempts-then-escalate rule replaces the dial with a checkable invariant.
4. "What is the turn force-abort ceiling for a turn that stalls while holding the one conversation's lock, given the tool heartbeat keeps a long command alive?" — Recommended default: 30 minutes idle (Hermes's 1800 s), configurable, aborting through the lifted `request_stop` so subprocesses are not killed silently; a receipt names the abort.
5. "One always-failing job currently trips the global anomaly streak and safe-modes every job. Do we move to per-job auto-disable (10 run failures / 3 schedule errors) with system jobs exempt, and reserve safe mode for cross-job anomalies?" — Recommended default: yes; safe mode stays available as the manual ESTOP (HM01-C1) and for anomalies that are not one job's streak.
6. "Scheduler timezone posture: one configured wall-clock zone everywhere (removing the executor's UTC default), with DST and config changes compared as wall clock and boot catch-up serving an offset-only change?" — Recommended default: yes; no migration of stored rows (there are none to migrate).
7. "Do we open user-created scheduled jobs — a deterministic schedule parser, a create endpoint, a dashboard form — or keep the scheduler registry-task-only with a list/history/cancel view?" — Recommended default: ship the list/history/cancel view and the parser now (the view fixes the dead client functions and the cancel bug together); open creation after P1 and P3 land and the founder names the first job they want, with M9's rule (no inference-slot field on the agent-facing tool) recorded in DECISIONS.md.
