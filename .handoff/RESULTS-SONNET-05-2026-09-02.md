# RESULTS — SONNET-05 CI gates, test rot, licence manifest, knowledge-index plumbing, document resync

**Packet:** `DISPATCH-2026-09-01-SONNET-05-ci-tests-docs.md`
**Branch:** `chore/ci-tests-docs`, fresh worktree at `/Volumes/4TB-BAD/Halbert-worktrees/chore-ci-tests-docs`, off `main` @ `25ba6ab5` (the state right after SONNET-04's merge, per the dispatching session's instructions).
**Date:** 2026-09-02
**Status:** DONE. All six commits are local to this branch (a final verification-only commit, `66ac2bde`, followed the original five after re-checking U6 S3/S4 against code and finding a stale `SE-15` claim in this doc's own review-remediation section, corrected before finalizing — see §7). **Nothing pushed to origin** — confirmed via `git status --short` (clean), `git branch -vv` (no upstream tracking ref), and by never running `git push` at any point this session. `gh` is still not installed on this machine, so the "confirm remote CI status" sub-item of Task 1 was skipped per the dispatching session's explicit deviation note. One pre-existing, out-of-scope test failure remains on this branch (already fixed on `main` after the fork point) — see §1.

Note on scope: while this packet ran, a separate concurrent session kept committing an unrelated "alignment review" workstream directly to `main` (`ROADMAP.md`, `DECISIONS.md`, `documentation/design/CORE-CONCEPTS-AND-ALIGNMENT-2026-09-02.md`, and more — `origin/main` had reached `5c49c8c1`, 41 commits past this branch's fork point, by the time this packet finished). None of that was touched, reverted, or incorporated here, per instruction — this branch's merge-base with `main` stays exactly `25ba6ab5`.

---

## 1. Commits (6, in order)

| SHA | Subject |
|---|---|
| `bc33d25f` | ci: install vision extra, align Node 22, add Rust and marketing-v7 jobs |
| `266b9bca` | fix(test): scope order-dependent tests, fix App Store licence-gate parser |
| `f63e61ec` | feat(rag): wire assigned_to_role for skill routing, land the CUDA doc in the real corpus |
| `53d6c09d` | docs: resync FEATURES.md and 21 handoffs against what actually shipped |
| `5db950d0` | docs(handoff): resync MASTER-TODO.md against RESULTS-* docs, not the stale audit |
| `66ac2bde` | docs(handoff): verify U6 S3/S4 done against code, not just ROADMAP pointer |

Test counts, before this packet's changes (verified fresh at branch creation, matching the dispatching session's own just-verified baseline exactly): **18 failed / 4,711 passed / 40 skipped** (`test_corpus_license_gate.py` 2, `test_cv_extensions.py` 11, `test_llm_config_parse_cache.py` 2, `test_multi_instance.py` 1, `test_vision_tools.py` 2).

Test counts, after, `halbert_core/tests` only (run repeatedly through the session): **0 failed / 4,755 passed / 14 skipped.** The 26 skip-count drop is the `vision` extra unskipping unrelated cv2-dependent tests elsewhere in the suite.

Test counts, after, the full CI-representative surface (`halbert_core/tests` + root `tests/` together, matching what `ci.yml`'s two pytest steps actually cover, run once at the very end after all 6 commits): **1 failed / 4,900 passed / 14 skipped.** The 1 failure — `tests/test_legal_metadata.py::test_all_first_party_sources_have_spdx_headers` (flagging `build_aec_arm64.sh` + `test_terminal_e2e.py`) — is **pre-existing and out of this packet's scope**: verified it is unrelated to anything this packet touched, and confirmed it is **already fixed on `main`** at `382eadbe4` ("chore(legal): add SPDX headers to two untagged first-party files (DEV-02)", 2026-09-02, a different concurrent session) — that commit landed on `main` after this branch forked at `25ba6ab5`, so it isn't present here. Not fixed in this branch (out of scope, and fixing it here would just be redone by that commit on integration); flagged rather than silently left unexplained. 10 new tests this packet added (`test_nvidia_cuda_knowledge.py` ×6, 4 new in `test_sourceprep_setup.py`) are included in both counts above and all pass.

Frontend, re-verified in this worktree after `npm install` (fresh worktree, no `node_modules`): dashboard `vitest` 86 files / 833 passed, `tsc --noEmit` clean; design-system `vitest` 74/74, `tsc --noEmit` clean. `scripts/check_contrast.py`, `gen_shadcn_theme.py --check`, `check_literal_colors.py --check` (211, none gained), `vendor_fonts.py --verify` all pass. `marketing/web-v7`: `npm install && npm run build` succeeds. The new Rust CI job's command (`cargo test`, default features, `src-tauri/`) was run locally first: 24 passed, 0 failed.

**Pre-existing gap found, not fixed (out of scope):** `scripts/sync_fonts.py --check`, the last step of the `design-tokens` CI job, fails on *any* fresh checkout — `packages/design-system/public/fonts/` is entirely gitignored (`**/public/fonts/`) and nothing in `ci.yml`'s `design-tokens` job (or any `npm install`/build step) populates it before the check runs. Confirmed this is not caused by anything in this packet: verified the directory is empty in the main checkout too, and no `package.json` script anywhere invokes `sync_fonts.py`. Flagging for whoever owns CI next; not in this packet's task list.

---

## 2. Task 1 — CI

- `pip install -e "halbert_core/[dashboard,dev,vision]"` — the `vision` extra was missing, causing 13 of the then-red tests. Verified locally: those 13 now pass with the extra installed in the shared `.venv`.
- Node `20` → `22` in all three node-based jobs (`design-system`, `model-picker`, `dashboard-frontend`), matching `.nvmrc`.
- New `rust-tauri` job (`macos-latest`, `dtolnay/rust-toolchain@stable`, `cargo test --locked` in `src-tauri/`) — 28 `#[test]`s never ran in CI before; 24 actually execute (some `#[cfg(test)]` blocks may be feature-gated behind `voice-capture`/`aec`, not investigated further). Cargo registry/target cached via `actions/cache`.
- New `marketing-web-v7` job (`ubuntu-latest`, `npm install && npm run build`) — no test/typecheck existed for this app before; a build smoke is the cheapest gate against a `@halbert/design-system` API break going unnoticed.
- `halbert_core/halbert_core/dashboard/frontend/package.json` gained a `"typecheck": "tsc --noEmit"` script, so the root `npm run typecheck` (`--workspaces --if-present`) actually reaches the dashboard instead of silently skipping it.
- `halbert_core/halbert_core/dashboard/frontend/e2e/README.md` (new): documents the two Playwright smoke scripts (`plan-b.smoke.mjs`, `continuity.smoke.mjs`) and why they stay out of CI (need a live backend/model).
- YAML validated with `python3 -c "import yaml; yaml.safe_load(...)"` after every edit.

## 3. Task 2 — Test rot and the licence gate

- `test_llm_config_parse_cache.py`: the `parses` fixture patched `yaml.safe_load` process-wide (`store.yaml` is the same module object every other `import yaml` sees), so it also counted `load_being_config`'s per-turn reload — a real, separate perf issue this test wasn't meant to measure. Rewrote the counter to filter by the immediate caller's frame (`caller.f_code.co_filename == store.__file__`), scoping it to `llm_config.py`'s own parses. 15/15 pass now (was 13 pass / 2 fail).
- `test_multi_instance.py::test_instance_info_home`: role now derives from `cognition_wiring._get_variant()` (being.yml > `HALBERT_VARIANT` env > `sysadmin`), not `persona_id` — confirmed correct per REV-03 F8. The test set only the env var; on the machine this ran on, the developer's own real being.yml has an *explicit* `variant: sysadmin`, which wins over the env var per that same priority chain, so the test failed regardless of the env-var fix everyone assumed would be enough. Isolated `HALBERT_CONFIG_DIR` to an empty `tmp_path`, matching the pattern `test_capabilities.py::TestVariantResolutionFollowsEnv` (added by SONNET-03 this same round) already established. 27/27 pass now (was 26/27).
- `test_llm_discover.py` and `test_peer_tool_proxy.py`: already fixed by SONNET-03 and (apparently) OPUS-03 respectively before this packet started — verified, no action needed.
- `scripts/check_appstore_deps.py::parse_pyproject`: the self-referential extras `light = ["halbert-core[dashboard]"]` and `full = ["halbert-core[rag-legacy,dashboard,cloud-apis,vision]"]` were parsed as third-party dependencies named `halbert-core`, which have no (and should have no) licence-register entry — this alone failed the gate before any real dependency was even considered. Added `_project_name()` (reads `[project].name` from the TOML text) and `_is_self_reference()`, applied in both the `tomllib` and Python-3.10-fallback parsing branches.
- `config/dependency-licenses.yml`: registered the 9 real dependencies the parser fix exposed — see §4 below for sourcing.
- `documentation/legal/LEGAL-AND-LICENSING-TODO.md`: the "Currently passing" claim about the App Store licence gate was false from roughly 2026-08-29 (the checker bug + missing entries) until this packet fixed it — corrected with a dated note.
- `test_corpus_license_gate.py`: 51/51 (was 49/51 — the two `test_real_dependency_manifests_pass_the_app_store_check` / `test_every_declared_dependency_is_registered` failures).

Explicitly not touched, per the packet's coordination notes (already fixed elsewhere or owned by another packet): the REV-06 seam tests, `speaker_role` doubles, `meta_tools` CRAG kwarg expectations (OPUS-01), the registry autouse fixture (SONNET-03).

## 4. Licence classifications — sourcing

All nine were previously unclassified, failing the App Store dependency gate outright (`no entry in config/dependency-licenses.yml`). None were guessed; each is sourced below.

| Dependency | Ecosystem | Licence | Source |
|---|---|---|---|
| `mss` | python | MIT | PyPI JSON metadata (license field + OSI classifier) |
| `opencv-python` | python | Apache-2.0 (own code) | PyPI metadata + `opencv/opencv-python#353` — **flagged, not resolved**: wheels are documented to bundle FFmpeg (LGPL-2.1) and, on non-headless Linux, Qt5 (LGPL-3.0). This register's SPDX model only sees the package's own declared licence, not what a wheel bundles — it cannot catch this mechanically. Two questions need a founder legal call before shipping the `vision` extra in an App Store build: whether the macOS wheel used bundles FFmpeg at all (vs. a `WITH_FFMPEG=OFF`/AVFoundation build), and if so whether it's linked dynamically (satisfies LGPL) or statically. See `DISPATCH-2026-09-01-FOUNDER-DECISIONS.md`. |
| `sherpa-onnx` | python | Apache-2.0 | PyPI metadata |
| `openwakeword` | python | Apache-2.0 (own code) | PyPI metadata (OSI classifier) — **flagged, not a copyleft-code issue**: its documented default pretrained wake-word models use CC-BY-NC-4.0/CC-BY-NC-SA-4.0 (training-data restriction), a content-licensing question this checker's SPDX model can't see at all (it only reads declared package manifests, not runtime-downloaded model weights). Halbert's own extra comment implies a custom-trained model, not the stock ones — needs confirmation before shipping. See founder-decisions doc. |
| `pyacoustid` | python | MIT | PyPI metadata (SPDX field). Needs the separately-installed `chromaprint` C library at runtime (not a pip dependency, never appears in any manifest this checker parses) — chromaprint's own code is MIT but incorporates LGPL-2.1 FFmpeg fragments. Not a blocker: nothing in this repo bundles a compiled chromaprint binary, users provide their own. |
| `cpal` | rust | Apache-2.0 | crates.io metadata |
| `webrtc-audio-processing` | rust | BSD-3-Clause | crates.io flags this "non-standard" — turned out to be a Cargo.toml packaging quirk (`license-file = "COPYING"` instead of an SPDX `license` string), not unusual terms. Fetched both COPYING files directly (the Rust crate's own, and the vendored C++ submodule's — a PulseAudio fork of Google's original WebRTC AudioProcessing module): both are Google's verbatim 2011 BSD-3-Clause text. Resolved, no founder action needed. |
| `@halbert/design-system` | npm | `LicenseRef-Halbert-FirstParty` | First-party monorepo package, same author, same convention as the pre-existing `haloysius` entry |
| `@halbert/model-picker` | npm | `LicenseRef-Halbert-FirstParty` | Same as above |

## 5. Task 3 — Knowledge index plumbing

- `data/knowledge/linux/nvidia_cuda_compatibility.md` (a directory nothing stages or indexes — the real corpus is `data/{linux,macos,bsd,common}`) moved into `data/linux/nvidia-docs/nvidia_cuda_compatibility.jsonl`, matching the JSONL-per-topic convention already used by its sibling `nvidia_cuda_docs.jsonl` in the same directory. Confirmed `rag/jsonl_to_markdown.py::convert_corpus`'s `data_dir.rglob("*.jsonl")` discovery walk actually finds it (it walks all of `data/`, not just `data/linux`). Matrix updated to 2026 values (580.x / CUDA 13.0 as newest production, per `.handoff/GPU-DEEP-SCAN-REBUILD-PLAN-2026-08-29.md` Step 7). `data/manifest.json`'s `vendor_and_distro_docs.document_count` and top-level `total_documents` incremented (82→83, 28956→28957; both re-verified to sum correctly). `dashboard/routes/gpu.py`'s stale docstring pointing at the dead path corrected.
  - **Important catch**: the new `.jsonl` file was initially invisible to `git status` — `.gitignore` has a blanket `*.jsonl` rule with only two narrow exceptions (`data/macos/support/*.jsonl`, `data/non-commercial/**/*.jsonl`), neither of which covers `data/linux/`. Force-added with `git add -f`. A naive fix here would have silently produced a file that never ships.
  - New `halbert_core/tests/test_nvidia_cuda_knowledge.py` (6 tests): proves the old directory is gone, the record lives on the real discovery path, its schema and licence metadata are well-formed, the matrix content is current, `gpu.py` no longer points at the dead path, and — the load-bearing one — feeds the record through the REAL `convert_jsonl_file` staging function (not the daemon) and confirms the generated markdown carries the 2026 matrix.
  - **Not done, founder-queued**: re-embedding the corpus (stage 2 of `scripts/staged_knowledge_embed.py`, or a full `SourcePrepSetup().apply()`) — the packet explicitly forbids running this, and the daemon was reported wedged this session anyway.
- `sourceprep_template.yml`: added `assigned_to_role` to the `host` scope (`config-ops`) and the four `*_admin` scopes with a genuine matching skill (`network_admin`→`network-ops`, `service_admin`→`service-ops`, `storage_admin`→`storage-ops`, `security_admin`→`security-ops`). Traced the actual mismatch carefully before mapping anything: `config/roles.py`'s `ROLES` dict names 9 `*_admin` role-scopes (a different, underscored vocabulary from the skills' hyphenated `-ops` roles), and `skills/builtin/*/SKILL.md` names 8 roles total. Only 4 have a genuine 1:1 counterpart on both sides. `credentials_admin`/`shell_admin`/`package_admin`/`boot_admin`/`sharing_admin` are real, file-backed scopes with no matching skill yet (documented inline, left unmapped rather than guessed at); `discovery-ops`/`frigate-ops`/`home-ops` are skills with no `config/roles.py` entry or `config/scopes/*.yml` manifest at all — nothing in this template's `host`/`knowledge` trees is staged *for* them, so mapping them to any scope would have been actively wrong.
  - 4 new tests in `halbert_core/tests/test_sourceprep_setup.py`: every `assigned_to_role` in the template is a real skill role (catches drift either direction), no two scopes share one (the daemon's `_roles_to_scope` dict would silently drop one), the four+one intended mappings are exactly right, and — end to end through the real `SourcePrepRetrievalBackend.resolve_role()` code (fed a fake `client.list_scopes()` built from the template's own scopes, not the live daemon) — confirms it resolves the right scope for every mapped role and returns `None` cleanly (not an exception, not a wrong scope) for the three deliberately-unmapped ones.
  - **Not done, founder-queued**: re-applying the template to the live daemon (`RAG-05`).
- `sourceprep_setup.py::_reconcile_scopes` (`RAG-07`): documented, not fixed — the real `GET /projects/{id}/scopes` response carries `path_count` (an int) per scope, not a `paths` list, so the diff this method computes always reads "nothing currently assigned" and can never remove a path a template stopped listing. No per-scope detail endpoint exists client-side to fix it against, and confirming whether the daemon has one needs a live, healthy daemon to probe — deferred (daemon reported wedged this session). Extensive docstring added explaining the exact mechanism.
- `RAG-10`: deleted `.handoff/CODEINDEX-BUILD-LOCK.txt` (its PID, 66131, confirmed not running via `ps -p`) and corrected `HANDOFF-STAGED-CODEINDEX-BUILD-2026-08-25.md`'s stale "no build has run" status (it ran and finished 2026-08-26, 71,050 chunks, per `HANDOFF-SCOPE-FILTER-REVIEW-2026-08-26.md`).
- `RAG-01`: **reported, not touched**, per the packet — confirmed still present and slightly larger than the original audit found: `/Volumes/4TB-BAD/HumanAI/CoDRAG` has uncommitted changes in `search.py` (+85), `models.py` (+1), `core/index.py` (+6, matches the original audit exactly), plus two more files that have drifted uncommitted since the original audit (`scripts/dev.sh`, `src/prep/dashboard/vite.config.ts` — not reviewed for content, just flagged as present). A clean daemon restart reverts Halbert's retrieval to 1-chunk file-head responses until these are committed.

## 6. Task 4 — Document corrections

Full list of 24 documents changed is in the commit `53d6c09d` message. Highlights beyond what's already covered in §5 above:

- **`documentation/FEATURES.md`** — rewritten section by section, each claim re-verified against current code (route files, `Settings.tsx` + its tab components, `AgentChat.tsx`, `dashboard/routes/settings.py` for the Anomaly/Recovery/Dry-run endpoints). Removed eight Backend API endpoints that don't exist; corrected the scanner table (GPU/Development/Containers are dashboard route modules, not discovery scanners); Settings/Chat/Terminal/Debug rewritten to the current UI (eleven Settings tabs across five sections, lazy-mounted; three-panel shell with `PresencePill`; inline terminal tiles). Anomaly Detection / Recovery Playbooks / Dry-run Simulation confirmed backend-only via a frontend grep (zero matches on any of their route paths).
- **`documentation/RAG-DATA-SOURCES-2026-08-24.md` §1.1** — this turned out to be a bigger finding than the original audit's "13 files gitignored" framing suggested: those 13 files are **four entire manifest sources** (`arch_wiki` 2,397 docs, `tldr_pages` 7,049 docs across four platform subdirectories, `macos_man_pages` 5,280 docs, and six of `common_tools`'s seven files), summing to exactly 71 MB and 13 files — matching the original estimate precisely once the actual file list was verified, but revealing it's roughly **half the corpus's claimed 28,869 documents**, not a marginal gap. A fresh `git clone` today silently builds half a knowledge base with no error. Full file list and sizes are in the corrected doc.
- **`IMPL-PLAN-SINGULAR-ENTITY-TASKS-2026-08-31.md`, `HANDOFF-VOICE-MODE-OPUS-RESULTS-2026-09-01.md`, `HARDWARE-VALIDATION-MATRIX-2026-08-31.md`, `REV-03-RESUBMISSION-2026-08-31.md`** — corrected against OPUS-02/OPUS-03's actual results docs rather than the original audit's pre-fix findings, per the dispatching session's explicit instruction to re-check rather than trust this packet's own (pre-dated) claims.
- **`documentation/GAPS.md`, `RAG_AUDIT_REPORT.md`, `documentation/design/prebuilt-knowledge-index.md`** — moved to `documentation/archive/` with banners (ChromaDB-era, describe an architecture SourcePrep/CodeIndex replaced).

## 7. Task 5 — `MASTER-TODO.md` resync

Rewrote §0 (struck all six Ultracode batches with accurate, sourced status), added a new §2a "Review remediation backlog" (one row per `REV-04/05/06/08/09/10/11` finding, its packet owner, and done/open status — the finding-id granularity the concurrent alignment-review session's own `ROADMAP.md` doesn't carry), reduced §3 to per-subsystem pointers into §2a/`ROADMAP.md` (its own prior banner, written by that concurrent session, had already called §0/§3 stale and asked for exactly this), added rows for Voice Mode visual UI / singular entity / branch hygiene / CI status, moved founder-gated items to a pointer at `DISPATCH-2026-09-01-FOUNDER-DECISIONS.md`, and added the Rust section's `HA-01` deferral note (the section's own "Recommended start: R0... can begin now" line contradicted the founder's 2026-09-01 direction).

**One real error caught and fixed during a self-review pass before finalizing**: an earlier draft of §2a's REV-10 table marked `SE-15` (UI pairing cannot succeed — mDNS list hardcoded empty, manual entry throws, token never reaches the other machine's `being.yml`) as `~~Done~~ per OPUS-03 pairing work above`. Re-checked against OPUS-03's actual fixed-finding table in `RESULTS-OPUS-BATCH-2026-09-01.md` (`ROUTE-01`/`SE-16`/`SE-09`/`SE-08`/`R05-F1`/`FED-01` only — `SE-15` is not in it) and the original state-of-work audit (which lists `SE-15` as a distinct P0, separate from `SE-16`'s backend pairing-security fix): confirmed `SE-15` is genuinely still open. Corrected in `MASTER-TODO.md`'s §2a table, its own singular-entity summary bullet, and `IMPL-PLAN-SINGULAR-ENTITY-TASKS-2026-08-31.md`'s corrected Status block — all three consistent now. Also added a missing `SE-10` (`PeerToolProxy` never injected) row that had been omitted from §2a entirely.

Base for all of this: the actual `RESULTS-OPUS-BATCH-2026-09-01.md` and `RESULTS-SONNET-01/02/03/04-2026-09-02.md` docs, read in full before writing anything — not the original packet text's predictions, per the dispatching session's explicit instruction.

## 8. Founder-queue items (all recorded in `DISPATCH-2026-09-01-FOUNDER-DECISIONS.md`)

1. `LEG-GATE` resolved except two flagged licence questions: `opencv-python` bundling LGPL FFmpeg in its wheel; `openwakeword`'s default pretrained models being CC-BY-NC.
2. `RAG-05`/`U4-14` — re-apply the `sourceprep_template.yml` role mappings to the live daemon.
3. `U4-08` — re-embed the corpus so the CUDA compatibility doc is actually retrievable (code/data side is done; the embed run itself is not).
4. `RAG-01` — commit the daemon-side scope fixes in the CoDRAG checkout (uncommitted, growing).
5. `RAG-13` — now known to be ~half the corpus, not a marginal gap; decision needed (track vs. publish+download).
6. `RAG-07` — daemon API limitation (`path_count` not `paths`), needs a CoDRAG-side response-shape change or confirmation a fix already exists that this session couldn't safely probe for.
7. Daemon health — SONNET-01's finding that `/status` hangs and `pipeline/status`'s `any_running` flag was stuck for over an hour stands; nothing in this session restarted it (shared infrastructure, ~9 other concurrent `prep mcp` clients observed).

## 9. Confirmed not pushed

`git status --short` is clean on `chore/ci-tests-docs`. `git push` was never run this session, for `main`, this branch, or anything else. `origin/main` is unchanged by this packet (still whatever the concurrent alignment-review session left it at — verified at `5c49c8c1` as of this doc, 41 commits past this branch's `25ba6ab5` fork point, none of them touched here).
