# DISPATCH OPUS-02 — Voice chain: make spoken input and spoken output actually reach the agent

**Owner:** an Opus session. **Effort:** high. **Why Opus:** the work is wiring across app bootstrap, the channel-capability seam, the audio pipeline, Wyoming, and the frontend; the REV-09 report and today's audit agree the loop has never run end to end.
**Parent:** `.handoff/HANDOFF-STATE-OF-WORK-2026-09-01.md` §6.2. Evidence ids: `VM-STT`, `VM-22`, `VM-27`, `VM-15`, `VM-18` (F10); `U2-05`, `U2-07`, `U2-09`, `U2-14..27` (F13); `R3-F03`, `R3-F04`, `R3-F10b`, `R3-F01b`, `R3-F02-T`, `R9-F01..F15`, `R9-P5` (F7); `U6-BUG-03`, `U6-BUG-04` (F6). Reports: `REVIEW-RESULTS-REV-09-2026-08-31.md`, `REVIEW-RESULTS-REV-03-2026-08-31.md`, `REV-03-RESUBMISSION-2026-08-31.md`, `documentation/design/16-voice-mode-visual-ui-implementation-plan.md`, `HANDOFF-VOICE-MODE-OPUS-RESULTS-2026-09-01.md`.

## Shared rules
- Fresh worktree off main (`-b fix/voice-chain`). `git branch --show-current` before every commit. No trailers.
- Python tests ONLY via `arch -arm64 /Volumes/4TB-BAD/Halbert/.venv/bin/python wt_pytest.py <paths>`; frontend via `npx vitest run` / `npx tsc --noEmit` in `halbert_core/halbert_core/dashboard/frontend`; design-system likewise in `packages/design-system`. Baseline 71 python failures; no new ones.
- `sherpa-onnx`, `openwakeword`, the `wyoming` package and Piper voices are NOT installed in `.venv`; all 185 voice backend tests use stubs. For the real-audio step (Task 9) install the extra in a separate venv or ask the founder for the machine — do not pip-install into the shared `.venv`.
- Founder rules: never write "Sovereign"; never name AI models in UI copy; H2/H3 for sibling apps; no emoji in UI; colours from tokens.
- Files you own: `halbert_core/halbert_core/integrations/channel_capability.py`, `integrations/wyoming_agent.py`, `integrations/modality_wiring.py`, `integrations/app_seam.py` (channel-capability construction only), `audio/**`, `dashboard/routes/audio.py`, `dashboard/routes/tts_egress.py`, `prompts/agent_prompts.py` (defang only), `integrations/frigate/**`, `integrations/home_assistant/ha_event_mapper.py` (reference only), `dashboard/app.py` lines that bootstrap the coordinator/Wyoming (`:654-722`, `:753-770`), frontend `pages/VoiceMode.tsx`, `hooks/useVoiceModeMachine.ts`, `components/voice/**`, `components/audio/**` (logic; SONNET-04 does colour classes). Do NOT edit `agents/state_machine.py` (OPUS-01) — if a state-machine change is needed, write the test and hand the one-line change to OPUS-01 in your results.

## Task 1 — `U2-15` / `R9-F05`: production can never resolve VOICE (P0, unlocks everything downstream)
`channel_capability.py:150` does `from ..dashboard.routes.audio import get_audio_pipeline` — no such function exists (the coordinator lives at `request.app.state.audio_coordinator`, `routes/audio.py:121`; bootstrap at `app.py:673`); so `_get_pipeline()` is always `None`. `set_audio_pipeline` / `set_wyoming_active` have zero callers; `app_seam.py:418` builds `HalbertChannelCapability()` with defaults → `has_speaker()` False → `should_speak()` never true → `speech_segment` events, the O3 TTS egress hook, the `/voice` speaking state and the HUD relay are unreachable. `build_modality_context`'s `speaker_role` parameter is dead (`modality_wiring.py:245-324`). Fix: after coordinator start in `app.py`, call `get_app_seam().get_channel_capability().set_audio_pipeline(coordinator)` (or define `get_audio_pipeline` reading `app.state`); wrap Wyoming turns with `set_wyoming_active(True/False)`; thread `ctx.speaker_role` into `build_modality_context`. Test: with a running (stub) coordinator + TTS, a turn emits `speech_segment` **without** monkeypatching the modality. The results handoff's "O3 TTS egress end-to-end" claim is only true under a mocked modality today.

## Task 2 — `VM-STT`: spoken input never becomes an agent turn (P0)
`coordinator.on_voice_turn` is declared (`audio/pipeline.py:103`) and invoked (`:443`, `:528`) but `app.py:654-722` only sets `on_acoustic_event` (via `attach_acoustic_bridge`); `pipeline.get_status()` deliberately excludes transcript text (`:556-557`); `VoiceMode.tsx:32-35` and `:333-338` say the STT observation channel is not live and submit nothing on end-of-speech. So "Tap to speak" ends with an empty turn and the on-screen keyboard is the only working input. Wire `on_voice_turn` to deliver `VoiceTurnObservation.text` to the browser session (per-session SSE/WS or a status-style channel keyed by session) and call `submitTurn(transcript)` in `VoiceMode.tsx`. Tests: backend observation → channel; frontend transcript → `sendMessage`. Doc 16 defers STT in O7's text and no task owns it — you do now.

## Task 3 — `R9-F01` Wyoming server unauthenticated on `0.0.0.0:10400` (P0 security)
`wyoming_agent.py:37` `DEFAULT_HOST='0.0.0.0'`, `:50` `enabled=True`, `:62` `WYOMING_ENABLED` default `'1'`; `app.py:753-770` starts it whenever `wyoming_cfg.enabled` (no capability gate); `_handle_client` (`:236-330`) accepts any transcript; turns run at `speaker_role='unknown'` whose RoleGate cap is MEDIUM without confirmation. Fix: default to `127.0.0.1` (or disabled) unless a shared token is configured; require a token in transcript frames (reuse `PeersConfig` tokens); gate startup on a capability; consider confirmation for MEDIUM tools on unknown voice turns. `R9-F12`: `wyoming_agent` and `WyomingIngress` both default to port 10400 — separate them.

## Task 4 — `R9-F02` second event loop clobbers the shared state machine (P1)
`app.py:763-765` runs Wyoming on `asyncio.new_event_loop()` in a daemon thread; `wyoming_agent.py:231-232` uses the dashboard's `get_agent()` singleton; `state_machine.py:341-345` builds a fresh `asyncio.Lock` per loop, so voice and dashboard turns interleave on one machine. Submit voice turns to the uvicorn loop via `asyncio.run_coroutine_threadsafe` (or make the Wyoming thread enqueue-only), or give the voice channel its own `AgentStateMachine`. Also `R06-F6` (with OPUS-01): the Wyoming turn abandons `agent.process()` without `aclosing`, holding the turn lock until GC. And `R3-F10b`: `_close_safely` (`:373-377`) calls `Server.aclose()`, which does not exist on Python 3.10 (AttributeError reproduced; the TCP server is never closed) — use `self._server.close()` then `ensure_future(self._server.wait_closed(), loop=self._loop)`; two-loop stop test.

## Task 5 — Audio pipeline correctness (P1)
- `R9-F03`/`U2-14`: `audio/pipeline.py:334` slices 960-byte frames (480 samples) while Silero needs 512 (`audio/speech/vad.py:31`, `:164-166` returns False below 512; `:175` `flush()` per frame) → `is_speech` always False; LIVE since `d967cc8b` bootstraps the coordinator in `app.py`. Slice 1024-byte frames or accumulate to 512; stop per-frame flush; keep one segment stream; regression test with a fake VAD that records chunk lengths. Blocks wake, ASR turns, speaker-ID and VAD barge-in.
- `R9-F04`/`U2-18`: enrolled voiceprints never loaded — `routes/audio.py:195-208` persists via `SpeakerProfileStore.enroll` only; `:236` builds a fresh `SpeakerIdentifier()` with an empty manager; `pipeline.py:265` likewise. Load `store.list_all()` on identifier construction; enroll/remove through the identifier; seam test. Then `R9-F06` (enrollment role overrides biometric confidence bands).
- `R3-F04`: `wyoming_agent.py:291` reads `payload_length` from `data{}`; the header carries it (`audio/ingress/wyoming_ingress.py:75`). Reproduced: one canonical audio-chunk frame → 8 "Invalid JSON" warnings and no pong. Reuse `read_wyoming_frame`; test header+PCM+ping → pong. `R3-F03`: describe→info payload keys not per spec. `R9-F10`: non-canonical JSONL dialect.
- `R9-F13`: blocking ONNX inference and per-sample loops now run on uvicorn's loop — move to a thread/executor.
- `R9-F07`: Piper synthesises the whole clip before the first chunk so barge-in cannot abort generation (per-segment mitigation exists) — stream per sentence. `R9-F08`: HIGH-risk confirmation unreachable from a voice turn (satellite hears a non-sequitur) — route the confirmation prompt through the voice channel. `R9-F11`: wake-word runs on the single VAD-trigger frame.

## Task 6 — Output text and prompt hygiene (P1/P2)
- `U2-05`: `wyoming_agent.py:182-196` joins `response_chunk` text and returns it raw — HA TTS reads `## headers`. Apply `_strip_markdown_for_speech` (+ `apply_pronunciation`) in `_process_agent_turn`; test. (The stripper exists only inside `wyoming_agent.py`; the packet's `audio/speech/text_preprocessor.py` was never created — either extract it or leave it, but cover `tts_engine.py` too.)
- `U2-07`: `prompts/agent_prompts.py:197` `_CONTINUITY_TAG_RE` covers only `<continuity>`; extend `_defang_continuity` to `<speech>`, `<text>`, `<modality_context>` so tool stdout/history rows and the no-engine path are covered; `TestLiveDefangEntryPoint` case.
- `U2-09`: the engine's `ModalityAwarePromptBuilder` (`<modality_context>` XML) is never called by Halbert — wire it (`U2-08`) or document that the modality context reaches the prompt another way.
- `VM-18`: one dead AEC privacy switch remains in `AudioSettings`.

## Task 7 — Frigate follow-ups (P2, from the U6 continuation handoff)
- `U6-BUG-03`: `integrations/frigate/frigate_event_mapper.py:120` `_pending_events: List = []`, `:135` append with no cap (HA's mapper got `MAX_PENDING_EVENTS=500` drop-oldest in `7d01720e`); drained only by the cognition tick (requires haloysius) or the never-instantiated `HomeCognitiveLoop` → unbounded growth on installs without haloysius. Mirror the HA cap + test.
- `U6-BUG-04`: `frigate_tools.py:239-240`, `:256-257` return `data:image/jpeg;base64,…` strings; `state_machine.py:2492`/`:1621` route to `ctx.images` only for `dict['image']` — return the vision-tool contract dict. Test on the tool side; if a state-machine assertion is needed, hand it to OPUS-01.
- `R3-F01b`/`LOOP-01`: `HomeCognitiveLoop` is still never instantiated (founder decision; a wiring draft is saved at `.handoff/audit-2026-09-01/drafts/` after SONNET-02 runs). `R3-F02-T`: no tests cover the chat-path AutonomyGate or the `/home/service` 403 — add them.

## Task 8 — Decisions to surface (do not decide alone)
`VM-22` Python consumer for the Rust AEC loopback socket 18400 (nothing references it; doc 16 Decision 1 made the browser the audio terminal — recommend marking `audio_capture.rs` dormant); `FDR-08` `macos-private-api` App Store channel; `VM-01/02` parked product calls (gUM-denied as machine error; quiet-dial suppression of severity-2 wakes). `R9-P5` Rust AEC capture has no consumer.

## Task 9 — Verification the feature has never had (`VM-27`, `VM-15`)
Minimum to call Voice Mode "tested": (1) on one machine with the sherpa-onnx extra + a Piper voice, `audio_config.yml` enabled, open `/voice` in Chromium, confirm TTS audio + mark sync on a keyboard turn; (2) after Task 2, confirm a spoken turn round-trips; (3) hand the 22-row hardware matrix (`HARDWARE-VALIDATION-MATRIX-2026-08-31.md`, all cells empty; its "not yet built" notes are stale) to the founder for the N150 + touch panel. Add one opt-in integration test gated on `sherpa_onnx` importability.

## Results
`.handoff/RESULTS-OPUS-02-<date>.md`: per task commits and tests; what a real-audio run showed; the decisions list with your recommendation; any state-machine change handed to OPUS-01.
