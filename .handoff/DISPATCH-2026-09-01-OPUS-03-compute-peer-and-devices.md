# DISPATCH OPUS-03 — Compute peer, Devices page, pairing, federation 9.3: make the singular-entity feature work end to end

**Owner:** an Opus session. **Effort:** highest of the packets — this is the largest remaining build implied by "finish current features": the home compute-peer card already ships, and every turn through it is a 404 today.
**Parent:** `.handoff/HANDOFF-STATE-OF-WORK-2026-09-01.md` §6.3. Evidence ids: `SE-05..37` (F3), `R05-F1`, `R10-F1..F11`, `R10-N1`, `R05-10-TEST` (F12), `ROUTE-01`, `FED-01/02`, `ORPHAN-02` (F15), `SEO-06/07` (F11, verified), `PERS-02..06` (F25), `R2-F6` (F1). Reports: `REVIEW-RESULTS-REV-10-2026-08-31.md`, `REVIEW-RESULTS-REV-05-2026-08-31.md`, `HANDOFF-SINGULAR-ENTITY-MULTI-BODY-2026-08-31.md`, `IMPL-PLAN-SINGULAR-ENTITY-TASKS-2026-08-31.md`, `HANDOFF-G12-DEVICES-PAGE-DESIGN-REVIEW-2026-08-31.md`, `HANDOFF-FEDERATED-MULTI-NODE-COMPUTE-AND-FLEET-2026-08-29.md`, `federation/README.md`.

## Shared rules
- Fresh worktree off main (`-b feat/compute-peer-e2e`). `git branch --show-current` before every commit. No trailers.
- Python tests ONLY via `arch -arm64 /Volumes/4TB-BAD/Halbert/.venv/bin/python wt_pytest.py <paths>`; frontend via `npx vitest run`/`npx tsc --noEmit`. Baseline 71 failures; no new ones. Use `TestClient` against `create_app()` for route tests (the existing `test_device_routes.py` builds its own `FastAPI()` with `prefix="/api"`, which is exactly how `ROUTE-01` slipped through) — pattern: `test_compute_probe.py::TestRouteIsMounted`.
- Founder rules: `apple-foundation` and `secure_model` are NEVER offered to peers; peers get `ollama`/`vllm` only; never name AI models in UI copy; H2/H3 for sibling apps; no "Sovereign".
- Files you own: `halbert_core/halbert_core/federation/**`, `dashboard/routes/peers.py`, `routes/devices.py`, `routes/fleet.py`, `routes/agent.py` (streaming dispatch `:1065-1110` only), `model/providers/peer.py`, `model/client.py` (provider sets only), `integrations/app_seam.py` (router selection only), `persona/**`, `routes/persona.py`, `dashboard/app.py` router mounts (`:272-308`), frontend `lib/peerApi.ts` (logic; SONNET-04 fixes its `API_BASE`), `components/fleet/**`, `components/settings/devices/**`, `PeerPairingModal.tsx`, `DiscoveredPeerCard.tsx`, `ComputePeerCard.tsx`, tests for all of these. Do NOT edit `agents/state_machine.py` (OPUS-01), `capabilities.py`/`routes/llm.py` (SONNET-03), `audio/**` (OPUS-02).

## Task 0 — Two one-line fixes that restore shipped pages (P0, do first, separate commits)
- `ROUTE-01`/`R10-N1`: `dashboard/app.py:306` `app.include_router(devices.router, tags=["devices"])` has no prefix; routes are `/devices/*` while `peerApi.ts:388-455` and the G12 handoff use `/api/devices/*`. Add `prefix="/api"`; add a mounted-route test through `create_app()`. Introduced by `922122b2`/`5ab70760`.
- `R10-F5` (second half): the new `DELETE /devices/{node_id}` (`routes/devices.py:418-439`, `?forget=true` → `delete_peer`) and `PUT /devices/entity-mode|body-name|peer-token` (`:221`, `:270`, `:387`) have **no auth dependency at all** — any LAN caller can revoke a peer or rewrite `being.yml`. Restrict to local-admin (loopback origin or the dashboard's auth posture) now; the peers-route half (`routes/peers.py:284-306`, any authenticated peer can revoke any other) is Task 3.

## Task 1 — Workstation compute endpoint (`SE-09`/`R10-F2`, `SE-08`/`R10-F3`) (P0)
`federation/compute_endpoint.py` is never mounted (`app.py:272-308` mounts `routes.compute`, the capacity probe — a different module); `_submit_to_broker` raises `NotImplementedError` (`:265`); the models route returns `[]` (`:220-246`); `compute_broker.py:166/202/218` `start/submit/_worker_loop` raise. Three components disagree on the health route: `compute_router.py:626` and `config_wizard.py:437` probe `/api/compute/v1/health`, `providers/peer.py:106,378` probe `/api/compute/v1/models`, and neither exists (`GET /api/compute/v1/health` → 404 on `create_app()`). Do: mount the router; implement `_submit_to_broker` as a direct local-model call for the 1:1 case (Ollama/vLLM, never apple-foundation, never the secure slot); populate `/models`; add `GET /api/compute/v1/health` (auth'd, no GPU cost) and encode the path as ONE shared constant used by `compute_router`, `config_wizard` and `PeerProvider`; unskip `test_peer_redaction.py:91`; add a `TestClient` test that mounts the real app and completes a chat round-trip. `SE-22` egress redaction on the endpoint (Phase 9.4) — apply `mcp_response()`-style redaction to what leaves the workstation.

## Task 2 — Satellite streaming (`R05-F1`) (P1)
`routes/agent.py:1065-1110` dispatches only OpenAI-compatible / anthropic / else-Ollama (`url = f"{endpoint}/api/chat"`, `:1112`); `model/client.py:68` `OPENAI_COMPATIBLE_PROVIDERS` excludes `peer` while `:75-76` `CHAT_CAPABLE_PROVIDERS` includes it; `state_machine.py:2778` prefers `stream()` — so a `peer://` chat turn raises `NonHttpUrlClientError`. Add a `peer` branch (OpenAI-shaped wire, `http://` rewrite of `peer://`, bearer from `api_key_for`) once Task 1's endpoint serves SSE, or delegate to `chat()` via `asyncio.to_thread` and yield one buffered chunk as the interim. Test: streaming a `TurnModel(provider='peer')` raises nothing and yields text.

## Task 3 — Pairing is a real, confirmed handshake (`SE-15`, `SE-16`/`R10-F1`, `R10-F5`, `SE-18`/`R10-F6`) (P0 security)
`routes/peers.py:159-197` `request_pairing` returns the 4-digit PIN to the requester; `:200-251` `verify_pairing` issues a raw bearer on PIN match only; `_pending_pairings` (`:76`) has no expiry/attempt counter; the "desktop confirmation" is a `TODO(federation-9.1)` comment at `:195`. UI: `/api/peers/discovered` returns hardcoded `[]` (`:414-430`; mDNS 9.7 stubs raise), `PeerPairingModal.tsx:161-170` throws `'Manual pairing flow — TODO'`, `DiscoveredPeerCard.tsx:57-65` auto-verifies the PIN ("development mode"), and the issued token never reaches the other machine's `being.yml` (`PUT /api/devices/peer-token` exists). Build: workstation enters the canonical host URL → calls the HOST's `/pair` → host UI shows PIN/approval (WS event via the existing manager) → workstation submits PIN to host `/verify` → token persisted via `peer-token`; PIN out-of-band, explicit approval-state gate on `/verify`, 60 s expiry, attempt limiting, `/pair` restricted to local/admin; revoke = self or local-admin only; remove the localStorage-only path. `SE-18`: one token, one validation path for MCP + peers (`R2-F6` FleetProxy needs the raw token while `PeersConfig` stores hashes — resolve here). Component test for the manual flow; TestClient tests for the security properties.

## Task 4 — Wire the decision layer or defer it explicitly (`SE-05`, `SE-06`, `SE-10`, `SE-12`, `SEO-06/07`) (P1)
`ComputeRouter.route()` (`compute_router.py:311-417`, cloud > local > peer > WoL > template > no-AI) is unit-tested but never instantiated by production code — the chat path still uses `TierRouter` via `integrations/app_seam.py`; the connectivity probe, the P4c `[no thinking power]` degraded marker, per-peer `wol_timeout` (dead config), and `PeerToolProxy` + executor fallback (`SE-10`, never injected; targets the dashboard URL with a `peers.json` token while the MCP HTTP transport is a separate process with its own static token) all have no runtime consumer. Founder decision `SE-05`: wire `ComputeRouter.route()` into the turn path (then `SE-08` health route and `SE-12` bounded deferred queue + `replay_deferred` first) or mark P4b/P5 as a Phase 9.3 decision layer in the plan. Recommend wiring for the HOME variant only (that is where the compute-peer card ships). `SE-26`: `body_name` is never surfaced to the entity.

## Task 5 — Persona sources of truth (`PERS-02/03/05`, `R10-F7/F8`) (P1)
`persona/manager.py:25-29` hardcoded `Persona` enum drives `POST /switch` (`routes/persona.py:102`, `persona_state.json`) while `/activate` (`:230`) drives `PersonaStore` (`being.yml` symlink); `/status` reports the manager view, `/list` the store view. Pick `PersonaStore` (design recommendation A): route `/switch` and `/status` through it, replace the enum with directory personas or delete the old surface; per-persona memory dirs become a field on the persona YAML. `PERS-03`: fixed-name temp symlink is racy across processes; `PERS-05`: `_save_state` non-atomic.

## Task 6 — Scaffolds a user can see (`FED-01`, `R10-F10/F11`, `ORPHAN-02`) (P2/P3)
Five `/api/fleet/*` routes raise `NotImplementedError` (500) and `NodeFleetCockpit.tsx` shows `TODO(federation-9.9)` text (component is never mounted — only `/api/fleet/nodes` is live); mDNS beacon/listener raise when zeroconf IS installed and `parse_txt_record` has an unguarded `int()`; Frigate has 8 backend routes + a backend-served `/frigate` SPA route with zero frontend code. Return 404/501 with a clear message instead of 500; hide unmounted cockpit text; decide (with the founder) whether Frigate's SPA route stays.

## Task 7 — Tests the feature never had (`SE-11`, `SE-28`, `R05-10-TEST`)
Fix the 4 order-dependent `TestExecutorPeerFallback` tests (`test_peer_tool_proxy.py:268-311`, `asyncio.run`); add the mounted-app tests above; and write one two-process test (two `create_app()` instances on different ports with scratch config dirs) that pairs, links a compute peer, and completes a turn — this has never been run on two machines (`SE-28`). Then ask the founder for one real two-machine LAN validation.

## Task 8 — Docs you must correct in your results (SONNET-05 applies them)
`IMPL-PLAN-SINGULAR-ENTITY-TASKS-2026-08-31.md` "Status: COMPLETE" and Phase 7 acceptance ("pair through the UI, no YAML") are not met; `compute_router.py:348-351` docstring says PeerProvider HTTP is TODO (it is implemented — the workstation side was missing); `HANDOFF-README-PEER-COMPUTE.md` presents peer compute as the recommended home build.

## Results
`.handoff/RESULTS-OPUS-03-<date>.md`: per task commits and tests; the end-to-end two-process test output; the `SE-05` recommendation; what still needs the founder's hardware.
