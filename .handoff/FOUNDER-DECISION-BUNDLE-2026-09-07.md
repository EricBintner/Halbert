# FOUNDER DECISION BUNDLE — every open ruling from the lift program, in one pass

**Date:** 2026-09-07
**Purpose:** all six founder-gated questions accumulated across the OSS-lift program, each with its evidence base and a quotable decision line. Answer in any order; each closes independently. Full details live in the cited docs — this is the clearing-house.
**Standing state:** all three waves are executed and merged (or merging); nothing below blocks any other item.

---

## D-1 — F-2: the reply spot-check (gates engine EN-2/3/4/5 — the largest remaining build)

**The question:** do the three agent-produced ecosystem replies (BrightestMinds, Halley, Haloysius-engine) survive your spot-check? Their file:line claims are the evidence EN-2's merge preconditions lean on.

**What to check (15 minutes):** the three replies — `/Volumes/4TB-BAD/BrightestMinds/.handoff/REPLY-…`, `/Volumes/4TB-BAD/HumanAI/LinuxBrain/.handoff/REPLY-…`, `/Volumes/4TB-BAD/Haloysius/.handoff/REPLY-…` (all `REPLY-OSS-RESEARCH-PROGRAM-2026-09-07.md`). Spot-check two load-bearing claims each: the engine reply's `store.py:669` `.access()`-on-search and `store.py:140-155` non-atomic full-store write; Halley's `app.py:1255/2268` store-path claims; BM's `app.py:790/1994` call chain. Grep them.

**If they pass:** EN-2 (opus/max — the A2 event table + loop-stop + atomic persistence, the highest-blast-radius change in the engine program) dispatches immediately with its preconditions already recorded greppable (`/Volumes/4TB-BAD/Haloysius/.handoff/EN2-PRECONDITIONS-RECORD-2026-09-07.md`); EN-3/EN-4 follow it; EN-5 follows those.

**Decision line:** *"F-2: replies verified — dispatch EN-2/3/4/5 per the engine program."* (Or name the specific refuted claim; the affected dispatch re-plans.)

## D-2 — F-3: the joint curated-core coexistence session (gates Halbert 01-B + engine EN-6)

**The question:** if/when a curated always-in-context memory core exists, how do Halbert's and the engine's (and BrightestMinds' consumer-RAG as a third competitor) coexist without double-injecting the same claims?

**Inputs ready:** the D-1 session-tree design (its §F-3 items: the messages[0] two-tenant budget, path-projection positional stability as a vote against frozen-snapshot cores, the four-signal-site set for packet 01); the engine reply's §1.6 budget math (a ~500-token core = 12.5% of the engine's 4000 default; Halley overrides to 8000; BM's RAG invisible to the engine budget); the engine's Q2 recommendation (query-time assembly + a read-only inspection artifact; file-as-authority is a sub-decision); the write-approval staging governance shape.

**Suggested shape:** one session, both repos' plans open, one decision recorded in both. Until then: 01-B and EN-6 both stay gated — no curated core gets built from either side.

**Decision line:** *"F-3: scheduled/not yet — 01-B and EN-6 stay gated until the joint session rules on coexistence, budget split, and query-time-vs-file."*

## D-3 — F-4: the prompted heartbeat (memo landed — quote the line)

**The recommendation on the table:** `DECISION-MEMO-F4-PROMPTED-HEARTBEAT-2026-09-07.md` recommends **Option A — no prompted heartbeat now**: the merged scheduler already delivers the desire LLM-free (monitor-hash suppression, status taxonomy, restart budgets, catch-up), `HomeCognitiveLoop` is rejected as substrate (unproven stance-compounding; attunement-coupled), and Option C remains the pre-approved design if the three named evidence classes ever appear.

**Decision line (quotable from the memo's closing):** *"F-4: Option A — no prompted heartbeat; extend monitor-hash coverage instead; Option C pre-approved behind its evidence standard."*

## D-4 — Channel Q1/Q3: is the terminal a talk channel, and what is its claim strength?

**The question (from `DESIGN-CHANNEL-LAYER-2026-09-07.md`'s five founder questions):** Q3 — whether the terminal becomes a third talk channel at all (if not, the channel set is two and C5 is cut); Q1 — if yes, its claim strength: the design recommends **ASSERTED via dashboard token** over inventing a VERIFIED `local_console` source.

**Why it matters now:** C1 landed with the terminal deliberately *undeclared* (a declared channel is an admitted ingress; pinned by test). C2–C4 (voice honesty wiring, busy unification, the event tee) proceed regardless.

**Decision line:** *"D-4 Q1/Q3: terminal is/isn't a talk channel; claim strength = [dashboard_token-ASSERTED / local_console-VERIFIED / n-a]."*

## D-5 — Session-tree Q3 + P3c: the one-leaf index ruling (gates the tree's structural enforcement)

**The question (from T1's held-back STOP condition):** the design's one-leaf partial unique index conflicts with three merged realities — the P3d sanctioned two-open-thread race on the canonical HA store (the index would silently drop the loser's turn rows), legacy A6b duplicate-open rows (the index would brick the store open), and ~15 tests confirming multi-open is today's public surface. T1's executor recommends: **resolve D-1's Q3 (one leaf vs per-persona leaves) and land P3c's atomic get-or-open before the index lands** — then it's one statement + a v5 heal step.

**Decision line:** *"D-5: [one-leaf global / per-persona leaves]; P3c atomic get-or-open is [required first / waived, accepting the lost-row change]; legacy duplicate rows heal [on open, demoted to paused]."*

## D-6 — Push cadence + repo hygiene (standing, minor)

Nothing is blocked, but for the record: the program's commits ride whichever session pushes; if you want the lift program's waves pushed deliberately after each merge pass, say so and it becomes standing practice. Two untracked docs from your concurrent session (`HANDOFF-SOURCEPREP-REVIEW-2026-09-07.md`, `RESEARCH-UNIFIED-SYSTEM-ONTOLOGY-2026-09-07.md`) remain theirs to commit. The sec branch (`worktree-sec-1-one-door`) still has 6 unmerged commits — D-3's mapping found its key auth work already reached main through earlier merges, so the branch's remaining value is its held-back classifier/seatbelt/path rulings: worth a merge-or-close decision at your convenience.

---

**What does NOT need a ruling** (recorded, proceeding on program rules): D3-P2..P6 follow their packet series with P5/P6 review-gated; channel C2–C4 are buildable now per the design; the D-2 skills packets (SK-1..7) await dispatch as their own wave; engine candidates (IngressDecision lift, eval-protocol lift, scheduler status split) hold at their two-instance lift conditions; the ecosystem residues are Halley/BM/DebateHaus's own cleanups.
---

## RESOLVED (founder rulings, 2026-09-07)

- **D-1 (F-2): APPROVED — dispatch EN-2** on the replies as they stand; EN-3/EN-4 follow EN-2; EN-5 follows those.
- **D-3 (F-4): Option A — no prompted heartbeat.** The monitor-hash extension follow-up is recorded (needs check-on-X job candidates); Option C stays pre-approved behind its evidence standard.
- **D-4: YES — the terminal becomes a third talk channel, ASSERTED via dashboard token.** FOUNDER CAUTION, binding on the C5 executor: **two terminals are installed in the app — verify-first which is the correct one before wiring anything.**
- **D-5: P3c first, then the index.** Land the atomic get-or-open + a v5 heal for legacy duplicate rows, then the one-leaf partial unique index lands as one statement. No lost rows, no bricked stores.
- **D-2 (F-3): still open** — the joint curated-core session remains unscheduled; 01-B and EN-6 stay gated until it rules.
- **D-6: standing as written** — push after each merge pass (observed practice), sec-branch merge-or-close at the founder's convenience.
